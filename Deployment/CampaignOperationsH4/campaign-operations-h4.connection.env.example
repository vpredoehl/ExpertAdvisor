# Owner-only (0600) host/database binding. No credentials are put in argv or logs.
# LSTM_Release's existing connection builder explicitly uses user=pqxx; do not
# add PGSERVICE/PGUSER here because they cannot override that reviewed login.
LSTM_DB_HOST=127.0.0.1
LSTM_DB_NAME=LSTM
