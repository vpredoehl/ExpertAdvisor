Use model_reasoning_effort="xhigh".

Perform an independent re-review of the corrected, still-uncommitted Phase 5 Step 5c implementation.

This is a focused re-review after a prior independent review concluded NOT READY TO COMMIT and a subsequent hardening pass reported READY FOR INDEPENDENT RE-REVIEW.

Do not assume the corrections are correct because the hardening pass reported success.

Do not redesign Step 5c.
Do not add new features.
Do not broaden Phase 5 scope.
Do not modify code unless you identify a concrete defect that must be corrected before commit.

Repository safety

- Do NOT pull.
- Do NOT fetch.
- Do NOT merge.
- Do NOT rebase.
- Do NOT reset.
- Do NOT switch branches.
- Work only from the current local checkout.
- Preserve the current branch and HEAD.
- Preserve unrelated worktree changes.
- Do not create a commit.
- Do not stage files.
- Do not clean DerivedData.
- Reuse existing DerivedData.
- Perform an Xcode Release build only.
- Do not perform a Debug build.

Expected baseline

- Branch: phase5
- HEAD remains the Phase 5 Step 5b commit.
- Step 5c remains uncommitted.
- Intended Step 5c changes should be limited to:
  - outcome policy header and implementation;
  - focused policy tests;
  - Xcode project registration;
  - Step 5 outcome documentation;
  - Volume VIII architecture documentation.
- Nothing should be staged.

Phase boundary

Step 5c is only a pure, deterministic, database-free, read-only, non-persistent, advisory policy over an immutable Step 5a campaign outcome assessment.

It must not:

- access PostgreSQL;
- add SQL or transactions;
- introduce repositories or services;
- add CLI behavior;
- change scheduler behavior;
- create or mutate experiments;
- create recommendations;
- persist operator review;
- approve anything;
- queue, activate, retry, or launch anything;
- authorize follow-up;
- declare campaign success.

Primary re-review objective

Determine whether all mandatory findings from the prior review are now correctly resolved and whether the corrected implementation is ready to commit.

Focus especially on:

1. fixed golden identity vectors;
2. safe malformed-input and unsupported-version validation;
3. validation-seam fidelity;
4. favorable-only operator-review eligibility;
5. canonical identity completeness and unambiguity;
6. corrected documentation terminology;
7. regression safety.

────────────────────────────────────────────────────────
1. GOLDEN IDENTITY VECTOR REVIEW
────────────────────────────────────────────────────────

Verify the tests contain literal, fixed expected values for:

- default version-1 policy canonical text;
- default version-1 policy hash;
- one representative version-1 decision canonical text;
- that representative decision hash.

Confirm:

- expected hashes are not computed from expected canonical strings inside the test;
- expected canonical text is not generated by production serialization inside the assertion;
- the representative decision is nontrivial and includes meaningful member and metric evidence;
- the golden vectors fail if canonical serialization changes accidentally;
- locale and input-order independence remain separately tested;
- contract versions were not changed merely to avoid correcting version-1 behavior.

Check the reported expected hashes:

- policy: fnv1a64:83a08cff5aad41ad
- decision: fnv1a64:bde8fbe1fa40c2f2

Do not accept these hashes merely because they match current output. Independently inspect whether the corresponding literal canonical strings fully and unambiguously represent the intended contracts.

────────────────────────────────────────────────────────
2. VALIDATION-SEAM REVIEW
────────────────────────────────────────────────────────

Inspect the new plain-data validation view or equivalent test seam.

Determine whether it is:

- narrow;
- internal or otherwise appropriately bounded;
- non-capability-bearing;
- side-effect free;
- used consistently by production validation and tests;
- faithful to the real immutable assessment and policy structures;
- resistant to drift.

Look specifically for a dangerous pattern where:

- tests validate one plain-data representation;
- production objects are converted differently;
- or important fields are omitted when constructing the validation view.

Verify that the production path cannot bypass validation.

Verify malformed evidence fails closed.

Review coverage for:

- malformed policy canonical/hash pairing;
- malformed assessment canonical/hash pairing;
- unsupported assessment contract version;
- malformed campaign canonical/hash pairing;
- malformed materialization canonical/hash pairing;
- campaign/materialization identity mismatch;
- malformed member ordinal or identity shape;
- duplicate materialization-member IDs;
- duplicate ranking-member IDs;
- duplicate recommendation IDs;
- duplicate proposal IDs;
- duplicate comparison metric identities.

Confirm no test uses:

- const_cast;
- raw-memory mutation;
- placement tricks;
- undefined behavior;
- ABI assumptions;
- weakened immutable constructors.

If compile-time construction restrictions are used, verify they actually prove the public malformed-construction path is unavailable.

────────────────────────────────────────────────────────
3. FAVORABLE-ONLY ELIGIBILITY REVIEW
────────────────────────────────────────────────────────

The hardening pass reported a concrete semantic correction:

operator-review eligibility now requires a favorable campaign interpretation rather than merely a conclusive interpretation.

Verify the exact rule is:

- evidence is sufficient;
- every member succeeded comparably;
- the minimum comparable-member count is met;
- every required metric is comparable;
- campaign interpretation is favorable;
- follow-up authorization remains false.

Confirm explicit tests establish:

- favorable -> eligible_for_operator_review;
- neutral -> not_eligible;
- unfavorable -> not_eligible;
- mixed -> not_eligible;
- inconclusive -> not_eligible;
- sufficient evidence alone is not enough;
- follow_up_authorized is false in every case.

Verify the reason:

    favorable_campaign_interpretation_required

is:

- emitted where appropriate;
- not emitted for a favorable eligible result;
- canonically ordered;
- included in decision identity;
- documented accurately.

Check that this correction did not accidentally turn eligibility into recommendation, approval, or authority.

────────────────────────────────────────────────────────
4. AGGREGATE PRECEDENCE REVIEW
────────────────────────────────────────────────────────

Review the new compact aggregate-precedence matrix and production implementation together.

Confirm the intended outcomes are coherent for:

- all favorable comparable members -> favorable;
- all neutral comparable members -> neutral;
- all unfavorable comparable members -> unfavorable;
- favorable + unfavorable -> mixed;
- favorable + neutral -> favorable;
- unfavorable + neutral -> unfavorable;
- favorable + failed -> mixed;
- all failed -> unfavorable;
- cancelled present -> inconclusive;
- not-ready present -> inconclusive;
- inconsistent present -> inconclusive;
- context-changed present -> inconclusive;
- metric-gap present -> inconclusive;
- comparable count below minimum -> inconclusive unless all members failed.

Look for precedence inconsistencies between:

- member interpretation;
- campaign interpretation;
- evidence sufficiency;
- follow-up eligibility;
- reason vectors.

Confirm no adverse or inconclusive evidence can still produce a favorable campaign result.

────────────────────────────────────────────────────────
5. CANONICAL IDENTITY COMPLETENESS REVIEW
────────────────────────────────────────────────────────

Verify policy identity binds every behavior-affecting policy field:

- policy contract version;
- minimum comparable-member count;
- sorted required metric identities;
- metric directions;
- all-member consistency requirement;
- terminal-state requirement;
- successful-member comparability requirement;
- favorable-only follow-up review requirement;
- explicit non-authorizing semantics.

Verify decision identity binds, directly or through an explicitly justified complete upstream canonical binding:

- decision contract version;
- complete policy canonical text and hash;
- assessment contract version;
- complete assessment canonical text and hash;
- full campaign identity;
- full materialization identity;
- ordered member identities;
- expectedExperimentId where present;
- assessment lifecycle;
- assessment consistency;
- assessment outcome;
- assessment diagnostics;
- policy evidence classification;
- metric identity, direction, Step 5a classification, Step 5a delta, and policy judgment;
- member interpretation;
- summary counts;
- evidence reasons;
- interpretation reasons;
- follow-up reasons;
- evidence sufficiency;
- campaign interpretation;
- follow-up eligibility;
- follow-up authorization false.

Pay special attention to serialization ambiguity.

Check:

- length prefixes are used where delimiter-containing strings can occur;
- optional IDs and optional values have unambiguous encodings;
- diagnostics and reason vectors have deterministic counts and ordering;
- member boundaries are unambiguous;
- metric-evaluation boundaries are unambiguous;
- negative zero is canonicalized;
- nonfinite values fail closed;
- global locale cannot influence canonical output.

Determine whether any field is duplicated unnecessarily. Duplication is acceptable if deterministic and intentional, but the documentation or code rationale should make clear that the complete upstream assessment canonical is the authoritative evidence snapshot while direct repeated fields make the policy decision self-describing.

────────────────────────────────────────────────────────
6. DOCUMENTATION REVIEW
────────────────────────────────────────────────────────

Verify all outcome documentation now distinguishes:

- authoritative persisted evidence;
- from a non-authoritative, point-in-time assessment built from that evidence.

Ensure no remaining text calls the assessment itself authoritative.

Verify documentation accurately states:

- minimumComparableMemberCount is only an advisory evidence-coverage threshold;
- it is not statistical significance, confidence, causal evidence, profitability evidence, repeatability, or campaign success;
- only favorable conclusive evidence can become eligible for operator review;
- eligibility is not recommendation, approval, or authorization;
- follow_up_authorized remains false;
- Step 5c has no repository, service, CLI, persistence, scheduler, or lifecycle behavior.

Confirm Volume VIII and the focused Step 5 document agree.

────────────────────────────────────────────────────────
7. BOUNDARY AND REGRESSION REVIEW
────────────────────────────────────────────────────────

Inspect the full diff and dependency graph.

Confirm Step 5c:

- includes only the Step 5a assessment contract plus general canonical/hash helpers;
- is not referenced from scheduler, repositories, services, or CLI;
- adds no static initialization with side effects;
- performs no I/O;
- performs no environment access;
- performs no database access;
- performs no process or worker interaction.

Check whether the new validation seam unintentionally became public API or introduced coupling that future code could misuse.

Review whether any production semantic change beyond favorable-only eligibility was introduced. Any additional semantic change must be explicitly justified.

────────────────────────────────────────────────────────
8. TEST ADEQUACY REVIEW
────────────────────────────────────────────────────────

Determine whether tests now adequately cover:

- all prior mandatory review findings;
- golden policy identity;
- golden decision identity;
- malformed canonical/hash pairs;
- unsupported assessment version;
- validation-view fidelity;
- duplicate identity rejection;
- aggregate precedence;
- eligibility matrix;
- reason ordering;
- canonical ordering;
- locale independence;
- timestamp exclusion;
- policy evolution changing identity;
- evidence evolution changing identity;
- explicit non-authorization.

Identify any remaining blocker-level gap separately from optional future improvements.

────────────────────────────────────────────────────────
9. VERIFICATION
────────────────────────────────────────────────────────

Run or independently confirm:

1. Step 5c focused tests.
2. Step 5c ASan.
3. Step 5c UBSan.
4. Step 5a assessment tests.
5. Step 5b integration/formatting tests.
6. Campaign-status tests.
7. Affected Phase 4C/4D regressions.
8. Phase 5 execution, activation, and launch regressions.
9. Existing database-backed Step 5b/status verification where already configured.
10. Existing Release CLI regressions.
11. git diff --check.
12. git diff --cached --check.
13. Whitespace checks for untracked Step 5c files.
14. Xcode project validation.
15. Incremental Release build only:

    xcodebuild -project ExpertAdvisor.xcodeproj \
      -scheme "LSTM Release" \
      -configuration Release \
      -destination "platform=macOS,arch=arm64" \
      build

Do not run a Debug build.
Do not clean DerivedData.

Treat the existing LLVM22.xctoolchain Info.plist warning as environmental only if it remains unchanged and non-fatal.

────────────────────────────────────────────────────────
10. WORKTREE SAFETY
────────────────────────────────────────────────────────

At completion confirm:

- branch remains phase5;
- HEAD is unchanged;
- no commit was created;
- nothing was staged;
- no unrelated files were modified;
- all intended Step 5c files are listed;
- no repository operation prohibited above occurred.

────────────────────────────────────────────────────────
DELIVERABLE
────────────────────────────────────────────────────────

Provide:

1. Executive summary.
2. Prior mandatory finding resolution.
3. Golden-vector assessment.
4. Validation-seam and malformed-input assessment.
5. Eligibility and aggregate-policy assessment.
6. Canonical identity completeness and ambiguity assessment.
7. Documentation assessment.
8. Boundary and regression assessment.
9. Verification results.
10. Worktree safety audit.
11. Mandatory corrections, if any.
12. Optional future improvements, if any.

Do not modify code unless a concrete commit-blocking defect is identified.

If a correction is necessary, report the defect clearly before editing, make only the narrow required correction, rerun affected verification, and describe the exact change.

Conclude with exactly one of:

READY TO COMMIT

or

NOT READY TO COMMIT

If NOT READY TO COMMIT, distinguish mandatory corrections from optional improvements.
