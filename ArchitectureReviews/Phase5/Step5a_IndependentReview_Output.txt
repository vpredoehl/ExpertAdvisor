Reading prompt from stdin...
OpenAI Codex v0.144.6
--------
workdir: /Volumes/Developer SSD/ExpertAdvisor
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: xhigh
reasoning summaries: none
session id: 019f7db3-61f1-7fb0-8286-b0e0045270d0
--------
user
Use model_reasoning_effort="xhigh".
You are performing an INDEPENDENT architectural and implementation review of the current local checkout.

This is NOT a continuation of previous work.
Review the code as though it was written by another senior engineer.

Repository rules

- Do NOT pull.
- Do NOT fetch.
- Do NOT merge.
- Do NOT rebase.
- Work only from the current local checkout.
- Preserve the existing branch.
- Do not create commits.

Primary objective

Determine whether Phase 5 Step 5a is actually ready to commit.

Review the implementation for:

• architectural correctness
• lifecycle correctness
• deterministic behavior
• domain modeling
• invariants
• consistency
• ownership
• validation
• exception safety
• test completeness
• future compatibility with Step 5b

Pay particular attention to:

- lifecycle modeling
- canonical identities
- metric collections
- provenance validation
- aggregate computation
- output invariants
- builder usage
- duplicate handling
- deterministic ordering
- comparison semantics
- validation diagnostics
- serialization/versioning
- API cleanliness

You are encouraged to challenge assumptions.

If you find:

- architectural weaknesses
- contract violations
- incorrect invariants
- incorrect ownership
- incomplete validation
- missing tests
- undefined behavior
- hidden edge cases

then fix them.

Do NOT make stylistic changes.

Do NOT perform unrelated refactoring.

Do NOT modify scheduler behavior.

Do NOT modify experiment logic.

Do NOT modify database schema.

Do NOT modify Phase 5b.

Verification

After changes:

1. Run the focused Step 5a tests.

2. Run AddressSanitizer and UndefinedBehaviorSanitizer where supported.

3. Run any nearby regression tests affected by the changes.

4. Perform an incremental Xcode Release build only.

Do NOT clean DerivedData.

Do NOT create a fresh DerivedData directory unless absolutely required.

Success criteria

The implementation should be something you would personally approve for commit.

At completion provide:

- architectural assessment
- defects corrected
- remaining concerns (if any)
- files modified
- tests executed
- build results
- explicit recommendation:

READY TO COMMIT

or

NOT READY TO COMMIT


codex
I’ll treat the checkout as an independent review, first mapping the Step 5a surface and repository rules, then tracing its contracts through implementation and tests before making only evidence-backed fixes. I’ll preserve the branch and local state, avoid all remote Git operations, and finish with focused tests, supported sanitizers, nearby regressions, and an incremental Xcode Release build.
  → Inspect repository rules, worktree state, and identify the Phase 5 Step 5a implementation/test surface
  • Review architecture, lifecycle/domain invariants, validation, determinism, serialization, and Step 5b compatibility
  • Implement narrowly scoped corrections and add missing tests
  • Run focused tests, sanitizers, nearby regressions, and incremental Xcode Release build
  • Audit final diff and deliver commit-readiness assessment
exec
/bin/bash -lc "pwd && rg --files -g 'AGENTS.md' -g '"'!DerivedData'"' -g '"'!build'"' -g '"'!Build'"' | sed -n '1,80p' && git status --short --branch && git log -n 8 --oneline --decorate" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
/Volumes/Developer SSD/ExpertAdvisor
## phase5...github/phase5
 M ExpertAdvisor.xcodeproj/project.pbxproj
?? Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
?? Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
?? Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
?? docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
8b9939e (HEAD -> phase5, github/phase5) Phase 5 Step 4: Add recommendation campaign operational status
05383ad Phase 5 Step 3: Add atomic campaign launch
892cf46 Phase 5 Step 2: Add atomic campaign activation
3b165ea Phase 5 Step 1: Add atomic campaign conversion execution
84cafa2 (GPU) Merge branch 'phase0-architecture' into GPU
7457ea1 (github/phase0-architecture, phase0-architecture) Phase 4D Completion Audit: Harden campaign review concurrency tests
8ff8bb8 Phase 4D Step 6: Add campaign-wide proposal review
606259f Phase 4D Step 5: Review recommendation campaign handoff implementation

exec
/bin/bash -lc "rg -n \"Step 5a|5a|OutcomeAssessment|outcome assessment|assessment\" . -g '"'!DerivedData/**'"' -g '"'!build/**'"' -g '"'!Build/**'"' -g '"'!*.xcuserstate'"'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
./docs/Phase4CExperimentRecommendationConversionActivation.rst:59:Phase 5 Step 2 reuses this exact assessment, identity, execution-scoped lock,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:11:    kRecommendationCampaignOutcomeAssessmentContractVersion = 2;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:13:    kMaximumRecommendationCampaignOutcomeAssessmentMembers = 1000;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:16:// non-persistent, and non-authoritative evidence assessment. They intentionally
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:18:struct RecommendationCampaignOutcomeAssessmentCampaignIdentity
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:24:    RecommendationCampaignOutcomeAssessmentCampaignIdentity(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:29:        const RecommendationCampaignOutcomeAssessmentCampaignIdentity&) const =
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:33:struct RecommendationCampaignOutcomeAssessmentMaterializationIdentity
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:42:    RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:50:        const RecommendationCampaignOutcomeAssessmentMaterializationIdentity&)
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:54:struct RecommendationCampaignOutcomeAssessmentMemberIdentity
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:64:    RecommendationCampaignOutcomeAssessmentMemberIdentity(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:73:        const RecommendationCampaignOutcomeAssessmentMemberIdentity&) const =
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:77:enum class RecommendationCampaignOutcomeAssessmentMetricComparisonSupport
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:83:struct RecommendationCampaignOutcomeAssessmentMetric
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:87:    const RecommendationCampaignOutcomeAssessmentMetricComparisonSupport
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:90:    RecommendationCampaignOutcomeAssessmentMetric(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:93:        RecommendationCampaignOutcomeAssessmentMetricComparisonSupport
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:96:        const RecommendationCampaignOutcomeAssessmentMetric&) const = default;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:99:struct RecommendationCampaignOutcomeAssessmentMetricCollection
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:102:    const std::vector<RecommendationCampaignOutcomeAssessmentMetric> metrics;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:104:    explicit RecommendationCampaignOutcomeAssessmentMetricCollection(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:105:        std::vector<RecommendationCampaignOutcomeAssessmentMetric> metrics =
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:108:        const RecommendationCampaignOutcomeAssessmentMetricCollection&) const =
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:112:struct RecommendationCampaignOutcomeAssessmentComparisonContext
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:122:    RecommendationCampaignOutcomeAssessmentComparisonContext(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:131:        const RecommendationCampaignOutcomeAssessmentComparisonContext&) const =
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:135:struct RecommendationCampaignOutcomeAssessmentSourceEvidence
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:140:    const RecommendationCampaignOutcomeAssessmentComparisonContext context;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:141:    const RecommendationCampaignOutcomeAssessmentMetricCollection metrics;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:143:    RecommendationCampaignOutcomeAssessmentSourceEvidence(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:147:        RecommendationCampaignOutcomeAssessmentComparisonContext context,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:148:        RecommendationCampaignOutcomeAssessmentMetricCollection metrics);
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:150:        const RecommendationCampaignOutcomeAssessmentSourceEvidence&) const =
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:154:struct RecommendationCampaignOutcomeAssessmentResultIdentity
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:159:    RecommendationCampaignOutcomeAssessmentResultIdentity(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:163:        const RecommendationCampaignOutcomeAssessmentResultIdentity&) const =
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:167:struct RecommendationCampaignOutcomeAssessmentResultEvidence
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:172:    const std::vector<RecommendationCampaignOutcomeAssessmentResultIdentity>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:175:        RecommendationCampaignOutcomeAssessmentComparisonContext>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:177:    const RecommendationCampaignOutcomeAssessmentMetricCollection metrics;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:179:    RecommendationCampaignOutcomeAssessmentResultEvidence(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:182:        std::vector<RecommendationCampaignOutcomeAssessmentResultIdentity>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:184:        std::optional<RecommendationCampaignOutcomeAssessmentComparisonContext>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:186:        RecommendationCampaignOutcomeAssessmentMetricCollection metrics);
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:188:        const RecommendationCampaignOutcomeAssessmentResultEvidence&) const =
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:192:enum class RecommendationCampaignOutcomeAssessmentLifecycleState
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:201:struct RecommendationCampaignOutcomeAssessmentMemberEvidence
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:203:    const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:204:    const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:205:    const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:207:    const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:210:    RecommendationCampaignOutcomeAssessmentMemberEvidence(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:211:        RecommendationCampaignOutcomeAssessmentMemberIdentity identity,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:212:        RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:213:        std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:215:        std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:218:        const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:222:enum class RecommendationCampaignOutcomeAssessmentConsistencyState
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:228:enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:243:enum class RecommendationCampaignOutcomeAssessmentMemberOutcome
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:254:enum class RecommendationCampaignOutcomeAssessmentMetricComparisonClassification
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:264:enum class RecommendationCampaignOutcomeAssessmentContextDifference
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:274:struct RecommendationCampaignOutcomeAssessmentBuilder;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:276:struct RecommendationCampaignOutcomeAssessmentMetricComparison
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:279:    const RecommendationCampaignOutcomeAssessmentMetricComparisonClassification
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:285:        RecommendationCampaignOutcomeAssessmentContextDifference>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:288:    RecommendationCampaignOutcomeAssessmentMetricComparison(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:289:        const RecommendationCampaignOutcomeAssessmentMetricComparison&) =
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:291:    RecommendationCampaignOutcomeAssessmentMetricComparison(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:292:        RecommendationCampaignOutcomeAssessmentMetricComparison&&) = default;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:294:        const RecommendationCampaignOutcomeAssessmentMetricComparison&) const =
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:298:    RecommendationCampaignOutcomeAssessmentMetricComparison(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:300:        RecommendationCampaignOutcomeAssessmentMetricComparisonClassification
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:305:        std::vector<RecommendationCampaignOutcomeAssessmentContextDifference>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:307:    friend struct RecommendationCampaignOutcomeAssessmentBuilder;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:310:enum class RecommendationCampaignOutcomeAssessmentAggregateOutcome
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:321:struct RecommendationCampaignOutcomeAssessmentSummary
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:323:    const RecommendationCampaignOutcomeAssessmentAggregateOutcome outcome;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:353:    RecommendationCampaignOutcomeAssessmentSummary(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:354:        const RecommendationCampaignOutcomeAssessmentSummary&) = default;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:355:    RecommendationCampaignOutcomeAssessmentSummary(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:356:        RecommendationCampaignOutcomeAssessmentSummary&&) = default;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:358:        const RecommendationCampaignOutcomeAssessmentSummary&) const = default;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:361:    RecommendationCampaignOutcomeAssessmentSummary(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:362:        RecommendationCampaignOutcomeAssessmentAggregateOutcome outcome,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:388:    friend struct RecommendationCampaignOutcomeAssessmentBuilder;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:391:struct RecommendationCampaignOutcomeAssessmentMember
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:393:    const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:394:    const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:395:    const RecommendationCampaignOutcomeAssessmentConsistencyState consistency;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:396:    const RecommendationCampaignOutcomeAssessmentMemberOutcome outcome;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:398:    const std::vector<RecommendationCampaignOutcomeAssessmentDiagnosticCode>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:400:    const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:402:    const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:406:        RecommendationCampaignOutcomeAssessmentMetricComparison>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:409:    RecommendationCampaignOutcomeAssessmentMember(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:410:        const RecommendationCampaignOutcomeAssessmentMember&) = default;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:411:    RecommendationCampaignOutcomeAssessmentMember(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:412:        RecommendationCampaignOutcomeAssessmentMember&&) = default;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:414:        const RecommendationCampaignOutcomeAssessmentMember&) const = default;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:417:    RecommendationCampaignOutcomeAssessmentMember(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:418:        RecommendationCampaignOutcomeAssessmentMemberIdentity identity,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:419:        RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:420:        RecommendationCampaignOutcomeAssessmentConsistencyState consistency,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:421:        RecommendationCampaignOutcomeAssessmentMemberOutcome outcome,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:422:        std::vector<RecommendationCampaignOutcomeAssessmentDiagnosticCode>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:424:        std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:426:        std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:428:        std::vector<RecommendationCampaignOutcomeAssessmentMetricComparison>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:430:    friend struct RecommendationCampaignOutcomeAssessmentBuilder;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:433:struct RecommendationCampaignOutcomeAssessmentIdentity
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:439:    RecommendationCampaignOutcomeAssessmentIdentity(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:440:        const RecommendationCampaignOutcomeAssessmentIdentity&) = default;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:441:    RecommendationCampaignOutcomeAssessmentIdentity(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:442:        RecommendationCampaignOutcomeAssessmentIdentity&&) = default;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:444:        const RecommendationCampaignOutcomeAssessmentIdentity&) const = default;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:447:    RecommendationCampaignOutcomeAssessmentIdentity(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:451:    friend struct RecommendationCampaignOutcomeAssessmentBuilder;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:454:struct RecommendationCampaignOutcomeAssessment
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:462:    const RecommendationCampaignOutcomeAssessmentIdentity identity;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:463:    const RecommendationCampaignOutcomeAssessmentCampaignIdentity
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:465:    const RecommendationCampaignOutcomeAssessmentMaterializationIdentity
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:468:    const RecommendationCampaignOutcomeAssessmentSummary summary;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:469:    const std::vector<RecommendationCampaignOutcomeAssessmentMember> members;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:471:    RecommendationCampaignOutcomeAssessment(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:472:        const RecommendationCampaignOutcomeAssessment&) = default;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:473:    RecommendationCampaignOutcomeAssessment(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:474:        RecommendationCampaignOutcomeAssessment&&) = default;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:476:        const RecommendationCampaignOutcomeAssessment&) const = default;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:479:    RecommendationCampaignOutcomeAssessment(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:480:        RecommendationCampaignOutcomeAssessmentIdentity identity,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:481:        RecommendationCampaignOutcomeAssessmentCampaignIdentity
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:483:        RecommendationCampaignOutcomeAssessmentMaterializationIdentity
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:486:        RecommendationCampaignOutcomeAssessmentSummary summary,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:487:        std::vector<RecommendationCampaignOutcomeAssessmentMember> members);
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:488:    friend struct RecommendationCampaignOutcomeAssessmentBuilder;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:491:RecommendationCampaignOutcomeAssessment BuildRecommendationCampaignOutcomeAssessment(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:492:    const RecommendationCampaignOutcomeAssessmentCampaignIdentity&
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:494:    const RecommendationCampaignOutcomeAssessmentMaterializationIdentity&
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:497:    const std::vector<RecommendationCampaignOutcomeAssessmentMemberEvidence>&
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:500:std::string RecommendationCampaignOutcomeAssessmentMetricComparisonSupportText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:501:    RecommendationCampaignOutcomeAssessmentMetricComparisonSupport value);
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:502:std::string RecommendationCampaignOutcomeAssessmentLifecycleStateText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:503:    RecommendationCampaignOutcomeAssessmentLifecycleState value);
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:504:std::string RecommendationCampaignOutcomeAssessmentConsistencyStateText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:505:    RecommendationCampaignOutcomeAssessmentConsistencyState value);
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:506:std::string RecommendationCampaignOutcomeAssessmentDiagnosticCodeText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:507:    RecommendationCampaignOutcomeAssessmentDiagnosticCode value);
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:508:std::string RecommendationCampaignOutcomeAssessmentMemberOutcomeText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:509:    RecommendationCampaignOutcomeAssessmentMemberOutcome value);
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:511:RecommendationCampaignOutcomeAssessmentMetricComparisonClassificationText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:512:    RecommendationCampaignOutcomeAssessmentMetricComparisonClassification
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:514:std::string RecommendationCampaignOutcomeAssessmentContextDifferenceText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:515:    RecommendationCampaignOutcomeAssessmentContextDifference value);
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:516:std::string RecommendationCampaignOutcomeAssessmentAggregateOutcomeText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:517:    RecommendationCampaignOutcomeAssessmentAggregateOutcome value);
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:1:#include "ExperimentRecommendationCampaignOutcomeAssessment.hpp"
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:23:    RecommendationCampaignOutcomeAssessmentAggregateOutcome;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:24:using Assessment = RecommendationCampaignOutcomeAssessment;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:26:    RecommendationCampaignOutcomeAssessmentCampaignIdentity;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:28:    RecommendationCampaignOutcomeAssessmentMetricComparisonClassification;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:29:using Consistency = RecommendationCampaignOutcomeAssessmentConsistencyState;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:30:using Context = RecommendationCampaignOutcomeAssessmentComparisonContext;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:31:using Diagnostic = RecommendationCampaignOutcomeAssessmentDiagnosticCode;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:32:using Difference = RecommendationCampaignOutcomeAssessmentContextDifference;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:33:using Lifecycle = RecommendationCampaignOutcomeAssessmentLifecycleState;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:35:    RecommendationCampaignOutcomeAssessmentMaterializationIdentity;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:36:using Member = RecommendationCampaignOutcomeAssessmentMember;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:37:using MemberEvidence = RecommendationCampaignOutcomeAssessmentMemberEvidence;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:39:    RecommendationCampaignOutcomeAssessmentMemberIdentity;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:40:using MemberOutcome = RecommendationCampaignOutcomeAssessmentMemberOutcome;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:41:using Metric = RecommendationCampaignOutcomeAssessmentMetric;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:43:    RecommendationCampaignOutcomeAssessmentMetricCollection;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:45:    RecommendationCampaignOutcomeAssessmentMetricComparison;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:47:    RecommendationCampaignOutcomeAssessmentMetricComparisonSupport;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:49:    RecommendationCampaignOutcomeAssessmentResultEvidence;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:51:    RecommendationCampaignOutcomeAssessmentResultIdentity;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:53:    RecommendationCampaignOutcomeAssessmentSourceEvidence;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:54:using Summary = RecommendationCampaignOutcomeAssessmentSummary;
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:238:    return BuildRecommendationCampaignOutcomeAssessment(Campaign(),
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:688:        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:702:        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:718:    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:720:    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:722:    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:724:    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:726:    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:728:    assert(RecommendationCampaignOutcomeAssessmentConsistencyStateText(
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:730:    assert(RecommendationCampaignOutcomeAssessmentConsistencyStateText(
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:732:    assert(RecommendationCampaignOutcomeAssessmentMemberOutcomeText(
./Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:734:    assert(RecommendationCampaignOutcomeAssessmentAggregateOutcomeText(
./Sources/ExperimentRecommendationCampaignActivationRepository.cpp:143:        const auto assessment = AssessRecommendationConversionActivation(
./Sources/ExperimentRecommendationCampaignActivationRepository.cpp:145:        if (!assessment.execution ||
./Sources/ExperimentRecommendationCampaignActivationRepository.cpp:146:            assessment.execution->proposalId != stored.conversionProposalId ||
./Sources/ExperimentRecommendationCampaignActivationRepository.cpp:147:            assessment.execution->experimentId <= 0)
./Sources/ExperimentRecommendationCampaignActivationRepository.cpp:155:            assessment.execution->reviewDecisionId;
./Sources/ExperimentRecommendationCampaignActivationRepository.cpp:156:        member.executionId = assessment.execution->executionId;
./Sources/ExperimentRecommendationCampaignActivationRepository.cpp:157:        member.experimentId = assessment.execution->experimentId;
./Sources/ExperimentRecommendationCampaignActivationRepository.cpp:158:        member.experimentStatus = assessment.experimentStatus;
./Sources/ExperimentRecommendationCampaignActivationRepository.cpp:159:        member.experimentPhase = assessment.experimentPhase;
./Sources/ExperimentRecommendationCampaignActivationRepository.cpp:160:        if (assessment.activation)
./Sources/ExperimentRecommendationCampaignActivationRepository.cpp:161:            member.activationId = assessment.activation->activationId;
./Sources/ExperimentRecommendationCampaignActivationRepository.cpp:167:        else if (assessment.state ==
./Sources/ExperimentRecommendationCampaignActivationRepository.cpp:173:        else if (assessment.state ==
./Sources/ExperimentRecommendationCampaignActivationRepository.cpp:182:            member.evidenceDiagnostic = assessment.reason.empty()
./Sources/ExperimentRecommendationCampaignActivationRepository.cpp:184:                : "campaign_" + assessment.reason;
./docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst:7:Step 5a defines a pure, immutable, point-in-time domain assessment of the
./docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst:28:order does not affect output or assessment identity.  A comparison is derived
./docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst:40:assessment.
./docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst:50:Step 5a does not provide a repository, database access, persistence, service,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1:#include "ExperimentRecommendationCampaignOutcomeAssessment.hpp"
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:22:    RecommendationCampaignOutcomeAssessmentAggregateOutcome;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:24:    RecommendationCampaignOutcomeAssessmentMetricComparisonClassification;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:26:    RecommendationCampaignOutcomeAssessmentMetricComparison;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:27:using Consistency = RecommendationCampaignOutcomeAssessmentConsistencyState;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:28:using Context = RecommendationCampaignOutcomeAssessmentComparisonContext;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:29:using Diagnostic = RecommendationCampaignOutcomeAssessmentDiagnosticCode;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:30:using Difference = RecommendationCampaignOutcomeAssessmentContextDifference;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:31:using Lifecycle = RecommendationCampaignOutcomeAssessmentLifecycleState;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:32:using Member = RecommendationCampaignOutcomeAssessmentMember;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:33:using MemberEvidence = RecommendationCampaignOutcomeAssessmentMemberEvidence;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:34:using MemberOutcome = RecommendationCampaignOutcomeAssessmentMemberOutcome;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:35:using Metric = RecommendationCampaignOutcomeAssessmentMetric;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:37:    RecommendationCampaignOutcomeAssessmentMetricCollection;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:39:    RecommendationCampaignOutcomeAssessmentMetricComparisonSupport;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:41:    RecommendationCampaignOutcomeAssessmentResultIdentity;
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:65:        "recommendation_campaign_outcome_assessment_metric_support_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:80:        "recommendation_campaign_outcome_assessment_lifecycle_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:96:                "recommendation_campaign_outcome_assessment_metric_identity_duplicate");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:118:                "recommendation_campaign_outcome_assessment_result_identity_duplicate");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:166:        << RecommendationCampaignOutcomeAssessmentMetricComparisonSupportText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:183:        RecommendationCampaignOutcomeAssessmentSourceEvidence>& evidence)
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:201:        RecommendationCampaignOutcomeAssessmentResultEvidence>& evidence)
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:222:        << RecommendationCampaignOutcomeAssessmentMetricComparisonClassificationText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:231:            << RecommendationCampaignOutcomeAssessmentContextDifferenceText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:237:    const RecommendationCampaignOutcomeAssessmentSummary& summary)
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:240:        << RecommendationCampaignOutcomeAssessmentAggregateOutcomeText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:281:    const RecommendationCampaignOutcomeAssessmentCampaignIdentity& campaign,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:282:    const RecommendationCampaignOutcomeAssessmentMaterializationIdentity&
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:284:    const RecommendationCampaignOutcomeAssessmentSummary& summary,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:289:    out << "experiment_recommendation_campaign_outcome_assessment_v2"
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:290:        << ";assessment_contract_version="
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:291:        << kRecommendationCampaignOutcomeAssessmentContractVersion
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:309:            << RecommendationCampaignOutcomeAssessmentLifecycleStateText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:312:            << RecommendationCampaignOutcomeAssessmentConsistencyStateText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:315:            << RecommendationCampaignOutcomeAssessmentMemberOutcomeText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:320:                << RecommendationCampaignOutcomeAssessmentDiagnosticCodeText(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:508:RecommendationCampaignOutcomeAssessmentCampaignIdentity::
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:509:    RecommendationCampaignOutcomeAssessmentCampaignIdentity(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:519:            "recommendation_campaign_outcome_assessment_campaign_id_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:521:        "recommendation_campaign_outcome_assessment_campaign_identity_missing");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:524:            "recommendation_campaign_outcome_assessment_campaign_identity_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:527:RecommendationCampaignOutcomeAssessmentMaterializationIdentity::
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:528:    RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:544:            "recommendation_campaign_outcome_assessment_materialization_id_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:546:        memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:548:            "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:550:        "recommendation_campaign_outcome_assessment_materialization_identity_missing");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:553:            "recommendation_campaign_outcome_assessment_materialization_identity_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:556:RecommendationCampaignOutcomeAssessmentMemberIdentity::
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:557:    RecommendationCampaignOutcomeAssessmentMemberIdentity(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:577:            "recommendation_campaign_outcome_assessment_member_identity_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:579:        "recommendation_campaign_outcome_assessment_expected_experiment_id_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:582:RecommendationCampaignOutcomeAssessmentMetric::
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:583:    RecommendationCampaignOutcomeAssessmentMetric(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:594:        "recommendation_campaign_outcome_assessment_metric_identity_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:597:            "recommendation_campaign_outcome_assessment_metric_value_nonfinite");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:601:RecommendationCampaignOutcomeAssessmentMetricCollection::
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:602:    RecommendationCampaignOutcomeAssessmentMetricCollection(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:608:RecommendationCampaignOutcomeAssessmentComparisonContext::
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:609:    RecommendationCampaignOutcomeAssessmentComparisonContext(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:626:        "recommendation_campaign_outcome_assessment_context_symbol_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:628:        "recommendation_campaign_outcome_assessment_context_label_definition_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:630:        "recommendation_campaign_outcome_assessment_context_inference_range_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:632:        "recommendation_campaign_outcome_assessment_context_inference_range_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:635:            "recommendation_campaign_outcome_assessment_context_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:638:RecommendationCampaignOutcomeAssessmentSourceEvidence::
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:639:    RecommendationCampaignOutcomeAssessmentSourceEvidence(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:653:            "recommendation_campaign_outcome_assessment_source_experiment_id_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:655:        "recommendation_campaign_outcome_assessment_source_model_id_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:657:        "recommendation_campaign_outcome_assessment_source_analysis_id_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:660:RecommendationCampaignOutcomeAssessmentResultIdentity::
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:661:    RecommendationCampaignOutcomeAssessmentResultIdentity(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:667:        "recommendation_campaign_outcome_assessment_result_identity_kind_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:670:            "recommendation_campaign_outcome_assessment_result_identity_id_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:673:RecommendationCampaignOutcomeAssessmentResultEvidence::
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:674:    RecommendationCampaignOutcomeAssessmentResultEvidence(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:688:        "recommendation_campaign_outcome_assessment_result_experiment_id_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:690:        "recommendation_campaign_outcome_assessment_result_model_id_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:693:RecommendationCampaignOutcomeAssessmentMemberEvidence::
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:694:    RecommendationCampaignOutcomeAssessmentMemberEvidence(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:695:        RecommendationCampaignOutcomeAssessmentMemberIdentity identityValue,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:698:            RecommendationCampaignOutcomeAssessmentSourceEvidence>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:701:            RecommendationCampaignOutcomeAssessmentResultEvidence>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:711:            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:714:RecommendationCampaignOutcomeAssessmentMetricComparison::
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:715:    RecommendationCampaignOutcomeAssessmentMetricComparison(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:732:        "recommendation_campaign_outcome_assessment_comparison_metric_identity_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:737:            "recommendation_campaign_outcome_assessment_comparison_number_nonfinite");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:741:            "recommendation_campaign_outcome_assessment_comparison_context_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:746:                "recommendation_campaign_outcome_assessment_comparable_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:750:            "recommendation_campaign_outcome_assessment_noncomparable_delta_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:753:RecommendationCampaignOutcomeAssessmentSummary::
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:754:    RecommendationCampaignOutcomeAssessmentSummary(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:829:            "recommendation_campaign_outcome_assessment_summary_invariant_failed");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:832:RecommendationCampaignOutcomeAssessmentMember::
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:833:    RecommendationCampaignOutcomeAssessmentMember(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:834:        RecommendationCampaignOutcomeAssessmentMemberIdentity identityValue,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:840:            RecommendationCampaignOutcomeAssessmentSourceEvidence>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:843:            RecommendationCampaignOutcomeAssessmentResultEvidence>
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:860:            "recommendation_campaign_outcome_assessment_member_invariant_failed");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:863:RecommendationCampaignOutcomeAssessmentIdentity::
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:864:    RecommendationCampaignOutcomeAssessmentIdentity(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:873:            kRecommendationCampaignOutcomeAssessmentContractVersion ||
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:876:            "recommendation_campaign_outcome_assessment_identity_invariant_failed");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:878:        "recommendation_campaign_outcome_assessment_identity_missing");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:881:RecommendationCampaignOutcomeAssessment::
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:882:    RecommendationCampaignOutcomeAssessment(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:883:        RecommendationCampaignOutcomeAssessmentIdentity identityValue,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:884:        RecommendationCampaignOutcomeAssessmentCampaignIdentity
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:886:        RecommendationCampaignOutcomeAssessmentMaterializationIdentity
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:889:        RecommendationCampaignOutcomeAssessmentSummary summaryValue,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:899:        "recommendation_campaign_outcome_assessment_observed_at_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:902:            "recommendation_campaign_outcome_assessment_member_summary_invariant_failed");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:905:struct RecommendationCampaignOutcomeAssessmentBuilder
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1018:    static RecommendationCampaignOutcomeAssessment Build(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1019:        const RecommendationCampaignOutcomeAssessmentCampaignIdentity&
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1021:        const RecommendationCampaignOutcomeAssessmentMaterializationIdentity&
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1027:            "recommendation_campaign_outcome_assessment_observed_at_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1031:                "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1035:                "recommendation_campaign_outcome_assessment_materialization_version_unsupported");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1039:                "recommendation_campaign_outcome_assessment_member_count_mismatch");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1042:                                 kMaximumRecommendationCampaignOutcomeAssessmentMembers))
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1044:                "recommendation_campaign_outcome_assessment_member_count_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1088:                    "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1190:        RecommendationCampaignOutcomeAssessmentSummary summary(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1211:        RecommendationCampaignOutcomeAssessmentIdentity identity(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1212:            kRecommendationCampaignOutcomeAssessmentContractVersion,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1214:        return RecommendationCampaignOutcomeAssessment(std::move(identity),
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1220:RecommendationCampaignOutcomeAssessment BuildRecommendationCampaignOutcomeAssessment(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1221:    const RecommendationCampaignOutcomeAssessmentCampaignIdentity&
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1223:    const RecommendationCampaignOutcomeAssessmentMaterializationIdentity&
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1228:    return RecommendationCampaignOutcomeAssessmentBuilder::Build(
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1240:            "recommendation_campaign_outcome_assessment_enum_invalid");      \
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1245:    RecommendationCampaignOutcomeAssessmentMetricComparisonSupportText,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1246:    RecommendationCampaignOutcomeAssessmentMetricComparisonSupport,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1252:    RecommendationCampaignOutcomeAssessmentLifecycleStateText,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1253:    RecommendationCampaignOutcomeAssessmentLifecycleState,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1261:    RecommendationCampaignOutcomeAssessmentConsistencyStateText,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1262:    RecommendationCampaignOutcomeAssessmentConsistencyState,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1267:    RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1268:    RecommendationCampaignOutcomeAssessmentDiagnosticCode,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1293:    RecommendationCampaignOutcomeAssessmentMemberOutcomeText,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1294:    RecommendationCampaignOutcomeAssessmentMemberOutcome,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1310:    RecommendationCampaignOutcomeAssessmentMetricComparisonClassificationText,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1311:    RecommendationCampaignOutcomeAssessmentMetricComparisonClassification,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1325:    RecommendationCampaignOutcomeAssessmentContextDifferenceText,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1326:    RecommendationCampaignOutcomeAssessmentContextDifference,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1338:    RecommendationCampaignOutcomeAssessmentAggregateOutcomeText,
./Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1339:    RecommendationCampaignOutcomeAssessmentAggregateOutcome,
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:256:    RecommendationConversionActivationAssessment assessment;
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:257:    assessment.execution = FindRecommendationConversionExecution(
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:259:    if (!assessment.execution)
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:261:        assessment.state =
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:263:        assessment.reason = "conversion_execution_not_found";
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:264:        return assessment;
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:268:        transaction, assessment.execution->experimentId, lockExperiment);
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:269:    assessment.experimentStatus = experiment.status;
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:270:    assessment.experimentPhase = experiment.phase;
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:272:        transaction, *assessment.execution);
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:273:    const auto identity = ExpectedIdentity(*assessment.execution);
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:274:    assessment.activation = FindByColumn(
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:276:    if (assessment.activation)
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:279:                *assessment.activation, *assessment.execution, identity))
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:281:            assessment.state =
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:283:            assessment.reason = "activation_identity_conflict";
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:284:            return assessment;
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:286:        assessment.state = RecommendationConversionActivationAssessmentState::
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:288:        return assessment;
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:292:        assessment.state =
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:294:        assessment.reason = *reason;
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:295:        return assessment;
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:297:    assessment.state =
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:299:    return assessment;
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:307:    const auto assessment = AssessRecommendationConversionActivation(
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:309:    switch (assessment.state)
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:313:                    std::nullopt, assessment.reason};
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:316:                    std::nullopt, assessment.reason};
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:319:                    assessment.activation, assessment.reason};
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:323:                assessment.activation,
./Sources/ExperimentRecommendationConversionActivationRepository.cpp:329:    const auto& execution = *assessment.execution;
./Sources/ExperimentRecommendationConversionActivationRepository.hpp:82:// Read-only assessment of the authoritative Phase 4C activation contract.
./ExpertAdvisor.xcodeproj/project.pbxproj:152:		0B580002307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp in Sources */ = {isa = PBXBuildFile; fileRef = 0B580000307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp */; };
./ExpertAdvisor.xcodeproj/project.pbxproj:153:		0B580003307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp in Sources */ = {isa = PBXBuildFile; fileRef = 0B580000307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp */; };
./ExpertAdvisor.xcodeproj/project.pbxproj:383:		0B580000307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.cpp.cpp; name = ExperimentRecommendationCampaignOutcomeAssessment.cpp; path = Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp; sourceTree = SOURCE_ROOT; };
./ExpertAdvisor.xcodeproj/project.pbxproj:384:		0B580001307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.hpp */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.cpp.h; name = ExperimentRecommendationCampaignOutcomeAssessment.hpp; path = Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp; sourceTree = SOURCE_ROOT; };
./ExpertAdvisor.xcodeproj/project.pbxproj:582:				0B580000307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp */,
./ExpertAdvisor.xcodeproj/project.pbxproj:583:				0B580001307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.hpp */,
./ExpertAdvisor.xcodeproj/project.pbxproj:828:				0B580002307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp in Sources */,
./ExpertAdvisor.xcodeproj/project.pbxproj:902:				0B580003307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp in Sources */,

exec
/bin/bash -lc "rg --files -g '"'!DerivedData/**'"' -g '"'!build/**'"' -g '"'!Build/**'"' | sed -n '1,240p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
LSTM/ExperimentMetaAnalyzer.cpp
LSTM/main.cpp
LSTM/Tensor.cpp
LSTM/LSTM.cpp
MetaNN/MetaNN.workspace
MetaNN/MetaNN/facilities/_.h
MetaNN/MetaNN/facilities/var_type_dict.h
MetaNN/MetaNN/facilities/cont_metafuns/_.h
MetaNN/MetaNN/facilities/cont_metafuns/set.h
MetaNN/MetaNN/facilities/cont_metafuns/helpers.h
MetaNN/MetaNN/facilities/cont_metafuns/value_sequential.h
MetaNN/MetaNN/facilities/cont_metafuns/sequential.h
MetaNN/MetaNN/facilities/cont_metafuns/multi_map.h
MetaNN/MetaNN/facilities/cont_metafuns/map.h
MetaNN/MetaNN/facilities/type_id.h
MetaNN/MetaNN/facilities/null_param.h
MetaNN/MetaNN/facilities/traits.h
MetaNN/MetaNN/data/_.h
MetaNN/MetaNN/data/bias_vector.h
MetaNN/MetaNN/data/facilities/category_tags.h
MetaNN/MetaNN/data/facilities/continuous_memory.h
MetaNN/MetaNN/data/facilities/allocators.h
MetaNN/MetaNN/data/facilities/continuous_memory_metal.mm
MetaNN/MetaNN/data/facilities/device_tags.h
MetaNN/MetaNN/data/facilities/shape.h
MetaNN/MetaNN/data/facilities/lower_access.h
MetaNN/MetaNN/data/facilities/traits.h
MetaNN/MetaNN/data/tensor.h
MetaNN/MetaNN/data/batch/batch_sequence.h
MetaNN/MetaNN/data/dynamic.h
MetaNN/MetaNN/data/trivial_tensor.h
MetaNN/MetaNN/data/scalable_tensor.h
MetaNN/MetaNN/data/zero_tensor.h
MetaNN/MetaNN/model/_.h
MetaNN/MetaNN/model/facilities/weight_buffer.h
MetaNN/MetaNN/model/param_initializer/_.h
MetaNN/MetaNN/model/param_initializer/facilities/policies.h
MetaNN/MetaNN/model/param_initializer/facilities/fill_with_spec_dist.h
MetaNN/MetaNN/model/param_initializer/param_initializer.h
MetaNN/MetaNN/model/param_initializer/gaussian_filler.h
MetaNN/MetaNN/model/param_initializer/uniform_filler.h
MetaNN/MetaNN/model/param_initializer/var_scale_filler.h
MetaNN/MetaNN/model/param_initializer/constant_filler.h
MetaNN/MetaNN/model/weight_cont/_.h
MetaNN/MetaNN/model/weight_cont/grad_collector.h
MetaNN/MetaNN/model/weight_cont/load_buffer.h
MetaNN/MetaNN/data_copy/metal_copy.mm
MetaNN/MetaNN/data_copy/metal_copy.h
MetaNN/MetaNN/data_copy/data_copy.h
MetaNN/MetaNN/evaluate/eval_handle.h
MetaNN/MetaNN/evaluate/eval_plan.h
MetaNN/MetaNN/evaluate/eval_buffer.h
MetaNN/MetaNN/evaluate/eval_dispatcher.h
MetaNN/MetaNN/evaluate/eval_item.h
MetaNN/MetaNN/evaluate/eval_group.h
MetaNN/MetaNN/MetaNN.project
MetaNN/MetaNN/meta_nn.h
MetaNN/MetaNN/policies/_.h
MetaNN/MetaNN/policies/policy_container.h
MetaNN/MetaNN/policies/policy_macro_begin.h
MetaNN/MetaNN/policies/policy_macro_end.h
MetaNN/MetaNN/policies/policy_operations.h
MetaNN/MetaNN/MetaNN.metal
MetaNN/MetaNN/metal/add.metal
MetaNN/MetaNN/metal/metal_add.h
MetaNN/MetaNN/metal/metal_add.mm
MetaNN/MetaNN/metal/matmul.metal
MetaNN/MetaNN/metal/metal_matmul.h
MetaNN/MetaNN/metal/metal_matmul.mm
MetaNN/MetaNN/layers/_.h
MetaNN/MetaNN/layers/facilities/_.h
MetaNN/MetaNN/layers/facilities/layer_in_map.h
MetaNN/MetaNN/layers/facilities/interface_fun.h
MetaNN/MetaNN/layers/facilities/policies.h
MetaNN/MetaNN/layers/facilities/make_layer.h
MetaNN/MetaNN/layers/facilities/traits.h
MetaNN/MetaNN/layers/composite/_.h
MetaNN/MetaNN/layers/composite/bias_layer.h
MetaNN/MetaNN/layers/composite/compose_kernel.h
MetaNN/MetaNN/layers/composite/linear_layer.h
MetaNN/MetaNN/layers/composite/weight_layer.h
MetaNN/MetaNN/layers/composite/single_layer_perceptron.h
MetaNN/MetaNN/layers/math/_.h
MetaNN/MetaNN/layers/recurrent/_.h
MetaNN/MetaNN/layers/recurrent/gru_step.h
MetaNN/MetaNN/layers/recurrent/recurrent_layer.h
MetaNN/MetaNN/layers/principal/_.h
MetaNN/MetaNN/layers/principal/tensor/_.h
MetaNN/MetaNN/layers/principal/tensor/permute_layer.h
MetaNN/MetaNN/layers/principal/tensor/dot_layer.h
MetaNN/MetaNN/layers/principal/math/_.h
MetaNN/MetaNN/layers/principal/math/cos_layer.h
MetaNN/MetaNN/layers/principal/math/acosh_layer.h
MetaNN/MetaNN/layers/principal/math/substract_layer.h
MetaNN/MetaNN/layers/principal/math/acos_layer.h
MetaNN/MetaNN/layers/principal/math/asinh_layer.h
MetaNN/MetaNN/layers/principal/math/tan_layer.h
MetaNN/MetaNN/layers/principal/math/multiply_layer.h
MetaNN/MetaNN/layers/principal/math/tanh_layer.h
MetaNN/MetaNN/layers/principal/math/sin_layer.h
MetaNN/MetaNN/layers/principal/math/sigmoid_layer.h
MetaNN/MetaNN/layers/principal/math/asin_layer.h
MetaNN/MetaNN/layers/principal/math/square_layer.h
MetaNN/MetaNN/layers/principal/math/add_layer.h
MetaNN/MetaNN/layers/principal/math/abs_layer.h
MetaNN/MetaNN/layers/principal/others/_.h
MetaNN/MetaNN/layers/principal/others/param_source_layer.h
MetaNN/MetaNN/layers/principal/others/value_source_layer.h
MetaNN/MetaNN/layers/principal/others/interpolate_layer.h
MetaNN/MetaNN/layers/principal/nn/_.h
MetaNN/MetaNN/layers/principal/nn/nll_loss_layer.h
MetaNN/MetaNN/layers/principal/nn/relu_layer.h
MetaNN/MetaNN/layers/principal/nn/softmax_layer.h
migrate_lstm_db.sh
MetaNN/MetaNN/operation/_.h
MetaNN/readme.md
MetaNN/readme_eng.md
MetaNN/MetaNN/operation/conv.h
MetaNN/MetaNN/operation/facilities/_.h
MetaNN/MetaNN/operation/facilities/tail_calculator.h
MetaNN/MetaNN/operation/facilities/instance_id.h
MetaNN/MetaNN/operation/facilities/tags.h
MetaNN/MetaNN/operation/facilities/organizer.h
MetaNN/MetaNN/operation/facilities/policies.h
MetaNN/MetaNN/operation/facilities/operation_frame.h
MetaNN/MetaNN/operation/tensor/_.h
MetaNN/MetaNN/operation/tensor/dot.h
MetaNN/MetaNN/operation/tensor/reshape.h
MetaNN/MetaNN/operation/tensor/slice.h
MetaNN/MetaNN/operation/tensor/tile.h
MetaNN/MetaNN/operation/tensor/permute.h
docs/Phase4CExperimentRecommendationConversionProposalReview.rst
docs/Phase4CExperimentRecommendationConversionWorkflow.rst
docs/Phase4BExperimentRecommendationRanking.rst
docs/Phase5ExperimentRecommendationCampaignExecution.rst
docs/Phase4DExperimentRecommendationCampaignMaterialization.rst
docs/Phase4DExperimentRecommendationCampaignProposalReview.rst
docs/Phase4DExperimentRecommendationCampaignPlanning.rst
docs/Phase4CExperimentRecommendationConversionActivation.rst
docs/Phase4CExperimentRecommendationConversion.rst
Tests/ContinuationPolicyInheritanceTests.cpp
Tests/ExperimentRecommendationCampaignProposalReviewTests.cpp
Tests/ExperimentRecommendationCampaignApprovalTests.cpp
Tests/ExperimentRecommendationRepositoryTests.cpp
Tests/ExperimentRecommendationCampaignMaterializationRepositoryTests.cpp
Tests/ExperimentRecommendationRankingTests.cpp
Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
Tests/ExperimentRecommendationCandidateGeneratorTests.cpp
Tests/ExperimentRecommendationCampaignExecutionTests.cpp
Tests/ExperimentRecommendationConversionProposalReviewTests.cpp
Tests/ExperimentRecommendationConversionWorkflowRepositoryTests.cpp
Tests/ExperimentRecommendationCampaignPlanningTests.cpp
Tests/ExperimentRecommendationEvaluationTests.cpp
Tests/ExperimentRecommendationConversionMigrationTests.sql
Tests/ExperimentRecommendationCampaignActivationTests.cpp
Tests/ExperimentRecommendationRankingRepositoryTests.cpp
Tests/ExperimentRecommendationConversionExecutionRepositoryTests.cpp
Tests/ExperimentRecommendationReviewMigrationTests.sql
Tests/ExperimentRecommendationCampaignMaterializationMigrationTests.sql
Tests/ExperimentRecommendationConversionWorkflowTests.cpp
Tests/ExperimentRecommendationConversionExecutionMigrationTests.sql
Tests/ExperimentRecommendationScoringMigrationTests.sql
Tests/ExperimentRecommendationConversionProposalReviewMigrationTests.sql
Tests/ExperimentRecommendationReviewTests.cpp
Tests/ExperimentRecommendationConversionTests.cpp
Tests/ExperimentRecommendationEvaluationRepositoryTests.cpp
Tests/ExperimentRecommendationReviewRepositoryTests.cpp
Tests/ExperimentRecommendationConversionProposalReviewRepositoryTests.cpp
Tests/ExperimentRecommendationConversionRepositoryTests.cpp
Tests/ExperimentRecommendationServiceTests.cpp
Tests/ExperimentRecommendationScoringRepositoryTests.cpp
Tests/ExperimentRecommendationCampaignStatusTests.cpp
Tests/ExperimentRecommendationScoringTests.cpp
Tests/ExperimentRecommendationConversionActivationTests.cpp
Tests/ExperimentRecommendationRankingMigrationTests.sql
Tests/ExperimentRecommendationCampaignPlanningRepositoryTests.cpp
Tests/ExperimentRecommendationConversionActivationRepositoryTests.cpp
Tests/ContinuationPolicyPersistenceTests.cpp
Tests/ExperimentRecommendationCampaignReviewTests.cpp
Tests/ExperimentRecommendationCampaignActivationRepositoryTests.cpp
Tests/ExperimentRecommendationCampaignMaterializationTests.cpp
Tests/ExperimentRecommendationTests.cpp
Tests/ExperimentRecommendationCampaignHandoffRepositoryTests.cpp
Tests/ExperimentRecommendationEvaluationMigrationTests.sql
Tests/ExperimentRecommendationCampaignApprovalRepositoryTests.cpp
Tests/ExperimentRecommendationCampaignHandoffTests.cpp
Tests/SchedulerChildStatusTests.cpp
Tests/ExperimentRecommendationConversionActivationMigrationTests.sql
Tests/ExperimentRecommendationCampaignProposalReviewRepositoryTests.cpp
Tests/ExperimentRecommendationCampaignExecutionRepositoryTests.cpp
Tests/ExperimentRecommendationPersistenceTests.cpp
Tests/ExperimentRecommendationCampaignStatusRepositoryTests.cpp
Tests/ExperimentRecommendationCampaignLaunchRepositoryTests.cpp
Tests/ExperimentRecommendationCampaignApprovalMigrationTests.sql
Tests/ExperimentRecommendationCampaignLaunchTests.cpp
docs/Phase4AExperimentRecommendationReview.rst
docs/Phase5ExperimentRecommendationCampaignStatus.rst
docs/Phase4DExperimentRecommendationCampaignHandoff.rst
docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
docs/Phase4DExperimentRecommendationCampaignApproval.rst
docs/Phase4AExperimentRecommendationFoundation.rst
docs/architecture/VOLUME_TEMPLATE.md
docs/architecture/Volume_III_Label_Generation.md
docs/architecture/Volume_X_Research_Automation.md
docs/architecture/Volume_XI_Scheduler.md
docs/architecture/Volume_VI_Inference_Evaluation.md
docs/architecture/Volume_XII_Database.md
docs/architecture/Volume_VII_Experiment_Lifecycle.md
docs/architecture/README.md
docs/architecture/Volume_II_Data_Pipeline.md
docs/architecture/Volume_I_Foundation.md
docs/architecture/Volume_IX_Trading_Profitability.md
make_lstm_results_csv.sh
MetaNN/license
extract_lstm_analysis.sh
docs/Phase4CExperimentRecommendationConversionPersistence.rst
docs/Phase4AExperimentRecommendationScoring.rst
docs/Phase5ExperimentRecommendationCampaignActivation.rst
docs/Phase4CExperimentRecommendationConversionExecution.rst
docs/Phase4AExperimentRecommendationPersistence.rst
docs/Phase5ExperimentRecommendationCampaignLaunch.rst
docs/architecture/Volume_IV_Model_Architecture.md
docs/Phase4DExperimentRecommendationCampaignReview.rst
docs/Phase4BExperimentRecommendationEvaluation.rst
docs/architecture/Volume_VIII_Recommendation_Engine.md
docs/architecture/Volume_V_Training_Engine.md
Common/PricePoint.cpp
Common/db_cursor.cpp
MetaNN/Tests/calculate_tags.h
docs/architecture/adr/ADR-0004-scheduler-ownership-boundaries.md
docs/architecture/adr/ADR-0005-manual-recommendation-conversion.md
docs/architecture/adr/README.md
docs/architecture/adr/ADR-0001-postgresql-source-of-truth.md
docs/architecture/adr/ADR-0003-advisory-recommendation-evaluation.md
docs/architecture/adr/ADR_TEMPLATE.md
docs/architecture/adr/ADR-0002-deterministic-experiment-identity.md
MetaNN/MetalSwiftTest/main.mm
Database/backups/LSTM_latest.dump
Database/backups/LSTM_latest.dump.json
Database/README.md

exec
/bin/bash -lc "wc -l Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst docs/Phase5ExperimentRecommendationCampaignExecution.rst && sed -n '1,560p' Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
     519 Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
    1357 Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
     738 Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
      52 docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
     119 docs/Phase5ExperimentRecommendationCampaignExecution.rst
    2785 total
#pragma once

#include <optional>
#include <string>
#include <vector>

namespace EA::ExperimentRecommendation
{

inline constexpr int
    kRecommendationCampaignOutcomeAssessmentContractVersion = 2;
inline constexpr int
    kMaximumRecommendationCampaignOutcomeAssessmentMembers = 1000;

// These immutable value types describe a read-only, point-in-time,
// non-persistent, and non-authoritative evidence assessment. They intentionally
// contain no campaign success decision or follow-up authorization.
struct RecommendationCampaignOutcomeAssessmentCampaignIdentity
{
    const long long campaignApprovalId;
    const std::string identityCanonical;
    const std::string identityHash;

    RecommendationCampaignOutcomeAssessmentCampaignIdentity(
        long long campaignApprovalId,
        std::string identityCanonical,
        std::string identityHash);
    bool operator==(
        const RecommendationCampaignOutcomeAssessmentCampaignIdentity&) const =
        default;
};

struct RecommendationCampaignOutcomeAssessmentMaterializationIdentity
{
    const long long materializationId;
    const long long campaignApprovalId;
    const int contractVersion;
    const int memberCount;
    const std::string identityCanonical;
    const std::string identityHash;

    RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
        long long materializationId,
        long long campaignApprovalId,
        int contractVersion,
        int memberCount,
        std::string identityCanonical,
        std::string identityHash);
    bool operator==(
        const RecommendationCampaignOutcomeAssessmentMaterializationIdentity&)
        const = default;
};

struct RecommendationCampaignOutcomeAssessmentMemberIdentity
{
    const int memberOrdinal;
    const long long materializationMemberId;
    const long long rankingMemberId;
    const long long recommendationId;
    const long long sourceExperimentId;
    const long long proposalId;
    const std::optional<long long> expectedExperimentId;

    RecommendationCampaignOutcomeAssessmentMemberIdentity(
        int memberOrdinal,
        long long materializationMemberId,
        long long rankingMemberId,
        long long recommendationId,
        long long sourceExperimentId,
        long long proposalId,
        std::optional<long long> expectedExperimentId);
    bool operator==(
        const RecommendationCampaignOutcomeAssessmentMemberIdentity&) const =
        default;
};

enum class RecommendationCampaignOutcomeAssessmentMetricComparisonSupport
{
    NumericDelta,
    Unsupported
};

struct RecommendationCampaignOutcomeAssessmentMetric
{
    const std::string identity;
    const std::optional<double> value;
    const RecommendationCampaignOutcomeAssessmentMetricComparisonSupport
        comparisonSupport;

    RecommendationCampaignOutcomeAssessmentMetric(
        std::string identity,
        std::optional<double> value,
        RecommendationCampaignOutcomeAssessmentMetricComparisonSupport
            comparisonSupport);
    bool operator==(
        const RecommendationCampaignOutcomeAssessmentMetric&) const = default;
};

struct RecommendationCampaignOutcomeAssessmentMetricCollection
{
    // Always sorted by metric identity. Duplicate identities are rejected.
    const std::vector<RecommendationCampaignOutcomeAssessmentMetric> metrics;

    explicit RecommendationCampaignOutcomeAssessmentMetricCollection(
        std::vector<RecommendationCampaignOutcomeAssessmentMetric> metrics =
            {});
    bool operator==(
        const RecommendationCampaignOutcomeAssessmentMetricCollection&) const =
        default;
};

struct RecommendationCampaignOutcomeAssessmentComparisonContext
{
    const std::string symbol;
    const int predictionHorizon;
    const double threshold;
    const int windowSize;
    const std::string labelDefinition;
    const std::string inferenceRangeStart;
    const std::string inferenceRangeEnd;

    RecommendationCampaignOutcomeAssessmentComparisonContext(
        std::string symbol,
        int predictionHorizon,
        double threshold,
        int windowSize,
        std::string labelDefinition,
        std::string inferenceRangeStart,
        std::string inferenceRangeEnd);
    bool operator==(
        const RecommendationCampaignOutcomeAssessmentComparisonContext&) const =
        default;
};

struct RecommendationCampaignOutcomeAssessmentSourceEvidence
{
    const long long sourceExperimentId;
    const std::optional<long long> sourceModelId;
    const std::optional<long long> sourceAnalysisId;
    const RecommendationCampaignOutcomeAssessmentComparisonContext context;
    const RecommendationCampaignOutcomeAssessmentMetricCollection metrics;

    RecommendationCampaignOutcomeAssessmentSourceEvidence(
        long long sourceExperimentId,
        std::optional<long long> sourceModelId,
        std::optional<long long> sourceAnalysisId,
        RecommendationCampaignOutcomeAssessmentComparisonContext context,
        RecommendationCampaignOutcomeAssessmentMetricCollection metrics);
    bool operator==(
        const RecommendationCampaignOutcomeAssessmentSourceEvidence&) const =
        default;
};

struct RecommendationCampaignOutcomeAssessmentResultIdentity
{
    const std::string kind;
    const long long id;

    RecommendationCampaignOutcomeAssessmentResultIdentity(
        std::string kind,
        long long id);
    bool operator==(
        const RecommendationCampaignOutcomeAssessmentResultIdentity&) const =
        default;
};

struct RecommendationCampaignOutcomeAssessmentResultEvidence
{
    const std::optional<long long> experimentId;
    const std::optional<long long> modelId;
    // Always sorted by (kind, id). Exact duplicates are rejected.
    const std::vector<RecommendationCampaignOutcomeAssessmentResultIdentity>
        resultIdentities;
    const std::optional<
        RecommendationCampaignOutcomeAssessmentComparisonContext>
        context;
    const RecommendationCampaignOutcomeAssessmentMetricCollection metrics;

    RecommendationCampaignOutcomeAssessmentResultEvidence(
        std::optional<long long> experimentId,
        std::optional<long long> modelId,
        std::vector<RecommendationCampaignOutcomeAssessmentResultIdentity>
            resultIdentities,
        std::optional<RecommendationCampaignOutcomeAssessmentComparisonContext>
            context,
        RecommendationCampaignOutcomeAssessmentMetricCollection metrics);
    bool operator==(
        const RecommendationCampaignOutcomeAssessmentResultEvidence&) const =
        default;
};

enum class RecommendationCampaignOutcomeAssessmentLifecycleState
{
    NotTerminal,
    Succeeded,
    Failed,
    Cancelled,
    Unknown
};

struct RecommendationCampaignOutcomeAssessmentMemberEvidence
{
    const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
    const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
    const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
        sourceEvidence;
    const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
        resultEvidence;

    RecommendationCampaignOutcomeAssessmentMemberEvidence(
        RecommendationCampaignOutcomeAssessmentMemberIdentity identity,
        RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle,
        std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
            sourceEvidence,
        std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
            resultEvidence);
    bool operator==(
        const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
        default;
};

enum class RecommendationCampaignOutcomeAssessmentConsistencyState
{
    Consistent,
    Inconsistent
};

enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
{
    SourceExperimentIdentityMismatch,
    ExpectedExperimentIdentityMissing,
    ResultEvidenceMissing,
    ResultExperimentIdentityMissing,
    ResultExperimentIdentityMismatch,
    ResultModelIdentityMissing,
    ResultIdentityMissing,
    ResultContextMissing,
    ResultExperimentReused,
    ResultIdentityReused,
    NumericDeltaNonFinite
};

enum class RecommendationCampaignOutcomeAssessmentMemberOutcome
{
    NotReady,
    SucceededComparable,
    SucceededContextChanged,
    SucceededMetricGap,
    TerminalFailed,
    TerminalCancelled,
    Inconsistent
};

enum class RecommendationCampaignOutcomeAssessmentMetricComparisonClassification
{
    Comparable,
    ContextChanged,
    MissingSourceMetric,
    MissingResultMetric,
    MetricValueUnavailable,
    Unsupported
};

enum class RecommendationCampaignOutcomeAssessmentContextDifference
{
    Symbol,
    PredictionHorizon,
    Threshold,
    WindowSize,
    LabelDefinition,
    InferenceRange
};

struct RecommendationCampaignOutcomeAssessmentBuilder;

struct RecommendationCampaignOutcomeAssessmentMetricComparison
{
    const std::string metricIdentity;
    const RecommendationCampaignOutcomeAssessmentMetricComparisonClassification
        classification;
    const std::optional<double> sourceValue;
    const std::optional<double> resultValue;
    const std::optional<double> delta;
    const std::vector<
        RecommendationCampaignOutcomeAssessmentContextDifference>
        contextDifferences;

    RecommendationCampaignOutcomeAssessmentMetricComparison(
        const RecommendationCampaignOutcomeAssessmentMetricComparison&) =
        default;
    RecommendationCampaignOutcomeAssessmentMetricComparison(
        RecommendationCampaignOutcomeAssessmentMetricComparison&&) = default;
    bool operator==(
        const RecommendationCampaignOutcomeAssessmentMetricComparison&) const =
        default;

private:
    RecommendationCampaignOutcomeAssessmentMetricComparison(
        std::string metricIdentity,
        RecommendationCampaignOutcomeAssessmentMetricComparisonClassification
            classification,
        std::optional<double> sourceValue,
        std::optional<double> resultValue,
        std::optional<double> delta,
        std::vector<RecommendationCampaignOutcomeAssessmentContextDifference>
            contextDifferences);
    friend struct RecommendationCampaignOutcomeAssessmentBuilder;
};

enum class RecommendationCampaignOutcomeAssessmentAggregateOutcome
{
    Inconsistent,
    HasFailures,
    HasCancellations,
    NotReady,
    SucceededContextChanged,
    SucceededMetricGap,
    SucceededComparable
};

struct RecommendationCampaignOutcomeAssessmentSummary
{
    const RecommendationCampaignOutcomeAssessmentAggregateOutcome outcome;

    const int memberCount;
    const int notTerminalLifecycleCount;
    const int succeededLifecycleCount;
    const int failedLifecycleCount;
    const int cancelledLifecycleCount;
    const int unknownLifecycleCount;
    const int consistentMemberCount;
    const int inconsistentMemberCount;

    const int notReadyMemberCount;
    const int succeededComparableMemberCount;
    const int succeededContextChangedMemberCount;
    const int succeededMetricGapMemberCount;
    const int terminalFailedMemberCount;
    const int terminalCancelledMemberCount;
    const int inconsistentOutcomeMemberCount;

    const int metricComparisonCount;
    const int comparableMetricCount;
    const int contextChangedMetricCount;
    const int missingSourceMetricCount;
    const int missingResultMetricCount;
    const int metricValueUnavailableCount;
    const int unsupportedMetricCount;
    const int positiveDeltaCount;
    const int zeroDeltaCount;
    const int negativeDeltaCount;

    RecommendationCampaignOutcomeAssessmentSummary(
        const RecommendationCampaignOutcomeAssessmentSummary&) = default;
    RecommendationCampaignOutcomeAssessmentSummary(
        RecommendationCampaignOutcomeAssessmentSummary&&) = default;
    bool operator==(
        const RecommendationCampaignOutcomeAssessmentSummary&) const = default;

private:
    RecommendationCampaignOutcomeAssessmentSummary(
        RecommendationCampaignOutcomeAssessmentAggregateOutcome outcome,
        int memberCount,
        int notTerminalLifecycleCount,
        int succeededLifecycleCount,
        int failedLifecycleCount,
        int cancelledLifecycleCount,
        int unknownLifecycleCount,
        int consistentMemberCount,
        int inconsistentMemberCount,
        int notReadyMemberCount,
        int succeededComparableMemberCount,
        int succeededContextChangedMemberCount,
        int succeededMetricGapMemberCount,
        int terminalFailedMemberCount,
        int terminalCancelledMemberCount,
        int inconsistentOutcomeMemberCount,
        int metricComparisonCount,
        int comparableMetricCount,
        int contextChangedMetricCount,
        int missingSourceMetricCount,
        int missingResultMetricCount,
        int metricValueUnavailableCount,
        int unsupportedMetricCount,
        int positiveDeltaCount,
        int zeroDeltaCount,
        int negativeDeltaCount);
    friend struct RecommendationCampaignOutcomeAssessmentBuilder;
};

struct RecommendationCampaignOutcomeAssessmentMember
{
    const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
    const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
    const RecommendationCampaignOutcomeAssessmentConsistencyState consistency;
    const RecommendationCampaignOutcomeAssessmentMemberOutcome outcome;
    // Empty for consistent members; otherwise sorted in fixed code order.
    const std::vector<RecommendationCampaignOutcomeAssessmentDiagnosticCode>
        diagnostics;
    const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
        sourceEvidence;
    const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
        resultEvidence;
    // One entry per metric identity in the source/result union, sorted by identity.
    const std::vector<
        RecommendationCampaignOutcomeAssessmentMetricComparison>
        comparisons;

    RecommendationCampaignOutcomeAssessmentMember(
        const RecommendationCampaignOutcomeAssessmentMember&) = default;
    RecommendationCampaignOutcomeAssessmentMember(
        RecommendationCampaignOutcomeAssessmentMember&&) = default;
    bool operator==(
        const RecommendationCampaignOutcomeAssessmentMember&) const = default;

private:
    RecommendationCampaignOutcomeAssessmentMember(
        RecommendationCampaignOutcomeAssessmentMemberIdentity identity,
        RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle,
        RecommendationCampaignOutcomeAssessmentConsistencyState consistency,
        RecommendationCampaignOutcomeAssessmentMemberOutcome outcome,
        std::vector<RecommendationCampaignOutcomeAssessmentDiagnosticCode>
            diagnostics,
        std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
            sourceEvidence,
        std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
            resultEvidence,
        std::vector<RecommendationCampaignOutcomeAssessmentMetricComparison>
            comparisons);
    friend struct RecommendationCampaignOutcomeAssessmentBuilder;
};

struct RecommendationCampaignOutcomeAssessmentIdentity
{
    const int contractVersion;
    const std::string canonicalText;
    const std::string hash;

    RecommendationCampaignOutcomeAssessmentIdentity(
        const RecommendationCampaignOutcomeAssessmentIdentity&) = default;
    RecommendationCampaignOutcomeAssessmentIdentity(
        RecommendationCampaignOutcomeAssessmentIdentity&&) = default;
    bool operator==(
        const RecommendationCampaignOutcomeAssessmentIdentity&) const = default;

private:
    RecommendationCampaignOutcomeAssessmentIdentity(
        int contractVersion,
        std::string canonicalText,
        std::string hash);
    friend struct RecommendationCampaignOutcomeAssessmentBuilder;
};

struct RecommendationCampaignOutcomeAssessment
{
    static constexpr bool readOnly = true;
    static constexpr bool pointInTime = true;
    static constexpr bool persistent = false;
    static constexpr bool authoritative = false;
    static constexpr bool declaresCampaignSuccess = false;

    const RecommendationCampaignOutcomeAssessmentIdentity identity;
    const RecommendationCampaignOutcomeAssessmentCampaignIdentity
        campaignIdentity;
    const RecommendationCampaignOutcomeAssessmentMaterializationIdentity
        materializationIdentity;
    const std::string observedAt;
    const RecommendationCampaignOutcomeAssessmentSummary summary;
    const std::vector<RecommendationCampaignOutcomeAssessmentMember> members;

    RecommendationCampaignOutcomeAssessment(
        const RecommendationCampaignOutcomeAssessment&) = default;
    RecommendationCampaignOutcomeAssessment(
        RecommendationCampaignOutcomeAssessment&&) = default;
    bool operator==(
        const RecommendationCampaignOutcomeAssessment&) const = default;

private:
    RecommendationCampaignOutcomeAssessment(
        RecommendationCampaignOutcomeAssessmentIdentity identity,
        RecommendationCampaignOutcomeAssessmentCampaignIdentity
            campaignIdentity,
        RecommendationCampaignOutcomeAssessmentMaterializationIdentity
            materializationIdentity,
        std::string observedAt,
        RecommendationCampaignOutcomeAssessmentSummary summary,
        std::vector<RecommendationCampaignOutcomeAssessmentMember> members);
    friend struct RecommendationCampaignOutcomeAssessmentBuilder;
};

RecommendationCampaignOutcomeAssessment BuildRecommendationCampaignOutcomeAssessment(
    const RecommendationCampaignOutcomeAssessmentCampaignIdentity&
        campaignIdentity,
    const RecommendationCampaignOutcomeAssessmentMaterializationIdentity&
        materializationIdentity,
    const std::string& observedAt,
    const std::vector<RecommendationCampaignOutcomeAssessmentMemberEvidence>&
        members);

std::string RecommendationCampaignOutcomeAssessmentMetricComparisonSupportText(
    RecommendationCampaignOutcomeAssessmentMetricComparisonSupport value);
std::string RecommendationCampaignOutcomeAssessmentLifecycleStateText(
    RecommendationCampaignOutcomeAssessmentLifecycleState value);
std::string RecommendationCampaignOutcomeAssessmentConsistencyStateText(
    RecommendationCampaignOutcomeAssessmentConsistencyState value);
std::string RecommendationCampaignOutcomeAssessmentDiagnosticCodeText(
    RecommendationCampaignOutcomeAssessmentDiagnosticCode value);
std::string RecommendationCampaignOutcomeAssessmentMemberOutcomeText(
    RecommendationCampaignOutcomeAssessmentMemberOutcome value);
std::string
RecommendationCampaignOutcomeAssessmentMetricComparisonClassificationText(
    RecommendationCampaignOutcomeAssessmentMetricComparisonClassification
        value);
std::string RecommendationCampaignOutcomeAssessmentContextDifferenceText(
    RecommendationCampaignOutcomeAssessmentContextDifference value);
std::string RecommendationCampaignOutcomeAssessmentAggregateOutcomeText(
    RecommendationCampaignOutcomeAssessmentAggregateOutcome value);

} // namespace EA::ExperimentRecommendation

exec
/bin/bash -lc "sed -n '1,470p' Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp && sed -n '471,940p' Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
#include "ExperimentRecommendationCampaignOutcomeAssessment.hpp"

#include "ExperimentRecommendation.hpp"
#include "ExperimentRecommendationCampaignMaterialization.hpp"

#include <algorithm>
#include <cmath>
#include <locale>
#include <map>
#include <set>
#include <sstream>
#include <stdexcept>
#include <tuple>
#include <utility>

namespace EA::ExperimentRecommendation
{
namespace
{

using AggregateOutcome =
    RecommendationCampaignOutcomeAssessmentAggregateOutcome;
using Classification =
    RecommendationCampaignOutcomeAssessmentMetricComparisonClassification;
using Comparison =
    RecommendationCampaignOutcomeAssessmentMetricComparison;
using Consistency = RecommendationCampaignOutcomeAssessmentConsistencyState;
using Context = RecommendationCampaignOutcomeAssessmentComparisonContext;
using Diagnostic = RecommendationCampaignOutcomeAssessmentDiagnosticCode;
using Difference = RecommendationCampaignOutcomeAssessmentContextDifference;
using Lifecycle = RecommendationCampaignOutcomeAssessmentLifecycleState;
using Member = RecommendationCampaignOutcomeAssessmentMember;
using MemberEvidence = RecommendationCampaignOutcomeAssessmentMemberEvidence;
using MemberOutcome = RecommendationCampaignOutcomeAssessmentMemberOutcome;
using Metric = RecommendationCampaignOutcomeAssessmentMetric;
using MetricCollection =
    RecommendationCampaignOutcomeAssessmentMetricCollection;
using MetricSupport =
    RecommendationCampaignOutcomeAssessmentMetricComparisonSupport;
using ResultIdentity =
    RecommendationCampaignOutcomeAssessmentResultIdentity;

void RequireText(const std::string& value, const char* error)
{
    if (value.empty() || value.find('\0') != std::string::npos)
        throw std::invalid_argument(error);
}

void RequireOptionalPositive(
    const std::optional<long long>& value,
    const char* error)
{
    if (value && *value <= 0) throw std::invalid_argument(error);
}

void RequireValidMetricSupport(MetricSupport value)
{
    switch (value)
    {
        case MetricSupport::NumericDelta:
        case MetricSupport::Unsupported:
            return;
    }
    throw std::invalid_argument(
        "recommendation_campaign_outcome_assessment_metric_support_invalid");
}

void RequireValidLifecycle(Lifecycle value)
{
    switch (value)
    {
        case Lifecycle::NotTerminal:
        case Lifecycle::Succeeded:
        case Lifecycle::Failed:
        case Lifecycle::Cancelled:
        case Lifecycle::Unknown:
            return;
    }
    throw std::invalid_argument(
        "recommendation_campaign_outcome_assessment_lifecycle_invalid");
}

std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
{
    std::vector<const Metric*> ordered;
    ordered.reserve(metrics.size());
    for (const auto& metric : metrics) ordered.push_back(&metric);
    std::sort(ordered.begin(), ordered.end(), [](const Metric* left,
                                              const Metric* right)
    {
        return left->identity < right->identity;
    });
    for (std::size_t index = 1; index < ordered.size(); ++index)
        if (ordered[index - 1]->identity == ordered[index]->identity)
            throw std::invalid_argument(
                "recommendation_campaign_outcome_assessment_metric_identity_duplicate");
    std::vector<Metric> canonical;
    canonical.reserve(ordered.size());
    for (const auto* metric : ordered) canonical.push_back(*metric);
    return canonical;
}

std::vector<ResultIdentity> CanonicalResultIdentities(
    std::vector<ResultIdentity> identities)
{
    std::vector<const ResultIdentity*> ordered;
    ordered.reserve(identities.size());
    for (const auto& identity : identities) ordered.push_back(&identity);
    std::sort(ordered.begin(), ordered.end(),
        [](const ResultIdentity* left, const ResultIdentity* right)
    {
        return std::tie(left->kind, left->id) <
            std::tie(right->kind, right->id);
    });
    for (std::size_t index = 1; index < ordered.size(); ++index)
        if (*ordered[index - 1] == *ordered[index])
            throw std::invalid_argument(
                "recommendation_campaign_outcome_assessment_result_identity_duplicate");
    std::vector<ResultIdentity> canonical;
    canonical.reserve(ordered.size());
    for (const auto* identity : ordered) canonical.push_back(*identity);
    return canonical;
}

std::string LengthPrefixed(const std::string& value)
{
    return std::to_string(value.size()) + ":" + value;
}

std::string OptionalLongLong(const std::optional<long long>& value)
{
    return value ? std::to_string(*value) : "null";
}

std::string OptionalDouble(const std::optional<double>& value)
{
    return value ? CanonicalRecommendationDouble(*value) : "null";
}

void AppendContext(std::ostringstream& out, const Context& context)
{
    out << LengthPrefixed(context.symbol) << ',' << context.predictionHorizon
        << ',' << CanonicalRecommendationDouble(context.threshold) << ','
        << context.windowSize << ',' << LengthPrefixed(context.labelDefinition)
        << ',' << LengthPrefixed(context.inferenceRangeStart) << ','
        << LengthPrefixed(context.inferenceRangeEnd);
}

void AppendOptionalContext(
    std::ostringstream& out,
    const std::optional<Context>& context)
{
    if (!context)
    {
        out << "absent";
        return;
    }
    out << "present,";
    AppendContext(out, *context);
}

void AppendMetric(std::ostringstream& out, const Metric& metric)
{
    out << LengthPrefixed(metric.identity) << ','
        << OptionalDouble(metric.value) << ','
        << RecommendationCampaignOutcomeAssessmentMetricComparisonSupportText(
               metric.comparisonSupport);
}

void AppendMetrics(std::ostringstream& out, const MetricCollection& collection)
{
    out << collection.metrics.size();
    for (const auto& metric : collection.metrics)
    {
        out << ',';
        AppendMetric(out, metric);
    }
}

void AppendSourceEvidence(
    std::ostringstream& out,
    const std::optional<
        RecommendationCampaignOutcomeAssessmentSourceEvidence>& evidence)
{
    if (!evidence)
    {
        out << "absent";
        return;
    }
    out << "present," << evidence->sourceExperimentId << ','
        << OptionalLongLong(evidence->sourceModelId) << ','
        << OptionalLongLong(evidence->sourceAnalysisId) << ",context=";
    AppendContext(out, evidence->context);
    out << ",metrics=";
    AppendMetrics(out, evidence->metrics);
}

void AppendResultEvidence(
    std::ostringstream& out,
    const std::optional<
        RecommendationCampaignOutcomeAssessmentResultEvidence>& evidence)
{
    if (!evidence)
    {
        out << "absent";
        return;
    }
    out << "present," << OptionalLongLong(evidence->experimentId) << ','
        << OptionalLongLong(evidence->modelId)
        << ",result_identity_count=" << evidence->resultIdentities.size();
    for (const auto& identity : evidence->resultIdentities)
        out << ',' << LengthPrefixed(identity.kind) << ',' << identity.id;
    out << ",context=";
    AppendOptionalContext(out, evidence->context);
    out << ",metrics=";
    AppendMetrics(out, evidence->metrics);
}

void AppendComparison(std::ostringstream& out, const Comparison& comparison)
{
    out << LengthPrefixed(comparison.metricIdentity) << ','
        << RecommendationCampaignOutcomeAssessmentMetricComparisonClassificationText(
               comparison.classification)
        << ",source_value=" << OptionalDouble(comparison.sourceValue)
        << ",result_value=" << OptionalDouble(comparison.resultValue)
        << ",delta=" << OptionalDouble(comparison.delta)
        << ",context_difference_count="
        << comparison.contextDifferences.size();
    for (const auto difference : comparison.contextDifferences)
        out << ','
            << RecommendationCampaignOutcomeAssessmentContextDifferenceText(
                   difference);
}

void AppendSummary(
    std::ostringstream& out,
    const RecommendationCampaignOutcomeAssessmentSummary& summary)
{
    out << ";aggregate_outcome="
        << RecommendationCampaignOutcomeAssessmentAggregateOutcomeText(
               summary.outcome)
        << ";member_count=" << summary.memberCount
        << ";lifecycle_not_terminal_count="
        << summary.notTerminalLifecycleCount
        << ";lifecycle_succeeded_count=" << summary.succeededLifecycleCount
        << ";lifecycle_failed_count=" << summary.failedLifecycleCount
        << ";lifecycle_cancelled_count=" << summary.cancelledLifecycleCount
        << ";lifecycle_unknown_count=" << summary.unknownLifecycleCount
        << ";consistent_member_count=" << summary.consistentMemberCount
        << ";inconsistent_member_count=" << summary.inconsistentMemberCount
        << ";not_ready_member_count=" << summary.notReadyMemberCount
        << ";succeeded_comparable_member_count="
        << summary.succeededComparableMemberCount
        << ";succeeded_context_changed_member_count="
        << summary.succeededContextChangedMemberCount
        << ";succeeded_metric_gap_member_count="
        << summary.succeededMetricGapMemberCount
        << ";terminal_failed_member_count="
        << summary.terminalFailedMemberCount
        << ";terminal_cancelled_member_count="
        << summary.terminalCancelledMemberCount
        << ";inconsistent_outcome_member_count="
        << summary.inconsistentOutcomeMemberCount
        << ";metric_comparison_count=" << summary.metricComparisonCount
        << ";comparable_metric_count=" << summary.comparableMetricCount
        << ";context_changed_metric_count="
        << summary.contextChangedMetricCount
        << ";missing_source_metric_count="
        << summary.missingSourceMetricCount
        << ";missing_result_metric_count="
        << summary.missingResultMetricCount
        << ";metric_value_unavailable_count="
        << summary.metricValueUnavailableCount
        << ";unsupported_metric_count=" << summary.unsupportedMetricCount
        << ";positive_delta_count=" << summary.positiveDeltaCount
        << ";zero_delta_count=" << summary.zeroDeltaCount
        << ";negative_delta_count=" << summary.negativeDeltaCount;
}

std::string AssessmentCanonicalText(
    const RecommendationCampaignOutcomeAssessmentCampaignIdentity& campaign,
    const RecommendationCampaignOutcomeAssessmentMaterializationIdentity&
        materialization,
    const RecommendationCampaignOutcomeAssessmentSummary& summary,
    const std::vector<Member>& members)
{
    std::ostringstream out;
    out.imbue(std::locale::classic());
    out << "experiment_recommendation_campaign_outcome_assessment_v2"
        << ";assessment_contract_version="
        << kRecommendationCampaignOutcomeAssessmentContractVersion
        << ";campaign_approval_id=" << campaign.campaignApprovalId
        << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
        << ";materialization_id=" << materialization.materializationId
        << ";materialization_contract_version="
        << materialization.contractVersion
        << ";materialization_identity_hash="
        << LengthPrefixed(materialization.identityHash);
    for (const auto& member : members)
    {
        out << ";member=" << member.identity.memberOrdinal << ','
            << member.identity.materializationMemberId << ','
            << member.identity.rankingMemberId << ','
            << member.identity.recommendationId << ','
            << member.identity.sourceExperimentId << ','
            << member.identity.proposalId << ','
            << OptionalLongLong(member.identity.expectedExperimentId)
            << ",lifecycle="
            << RecommendationCampaignOutcomeAssessmentLifecycleStateText(
                   member.lifecycle)
            << ",consistency="
            << RecommendationCampaignOutcomeAssessmentConsistencyStateText(
                   member.consistency)
            << ",outcome="
            << RecommendationCampaignOutcomeAssessmentMemberOutcomeText(
                   member.outcome)
            << ",diagnostic_count=" << member.diagnostics.size();
        for (const auto diagnostic : member.diagnostics)
            out << ','
                << RecommendationCampaignOutcomeAssessmentDiagnosticCodeText(
                       diagnostic);
        out << ",source=";
        AppendSourceEvidence(out, member.sourceEvidence);
        out << ",result=";
        AppendResultEvidence(out, member.resultEvidence);
        out << ",comparison_count=" << member.comparisons.size();
        for (const auto& comparison : member.comparisons)
        {
            out << ',';
            AppendComparison(out, comparison);
        }
    }
    AppendSummary(out, summary);
    return out.str();
}

std::vector<Difference> ContextDifferences(
    const Context& source,
    const Context& result)
{
    std::vector<Difference> differences;
    if (source.symbol != result.symbol)
        differences.push_back(Difference::Symbol);
    if (source.predictionHorizon != result.predictionHorizon)
        differences.push_back(Difference::PredictionHorizon);
    if (source.threshold != result.threshold)
        differences.push_back(Difference::Threshold);
    if (source.windowSize != result.windowSize)
        differences.push_back(Difference::WindowSize);
    if (source.labelDefinition != result.labelDefinition)
        differences.push_back(Difference::LabelDefinition);
    if (source.inferenceRangeStart != result.inferenceRangeStart ||
        source.inferenceRangeEnd != result.inferenceRangeEnd)
        differences.push_back(Difference::InferenceRange);
    return differences;
}

void CanonicalizeDiagnostics(std::vector<Diagnostic>& diagnostics)
{
    std::sort(diagnostics.begin(), diagnostics.end(),
        [](Diagnostic left, Diagnostic right)
    {
        return static_cast<int>(left) < static_cast<int>(right);
    });
    diagnostics.erase(
        std::unique(diagnostics.begin(), diagnostics.end()),
        diagnostics.end());
}

struct WorkingMember
{
    const MemberEvidence* evidence;
    std::vector<Diagnostic> diagnostics;
};

struct MutableSummary
{
    int memberCount = 0;
    int notTerminalLifecycleCount = 0;
    int succeededLifecycleCount = 0;
    int failedLifecycleCount = 0;
    int cancelledLifecycleCount = 0;
    int unknownLifecycleCount = 0;
    int consistentMemberCount = 0;
    int inconsistentMemberCount = 0;
    int notReadyMemberCount = 0;
    int succeededComparableMemberCount = 0;
    int succeededContextChangedMemberCount = 0;
    int succeededMetricGapMemberCount = 0;
    int terminalFailedMemberCount = 0;
    int terminalCancelledMemberCount = 0;
    int inconsistentOutcomeMemberCount = 0;
    int metricComparisonCount = 0;
    int comparableMetricCount = 0;
    int contextChangedMetricCount = 0;
    int missingSourceMetricCount = 0;
    int missingResultMetricCount = 0;
    int metricValueUnavailableCount = 0;
    int unsupportedMetricCount = 0;
    int positiveDeltaCount = 0;
    int zeroDeltaCount = 0;
    int negativeDeltaCount = 0;
};

AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
{
    if (counts.inconsistentMemberCount > 0)
        return AggregateOutcome::Inconsistent;
    if (counts.terminalFailedMemberCount > 0)
        return AggregateOutcome::HasFailures;
    if (counts.terminalCancelledMemberCount > 0)
        return AggregateOutcome::HasCancellations;
    if (counts.notReadyMemberCount > 0)
        return AggregateOutcome::NotReady;
    if (counts.succeededContextChangedMemberCount > 0)
        return AggregateOutcome::SucceededContextChanged;
    if (counts.succeededMetricGapMemberCount > 0)
        return AggregateOutcome::SucceededMetricGap;
    return AggregateOutcome::SucceededComparable;
}

void CountMember(MutableSummary& counts, const Member& member)
{
    ++counts.memberCount;
    switch (member.lifecycle)
    {
        case Lifecycle::NotTerminal:
            ++counts.notTerminalLifecycleCount;
            break;
        case Lifecycle::Succeeded:
            ++counts.succeededLifecycleCount;
            break;
        case Lifecycle::Failed:
            ++counts.failedLifecycleCount;
            break;
        case Lifecycle::Cancelled:
            ++counts.cancelledLifecycleCount;
            break;
        case Lifecycle::Unknown:
            ++counts.unknownLifecycleCount;
            break;
    }
    if (member.consistency == Consistency::Consistent)
        ++counts.consistentMemberCount;
    else
        ++counts.inconsistentMemberCount;

    switch (member.outcome)
    {
        case MemberOutcome::NotReady:
            ++counts.notReadyMemberCount;
            break;
        case MemberOutcome::SucceededComparable:
            ++counts.succeededComparableMemberCount;
            break;
        case MemberOutcome::SucceededContextChanged:
            ++counts.succeededContextChangedMemberCount;
            break;
        case MemberOutcome::SucceededMetricGap:
            ++counts.succeededMetricGapMemberCount;
            break;
        case MemberOutcome::TerminalFailed:
            ++counts.terminalFailedMemberCount;
            break;
        case MemberOutcome::TerminalCancelled:
            ++counts.terminalCancelledMemberCount;
            break;
        case MemberOutcome::Inconsistent:
            ++counts.inconsistentOutcomeMemberCount;
            break;
    }

    for (const auto& comparison : member.comparisons)
    {
        ++counts.metricComparisonCount;
        switch (comparison.classification)
        {
            case Classification::Comparable:
                ++counts.comparableMetricCount;
                if (*comparison.delta > 0.0)
                    ++counts.positiveDeltaCount;
                else if (*comparison.delta < 0.0)
                    ++counts.negativeDeltaCount;
                else
                    ++counts.zeroDeltaCount;
                break;
            case Classification::ContextChanged:
                ++counts.contextChangedMetricCount;
                break;
            case Classification::MissingSourceMetric:
                ++counts.missingSourceMetricCount;
                break;
            case Classification::MissingResultMetric:
                ++counts.missingResultMetricCount;
                break;
            case Classification::MetricValueUnavailable:
                ++counts.metricValueUnavailableCount;
                break;
            case Classification::Unsupported:
                ++counts.unsupportedMetricCount;
                break;
        }
    }
}

} // namespace

RecommendationCampaignOutcomeAssessmentCampaignIdentity::
    RecommendationCampaignOutcomeAssessmentCampaignIdentity(
        long long campaignApprovalIdValue,
        std::string identityCanonicalValue,
        std::string identityHashValue)
    : campaignApprovalId(campaignApprovalIdValue),
      identityCanonical(std::move(identityCanonicalValue)),
      identityHash(std::move(identityHashValue))
{
    if (campaignApprovalId <= 0)
        throw std::invalid_argument(
            "recommendation_campaign_outcome_assessment_campaign_id_invalid");
    RequireText(identityCanonical,
        "recommendation_campaign_outcome_assessment_campaign_identity_missing");
    if (identityHash != RecommendationCanonicalHash(identityCanonical))
        throw std::invalid_argument(
            "recommendation_campaign_outcome_assessment_campaign_identity_invalid");
}

RecommendationCampaignOutcomeAssessmentMaterializationIdentity::
    RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
        long long materializationIdValue,
        long long campaignApprovalIdValue,
        int contractVersionValue,
        int memberCountValue,
        std::string identityCanonicalValue,
        std::string identityHashValue)
    : materializationId(materializationIdValue),
      campaignApprovalId(campaignApprovalIdValue),
      contractVersion(contractVersionValue),
      memberCount(memberCountValue),
      identityCanonical(std::move(identityCanonicalValue)),
      identityHash(std::move(identityHashValue))
{
    if (materializationId <= 0 || campaignApprovalId <= 0)
        throw std::invalid_argument(
            "recommendation_campaign_outcome_assessment_materialization_id_invalid");
    if (memberCount <= 0 ||
        memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
        throw std::invalid_argument(
            "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
    RequireText(identityCanonical,
        "recommendation_campaign_outcome_assessment_materialization_identity_missing");
    if (identityHash != RecommendationCanonicalHash(identityCanonical))
        throw std::invalid_argument(
            "recommendation_campaign_outcome_assessment_materialization_identity_invalid");
}

RecommendationCampaignOutcomeAssessmentMemberIdentity::
    RecommendationCampaignOutcomeAssessmentMemberIdentity(
        int memberOrdinalValue,
        long long materializationMemberIdValue,
        long long rankingMemberIdValue,
        long long recommendationIdValue,
        long long sourceExperimentIdValue,
        long long proposalIdValue,
        std::optional<long long> expectedExperimentIdValue)
    : memberOrdinal(memberOrdinalValue),
      materializationMemberId(materializationMemberIdValue),
      rankingMemberId(rankingMemberIdValue),
      recommendationId(recommendationIdValue),
      sourceExperimentId(sourceExperimentIdValue),
      proposalId(proposalIdValue),
      expectedExperimentId(expectedExperimentIdValue)
{
    if (memberOrdinal <= 0 || materializationMemberId <= 0 ||
        rankingMemberId <= 0 || recommendationId <= 0 ||
        sourceExperimentId <= 0 || proposalId <= 0)
        throw std::invalid_argument(
            "recommendation_campaign_outcome_assessment_member_identity_invalid");
    RequireOptionalPositive(expectedExperimentId,
        "recommendation_campaign_outcome_assessment_expected_experiment_id_invalid");
}

RecommendationCampaignOutcomeAssessmentMetric::
    RecommendationCampaignOutcomeAssessmentMetric(
        std::string identityValue,
        std::optional<double> valueValue,
        MetricSupport comparisonSupportValue)
    : identity(std::move(identityValue)),
      value(valueValue && *valueValue == 0.0
              ? std::optional<double>(0.0)
              : valueValue),
      comparisonSupport(comparisonSupportValue)
{
    RequireText(identity,
        "recommendation_campaign_outcome_assessment_metric_identity_invalid");
    if (value && !std::isfinite(*value))
        throw std::invalid_argument(
            "recommendation_campaign_outcome_assessment_metric_value_nonfinite");
    RequireValidMetricSupport(comparisonSupport);
}

RecommendationCampaignOutcomeAssessmentMetricCollection::
    RecommendationCampaignOutcomeAssessmentMetricCollection(
        std::vector<Metric> metricsValue)
    : metrics(CanonicalMetrics(std::move(metricsValue)))
{
}

RecommendationCampaignOutcomeAssessmentComparisonContext::
    RecommendationCampaignOutcomeAssessmentComparisonContext(
        std::string symbolValue,
        int predictionHorizonValue,
        double thresholdValue,
        int windowSizeValue,
        std::string labelDefinitionValue,
        std::string inferenceRangeStartValue,
        std::string inferenceRangeEndValue)
    : symbol(std::move(symbolValue)),
      predictionHorizon(predictionHorizonValue),
      threshold(thresholdValue == 0.0 ? 0.0 : thresholdValue),
      windowSize(windowSizeValue),
      labelDefinition(std::move(labelDefinitionValue)),
      inferenceRangeStart(std::move(inferenceRangeStartValue)),
      inferenceRangeEnd(std::move(inferenceRangeEndValue))
{
    RequireText(symbol,
        "recommendation_campaign_outcome_assessment_context_symbol_invalid");
    RequireText(labelDefinition,
        "recommendation_campaign_outcome_assessment_context_label_definition_invalid");
    RequireText(inferenceRangeStart,
        "recommendation_campaign_outcome_assessment_context_inference_range_invalid");
    RequireText(inferenceRangeEnd,
        "recommendation_campaign_outcome_assessment_context_inference_range_invalid");
    if (predictionHorizon <= 0 || windowSize <= 0 || !std::isfinite(threshold))
        throw std::invalid_argument(
            "recommendation_campaign_outcome_assessment_context_invalid");
}

RecommendationCampaignOutcomeAssessmentSourceEvidence::
    RecommendationCampaignOutcomeAssessmentSourceEvidence(
        long long sourceExperimentIdValue,
        std::optional<long long> sourceModelIdValue,
        std::optional<long long> sourceAnalysisIdValue,
        Context contextValue,
        MetricCollection metricsValue)
    : sourceExperimentId(sourceExperimentIdValue),
      sourceModelId(sourceModelIdValue),
      sourceAnalysisId(sourceAnalysisIdValue),
      context(std::move(contextValue)),
      metrics(std::move(metricsValue))
{
    if (sourceExperimentId <= 0)
        throw std::invalid_argument(
            "recommendation_campaign_outcome_assessment_source_experiment_id_invalid");
    RequireOptionalPositive(sourceModelId,
        "recommendation_campaign_outcome_assessment_source_model_id_invalid");
    RequireOptionalPositive(sourceAnalysisId,
        "recommendation_campaign_outcome_assessment_source_analysis_id_invalid");
}

RecommendationCampaignOutcomeAssessmentResultIdentity::
    RecommendationCampaignOutcomeAssessmentResultIdentity(
        std::string kindValue,
        long long idValue)
    : kind(std::move(kindValue)), id(idValue)
{
    RequireText(kind,
        "recommendation_campaign_outcome_assessment_result_identity_kind_invalid");
    if (id <= 0)
        throw std::invalid_argument(
            "recommendation_campaign_outcome_assessment_result_identity_id_invalid");
}

RecommendationCampaignOutcomeAssessmentResultEvidence::
    RecommendationCampaignOutcomeAssessmentResultEvidence(
        std::optional<long long> experimentIdValue,
        std::optional<long long> modelIdValue,
        std::vector<ResultIdentity> resultIdentitiesValue,
        std::optional<Context> contextValue,
        MetricCollection metricsValue)
    : experimentId(experimentIdValue),
      modelId(modelIdValue),
      resultIdentities(
          CanonicalResultIdentities(std::move(resultIdentitiesValue))),
      context(std::move(contextValue)),
      metrics(std::move(metricsValue))
{
    RequireOptionalPositive(experimentId,
        "recommendation_campaign_outcome_assessment_result_experiment_id_invalid");
    RequireOptionalPositive(modelId,
        "recommendation_campaign_outcome_assessment_result_model_id_invalid");
}

RecommendationCampaignOutcomeAssessmentMemberEvidence::
    RecommendationCampaignOutcomeAssessmentMemberEvidence(
        RecommendationCampaignOutcomeAssessmentMemberIdentity identityValue,
        Lifecycle lifecycleValue,
        std::optional<
            RecommendationCampaignOutcomeAssessmentSourceEvidence>
            sourceEvidenceValue,
        std::optional<
            RecommendationCampaignOutcomeAssessmentResultEvidence>
            resultEvidenceValue)
    : identity(std::move(identityValue)),
      lifecycle(lifecycleValue),
      sourceEvidence(std::move(sourceEvidenceValue)),
      resultEvidence(std::move(resultEvidenceValue))
{
    RequireValidLifecycle(lifecycle);
    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
        throw std::invalid_argument(
            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
}

RecommendationCampaignOutcomeAssessmentMetricComparison::
    RecommendationCampaignOutcomeAssessmentMetricComparison(
        std::string metricIdentityValue,
        Classification classificationValue,
        std::optional<double> sourceValueValue,
        std::optional<double> resultValueValue,
        std::optional<double> deltaValue,
        std::vector<Difference> contextDifferencesValue)
    : metricIdentity(std::move(metricIdentityValue)),
      classification(classificationValue),
      sourceValue(sourceValueValue),
      resultValue(resultValueValue),
      delta(deltaValue && *deltaValue == 0.0
              ? std::optional<double>(0.0)
              : deltaValue),
      contextDifferences(std::move(contextDifferencesValue))
{
    RequireText(metricIdentity,
        "recommendation_campaign_outcome_assessment_comparison_metric_identity_invalid");
    if ((sourceValue && !std::isfinite(*sourceValue)) ||
        (resultValue && !std::isfinite(*resultValue)) ||
        (delta && !std::isfinite(*delta)))
        throw std::invalid_argument(
            "recommendation_campaign_outcome_assessment_comparison_number_nonfinite");
    const bool changed = classification == Classification::ContextChanged;
    if (changed != !contextDifferences.empty())
        throw std::invalid_argument(
            "recommendation_campaign_outcome_assessment_comparison_context_invalid");
    if (classification == Classification::Comparable)
    {
        if (!sourceValue || !resultValue || !delta)
            throw std::invalid_argument(
                "recommendation_campaign_outcome_assessment_comparable_invalid");
    }
    else if (delta)
        throw std::invalid_argument(
            "recommendation_campaign_outcome_assessment_noncomparable_delta_invalid");
}

RecommendationCampaignOutcomeAssessmentSummary::
    RecommendationCampaignOutcomeAssessmentSummary(
        AggregateOutcome outcomeValue,
        int memberCountValue,
        int notTerminalLifecycleCountValue,
        int succeededLifecycleCountValue,
        int failedLifecycleCountValue,
        int cancelledLifecycleCountValue,
        int unknownLifecycleCountValue,
        int consistentMemberCountValue,
        int inconsistentMemberCountValue,
        int notReadyMemberCountValue,
        int succeededComparableMemberCountValue,
        int succeededContextChangedMemberCountValue,
        int succeededMetricGapMemberCountValue,
        int terminalFailedMemberCountValue,
        int terminalCancelledMemberCountValue,
        int inconsistentOutcomeMemberCountValue,
        int metricComparisonCountValue,
        int comparableMetricCountValue,
        int contextChangedMetricCountValue,
        int missingSourceMetricCountValue,
        int missingResultMetricCountValue,
        int metricValueUnavailableCountValue,
        int unsupportedMetricCountValue,
        int positiveDeltaCountValue,
        int zeroDeltaCountValue,
        int negativeDeltaCountValue)
    : outcome(outcomeValue),
      memberCount(memberCountValue),
      notTerminalLifecycleCount(notTerminalLifecycleCountValue),
      succeededLifecycleCount(succeededLifecycleCountValue),
      failedLifecycleCount(failedLifecycleCountValue),
      cancelledLifecycleCount(cancelledLifecycleCountValue),
      unknownLifecycleCount(unknownLifecycleCountValue),
      consistentMemberCount(consistentMemberCountValue),
      inconsistentMemberCount(inconsistentMemberCountValue),
      notReadyMemberCount(notReadyMemberCountValue),
      succeededComparableMemberCount(succeededComparableMemberCountValue),
      succeededContextChangedMemberCount(
          succeededContextChangedMemberCountValue),
      succeededMetricGapMemberCount(succeededMetricGapMemberCountValue),
      terminalFailedMemberCount(terminalFailedMemberCountValue),
      terminalCancelledMemberCount(terminalCancelledMemberCountValue),
      inconsistentOutcomeMemberCount(inconsistentOutcomeMemberCountValue),
      metricComparisonCount(metricComparisonCountValue),
      comparableMetricCount(comparableMetricCountValue),
      contextChangedMetricCount(contextChangedMetricCountValue),
      missingSourceMetricCount(missingSourceMetricCountValue),
      missingResultMetricCount(missingResultMetricCountValue),
      metricValueUnavailableCount(metricValueUnavailableCountValue),
      unsupportedMetricCount(unsupportedMetricCountValue),
      positiveDeltaCount(positiveDeltaCountValue),
      zeroDeltaCount(zeroDeltaCountValue),
      negativeDeltaCount(negativeDeltaCountValue)
{
    const int lifecycleTotal = notTerminalLifecycleCount +
        succeededLifecycleCount + failedLifecycleCount +
        cancelledLifecycleCount + unknownLifecycleCount;
    const int consistencyTotal =
        consistentMemberCount + inconsistentMemberCount;
    const int outcomeTotal = notReadyMemberCount +
        succeededComparableMemberCount + succeededContextChangedMemberCount +
        succeededMetricGapMemberCount + terminalFailedMemberCount +
        terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
    const int comparisonTotal = comparableMetricCount +
        contextChangedMetricCount + missingSourceMetricCount +
        missingResultMetricCount + metricValueUnavailableCount +
        unsupportedMetricCount;
    const int deltaTotal =
        positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
    if (memberCount <= 0 || lifecycleTotal != memberCount ||
        consistencyTotal != memberCount || outcomeTotal != memberCount ||
        comparisonTotal != metricComparisonCount ||
        deltaTotal != comparableMetricCount)
        throw std::logic_error(
            "recommendation_campaign_outcome_assessment_summary_invariant_failed");
}

RecommendationCampaignOutcomeAssessmentMember::
    RecommendationCampaignOutcomeAssessmentMember(
        RecommendationCampaignOutcomeAssessmentMemberIdentity identityValue,
        Lifecycle lifecycleValue,
        Consistency consistencyValue,
        MemberOutcome outcomeValue,
        std::vector<Diagnostic> diagnosticsValue,
        std::optional<
            RecommendationCampaignOutcomeAssessmentSourceEvidence>
            sourceEvidenceValue,
        std::optional<
            RecommendationCampaignOutcomeAssessmentResultEvidence>
            resultEvidenceValue,
        std::vector<Comparison> comparisonsValue)
    : identity(std::move(identityValue)),
      lifecycle(lifecycleValue),
      consistency(consistencyValue),
      outcome(outcomeValue),
      diagnostics(std::move(diagnosticsValue)),
      sourceEvidence(std::move(sourceEvidenceValue)),
      resultEvidence(std::move(resultEvidenceValue)),
      comparisons(std::move(comparisonsValue))
{
    const bool inconsistent = consistency == Consistency::Inconsistent;
    if (inconsistent != !diagnostics.empty() ||
        inconsistent != (outcome == MemberOutcome::Inconsistent) ||
        (inconsistent && !comparisons.empty()))
        throw std::logic_error(
            "recommendation_campaign_outcome_assessment_member_invariant_failed");
}

RecommendationCampaignOutcomeAssessmentIdentity::
    RecommendationCampaignOutcomeAssessmentIdentity(
        int contractVersionValue,
        std::string canonicalTextValue,
        std::string hashValue)
    : contractVersion(contractVersionValue),
      canonicalText(std::move(canonicalTextValue)),
      hash(std::move(hashValue))
{
    if (contractVersion !=
            kRecommendationCampaignOutcomeAssessmentContractVersion ||
        hash != RecommendationCanonicalHash(canonicalText))
        throw std::logic_error(
            "recommendation_campaign_outcome_assessment_identity_invariant_failed");
    RequireText(canonicalText,
        "recommendation_campaign_outcome_assessment_identity_missing");
}

RecommendationCampaignOutcomeAssessment::
    RecommendationCampaignOutcomeAssessment(
        RecommendationCampaignOutcomeAssessmentIdentity identityValue,
        RecommendationCampaignOutcomeAssessmentCampaignIdentity
            campaignIdentityValue,
        RecommendationCampaignOutcomeAssessmentMaterializationIdentity
            materializationIdentityValue,
        std::string observedAtValue,
        RecommendationCampaignOutcomeAssessmentSummary summaryValue,
        std::vector<Member> membersValue)
    : identity(std::move(identityValue)),
      campaignIdentity(std::move(campaignIdentityValue)),
      materializationIdentity(std::move(materializationIdentityValue)),
      observedAt(std::move(observedAtValue)),
      summary(std::move(summaryValue)),
      members(std::move(membersValue))
{
    RequireText(observedAt,
        "recommendation_campaign_outcome_assessment_observed_at_invalid");
    if (summary.memberCount != static_cast<int>(members.size()))
        throw std::logic_error(
            "recommendation_campaign_outcome_assessment_member_summary_invariant_failed");
}

struct RecommendationCampaignOutcomeAssessmentBuilder
{
    static std::vector<Comparison> BuildComparisons(
        const MemberEvidence& evidence,
        bool& nonFiniteDelta)
    {
        const auto* sourceMetrics = evidence.sourceEvidence
            ? &evidence.sourceEvidence->metrics.metrics
            : nullptr;
        const auto& resultMetrics = evidence.resultEvidence->metrics.metrics;
        std::vector<Difference> differences;
        if (evidence.sourceEvidence)
            differences = ContextDifferences(
                evidence.sourceEvidence->context,
                *evidence.resultEvidence->context);

        std::vector<Comparison> comparisons;
        std::size_t sourceIndex = 0;
        std::size_t resultIndex = 0;
        while ((sourceMetrics && sourceIndex < sourceMetrics->size()) ||
            resultIndex < resultMetrics.size())
        {
            const Metric* source = sourceMetrics &&
                    sourceIndex < sourceMetrics->size()
                ? &(*sourceMetrics)[sourceIndex]
                : nullptr;
            const Metric* result = resultIndex < resultMetrics.size()
                ? &resultMetrics[resultIndex]
                : nullptr;
            if (source && result && source->identity == result->identity)
            {
                if (!differences.empty())
                    comparisons.push_back(Comparison(source->identity,
                        Classification::ContextChanged, source->value,
                        result->value, std::nullopt, differences));
                else if (!source->value || !result->value)

exec
/bin/bash -lc "sed -n '941,1245p' Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp && sed -n '1246,1370p' Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp && sed -n '1,820p' Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
                    comparisons.push_back(Comparison(source->identity,
                        Classification::MetricValueUnavailable, source->value,
                        result->value, std::nullopt, {}));
                else if (source->comparisonSupport != MetricSupport::NumericDelta ||
                    result->comparisonSupport != MetricSupport::NumericDelta)
                    comparisons.push_back(Comparison(source->identity,
                        Classification::Unsupported, source->value,
                        result->value, std::nullopt, {}));
                else
                {
                    double delta = *result->value - *source->value;
                    if (!std::isfinite(delta))
                    {
                        nonFiniteDelta = true;
                        return {};
                    }
                    if (delta == 0.0) delta = 0.0;
                    comparisons.push_back(Comparison(source->identity,
                        Classification::Comparable, source->value,
                        result->value, delta, {}));
                }
                ++sourceIndex;
                ++resultIndex;
            }
            else if (source &&
                (!result || source->identity < result->identity))
            {
                comparisons.push_back(Comparison(source->identity,
                    Classification::MissingResultMetric, source->value,
                    std::nullopt, std::nullopt, {}));
                ++sourceIndex;
            }
            else
            {
                comparisons.push_back(Comparison(result->identity,
                    Classification::MissingSourceMetric, std::nullopt,
                    result->value, std::nullopt, {}));
                ++resultIndex;
            }
        }
        return comparisons;
    }

    static MemberOutcome DeriveMemberOutcome(
        Lifecycle lifecycle,
        const std::vector<Diagnostic>& diagnostics,
        const std::vector<Comparison>& comparisons)
    {
        if (!diagnostics.empty()) return MemberOutcome::Inconsistent;
        switch (lifecycle)
        {
            case Lifecycle::NotTerminal:
            case Lifecycle::Unknown:
                return MemberOutcome::NotReady;
            case Lifecycle::Failed:
                return MemberOutcome::TerminalFailed;
            case Lifecycle::Cancelled:
                return MemberOutcome::TerminalCancelled;
            case Lifecycle::Succeeded:
                break;
        }
        if (std::any_of(comparisons.begin(), comparisons.end(),
                [](const Comparison& comparison)
        {
            return comparison.classification == Classification::ContextChanged;
        }))
            return MemberOutcome::SucceededContextChanged;
        if (comparisons.empty() ||
            std::any_of(comparisons.begin(), comparisons.end(),
                [](const Comparison& comparison)
        {
            return comparison.classification != Classification::Comparable;
        }))
            return MemberOutcome::SucceededMetricGap;
        return MemberOutcome::SucceededComparable;
    }

    static RecommendationCampaignOutcomeAssessment Build(
        const RecommendationCampaignOutcomeAssessmentCampaignIdentity&
            campaignIdentity,
        const RecommendationCampaignOutcomeAssessmentMaterializationIdentity&
            materializationIdentity,
        const std::string& observedAt,
        const std::vector<MemberEvidence>& members)
    {
        RequireText(observedAt,
            "recommendation_campaign_outcome_assessment_observed_at_invalid");
        if (materializationIdentity.campaignApprovalId !=
            campaignIdentity.campaignApprovalId)
            throw std::invalid_argument(
                "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
        if (materializationIdentity.contractVersion !=
            kRecommendationCampaignMaterializationContractVersion)
            throw std::invalid_argument(
                "recommendation_campaign_outcome_assessment_materialization_version_unsupported");
        if (materializationIdentity.memberCount !=
            static_cast<int>(members.size()))
            throw std::invalid_argument(
                "recommendation_campaign_outcome_assessment_member_count_mismatch");
        if (members.empty() ||
            members.size() > static_cast<std::size_t>(
                                 kMaximumRecommendationCampaignOutcomeAssessmentMembers))
            throw std::invalid_argument(
                "recommendation_campaign_outcome_assessment_member_count_invalid");

        std::vector<const MemberEvidence*> ordered;
        ordered.reserve(members.size());
        for (const auto& member : members) ordered.push_back(&member);
        std::sort(ordered.begin(), ordered.end(),
            [](const MemberEvidence* left, const MemberEvidence* right)
        {
            return std::tie(left->identity.memberOrdinal,
                       left->identity.materializationMemberId,
                       left->identity.rankingMemberId,
                       left->identity.recommendationId,
                       left->identity.proposalId) <
                std::tie(right->identity.memberOrdinal,
                       right->identity.materializationMemberId,
                       right->identity.rankingMemberId,
                       right->identity.recommendationId,
                       right->identity.proposalId);
        });

        std::set<long long> materializationMemberIds;
        std::set<long long> rankingMemberIds;
        std::set<long long> recommendationIds;
        std::set<long long> proposalIds;
        std::set<long long> expectedExperimentIds;
        std::vector<WorkingMember> working;
        working.reserve(ordered.size());
        for (std::size_t index = 0; index < ordered.size(); ++index)
        {
            const auto& evidence = *ordered[index];
            if (evidence.identity.memberOrdinal != static_cast<int>(index) + 1 ||
                !materializationMemberIds
                     .insert(evidence.identity.materializationMemberId)
                     .second ||
                !rankingMemberIds.insert(evidence.identity.rankingMemberId)
                     .second ||
                !recommendationIds.insert(evidence.identity.recommendationId)
                     .second ||
                !proposalIds.insert(evidence.identity.proposalId).second ||
                (evidence.identity.expectedExperimentId &&
                    !expectedExperimentIds
                         .insert(*evidence.identity.expectedExperimentId)
                         .second))
                throw std::invalid_argument(
                    "recommendation_campaign_outcome_assessment_member_identity_set_invalid");

            WorkingMember item{&evidence, {}};
            if (evidence.sourceEvidence &&
                evidence.sourceEvidence->sourceExperimentId !=
                    evidence.identity.sourceExperimentId)
                item.diagnostics.push_back(
                    Diagnostic::SourceExperimentIdentityMismatch);
            if (evidence.lifecycle == Lifecycle::Succeeded)
            {
                if (!evidence.identity.expectedExperimentId)
                    item.diagnostics.push_back(
                        Diagnostic::ExpectedExperimentIdentityMissing);
                if (!evidence.resultEvidence)
                    item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                else
                {
                    if (!evidence.resultEvidence->experimentId)
                        item.diagnostics.push_back(
                            Diagnostic::ResultExperimentIdentityMissing);
                    else if (evidence.identity.expectedExperimentId &&
                        evidence.resultEvidence->experimentId !=
                            evidence.identity.expectedExperimentId)
                        item.diagnostics.push_back(
                            Diagnostic::ResultExperimentIdentityMismatch);
                    if (!evidence.resultEvidence->modelId)
                        item.diagnostics.push_back(
                            Diagnostic::ResultModelIdentityMissing);
                    if (evidence.resultEvidence->resultIdentities.empty())
                        item.diagnostics.push_back(
                            Diagnostic::ResultIdentityMissing);
                    if (!evidence.resultEvidence->context)
                        item.diagnostics.push_back(
                            Diagnostic::ResultContextMissing);
                }
            }
            working.push_back(std::move(item));
        }

        std::map<long long, std::vector<std::size_t>> resultExperiments;
        std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
            resultIdentities;
        for (std::size_t index = 0; index < working.size(); ++index)
        {
            const auto& result = working[index].evidence->resultEvidence;
            if (!result) continue;
            if (result->experimentId)
                resultExperiments[*result->experimentId].push_back(index);
            for (const auto& identity : result->resultIdentities)
                resultIdentities[{identity.kind, identity.id}].push_back(index);
        }
        for (const auto& [unused, indices] : resultExperiments)
        {
            static_cast<void>(unused);
            if (indices.size() < 2) continue;
            for (const auto index : indices)
                working[index].diagnostics.push_back(
                    Diagnostic::ResultExperimentReused);
        }
        for (const auto& [unused, indices] : resultIdentities)
        {
            static_cast<void>(unused);
            if (indices.size() < 2) continue;
            for (const auto index : indices)
                working[index].diagnostics.push_back(
                    Diagnostic::ResultIdentityReused);
        }

        std::vector<Member> assessedMembers;
        assessedMembers.reserve(working.size());
        for (auto& item : working)
        {
            CanonicalizeDiagnostics(item.diagnostics);
            std::vector<Comparison> comparisons;
            if (item.diagnostics.empty() &&
                item.evidence->lifecycle == Lifecycle::Succeeded)
            {
                bool nonFiniteDelta = false;
                comparisons = BuildComparisons(*item.evidence, nonFiniteDelta);
                if (nonFiniteDelta)
                {
                    item.diagnostics.push_back(
                        Diagnostic::NumericDeltaNonFinite);
                    comparisons.clear();
                }
            }
            CanonicalizeDiagnostics(item.diagnostics);
            const Consistency consistency = item.diagnostics.empty()
                ? Consistency::Consistent
                : Consistency::Inconsistent;
            const MemberOutcome outcome = DeriveMemberOutcome(
                item.evidence->lifecycle, item.diagnostics, comparisons);
            assessedMembers.push_back(Member(item.evidence->identity,
                item.evidence->lifecycle, consistency, outcome,
                item.diagnostics, item.evidence->sourceEvidence,
                item.evidence->resultEvidence, std::move(comparisons)));
        }

        MutableSummary counts;
        for (const auto& member : assessedMembers) CountMember(counts, member);
        const AggregateOutcome aggregateOutcome =
            DeriveAggregateOutcome(counts);
        RecommendationCampaignOutcomeAssessmentSummary summary(
            aggregateOutcome, counts.memberCount,
            counts.notTerminalLifecycleCount, counts.succeededLifecycleCount,
            counts.failedLifecycleCount, counts.cancelledLifecycleCount,
            counts.unknownLifecycleCount, counts.consistentMemberCount,
            counts.inconsistentMemberCount, counts.notReadyMemberCount,
            counts.succeededComparableMemberCount,
            counts.succeededContextChangedMemberCount,
            counts.succeededMetricGapMemberCount,
            counts.terminalFailedMemberCount,
            counts.terminalCancelledMemberCount,
            counts.inconsistentOutcomeMemberCount,
            counts.metricComparisonCount, counts.comparableMetricCount,
            counts.contextChangedMetricCount,
            counts.missingSourceMetricCount,
            counts.missingResultMetricCount,
            counts.metricValueUnavailableCount,
            counts.unsupportedMetricCount, counts.positiveDeltaCount,
            counts.zeroDeltaCount, counts.negativeDeltaCount);
        std::string canonical = AssessmentCanonicalText(campaignIdentity,
            materializationIdentity, summary, assessedMembers);
        RecommendationCampaignOutcomeAssessmentIdentity identity(
            kRecommendationCampaignOutcomeAssessmentContractVersion,
            canonical, RecommendationCanonicalHash(canonical));
        return RecommendationCampaignOutcomeAssessment(std::move(identity),
            campaignIdentity, materializationIdentity, observedAt,
            std::move(summary), std::move(assessedMembers));
    }
};

RecommendationCampaignOutcomeAssessment BuildRecommendationCampaignOutcomeAssessment(
    const RecommendationCampaignOutcomeAssessmentCampaignIdentity&
        campaignIdentity,
    const RecommendationCampaignOutcomeAssessmentMaterializationIdentity&
        materializationIdentity,
    const std::string& observedAt,
    const std::vector<MemberEvidence>& members)
{
    return RecommendationCampaignOutcomeAssessmentBuilder::Build(
        campaignIdentity, materializationIdentity, observedAt, members);
}

#define EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(name, type, ...)                 \
    std::string name(type value)                                             \
    {                                                                         \
        switch (value)                                                        \
        {                                                                     \
            __VA_ARGS__                                                       \
        }                                                                     \
        throw std::invalid_argument(                                          \
            "recommendation_campaign_outcome_assessment_enum_invalid");      \
    }
#define EA_OUTCOME_ASSESSMENT_TEXT_CASE(value, text) case value: return text;

EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
    RecommendationCampaignOutcomeAssessmentMetricComparisonSupportText,
    RecommendationCampaignOutcomeAssessmentMetricComparisonSupport,
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(MetricSupport::NumericDelta,
        "numeric_delta")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(MetricSupport::Unsupported, "unsupported"))

EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
    RecommendationCampaignOutcomeAssessmentLifecycleStateText,
    RecommendationCampaignOutcomeAssessmentLifecycleState,
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Lifecycle::NotTerminal, "not_terminal")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Lifecycle::Succeeded, "succeeded")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Lifecycle::Failed, "failed")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Lifecycle::Cancelled, "cancelled")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Lifecycle::Unknown, "unknown"))

EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
    RecommendationCampaignOutcomeAssessmentConsistencyStateText,
    RecommendationCampaignOutcomeAssessmentConsistencyState,
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Consistency::Consistent, "consistent")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Consistency::Inconsistent, "inconsistent"))

EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
    RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
    RecommendationCampaignOutcomeAssessmentDiagnosticCode,
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
        "source_experiment_identity_mismatch")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
        "expected_experiment_identity_missing")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
        "result_evidence_missing")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
        "result_experiment_identity_missing")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMismatch,
        "result_experiment_identity_mismatch")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
        "result_model_identity_missing")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
        "result_identity_missing")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
        "result_context_missing")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentReused,
        "result_experiment_reused")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityReused,
        "result_identity_reused")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::NumericDeltaNonFinite,
        "numeric_delta_nonfinite"))

EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
    RecommendationCampaignOutcomeAssessmentMemberOutcomeText,
    RecommendationCampaignOutcomeAssessmentMemberOutcome,
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(MemberOutcome::NotReady, "not_ready")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(MemberOutcome::SucceededComparable,
        "succeeded_comparable")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(MemberOutcome::SucceededContextChanged,
        "succeeded_context_changed")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(MemberOutcome::SucceededMetricGap,
        "succeeded_metric_gap")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(MemberOutcome::TerminalFailed,
        "terminal_failed")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(MemberOutcome::TerminalCancelled,
        "terminal_cancelled")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(MemberOutcome::Inconsistent,
        "inconsistent"))

EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
    RecommendationCampaignOutcomeAssessmentMetricComparisonClassificationText,
    RecommendationCampaignOutcomeAssessmentMetricComparisonClassification,
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Classification::Comparable, "comparable")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Classification::ContextChanged,
        "context_changed")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Classification::MissingSourceMetric,
        "missing_source_metric")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Classification::MissingResultMetric,
        "missing_result_metric")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Classification::MetricValueUnavailable,
        "metric_value_unavailable")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Classification::Unsupported,
        "unsupported"))

EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
    RecommendationCampaignOutcomeAssessmentContextDifferenceText,
    RecommendationCampaignOutcomeAssessmentContextDifference,
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Difference::Symbol, "symbol")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Difference::PredictionHorizon,
        "prediction_horizon")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Difference::Threshold, "threshold")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Difference::WindowSize, "window_size")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Difference::LabelDefinition,
        "label_definition")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Difference::InferenceRange,
        "inference_range"))

EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
    RecommendationCampaignOutcomeAssessmentAggregateOutcomeText,
    RecommendationCampaignOutcomeAssessmentAggregateOutcome,
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(AggregateOutcome::Inconsistent,
        "inconsistent")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(AggregateOutcome::HasFailures,
        "has_failures")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(AggregateOutcome::HasCancellations,
        "has_cancellations")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(AggregateOutcome::NotReady, "not_ready")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(AggregateOutcome::SucceededContextChanged,
        "succeeded_context_changed")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(AggregateOutcome::SucceededMetricGap,
        "succeeded_metric_gap")
    EA_OUTCOME_ASSESSMENT_TEXT_CASE(AggregateOutcome::SucceededComparable,
        "succeeded_comparable"))

#undef EA_OUTCOME_ASSESSMENT_TEXT_CASE
#undef EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION

} // namespace EA::ExperimentRecommendation
#include "ExperimentRecommendationCampaignOutcomeAssessment.hpp"

#include "ExperimentRecommendation.hpp"
#include "ExperimentRecommendationCampaignMaterialization.hpp"

#include <algorithm>
#include <cassert>
#include <cmath>
#include <limits>
#include <optional>
#include <stdexcept>
#include <string>
#include <type_traits>
#include <utility>
#include <vector>

using namespace EA::ExperimentRecommendation;

namespace
{

using AggregateOutcome =
    RecommendationCampaignOutcomeAssessmentAggregateOutcome;
using Assessment = RecommendationCampaignOutcomeAssessment;
using CampaignIdentity =
    RecommendationCampaignOutcomeAssessmentCampaignIdentity;
using Classification =
    RecommendationCampaignOutcomeAssessmentMetricComparisonClassification;
using Consistency = RecommendationCampaignOutcomeAssessmentConsistencyState;
using Context = RecommendationCampaignOutcomeAssessmentComparisonContext;
using Diagnostic = RecommendationCampaignOutcomeAssessmentDiagnosticCode;
using Difference = RecommendationCampaignOutcomeAssessmentContextDifference;
using Lifecycle = RecommendationCampaignOutcomeAssessmentLifecycleState;
using MaterializationIdentity =
    RecommendationCampaignOutcomeAssessmentMaterializationIdentity;
using Member = RecommendationCampaignOutcomeAssessmentMember;
using MemberEvidence = RecommendationCampaignOutcomeAssessmentMemberEvidence;
using MemberIdentity =
    RecommendationCampaignOutcomeAssessmentMemberIdentity;
using MemberOutcome = RecommendationCampaignOutcomeAssessmentMemberOutcome;
using Metric = RecommendationCampaignOutcomeAssessmentMetric;
using MetricCollection =
    RecommendationCampaignOutcomeAssessmentMetricCollection;
using MetricComparison =
    RecommendationCampaignOutcomeAssessmentMetricComparison;
using MetricSupport =
    RecommendationCampaignOutcomeAssessmentMetricComparisonSupport;
using ResultEvidence =
    RecommendationCampaignOutcomeAssessmentResultEvidence;
using ResultIdentity =
    RecommendationCampaignOutcomeAssessmentResultIdentity;
using SourceEvidence =
    RecommendationCampaignOutcomeAssessmentSourceEvidence;
using Summary = RecommendationCampaignOutcomeAssessmentSummary;

static_assert(Assessment::readOnly);
static_assert(Assessment::pointInTime);
static_assert(!Assessment::persistent);
static_assert(!Assessment::authoritative);
static_assert(!Assessment::declaresCampaignSuccess);
static_assert(!std::is_default_constructible_v<Assessment>);
static_assert(!std::is_default_constructible_v<Member>);
static_assert(!std::is_default_constructible_v<Summary>);
static_assert(!std::is_default_constructible_v<MetricComparison>);
static_assert(!std::is_aggregate_v<Assessment>);
static_assert(!std::is_aggregate_v<Member>);
static_assert(!std::is_aggregate_v<Summary>);
static_assert(!std::is_aggregate_v<MetricComparison>);
static_assert(!std::is_copy_assignable_v<Assessment>);
static_assert(!std::is_copy_assignable_v<Member>);
static_assert(!std::is_copy_assignable_v<Summary>);
static_assert(!std::is_copy_assignable_v<MetricComparison>);
static_assert(!std::is_constructible_v<MetricComparison, std::string,
    Classification, std::optional<double>, std::optional<double>,
    std::optional<double>, std::vector<Difference>>);
static_assert(!std::is_constructible_v<Member, MemberIdentity, Lifecycle,
    Consistency, MemberOutcome, std::vector<Diagnostic>,
    std::optional<SourceEvidence>, std::optional<ResultEvidence>,
    std::vector<MetricComparison>>);

template <std::size_t... Indices>
consteval bool SummaryConstructorIsPublic(std::index_sequence<Indices...>)
{
    return std::is_constructible_v<Summary, AggregateOutcome,
        decltype((void)Indices, int{})...>;
}

static_assert(!SummaryConstructorIsPublic(std::make_index_sequence<25>{}));

template <typename Function>
void AssertInvalidArgument(Function&& function)
{
    bool threw = false;
    try
    {
        function();
    }
    catch (const std::invalid_argument&)
    {
        threw = true;
    }
    assert(threw);
}

CampaignIdentity Campaign()
{
    const std::string canonical = "campaign-approval-canonical";
    return {41, canonical, RecommendationCanonicalHash(canonical)};
}

MaterializationIdentity Materialization(int memberCount)
{
    const std::string canonical =
        "materialization-canonical:" + std::to_string(memberCount);
    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
        memberCount, canonical, RecommendationCanonicalHash(canonical)};
}

Context ComparableContext(
    std::string symbol = "eurusd",
    int horizon = 12,
    double threshold = 0.0008,
    int windowSize = 96,
    std::string labelDefinition = "three_class_forward_return_v1",
    std::string rangeStart = "2026-01-01T00:00:00Z",
    std::string rangeEnd = "2026-03-31T23:59:59Z")
{
    return {std::move(symbol), horizon, threshold, windowSize,
        std::move(labelDefinition), std::move(rangeStart),
        std::move(rangeEnd)};
}

Metric NumericMetric(
    std::string identity,
    std::optional<double> value)
{
    return {std::move(identity), value, MetricSupport::NumericDelta};
}

Metric UnsupportedMetric(
    std::string identity,
    std::optional<double> value)
{
    return {std::move(identity), value, MetricSupport::Unsupported};
}

MetricCollection Metrics(std::vector<Metric> metrics)
{
    return MetricCollection(std::move(metrics));
}

long long SourceExperiment(int ordinal)
{
    return 500 + ordinal;
}

long long ResultExperiment(int ordinal)
{
    return 900 + ordinal;
}

long long ResultRecord(int ordinal)
{
    return 2000 + ordinal;
}

MemberIdentity Identity(
    int ordinal,
    std::optional<long long> expectedExperimentId)
{
    return {ordinal, 100 + ordinal, 200 + ordinal, 300 + ordinal,
        SourceExperiment(ordinal), 400 + ordinal, expectedExperimentId};
}

SourceEvidence Source(
    int ordinal,
    std::vector<Metric> metrics,
    Context context = ComparableContext(),
    std::optional<long long> sourceExperimentOverride = std::nullopt)
{
    return {sourceExperimentOverride.value_or(SourceExperiment(ordinal)),
        600 + ordinal, 700 + ordinal, std::move(context),
        Metrics(std::move(metrics))};
}

ResultEvidence Result(
    int ordinal,
    std::vector<Metric> metrics,
    Context context = ComparableContext(),
    std::optional<long long> experimentOverride = std::nullopt,
    std::optional<long long> resultRecordOverride = std::nullopt)
{
    return {experimentOverride.value_or(ResultExperiment(ordinal)),
        1000 + ordinal,
        {ResultIdentity("scientific_result",
            resultRecordOverride.value_or(ResultRecord(ordinal)))},
        std::move(context), Metrics(std::move(metrics))};
}

std::vector<Metric> BaseSourceMetrics()
{
    return {NumericMetric("inference_accuracy", 0.70),
        NumericMetric("leader_score", 0.80)};
}

std::vector<Metric> BaseResultMetrics()
{
    return {NumericMetric("inference_accuracy", 0.75),
        NumericMetric("leader_score", 0.78)};
}

MemberEvidence Succeeded(
    int ordinal,
    std::vector<Metric> sourceMetrics = BaseSourceMetrics(),
    std::vector<Metric> resultMetrics = BaseResultMetrics(),
    Context sourceContext = ComparableContext(),
    Context resultContext = ComparableContext())
{
    return {Identity(ordinal, ResultExperiment(ordinal)),
        Lifecycle::Succeeded,
        Source(ordinal, std::move(sourceMetrics), std::move(sourceContext)),
        Result(ordinal, std::move(resultMetrics), std::move(resultContext))};
}

MemberEvidence LifecycleOnly(
    int ordinal,
    Lifecycle lifecycle,
    std::optional<long long> expectedExperimentId = std::nullopt)
{
    return {Identity(ordinal, expectedExperimentId), lifecycle, std::nullopt,
        std::nullopt};
}

Assessment Assess(
    std::vector<MemberEvidence> members,
    std::string observedAt = "2026-07-19T22:00:00Z")
{
    return BuildRecommendationCampaignOutcomeAssessment(Campaign(),
        Materialization(static_cast<int>(members.size())), observedAt,
        members);
}

const MetricComparison& ComparisonFor(
    const Member& member,
    const std::string& identity)
{
    const auto found = std::find_if(member.comparisons.begin(),
        member.comparisons.end(), [&](const MetricComparison& comparison)
    {
        return comparison.metricIdentity == identity;
    });
    assert(found != member.comparisons.end());
    return *found;
}

bool HasDiagnostic(const Member& member, Diagnostic diagnostic)
{
    return std::find(member.diagnostics.begin(), member.diagnostics.end(),
               diagnostic) != member.diagnostics.end();
}

} // namespace

int main()
{
    // Multiple metrics, including a future metric, are canonicalized by metric
    // identity and compared independently.
    const std::vector<Metric> sourceMetrics{
        NumericMetric("leader_score", 0.80),
        NumericMetric("future_calibration", 0.50),
        NumericMetric("inference_accuracy", 0.70)};
    const std::vector<Metric> resultMetrics{
        NumericMetric("inference_accuracy", 0.75),
        NumericMetric("leader_score", 0.78),
        NumericMetric("future_calibration", 0.55)};
    const auto multiMetric = Assess(
        {Succeeded(1, sourceMetrics, resultMetrics)});
    const auto& multiMember = multiMetric.members[0];
    assert(multiMember.outcome == MemberOutcome::SucceededComparable);
    assert(multiMember.consistency == Consistency::Consistent);
    assert(multiMember.sourceEvidence->metrics.metrics.size() == 3);
    assert(multiMember.resultEvidence->metrics.metrics.size() == 3);
    assert(multiMember.comparisons.size() == 3);
    assert(multiMember.sourceEvidence->metrics.metrics[0].identity ==
        "future_calibration");
    assert(multiMember.sourceEvidence->metrics.metrics[1].identity ==
        "inference_accuracy");
    assert(multiMember.sourceEvidence->metrics.metrics[2].identity ==
        "leader_score");
    assert(multiMember.comparisons[0].metricIdentity == "future_calibration");
    assert(multiMember.comparisons[1].metricIdentity == "inference_accuracy");
    assert(multiMember.comparisons[2].metricIdentity == "leader_score");
    assert(ComparisonFor(multiMember, "inference_accuracy").delta &&
        *ComparisonFor(multiMember, "inference_accuracy").delta > 0.0);
    assert(ComparisonFor(multiMember, "leader_score").delta &&
        *ComparisonFor(multiMember, "leader_score").delta < 0.0);
    assert(ComparisonFor(multiMember, "future_calibration").delta &&
        *ComparisonFor(multiMember, "future_calibration").delta > 0.0);

    const auto reorderedMetrics = Assess({Succeeded(1,
        {sourceMetrics[2], sourceMetrics[0], sourceMetrics[1]},
        {resultMetrics[2], resultMetrics[1], resultMetrics[0]})});
    assert(reorderedMetrics.members == multiMetric.members);
    assert(reorderedMetrics.summary == multiMetric.summary);
    assert(reorderedMetrics.identity == multiMetric.identity);

    AssertInvalidArgument([&]
    {
        (void)Metrics({NumericMetric("leader_score", 0.1),
            NumericMetric("leader_score", 0.2)});
    });
    AssertInvalidArgument([]
    {
        (void)ResultEvidence(901, 1001,
            {ResultIdentity("analysis", 1),
                ResultIdentity("analysis", 1)},
            ComparableContext(), Metrics({}));
    });

    // Successful members distinguish comparable, changed-context, and metric
    // gap outcomes without mixing those scientific results into lifecycle.
    const auto comparable = Assess({Succeeded(1)});
    assert(comparable.members[0].lifecycle == Lifecycle::Succeeded);
    assert(comparable.members[0].outcome ==
        MemberOutcome::SucceededComparable);
    assert(comparable.summary.succeededComparableMemberCount == 1);
    assert(comparable.summary.comparableMetricCount == 2);
    assert(comparable.summary.positiveDeltaCount == 1);
    assert(comparable.summary.negativeDeltaCount == 1);

    const auto changedContext = Assess({Succeeded(1, BaseSourceMetrics(),
        BaseResultMetrics(), ComparableContext(), ComparableContext("gbpusd"))});
    assert(changedContext.members[0].outcome ==
        MemberOutcome::SucceededContextChanged);
    assert(changedContext.members[0].lifecycle == Lifecycle::Succeeded);
    assert(changedContext.members[0].consistency == Consistency::Consistent);
    for (const auto& comparison : changedContext.members[0].comparisons)
    {
        assert(comparison.classification == Classification::ContextChanged);
        assert(comparison.contextDifferences ==
            std::vector<Difference>{Difference::Symbol});
        assert(!comparison.delta);
    }

    const auto allContextDifferences = Assess({Succeeded(1,
        BaseSourceMetrics(), BaseResultMetrics(), ComparableContext(),
        ComparableContext("gbpusd", 24, 0.001, 192,
            "binary_forward_return_v2", "2026-02-01T00:00:00Z",
            "2026-04-30T23:59:59Z"))});
    assert(allContextDifferences.members[0].comparisons[0].contextDifferences ==
        std::vector<Difference>({Difference::Symbol,
            Difference::PredictionHorizon, Difference::Threshold,
            Difference::WindowSize, Difference::LabelDefinition,
            Difference::InferenceRange}));

    const auto metricGap = Assess({Succeeded(1,
        {NumericMetric("inference_accuracy", 0.70),
            NumericMetric("leader_score", 0.80)},
        {NumericMetric("future_calibration", 0.55),
            NumericMetric("inference_accuracy", 0.75)})});
    assert(metricGap.members[0].outcome == MemberOutcome::SucceededMetricGap);
    assert(ComparisonFor(metricGap.members[0], "future_calibration")
               .classification == Classification::MissingSourceMetric);
    assert(ComparisonFor(metricGap.members[0], "leader_score")
               .classification == Classification::MissingResultMetric);
    assert(metricGap.summary.succeededMetricGapMemberCount == 1);
    assert(metricGap.summary.metricComparisonCount == 3);
    assert(metricGap.summary.comparableMetricCount == 1);
    assert(metricGap.summary.missingSourceMetricCount == 1);
    assert(metricGap.summary.missingResultMetricCount == 1);

    const auto missingValue = Assess({Succeeded(1,
        {NumericMetric("inference_accuracy", std::nullopt)},
        {NumericMetric("inference_accuracy", 0.75)})});
    assert(missingValue.members[0].outcome ==
        MemberOutcome::SucceededMetricGap);
    assert(missingValue.members[0].comparisons[0].classification ==
        Classification::MetricValueUnavailable);
    const auto unsupported = Assess({Succeeded(1,
        {UnsupportedMetric("inference_accuracy", 0.70)},
        {NumericMetric("inference_accuracy", 0.75)})});
    assert(unsupported.members[0].comparisons[0].classification ==
        Classification::Unsupported);
    const auto supportedSingleMetric = Assess({Succeeded(1,
        {NumericMetric("inference_accuracy", 0.70)},
        {NumericMetric("inference_accuracy", 0.75)})});
    assert(supportedSingleMetric.members[0].comparisons[0].classification ==
        Classification::Comparable);
    assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);

    // Lifecycle states do not require result payload. Proposal-only is simply
    // not ready, never a missing-result scientific classification.
    const auto failed = Assess(
        {LifecycleOnly(1, Lifecycle::Failed, ResultExperiment(1))});
    assert(failed.members[0].outcome == MemberOutcome::TerminalFailed);
    assert(failed.members[0].consistency == Consistency::Consistent);
    assert(!failed.members[0].resultEvidence);
    assert(failed.members[0].comparisons.empty());

    const auto cancelled = Assess(
        {LifecycleOnly(1, Lifecycle::Cancelled, ResultExperiment(1))});
    assert(cancelled.members[0].outcome == MemberOutcome::TerminalCancelled);
    assert(!cancelled.members[0].resultEvidence);

    const auto nonterminal = Assess(
        {LifecycleOnly(1, Lifecycle::NotTerminal, ResultExperiment(1))});
    assert(nonterminal.members[0].outcome == MemberOutcome::NotReady);
    assert(nonterminal.members[0].diagnostics.empty());
    assert(!nonterminal.members[0].resultEvidence);

    const auto unknown = Assess({LifecycleOnly(1, Lifecycle::Unknown)});
    assert(unknown.members[0].lifecycle == Lifecycle::Unknown);
    assert(unknown.members[0].outcome == MemberOutcome::NotReady);

    const auto proposalOnly = Assess(
        {LifecycleOnly(1, Lifecycle::NotTerminal)});
    assert(!proposalOnly.members[0].identity.expectedExperimentId);
    assert(!proposalOnly.members[0].sourceEvidence);
    assert(!proposalOnly.members[0].resultEvidence);
    assert(proposalOnly.members[0].outcome == MemberOutcome::NotReady);
    assert(proposalOnly.members[0].diagnostics.empty());
    assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
        std::string::npos);

    AssertInvalidArgument([]
    {
        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
            Lifecycle::Failed, std::nullopt,
            Result(1, BaseResultMetrics()));
    });

    // Malformed downstream evidence is retained when member identity remains
    // trustworthy, with fixed-order diagnostics and no scientific comparison.
    const MemberEvidence incompleteResult(
        Identity(1, std::nullopt), Lifecycle::Succeeded,
        Source(1, {}, ComparableContext(), 999),
        ResultEvidence(901, std::nullopt, {}, std::nullopt, Metrics({})));
    const auto inconsistent = Assess({incompleteResult});
    assert(inconsistent.members[0].lifecycle == Lifecycle::Succeeded);
    assert(inconsistent.members[0].consistency == Consistency::Inconsistent);
    assert(inconsistent.members[0].outcome == MemberOutcome::Inconsistent);
    assert(inconsistent.members[0].resultEvidence);
    assert(inconsistent.members[0].comparisons.empty());
    assert(inconsistent.members[0].diagnostics ==
        std::vector<Diagnostic>({
            Diagnostic::SourceExperimentIdentityMismatch,
            Diagnostic::ExpectedExperimentIdentityMissing,
            Diagnostic::ResultModelIdentityMissing,
            Diagnostic::ResultIdentityMissing,
            Diagnostic::ResultContextMissing}));
    assert(inconsistent.summary.inconsistentMemberCount == 1);
    assert(inconsistent.summary.metricComparisonCount == 0);

    const MemberEvidence noResult(Identity(1, ResultExperiment(1)),
        Lifecycle::Succeeded, Source(1, BaseSourceMetrics()), std::nullopt);
    const auto noResultAssessment = Assess({noResult});
    assert(noResultAssessment.members[0].diagnostics ==
        std::vector<Diagnostic>{Diagnostic::ResultEvidenceMissing});
    assert(noResultAssessment.members[0].outcome ==
        MemberOutcome::Inconsistent);

    const MemberEvidence missingResultExperiment(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, BaseSourceMetrics()),
        ResultEvidence(std::nullopt, 1001,
            {ResultIdentity("scientific_result", ResultRecord(1))},
            ComparableContext(), Metrics(BaseResultMetrics())));
    const auto missingExperiment = Assess({missingResultExperiment});
    assert(missingExperiment.members[0].diagnostics ==
        std::vector<Diagnostic>{
            Diagnostic::ResultExperimentIdentityMissing});

    const MemberEvidence provenanceMismatch(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, BaseSourceMetrics()),
        Result(1, BaseResultMetrics(), ComparableContext(), 999));
    const auto malformedProvenance = Assess({provenanceMismatch});
    assert(malformedProvenance.members[0].diagnostics ==
        std::vector<Diagnostic>{
            Diagnostic::ResultExperimentIdentityMismatch});
    assert(malformedProvenance.members[0].resultEvidence->experimentId == 999);
    assert(malformedProvenance.members[0].consistency ==
        Consistency::Inconsistent);

    const MemberEvidence missingModel(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, BaseSourceMetrics()),
        ResultEvidence(ResultExperiment(1), std::nullopt,
            {ResultIdentity("scientific_result", ResultRecord(1))},
            ComparableContext(), Metrics(BaseResultMetrics())));
    const auto missingModelAssessment = Assess({missingModel});
    assert(missingModelAssessment.members[0].diagnostics ==
        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityMissing});
    assert(missingModelAssessment.members[0].consistency ==
        Consistency::Inconsistent);
    assert(missingModelAssessment.identity.hash !=
        malformedProvenance.identity.hash);

    // Cross-member experiment reuse marks every affected member, and the
    // mismatched member additionally retains its exact mismatch diagnostic.
    const MemberEvidence reusedExperimentOne(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, BaseSourceMetrics()), Result(1, BaseResultMetrics()));
    const MemberEvidence reusedExperimentTwo(
        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
        Source(2, BaseSourceMetrics()), Result(2, BaseResultMetrics(),
            ComparableContext(), ResultExperiment(1)));
    const auto experimentReuse =
        Assess({reusedExperimentTwo, reusedExperimentOne});
    assert(HasDiagnostic(experimentReuse.members[0],
        Diagnostic::ResultExperimentReused));
    assert(HasDiagnostic(experimentReuse.members[1],
        Diagnostic::ResultExperimentReused));
    assert(!HasDiagnostic(experimentReuse.members[0],
        Diagnostic::ResultExperimentIdentityMismatch));
    assert(HasDiagnostic(experimentReuse.members[1],
        Diagnostic::ResultExperimentIdentityMismatch));
    assert(experimentReuse.members[0].outcome == MemberOutcome::Inconsistent);
    assert(experimentReuse.members[1].outcome == MemberOutcome::Inconsistent);

    const MemberEvidence reusedResultOne(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, BaseSourceMetrics()), Result(1, BaseResultMetrics(),
            ComparableContext(), std::nullopt, 8888));
    const MemberEvidence reusedResultTwo(
        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
        Source(2, BaseSourceMetrics()), Result(2, BaseResultMetrics(),
            ComparableContext(), std::nullopt, 8888));
    const auto resultReuse = Assess({reusedResultTwo, reusedResultOne});
    assert(resultReuse.members[0].diagnostics ==
        std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
    assert(resultReuse.members[1].diagnostics ==
        std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});

    // Aggregate precedence is deterministic: inconsistency, failure,
    // cancellation, not-ready, then successful context/gap/comparable states.
    const auto allPrecedence = Assess({Succeeded(5),
        LifecycleOnly(2, Lifecycle::Failed, ResultExperiment(2)),
        provenanceMismatch,
        LifecycleOnly(4, Lifecycle::NotTerminal, ResultExperiment(4)),
        LifecycleOnly(3, Lifecycle::Cancelled, ResultExperiment(3))});
    assert(allPrecedence.summary.outcome == AggregateOutcome::Inconsistent);
    assert(Assess({Succeeded(3),
               LifecycleOnly(1, Lifecycle::Failed, ResultExperiment(1)),
               LifecycleOnly(2, Lifecycle::Cancelled, ResultExperiment(2))})
               .summary.outcome == AggregateOutcome::HasFailures);
    assert(Assess({Succeeded(2),
               LifecycleOnly(1, Lifecycle::Cancelled, ResultExperiment(1))})
               .summary.outcome == AggregateOutcome::HasCancellations);
    assert(Assess({Succeeded(2),
               LifecycleOnly(1, Lifecycle::NotTerminal,
                   ResultExperiment(1))})
               .summary.outcome == AggregateOutcome::NotReady);
    assert(Assess({Succeeded(1, BaseSourceMetrics(), BaseResultMetrics(),
                       ComparableContext(), ComparableContext("gbpusd")),
               Succeeded(2, {NumericMetric("a", 0.1)}, {})})
               .summary.outcome ==
        AggregateOutcome::SucceededContextChanged);
    assert(Assess({Succeeded(1, {NumericMetric("a", 0.1)}, {}),
               Succeeded(2)})
               .summary.outcome == AggregateOutcome::SucceededMetricGap);
    assert(comparable.summary.outcome ==
        AggregateOutcome::SucceededComparable);

    // Member outcomes and per-metric/delta counts remain separate.
    assert(multiMetric.summary.memberCount == 1);
    assert(multiMetric.summary.succeededLifecycleCount == 1);
    assert(multiMetric.summary.succeededComparableMemberCount == 1);
    assert(multiMetric.summary.metricComparisonCount == 3);
    assert(multiMetric.summary.comparableMetricCount == 3);
    assert(multiMetric.summary.positiveDeltaCount == 2);
    assert(multiMetric.summary.negativeDeltaCount == 1);
    assert(multiMetric.summary.zeroDeltaCount == 0);

    const auto zeroDelta = Assess({Succeeded(1,
        {NumericMetric("a", -0.0)}, {NumericMetric("a", 0.0)})});
    assert(zeroDelta.members[0].comparisons[0].delta);
    assert(*zeroDelta.members[0].comparisons[0].delta == 0.0);
    assert(!std::signbit(*zeroDelta.members[0].comparisons[0].delta));
    assert(zeroDelta.summary.zeroDeltaCount == 1);

    // Every contract dimension is bound into identity. observed_at alone is
    // intentionally excluded.
    const auto laterObservation = Assess({Succeeded(1)},
        "2026-07-19T22:05:00Z");
    assert(laterObservation.observedAt != comparable.observedAt);
    assert(laterObservation.identity == comparable.identity);

    const auto lifecycleNotTerminal =
        Assess({LifecycleOnly(1, Lifecycle::NotTerminal)});
    const auto lifecycleUnknown =
        Assess({LifecycleOnly(1, Lifecycle::Unknown)});
    assert(lifecycleNotTerminal.members[0].outcome ==
        lifecycleUnknown.members[0].outcome);
    assert(lifecycleNotTerminal.identity.hash !=
        lifecycleUnknown.identity.hash);

    assert(comparable.identity.hash != malformedProvenance.identity.hash);
    assert(malformedProvenance.identity.hash !=
        noResultAssessment.identity.hash);
    const auto changedMetricValue = Assess({Succeeded(1,
        BaseSourceMetrics(),
        {NumericMetric("inference_accuracy", 0.76),
            NumericMetric("leader_score", 0.78)})});
    assert(changedMetricValue.identity.hash != comparable.identity.hash);
    assert(metricGap.identity.hash != comparable.identity.hash);
    assert(changedContext.identity.hash != comparable.identity.hash);
    assert(unsupported.identity.hash != supportedSingleMetric.identity.hash);

    const MemberEvidence changedProvenance(
        Identity(1, 1901), Lifecycle::Succeeded,
        Source(1, BaseSourceMetrics()),
        ResultEvidence(1901, 1001,
            {ResultIdentity("scientific_result", 2901)},
            ComparableContext(), Metrics(BaseResultMetrics())));
    const auto changedProvenanceAssessment = Assess({changedProvenance});
    assert(changedProvenanceAssessment.members[0].outcome ==
        MemberOutcome::SucceededComparable);
    assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
    assert(comparable.identity.hash == RecommendationCanonicalHash(
        comparable.identity.canonicalText));
    assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
        std::string::npos);
    assert(malformedProvenance.identity.canonicalText.find(
               "result_experiment_identity_mismatch") != std::string::npos);

    // Member order, result identity order, diagnostics, metrics, comparisons,
    // canonical text, and hash are deterministic.
    const MemberEvidence twoResultIdentities(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, BaseSourceMetrics()),
        ResultEvidence(ResultExperiment(1), 1001,
            {ResultIdentity("z_future", 12),
                ResultIdentity("analysis", 11)},
            ComparableContext(), Metrics(BaseResultMetrics())));
    const MemberEvidence twoResultIdentitiesReordered(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, {BaseSourceMetrics()[1], BaseSourceMetrics()[0]}),
        ResultEvidence(ResultExperiment(1), 1001,
            {ResultIdentity("analysis", 11),
                ResultIdentity("z_future", 12)},
            ComparableContext(),
            Metrics({BaseResultMetrics()[1], BaseResultMetrics()[0]})));
    const auto canonicalOne = Assess({twoResultIdentities});
    const auto canonicalTwo = Assess({twoResultIdentitiesReordered});
    assert(canonicalOne.members == canonicalTwo.members);
    assert(canonicalOne.identity == canonicalTwo.identity);
    assert(canonicalOne.members[0].resultEvidence->resultIdentities[0].kind ==
        "analysis");
    assert(canonicalOne.members[0].resultEvidence->resultIdentities[1].kind ==
        "z_future");

    const auto orderedMembers = Assess({Succeeded(1), Succeeded(2)});
    const auto shuffledMembers = Assess({Succeeded(2), Succeeded(1)});
    assert(orderedMembers.members == shuffledMembers.members);
    assert(orderedMembers.summary == shuffledMembers.summary);
    assert(orderedMembers.identity == shuffledMembers.identity);
    assert(shuffledMembers.members[0].identity.memberOrdinal == 1);
    assert(shuffledMembers.members[1].identity.memberOrdinal == 2);

    // Invalid scalar input and untrustworthy top-level identity fail fast.
    AssertInvalidArgument([]
    {
        (void)Metric("inference_accuracy",
            std::numeric_limits<double>::quiet_NaN(),
            MetricSupport::NumericDelta);
    });
    AssertInvalidArgument([]
    {
        (void)ComparableContext("eurusd", 12,
            std::numeric_limits<double>::infinity());
    });
    AssertInvalidArgument([]
    {
        (void)ResultIdentity("analysis", 0);
    });
    AssertInvalidArgument([]
    {
        (void)Identity(1, 0);
    });
    AssertInvalidArgument([]
    {
        (void)Metric(std::string("bad\0identity", 12), 0.1,
            MetricSupport::NumericDelta);
    });
    AssertInvalidArgument([]
    {
        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
            Materialization(2), "2026-07-19T22:00:00Z",
            {Succeeded(1), Succeeded(1)});
    });
    AssertInvalidArgument([]
    {
        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
            std::nullopt, std::nullopt);
        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
            std::nullopt, std::nullopt);
        (void)Assess({first, second});
    });
    AssertInvalidArgument([]
    {
        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
            Materialization(1), "", {Succeeded(1)});
    });

    const MemberEvidence overflowingDelta(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, {NumericMetric("wide",-
            std::numeric_limits<double>::max())}),
        Result(1, {NumericMetric("wide",
            std::numeric_limits<double>::max())}));
    const auto overflowAssessment = Assess({overflowingDelta});
    assert(overflowAssessment.members[0].diagnostics ==
        std::vector<Diagnostic>{Diagnostic::NumericDeltaNonFinite});
    assert(overflowAssessment.members[0].comparisons.empty());
    assert(overflowAssessment.members[0].outcome == MemberOutcome::Inconsistent);

    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
               Lifecycle::NotTerminal) == "not_terminal");
    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
               Lifecycle::Succeeded) == "succeeded");
    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
               Lifecycle::Failed) == "failed");
    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
               Lifecycle::Cancelled) == "cancelled");
    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
               Lifecycle::Unknown) == "unknown");
    assert(RecommendationCampaignOutcomeAssessmentConsistencyStateText(
               Consistency::Consistent) == "consistent");
    assert(RecommendationCampaignOutcomeAssessmentConsistencyStateText(
               Consistency::Inconsistent) == "inconsistent");
    assert(RecommendationCampaignOutcomeAssessmentMemberOutcomeText(
               MemberOutcome::SucceededMetricGap) == "succeeded_metric_gap");
    assert(RecommendationCampaignOutcomeAssessmentAggregateOutcomeText(
               AggregateOutcome::HasFailures) == "has_failures");

    return 0;
}

exec
/bin/bash -lc "sed -n '1,260p' Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp && sed -n '261,520p' Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp && sed -n '521,780p' Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp && sed -n '1,180p' docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst && sed -n '1,180p' docs/Phase5ExperimentRecommendationCampaignExecution.rst" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
#include "ExperimentRecommendationCampaignOutcomeAssessment.hpp"

#include "ExperimentRecommendation.hpp"
#include "ExperimentRecommendationCampaignMaterialization.hpp"

#include <algorithm>
#include <cassert>
#include <cmath>
#include <limits>
#include <optional>
#include <stdexcept>
#include <string>
#include <type_traits>
#include <utility>
#include <vector>

using namespace EA::ExperimentRecommendation;

namespace
{

using AggregateOutcome =
    RecommendationCampaignOutcomeAssessmentAggregateOutcome;
using Assessment = RecommendationCampaignOutcomeAssessment;
using CampaignIdentity =
    RecommendationCampaignOutcomeAssessmentCampaignIdentity;
using Classification =
    RecommendationCampaignOutcomeAssessmentMetricComparisonClassification;
using Consistency = RecommendationCampaignOutcomeAssessmentConsistencyState;
using Context = RecommendationCampaignOutcomeAssessmentComparisonContext;
using Diagnostic = RecommendationCampaignOutcomeAssessmentDiagnosticCode;
using Difference = RecommendationCampaignOutcomeAssessmentContextDifference;
using Lifecycle = RecommendationCampaignOutcomeAssessmentLifecycleState;
using MaterializationIdentity =
    RecommendationCampaignOutcomeAssessmentMaterializationIdentity;
using Member = RecommendationCampaignOutcomeAssessmentMember;
using MemberEvidence = RecommendationCampaignOutcomeAssessmentMemberEvidence;
using MemberIdentity =
    RecommendationCampaignOutcomeAssessmentMemberIdentity;
using MemberOutcome = RecommendationCampaignOutcomeAssessmentMemberOutcome;
using Metric = RecommendationCampaignOutcomeAssessmentMetric;
using MetricCollection =
    RecommendationCampaignOutcomeAssessmentMetricCollection;
using MetricComparison =
    RecommendationCampaignOutcomeAssessmentMetricComparison;
using MetricSupport =
    RecommendationCampaignOutcomeAssessmentMetricComparisonSupport;
using ResultEvidence =
    RecommendationCampaignOutcomeAssessmentResultEvidence;
using ResultIdentity =
    RecommendationCampaignOutcomeAssessmentResultIdentity;
using SourceEvidence =
    RecommendationCampaignOutcomeAssessmentSourceEvidence;
using Summary = RecommendationCampaignOutcomeAssessmentSummary;

static_assert(Assessment::readOnly);
static_assert(Assessment::pointInTime);
static_assert(!Assessment::persistent);
static_assert(!Assessment::authoritative);
static_assert(!Assessment::declaresCampaignSuccess);
static_assert(!std::is_default_constructible_v<Assessment>);
static_assert(!std::is_default_constructible_v<Member>);
static_assert(!std::is_default_constructible_v<Summary>);
static_assert(!std::is_default_constructible_v<MetricComparison>);
static_assert(!std::is_aggregate_v<Assessment>);
static_assert(!std::is_aggregate_v<Member>);
static_assert(!std::is_aggregate_v<Summary>);
static_assert(!std::is_aggregate_v<MetricComparison>);
static_assert(!std::is_copy_assignable_v<Assessment>);
static_assert(!std::is_copy_assignable_v<Member>);
static_assert(!std::is_copy_assignable_v<Summary>);
static_assert(!std::is_copy_assignable_v<MetricComparison>);
static_assert(!std::is_constructible_v<MetricComparison, std::string,
    Classification, std::optional<double>, std::optional<double>,
    std::optional<double>, std::vector<Difference>>);
static_assert(!std::is_constructible_v<Member, MemberIdentity, Lifecycle,
    Consistency, MemberOutcome, std::vector<Diagnostic>,
    std::optional<SourceEvidence>, std::optional<ResultEvidence>,
    std::vector<MetricComparison>>);

template <std::size_t... Indices>
consteval bool SummaryConstructorIsPublic(std::index_sequence<Indices...>)
{
    return std::is_constructible_v<Summary, AggregateOutcome,
        decltype((void)Indices, int{})...>;
}

static_assert(!SummaryConstructorIsPublic(std::make_index_sequence<25>{}));

template <typename Function>
void AssertInvalidArgument(Function&& function)
{
    bool threw = false;
    try
    {
        function();
    }
    catch (const std::invalid_argument&)
    {
        threw = true;
    }
    assert(threw);
}

CampaignIdentity Campaign()
{
    const std::string canonical = "campaign-approval-canonical";
    return {41, canonical, RecommendationCanonicalHash(canonical)};
}

MaterializationIdentity Materialization(int memberCount)
{
    const std::string canonical =
        "materialization-canonical:" + std::to_string(memberCount);
    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
        memberCount, canonical, RecommendationCanonicalHash(canonical)};
}

Context ComparableContext(
    std::string symbol = "eurusd",
    int horizon = 12,
    double threshold = 0.0008,
    int windowSize = 96,
    std::string labelDefinition = "three_class_forward_return_v1",
    std::string rangeStart = "2026-01-01T00:00:00Z",
    std::string rangeEnd = "2026-03-31T23:59:59Z")
{
    return {std::move(symbol), horizon, threshold, windowSize,
        std::move(labelDefinition), std::move(rangeStart),
        std::move(rangeEnd)};
}

Metric NumericMetric(
    std::string identity,
    std::optional<double> value)
{
    return {std::move(identity), value, MetricSupport::NumericDelta};
}

Metric UnsupportedMetric(
    std::string identity,
    std::optional<double> value)
{
    return {std::move(identity), value, MetricSupport::Unsupported};
}

MetricCollection Metrics(std::vector<Metric> metrics)
{
    return MetricCollection(std::move(metrics));
}

long long SourceExperiment(int ordinal)
{
    return 500 + ordinal;
}

long long ResultExperiment(int ordinal)
{
    return 900 + ordinal;
}

long long ResultRecord(int ordinal)
{
    return 2000 + ordinal;
}

MemberIdentity Identity(
    int ordinal,
    std::optional<long long> expectedExperimentId)
{
    return {ordinal, 100 + ordinal, 200 + ordinal, 300 + ordinal,
        SourceExperiment(ordinal), 400 + ordinal, expectedExperimentId};
}

SourceEvidence Source(
    int ordinal,
    std::vector<Metric> metrics,
    Context context = ComparableContext(),
    std::optional<long long> sourceExperimentOverride = std::nullopt)
{
    return {sourceExperimentOverride.value_or(SourceExperiment(ordinal)),
        600 + ordinal, 700 + ordinal, std::move(context),
        Metrics(std::move(metrics))};
}

ResultEvidence Result(
    int ordinal,
    std::vector<Metric> metrics,
    Context context = ComparableContext(),
    std::optional<long long> experimentOverride = std::nullopt,
    std::optional<long long> resultRecordOverride = std::nullopt)
{
    return {experimentOverride.value_or(ResultExperiment(ordinal)),
        1000 + ordinal,
        {ResultIdentity("scientific_result",
            resultRecordOverride.value_or(ResultRecord(ordinal)))},
        std::move(context), Metrics(std::move(metrics))};
}

std::vector<Metric> BaseSourceMetrics()
{
    return {NumericMetric("inference_accuracy", 0.70),
        NumericMetric("leader_score", 0.80)};
}

std::vector<Metric> BaseResultMetrics()
{
    return {NumericMetric("inference_accuracy", 0.75),
        NumericMetric("leader_score", 0.78)};
}

MemberEvidence Succeeded(
    int ordinal,
    std::vector<Metric> sourceMetrics = BaseSourceMetrics(),
    std::vector<Metric> resultMetrics = BaseResultMetrics(),
    Context sourceContext = ComparableContext(),
    Context resultContext = ComparableContext())
{
    return {Identity(ordinal, ResultExperiment(ordinal)),
        Lifecycle::Succeeded,
        Source(ordinal, std::move(sourceMetrics), std::move(sourceContext)),
        Result(ordinal, std::move(resultMetrics), std::move(resultContext))};
}

MemberEvidence LifecycleOnly(
    int ordinal,
    Lifecycle lifecycle,
    std::optional<long long> expectedExperimentId = std::nullopt)
{
    return {Identity(ordinal, expectedExperimentId), lifecycle, std::nullopt,
        std::nullopt};
}

Assessment Assess(
    std::vector<MemberEvidence> members,
    std::string observedAt = "2026-07-19T22:00:00Z")
{
    return BuildRecommendationCampaignOutcomeAssessment(Campaign(),
        Materialization(static_cast<int>(members.size())), observedAt,
        members);
}

const MetricComparison& ComparisonFor(
    const Member& member,
    const std::string& identity)
{
    const auto found = std::find_if(member.comparisons.begin(),
        member.comparisons.end(), [&](const MetricComparison& comparison)
    {
        return comparison.metricIdentity == identity;
    });
    assert(found != member.comparisons.end());
    return *found;
}

bool HasDiagnostic(const Member& member, Diagnostic diagnostic)
{
    return std::find(member.diagnostics.begin(), member.diagnostics.end(),
               diagnostic) != member.diagnostics.end();
}

} // namespace

int main()
{
    // Multiple metrics, including a future metric, are canonicalized by metric
    // identity and compared independently.
    const std::vector<Metric> sourceMetrics{
        NumericMetric("leader_score", 0.80),
        NumericMetric("future_calibration", 0.50),
        NumericMetric("inference_accuracy", 0.70)};
    const std::vector<Metric> resultMetrics{
        NumericMetric("inference_accuracy", 0.75),
        NumericMetric("leader_score", 0.78),
        NumericMetric("future_calibration", 0.55)};
    const auto multiMetric = Assess(
        {Succeeded(1, sourceMetrics, resultMetrics)});
    const auto& multiMember = multiMetric.members[0];
    assert(multiMember.outcome == MemberOutcome::SucceededComparable);
    assert(multiMember.consistency == Consistency::Consistent);
    assert(multiMember.sourceEvidence->metrics.metrics.size() == 3);
    assert(multiMember.resultEvidence->metrics.metrics.size() == 3);
    assert(multiMember.comparisons.size() == 3);
    assert(multiMember.sourceEvidence->metrics.metrics[0].identity ==
        "future_calibration");
    assert(multiMember.sourceEvidence->metrics.metrics[1].identity ==
        "inference_accuracy");
    assert(multiMember.sourceEvidence->metrics.metrics[2].identity ==
        "leader_score");
    assert(multiMember.comparisons[0].metricIdentity == "future_calibration");
    assert(multiMember.comparisons[1].metricIdentity == "inference_accuracy");
    assert(multiMember.comparisons[2].metricIdentity == "leader_score");
    assert(ComparisonFor(multiMember, "inference_accuracy").delta &&
        *ComparisonFor(multiMember, "inference_accuracy").delta > 0.0);
    assert(ComparisonFor(multiMember, "leader_score").delta &&
        *ComparisonFor(multiMember, "leader_score").delta < 0.0);
    assert(ComparisonFor(multiMember, "future_calibration").delta &&
        *ComparisonFor(multiMember, "future_calibration").delta > 0.0);

    const auto reorderedMetrics = Assess({Succeeded(1,
        {sourceMetrics[2], sourceMetrics[0], sourceMetrics[1]},
        {resultMetrics[2], resultMetrics[1], resultMetrics[0]})});
    assert(reorderedMetrics.members == multiMetric.members);
    assert(reorderedMetrics.summary == multiMetric.summary);
    assert(reorderedMetrics.identity == multiMetric.identity);

    AssertInvalidArgument([&]
    {
        (void)Metrics({NumericMetric("leader_score", 0.1),
            NumericMetric("leader_score", 0.2)});
    });
    AssertInvalidArgument([]
    {
        (void)ResultEvidence(901, 1001,
            {ResultIdentity("analysis", 1),
                ResultIdentity("analysis", 1)},
            ComparableContext(), Metrics({}));
    });

    // Successful members distinguish comparable, changed-context, and metric
    // gap outcomes without mixing those scientific results into lifecycle.
    const auto comparable = Assess({Succeeded(1)});
    assert(comparable.members[0].lifecycle == Lifecycle::Succeeded);
    assert(comparable.members[0].outcome ==
        MemberOutcome::SucceededComparable);
    assert(comparable.summary.succeededComparableMemberCount == 1);
    assert(comparable.summary.comparableMetricCount == 2);
    assert(comparable.summary.positiveDeltaCount == 1);
    assert(comparable.summary.negativeDeltaCount == 1);

    const auto changedContext = Assess({Succeeded(1, BaseSourceMetrics(),
        BaseResultMetrics(), ComparableContext(), ComparableContext("gbpusd"))});
    assert(changedContext.members[0].outcome ==
        MemberOutcome::SucceededContextChanged);
    assert(changedContext.members[0].lifecycle == Lifecycle::Succeeded);
    assert(changedContext.members[0].consistency == Consistency::Consistent);
    for (const auto& comparison : changedContext.members[0].comparisons)
    {
        assert(comparison.classification == Classification::ContextChanged);
        assert(comparison.contextDifferences ==
            std::vector<Difference>{Difference::Symbol});
        assert(!comparison.delta);
    }

    const auto allContextDifferences = Assess({Succeeded(1,
        BaseSourceMetrics(), BaseResultMetrics(), ComparableContext(),
        ComparableContext("gbpusd", 24, 0.001, 192,
            "binary_forward_return_v2", "2026-02-01T00:00:00Z",
            "2026-04-30T23:59:59Z"))});
    assert(allContextDifferences.members[0].comparisons[0].contextDifferences ==
        std::vector<Difference>({Difference::Symbol,
            Difference::PredictionHorizon, Difference::Threshold,
            Difference::WindowSize, Difference::LabelDefinition,
            Difference::InferenceRange}));

    const auto metricGap = Assess({Succeeded(1,
        {NumericMetric("inference_accuracy", 0.70),
            NumericMetric("leader_score", 0.80)},
        {NumericMetric("future_calibration", 0.55),
            NumericMetric("inference_accuracy", 0.75)})});
    assert(metricGap.members[0].outcome == MemberOutcome::SucceededMetricGap);
    assert(ComparisonFor(metricGap.members[0], "future_calibration")
               .classification == Classification::MissingSourceMetric);
    assert(ComparisonFor(metricGap.members[0], "leader_score")
               .classification == Classification::MissingResultMetric);
    assert(metricGap.summary.succeededMetricGapMemberCount == 1);
    assert(metricGap.summary.metricComparisonCount == 3);
    assert(metricGap.summary.comparableMetricCount == 1);
    assert(metricGap.summary.missingSourceMetricCount == 1);
    assert(metricGap.summary.missingResultMetricCount == 1);

    const auto missingValue = Assess({Succeeded(1,
        {NumericMetric("inference_accuracy", std::nullopt)},
        {NumericMetric("inference_accuracy", 0.75)})});
    assert(missingValue.members[0].outcome ==
        MemberOutcome::SucceededMetricGap);
    assert(missingValue.members[0].comparisons[0].classification ==
        Classification::MetricValueUnavailable);
    const auto unsupported = Assess({Succeeded(1,
        {UnsupportedMetric("inference_accuracy", 0.70)},
        {NumericMetric("inference_accuracy", 0.75)})});
    assert(unsupported.members[0].comparisons[0].classification ==
        Classification::Unsupported);
    const auto supportedSingleMetric = Assess({Succeeded(1,
        {NumericMetric("inference_accuracy", 0.70)},
        {NumericMetric("inference_accuracy", 0.75)})});
    assert(supportedSingleMetric.members[0].comparisons[0].classification ==
        Classification::Comparable);
    assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);

    // Lifecycle states do not require result payload. Proposal-only is simply
    // not ready, never a missing-result scientific classification.
    const auto failed = Assess(
        {LifecycleOnly(1, Lifecycle::Failed, ResultExperiment(1))});
    assert(failed.members[0].outcome == MemberOutcome::TerminalFailed);
    assert(failed.members[0].consistency == Consistency::Consistent);
    assert(!failed.members[0].resultEvidence);
    assert(failed.members[0].comparisons.empty());

    const auto cancelled = Assess(
        {LifecycleOnly(1, Lifecycle::Cancelled, ResultExperiment(1))});
    assert(cancelled.members[0].outcome == MemberOutcome::TerminalCancelled);
    assert(!cancelled.members[0].resultEvidence);

    const auto nonterminal = Assess(
        {LifecycleOnly(1, Lifecycle::NotTerminal, ResultExperiment(1))});
    assert(nonterminal.members[0].outcome == MemberOutcome::NotReady);
    assert(nonterminal.members[0].diagnostics.empty());
    assert(!nonterminal.members[0].resultEvidence);

    const auto unknown = Assess({LifecycleOnly(1, Lifecycle::Unknown)});
    assert(unknown.members[0].lifecycle == Lifecycle::Unknown);
    assert(unknown.members[0].outcome == MemberOutcome::NotReady);

    const auto proposalOnly = Assess(
        {LifecycleOnly(1, Lifecycle::NotTerminal)});
    assert(!proposalOnly.members[0].identity.expectedExperimentId);
    assert(!proposalOnly.members[0].sourceEvidence);
    assert(!proposalOnly.members[0].resultEvidence);
    assert(proposalOnly.members[0].outcome == MemberOutcome::NotReady);
    assert(proposalOnly.members[0].diagnostics.empty());
    assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
        std::string::npos);

    AssertInvalidArgument([]
    {
        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
            Lifecycle::Failed, std::nullopt,
            Result(1, BaseResultMetrics()));
    });

    // Malformed downstream evidence is retained when member identity remains
    // trustworthy, with fixed-order diagnostics and no scientific comparison.
    const MemberEvidence incompleteResult(
        Identity(1, std::nullopt), Lifecycle::Succeeded,
        Source(1, {}, ComparableContext(), 999),
        ResultEvidence(901, std::nullopt, {}, std::nullopt, Metrics({})));
    const auto inconsistent = Assess({incompleteResult});
    assert(inconsistent.members[0].lifecycle == Lifecycle::Succeeded);
    assert(inconsistent.members[0].consistency == Consistency::Inconsistent);
    assert(inconsistent.members[0].outcome == MemberOutcome::Inconsistent);
    assert(inconsistent.members[0].resultEvidence);
    assert(inconsistent.members[0].comparisons.empty());
    assert(inconsistent.members[0].diagnostics ==
        std::vector<Diagnostic>({
            Diagnostic::SourceExperimentIdentityMismatch,
            Diagnostic::ExpectedExperimentIdentityMissing,
            Diagnostic::ResultModelIdentityMissing,
            Diagnostic::ResultIdentityMissing,
            Diagnostic::ResultContextMissing}));
    assert(inconsistent.summary.inconsistentMemberCount == 1);
    assert(inconsistent.summary.metricComparisonCount == 0);

    const MemberEvidence noResult(Identity(1, ResultExperiment(1)),
        Lifecycle::Succeeded, Source(1, BaseSourceMetrics()), std::nullopt);
    const auto noResultAssessment = Assess({noResult});
    assert(noResultAssessment.members[0].diagnostics ==
        std::vector<Diagnostic>{Diagnostic::ResultEvidenceMissing});
    assert(noResultAssessment.members[0].outcome ==
        MemberOutcome::Inconsistent);

    const MemberEvidence missingResultExperiment(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, BaseSourceMetrics()),
        ResultEvidence(std::nullopt, 1001,
            {ResultIdentity("scientific_result", ResultRecord(1))},
            ComparableContext(), Metrics(BaseResultMetrics())));
    const auto missingExperiment = Assess({missingResultExperiment});
    assert(missingExperiment.members[0].diagnostics ==
        std::vector<Diagnostic>{
            Diagnostic::ResultExperimentIdentityMissing});

    const MemberEvidence provenanceMismatch(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, BaseSourceMetrics()),
        Result(1, BaseResultMetrics(), ComparableContext(), 999));
    const auto malformedProvenance = Assess({provenanceMismatch});
    assert(malformedProvenance.members[0].diagnostics ==
        std::vector<Diagnostic>{
            Diagnostic::ResultExperimentIdentityMismatch});
    assert(malformedProvenance.members[0].resultEvidence->experimentId == 999);
    assert(malformedProvenance.members[0].consistency ==
        Consistency::Inconsistent);

    const MemberEvidence missingModel(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, BaseSourceMetrics()),
        ResultEvidence(ResultExperiment(1), std::nullopt,
            {ResultIdentity("scientific_result", ResultRecord(1))},
            ComparableContext(), Metrics(BaseResultMetrics())));
    const auto missingModelAssessment = Assess({missingModel});
    assert(missingModelAssessment.members[0].diagnostics ==
        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityMissing});
    assert(missingModelAssessment.members[0].consistency ==
        Consistency::Inconsistent);
    assert(missingModelAssessment.identity.hash !=
        malformedProvenance.identity.hash);

    // Cross-member experiment reuse marks every affected member, and the
    // mismatched member additionally retains its exact mismatch diagnostic.
    const MemberEvidence reusedExperimentOne(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, BaseSourceMetrics()), Result(1, BaseResultMetrics()));
    const MemberEvidence reusedExperimentTwo(
        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
        Source(2, BaseSourceMetrics()), Result(2, BaseResultMetrics(),
            ComparableContext(), ResultExperiment(1)));
    const auto experimentReuse =
        Assess({reusedExperimentTwo, reusedExperimentOne});
    assert(HasDiagnostic(experimentReuse.members[0],
        Diagnostic::ResultExperimentReused));
    assert(HasDiagnostic(experimentReuse.members[1],
        Diagnostic::ResultExperimentReused));
    assert(!HasDiagnostic(experimentReuse.members[0],
        Diagnostic::ResultExperimentIdentityMismatch));
    assert(HasDiagnostic(experimentReuse.members[1],
        Diagnostic::ResultExperimentIdentityMismatch));
    assert(experimentReuse.members[0].outcome == MemberOutcome::Inconsistent);
    assert(experimentReuse.members[1].outcome == MemberOutcome::Inconsistent);

    const MemberEvidence reusedResultOne(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, BaseSourceMetrics()), Result(1, BaseResultMetrics(),
            ComparableContext(), std::nullopt, 8888));
    const MemberEvidence reusedResultTwo(
        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
        Source(2, BaseSourceMetrics()), Result(2, BaseResultMetrics(),
            ComparableContext(), std::nullopt, 8888));
    const auto resultReuse = Assess({reusedResultTwo, reusedResultOne});
    assert(resultReuse.members[0].diagnostics ==
        std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
    assert(resultReuse.members[1].diagnostics ==
        std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});

    // Aggregate precedence is deterministic: inconsistency, failure,
    // cancellation, not-ready, then successful context/gap/comparable states.
    const auto allPrecedence = Assess({Succeeded(5),
        LifecycleOnly(2, Lifecycle::Failed, ResultExperiment(2)),
        provenanceMismatch,
        LifecycleOnly(4, Lifecycle::NotTerminal, ResultExperiment(4)),
        LifecycleOnly(3, Lifecycle::Cancelled, ResultExperiment(3))});
    assert(allPrecedence.summary.outcome == AggregateOutcome::Inconsistent);
    assert(Assess({Succeeded(3),
               LifecycleOnly(1, Lifecycle::Failed, ResultExperiment(1)),
               LifecycleOnly(2, Lifecycle::Cancelled, ResultExperiment(2))})
               .summary.outcome == AggregateOutcome::HasFailures);
    assert(Assess({Succeeded(2),
               LifecycleOnly(1, Lifecycle::Cancelled, ResultExperiment(1))})
               .summary.outcome == AggregateOutcome::HasCancellations);
    assert(Assess({Succeeded(2),
               LifecycleOnly(1, Lifecycle::NotTerminal,
                   ResultExperiment(1))})
               .summary.outcome == AggregateOutcome::NotReady);
    assert(Assess({Succeeded(1, BaseSourceMetrics(), BaseResultMetrics(),
                       ComparableContext(), ComparableContext("gbpusd")),
               Succeeded(2, {NumericMetric("a", 0.1)}, {})})
               .summary.outcome ==
        AggregateOutcome::SucceededContextChanged);
    assert(Assess({Succeeded(1, {NumericMetric("a", 0.1)}, {}),
               Succeeded(2)})
               .summary.outcome == AggregateOutcome::SucceededMetricGap);
    assert(comparable.summary.outcome ==
        AggregateOutcome::SucceededComparable);

    // Member outcomes and per-metric/delta counts remain separate.
    assert(multiMetric.summary.memberCount == 1);
    assert(multiMetric.summary.succeededLifecycleCount == 1);
    assert(multiMetric.summary.succeededComparableMemberCount == 1);
    assert(multiMetric.summary.metricComparisonCount == 3);
    assert(multiMetric.summary.comparableMetricCount == 3);
    assert(multiMetric.summary.positiveDeltaCount == 2);
    assert(multiMetric.summary.negativeDeltaCount == 1);
    assert(multiMetric.summary.zeroDeltaCount == 0);

    const auto zeroDelta = Assess({Succeeded(1,
        {NumericMetric("a", -0.0)}, {NumericMetric("a", 0.0)})});
    assert(zeroDelta.members[0].comparisons[0].delta);
    assert(*zeroDelta.members[0].comparisons[0].delta == 0.0);
    assert(!std::signbit(*zeroDelta.members[0].comparisons[0].delta));
    assert(zeroDelta.summary.zeroDeltaCount == 1);

    // Every contract dimension is bound into identity. observed_at alone is
    // intentionally excluded.
    const auto laterObservation = Assess({Succeeded(1)},
        "2026-07-19T22:05:00Z");
    assert(laterObservation.observedAt != comparable.observedAt);
    assert(laterObservation.identity == comparable.identity);

    const auto lifecycleNotTerminal =
        Assess({LifecycleOnly(1, Lifecycle::NotTerminal)});
    const auto lifecycleUnknown =
        Assess({LifecycleOnly(1, Lifecycle::Unknown)});
    assert(lifecycleNotTerminal.members[0].outcome ==
        lifecycleUnknown.members[0].outcome);
    assert(lifecycleNotTerminal.identity.hash !=
        lifecycleUnknown.identity.hash);

    assert(comparable.identity.hash != malformedProvenance.identity.hash);
    assert(malformedProvenance.identity.hash !=
        noResultAssessment.identity.hash);
    const auto changedMetricValue = Assess({Succeeded(1,
        BaseSourceMetrics(),
        {NumericMetric("inference_accuracy", 0.76),
            NumericMetric("leader_score", 0.78)})});
    assert(changedMetricValue.identity.hash != comparable.identity.hash);
    assert(metricGap.identity.hash != comparable.identity.hash);
    assert(changedContext.identity.hash != comparable.identity.hash);
    assert(unsupported.identity.hash != supportedSingleMetric.identity.hash);

    const MemberEvidence changedProvenance(
        Identity(1, 1901), Lifecycle::Succeeded,
        Source(1, BaseSourceMetrics()),
        ResultEvidence(1901, 1001,
            {ResultIdentity("scientific_result", 2901)},
            ComparableContext(), Metrics(BaseResultMetrics())));
    const auto changedProvenanceAssessment = Assess({changedProvenance});
    assert(changedProvenanceAssessment.members[0].outcome ==
        MemberOutcome::SucceededComparable);
    assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
    assert(comparable.identity.hash == RecommendationCanonicalHash(
        comparable.identity.canonicalText));
    assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
        std::string::npos);
    assert(malformedProvenance.identity.canonicalText.find(
               "result_experiment_identity_mismatch") != std::string::npos);

    // Member order, result identity order, diagnostics, metrics, comparisons,
    // canonical text, and hash are deterministic.
    const MemberEvidence twoResultIdentities(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, BaseSourceMetrics()),
        ResultEvidence(ResultExperiment(1), 1001,
            {ResultIdentity("z_future", 12),
                ResultIdentity("analysis", 11)},
            ComparableContext(), Metrics(BaseResultMetrics())));
    const MemberEvidence twoResultIdentitiesReordered(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, {BaseSourceMetrics()[1], BaseSourceMetrics()[0]}),
        ResultEvidence(ResultExperiment(1), 1001,
            {ResultIdentity("analysis", 11),
                ResultIdentity("z_future", 12)},
            ComparableContext(),
            Metrics({BaseResultMetrics()[1], BaseResultMetrics()[0]})));
    const auto canonicalOne = Assess({twoResultIdentities});
    const auto canonicalTwo = Assess({twoResultIdentitiesReordered});
    assert(canonicalOne.members == canonicalTwo.members);
    assert(canonicalOne.identity == canonicalTwo.identity);
    assert(canonicalOne.members[0].resultEvidence->resultIdentities[0].kind ==
        "analysis");
    assert(canonicalOne.members[0].resultEvidence->resultIdentities[1].kind ==
        "z_future");

    const auto orderedMembers = Assess({Succeeded(1), Succeeded(2)});
    const auto shuffledMembers = Assess({Succeeded(2), Succeeded(1)});
    assert(orderedMembers.members == shuffledMembers.members);
    assert(orderedMembers.summary == shuffledMembers.summary);
    assert(orderedMembers.identity == shuffledMembers.identity);
    assert(shuffledMembers.members[0].identity.memberOrdinal == 1);
    assert(shuffledMembers.members[1].identity.memberOrdinal == 2);

    // Invalid scalar input and untrustworthy top-level identity fail fast.
    AssertInvalidArgument([]
    {
        (void)Metric("inference_accuracy",
            std::numeric_limits<double>::quiet_NaN(),
            MetricSupport::NumericDelta);
    });
    AssertInvalidArgument([]
    {
        (void)ComparableContext("eurusd", 12,
            std::numeric_limits<double>::infinity());
    });
    AssertInvalidArgument([]
    {
        (void)ResultIdentity("analysis", 0);
    });
    AssertInvalidArgument([]
    {
        (void)Identity(1, 0);
    });
    AssertInvalidArgument([]
    {
        (void)Metric(std::string("bad\0identity", 12), 0.1,
            MetricSupport::NumericDelta);
    });
    AssertInvalidArgument([]
    {
        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
            Materialization(2), "2026-07-19T22:00:00Z",
            {Succeeded(1), Succeeded(1)});
    });
    AssertInvalidArgument([]
    {
        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
            std::nullopt, std::nullopt);
        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
            std::nullopt, std::nullopt);
        (void)Assess({first, second});
    });
    AssertInvalidArgument([]
    {
        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
            Materialization(1), "", {Succeeded(1)});
    });

    const MemberEvidence overflowingDelta(
        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
        Source(1, {NumericMetric("wide",-
            std::numeric_limits<double>::max())}),
        Result(1, {NumericMetric("wide",
            std::numeric_limits<double>::max())}));
    const auto overflowAssessment = Assess({overflowingDelta});
    assert(overflowAssessment.members[0].diagnostics ==
        std::vector<Diagnostic>{Diagnostic::NumericDeltaNonFinite});
    assert(overflowAssessment.members[0].comparisons.empty());
    assert(overflowAssessment.members[0].outcome == MemberOutcome::Inconsistent);

    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
               Lifecycle::NotTerminal) == "not_terminal");
    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
               Lifecycle::Succeeded) == "succeeded");
    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
               Lifecycle::Failed) == "failed");
    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
               Lifecycle::Cancelled) == "cancelled");
    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
               Lifecycle::Unknown) == "unknown");
    assert(RecommendationCampaignOutcomeAssessmentConsistencyStateText(
               Consistency::Consistent) == "consistent");
    assert(RecommendationCampaignOutcomeAssessmentConsistencyStateText(
               Consistency::Inconsistent) == "inconsistent");
    assert(RecommendationCampaignOutcomeAssessmentMemberOutcomeText(
               MemberOutcome::SucceededMetricGap) == "succeeded_metric_gap");
    assert(RecommendationCampaignOutcomeAssessmentAggregateOutcomeText(
               AggregateOutcome::HasFailures) == "has_failures");

    return 0;
}
Phase 5 Recommendation Campaign Outcome Assessment
==================================================

Purpose and boundary
--------------------

Step 5a defines a pure, immutable, point-in-time domain assessment of the
scientific outcomes associated with an exact recommendation-campaign
materialization.  It is read-only, non-persistent, non-authoritative, and does
not declare campaign success or authorize follow-up work.

Lifecycle, consistency, and comparison
--------------------------------------

Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
``cancelled``, or ``unknown``).  Consistency is independent
(``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
retained with deterministic diagnostic codes.  Scientific comparisons are
derived only for consistent succeeded members.  Member outcomes distinguish
not-ready, comparable success, context-changed success, metric-gap success,
terminal failure, terminal cancellation, and inconsistency.

Multi-metric evidence
---------------------

Source and result evidence contain metric collections.  Collections reject a
duplicate metric identity and are canonicalized by metric identity, so input
order does not affect output or assessment identity.  A comparison is derived
for every metric identity in the source/result union.  Member outcome counts
remain separate from metric-comparison and delta counts.

Provenance and deterministic identity
-------------------------------------

Each materialized member may identify its expected downstream experiment.
Succeeded-result evidence is checked against that exact experiment, and result
experiment and typed result identities may not be reused across members.
Provenance defects remain inconsistent members when top-level materialization
membership is trustworthy; invalid or ambiguous member identity fails the
assessment.

Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
source and result metrics, comparisons, exact provenance, and aggregate member
and metric summaries.  Canonical number formatting is locale-independent.
``observed_at`` is intentionally excluded from evidence identity.

Non-goals
---------

Step 5a does not provide a repository, database access, persistence, service,
CLI, scheduler, worker, filesystem behavior, campaign-success policy, or
follow-up authorization.
Phase 5 Step 1 — Atomic Campaign Conversion Execution
======================================================

Purpose and authority
---------------------

Phase 5 begins with one explicit operational convenience over the existing
Phase 4C conversion-execution authority. Given one persisted Phase 4D campaign
materialization, it atomically invokes the ordinary Phase 4C paused conversion
for every exact member. It adds no campaign execution table, mutable campaign
status, alternate proposal eligibility rule, activation authority, scheduler
consumer, or worker behavior.

Membership and eligibility
--------------------------

The sole member set is the ordered
``experiment_recommendation_campaign_materialization_member`` rows belonging to
the requested ``experiment_recommendation_campaign_materialization``. The
existing materialization loader verifies its version, canonical identity,
hashes, member count, ordinals, and exact proposal links. The existing handoff
projection validates each linked proposal and its greatest-ID Phase 4C review,
execution, activation, and experiment evidence.

First execution requires every member to have no execution and a current
ordinary Phase 4C ``approve`` decision. A pending, rejected, missing, malformed,
duplicate, oversized, or inconsistent member fails the whole request. The
command never infers membership or authorization from recommendation IDs,
ranking output, score, policy, symbol, horizon, or proposal attributes.

Transaction, locking, and retry
-------------------------------

A write uses one PostgreSQL transaction. It locks the exact proposal review-
sequence advisory keys in sorted proposal-ID order, reloads all workflow
evidence, builds the complete pure plan, and only then creates paused
experiments and immutable Phase 4C execution rows. The shared transaction-bound
Phase 4C primitive neither starts nor commits a transaction. Any member failure
rolls back every experiment and execution row from the request.

An exact retry in which all members already have valid Phase 4C executions
returns ``already_satisfied`` and inserts nothing. A mixture of executed and
unexecuted members is rejected as a partial-operation conflict; Phase 5 does not
claim or complete a campaign operation that may have been partially performed
through unrelated direct Phase 4C commands. Concurrent identical campaigns,
overlapping campaigns, direct proposal review, and direct Phase 4C execution
serialize on the same proposal-specific lock domain.

The version-1 operation identity binds the complete immutable materialization
identity and contract version. It contains no timestamp, sequence value, or
unordered input. Individual durable execution identity and idempotence remain
the existing Phase 4C contract.

CLI and dry run
---------------

::

   LSTM_Release \
     --execute-recommendation-campaign-materialization=MATERIALIZATION_ID \
     [--dry-run] --yes

The separated form is also accepted. A positive materialization ID is required,
duplicates and combinations with other standalone operations are rejected, and
``--yes`` is mandatory for a write. Parser failures occur before database
dispatch.

``--dry-run`` performs full validation in one repeatable-read, read-only
transaction. It acquires no advisory locks, inserts no row, creates no
experiment, and advances no sequence. Its result is advisory because another
explicit action may change Phase 4C evidence after the snapshot.

Output and safety boundary
--------------------------

``RECOMMENDATION_CAMPAIGN_EXECUTION`` is followed by one
``RECOMMENDATION_CAMPAIGN_EXECUTION_MEMBER`` row per persisted ordinal. Rows
report materialization and operation identities, exact proposal/review/
execution/experiment IDs, counts, deterministic diagnostics, dry-run state, and
explicit scheduler, worker, activation, queueing, and automatic-progression
safety fields. Optional IDs use ``null`` and variable text uses the production
percent encoder.

Each member's ``authorization_review_decision_id`` is the approving review used
to create its immutable execution. Before first execution this is the current
approving review; on an already-satisfied retry it remains the execution's
original authorizing review even if a later ordinary review reversed the
proposal disposition.

Created experiments are ordinary Phase 4C ``paused/train`` conversion output.
The command does not activate them, change them to ``pending``, queue them,
start the scheduler, launch workers, or automatically invoke another command.
Activation remains separately invoked. Phase 5 Step 2 provides an optional,
separately confirmed atomic convenience over the same Phase 4C activation
authority for every exact materialized member; Step 1 never invokes it.
Phase 5 Step 3 is a distinct, separately confirmed command that can establish
both execution and activation in one outer transaction. Step 1 does not invoke
Step 3, and Step 3 does not alter Step 1's execute-only contract.
Phase 5 Step 4 can observe the resulting exact member lifecycle in one read-only
snapshot; it never invokes or changes Step 1.

Schema
------

No migration or privilege change is required. Campaign scope comes from the
immutable Phase 4D materialization, while each durable outcome is an ordinary
Phase 4C execution row linked to its approving review and paused experiment.

References
----------

* ``docs/architecture/Volume_VIII_Recommendation_Engine.md``
* ``docs/architecture/adr/ADR-0004-scheduler-ownership-boundaries.md``
* ``docs/architecture/adr/ADR-0005-manual-recommendation-conversion.md``
* ``docs/Phase4CExperimentRecommendationConversionExecution.rst``
* ``docs/Phase4DExperimentRecommendationCampaignHandoff.rst``
* ``docs/Phase4DExperimentRecommendationCampaignProposalReview.rst``
* ``docs/Phase5ExperimentRecommendationCampaignActivation.rst``
* ``docs/Phase5ExperimentRecommendationCampaignLaunch.rst``

codex
The implementation is a new, isolated domain module with no repository or scheduler integration, which matches Step 5a’s stated boundary. I’m now validating whether its externally supplied evidence can actually be trusted: in particular, whether canonical campaign/materialization identities are cross-bound strongly enough, whether lifecycle/result provenance rules are complete, and whether every public immutable value type enforces the invariants claimed by its API.
exec
/bin/bash -lc "git diff -- ExpertAdvisor.xcodeproj/project.pbxproj && rg -n \"CampaignStatus|Lifecycle|lifecycle|MaterializationIdentity|identityCanonical|Canonical.*Materialization|contractVersion\" Sources/ExperimentRecommendationCampaignStatus.* Sources/ExperimentRecommendationCampaignMaterialization.* Tests/ExperimentRecommendationCampaignStatusTests.cpp Tests/ExperimentRecommendationCampaignMaterializationTests.cpp | sed -n '1,360p' && rg -n \"xcodebuild|sanitize|Sanitizer|OutcomeAssessmentTests|CampaignStatusTests\" README* docs Tests .github ExpertAdvisor.xcodeproj -g '"'!*.pbxproj'"' -g '"'!DerivedData/**'"'" in /Volumes/Developer SSD/ExpertAdvisor
 exited 2 in 0ms:
diff --git a/ExpertAdvisor.xcodeproj/project.pbxproj b/ExpertAdvisor.xcodeproj/project.pbxproj
index b1fa83b..c425d3b 100644
--- a/ExpertAdvisor.xcodeproj/project.pbxproj
+++ b/ExpertAdvisor.xcodeproj/project.pbxproj
@@ -149,6 +149,8 @@
 		0B570009307C000100AAA001 /* ExperimentRecommendationCampaignStatusRepository.cpp in Sources */ = {isa = PBXBuildFile; fileRef = 0B570002307C000100AAA001 /* ExperimentRecommendationCampaignStatusRepository.cpp */; };
 		0B57000A307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp in Sources */ = {isa = PBXBuildFile; fileRef = 0B570004307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp */; };
 		0B57000B307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp in Sources */ = {isa = PBXBuildFile; fileRef = 0B570004307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp */; };
+		0B580002307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp in Sources */ = {isa = PBXBuildFile; fileRef = 0B580000307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp */; };
+		0B580003307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp in Sources */ = {isa = PBXBuildFile; fileRef = 0B580000307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp */; };
 /* End PBXBuildFile section */
 
 /* Begin PBXContainerItemProxy section */
@@ -378,6 +380,8 @@
 		0B570003307C000100AAA001 /* ExperimentRecommendationCampaignStatusRepository.hpp */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.cpp.h; name = ExperimentRecommendationCampaignStatusRepository.hpp; path = Sources/ExperimentRecommendationCampaignStatusRepository.hpp; sourceTree = SOURCE_ROOT; };
 		0B570004307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.cpp.cpp; name = ExperimentRecommendationCampaignStatusService.cpp; path = Sources/ExperimentRecommendationCampaignStatusService.cpp; sourceTree = SOURCE_ROOT; };
 		0B570005307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.hpp */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.cpp.h; name = ExperimentRecommendationCampaignStatusService.hpp; path = Sources/ExperimentRecommendationCampaignStatusService.hpp; sourceTree = SOURCE_ROOT; };
+		0B580000307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.cpp.cpp; name = ExperimentRecommendationCampaignOutcomeAssessment.cpp; path = Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp; sourceTree = SOURCE_ROOT; };
+		0B580001307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.hpp */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.cpp.h; name = ExperimentRecommendationCampaignOutcomeAssessment.hpp; path = Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp; sourceTree = SOURCE_ROOT; };
 /* End PBXFileReference section */
 
 /* Begin PBXFrameworksBuildPhase section */
@@ -575,6 +579,8 @@
 				0B570003307C000100AAA001 /* ExperimentRecommendationCampaignStatusRepository.hpp */,
 				0B570004307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp */,
 				0B570005307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.hpp */,
+				0B580000307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp */,
+				0B580001307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.hpp */,
 				0A5000082FC0000100AAA001 /* WorkerLifecycleDiagnostics.hpp */,
 				0A4000002FB0000100AAA001 /* RunMetadata.cpp */,
 				0890B6A42F1DC54900CEDECF /* LSTM.cpp */,
@@ -819,6 +825,7 @@
 				0B570006307C000100AAA001 /* ExperimentRecommendationCampaignStatus.cpp in Sources */,
 				0B570008307C000100AAA001 /* ExperimentRecommendationCampaignStatusRepository.cpp in Sources */,
 				0B57000A307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp in Sources */,
+				0B580002307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp in Sources */,
 				0A4000012FB0000100AAA001 /* RunMetadata.cpp in Sources */,
 				08395C292EDA4EC2005B70A0 /* db_cursor.cpp in Sources */,
 				0890B6A52F1DC54900CEDECF /* LSTM.cpp in Sources */,
@@ -892,6 +899,7 @@
 				0B570007307C000100AAA001 /* ExperimentRecommendationCampaignStatus.cpp in Sources */,
 				0B570009307C000100AAA001 /* ExperimentRecommendationCampaignStatusRepository.cpp in Sources */,
 				0B57000B307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp in Sources */,
+				0B580003307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp in Sources */,
 				0A4000022FB0000100AAA001 /* RunMetadata.cpp in Sources */,
 				0A1000042F70000100AAA001 /* db_cursor.cpp in Sources */,
 				0A1000052F70000100AAA001 /* LSTM.cpp in Sources */,
Sources/ExperimentRecommendationCampaignMaterialization.cpp:159:    if (approval.campaignPlanIdentityCanonical != plan.identityCanonical ||
Sources/ExperimentRecommendationCampaignMaterialization.cpp:235:            kRecommendationCampaignMaterializationIdentityCanonicalMaximum ||
Tests/ExperimentRecommendationCampaignStatusTests.cpp:1:#include "../Sources/ExperimentRecommendationCampaignStatus.hpp"
Tests/ExperimentRecommendationCampaignStatusTests.cpp:13:RecommendationCampaignStatusMemberInput Member(int ordinal)
Tests/ExperimentRecommendationCampaignStatusTests.cpp:15:    RecommendationCampaignStatusMemberInput member;
Tests/ExperimentRecommendationCampaignStatusTests.cpp:27:RecommendationCampaignStatusExperimentEvidence Experiment(
Tests/ExperimentRecommendationCampaignStatusTests.cpp:32:    RecommendationCampaignStatusExperimentEvidence experiment;
Tests/ExperimentRecommendationCampaignStatusTests.cpp:45:void Execute(RecommendationCampaignStatusMemberInput& member, bool activate)
Tests/ExperimentRecommendationCampaignStatusTests.cpp:58:RecommendationCampaignStatusInput Input(
Tests/ExperimentRecommendationCampaignStatusTests.cpp:59:    std::vector<RecommendationCampaignStatusMemberInput> members)
Tests/ExperimentRecommendationCampaignStatusTests.cpp:61:    RecommendationCampaignStatusInput input;
Tests/ExperimentRecommendationCampaignStatusTests.cpp:73:RecommendationCampaignStatusSnapshot Snapshot(
Tests/ExperimentRecommendationCampaignStatusTests.cpp:74:    std::vector<RecommendationCampaignStatusMemberInput> members)
Tests/ExperimentRecommendationCampaignStatusTests.cpp:76:    return BuildRecommendationCampaignStatusSnapshot({71}, Input(std::move(members)));
Tests/ExperimentRecommendationCampaignStatusTests.cpp:80:    RecommendationCampaignStatusInput input,
Tests/ExperimentRecommendationCampaignStatusTests.cpp:84:    try { (void)BuildRecommendationCampaignStatusSnapshot({71}, std::move(input)); }
Tests/ExperimentRecommendationCampaignStatusTests.cpp:97:    assert(NormalizeRecommendationCampaignStatusRequest({71}).materializationId == 71);
Tests/ExperimentRecommendationCampaignStatusTests.cpp:98:    try { (void)NormalizeRecommendationCampaignStatusRequest({0}); assert(false); }
Tests/ExperimentRecommendationCampaignStatusTests.cpp:105:           RecommendationCampaignStatusOperationalState::proposalOnly);
Tests/ExperimentRecommendationCampaignStatusTests.cpp:201:           RecommendationCampaignStatusOperationalState::pausedAfterProgress);
Sources/ExperimentRecommendationCampaignStatus.hpp:10:inline constexpr int kRecommendationCampaignStatusContractVersion = 1;
Sources/ExperimentRecommendationCampaignStatus.hpp:11:inline constexpr int kMaximumRecommendationCampaignStatusMembers = 1000;
Sources/ExperimentRecommendationCampaignStatus.hpp:13:struct RecommendationCampaignStatusRequest
Sources/ExperimentRecommendationCampaignStatus.hpp:18:enum class RecommendationCampaignStatusExecutionState
Sources/ExperimentRecommendationCampaignStatus.hpp:26:enum class RecommendationCampaignStatusActivationState
Sources/ExperimentRecommendationCampaignStatus.hpp:34:enum class RecommendationCampaignStatusOperationalState
Sources/ExperimentRecommendationCampaignStatus.hpp:49:enum class RecommendationCampaignStatusTerminalResult
Sources/ExperimentRecommendationCampaignStatus.hpp:58:enum class RecommendationCampaignStatusConsistency
Sources/ExperimentRecommendationCampaignStatus.hpp:79:struct RecommendationCampaignStatusExperimentEvidence
Sources/ExperimentRecommendationCampaignStatus.hpp:112:struct RecommendationCampaignStatusMemberInput
Sources/ExperimentRecommendationCampaignStatus.hpp:132:    std::optional<RecommendationCampaignStatusExperimentEvidence> experiment;
Sources/ExperimentRecommendationCampaignStatus.hpp:135:struct RecommendationCampaignStatusInput
Sources/ExperimentRecommendationCampaignStatus.hpp:143:    std::vector<RecommendationCampaignStatusMemberInput> members;
Sources/ExperimentRecommendationCampaignStatus.hpp:146:struct RecommendationCampaignStatusMember
Sources/ExperimentRecommendationCampaignStatus.hpp:164:    RecommendationCampaignStatusExecutionState executionState =
Sources/ExperimentRecommendationCampaignStatus.hpp:165:        RecommendationCampaignStatusExecutionState::notExecuted;
Sources/ExperimentRecommendationCampaignStatus.hpp:166:    RecommendationCampaignStatusActivationState activationState =
Sources/ExperimentRecommendationCampaignStatus.hpp:167:        RecommendationCampaignStatusActivationState::notActivated;
Sources/ExperimentRecommendationCampaignStatus.hpp:168:    RecommendationCampaignStatusOperationalState operationalState =
Sources/ExperimentRecommendationCampaignStatus.hpp:169:        RecommendationCampaignStatusOperationalState::proposalOnly;
Sources/ExperimentRecommendationCampaignStatus.hpp:170:    RecommendationCampaignStatusTerminalResult terminalResult =
Sources/ExperimentRecommendationCampaignStatus.hpp:171:        RecommendationCampaignStatusTerminalResult::notTerminal;
Sources/ExperimentRecommendationCampaignStatus.hpp:172:    RecommendationCampaignStatusConsistency consistency =
Sources/ExperimentRecommendationCampaignStatus.hpp:173:        RecommendationCampaignStatusConsistency::consistent;
Sources/ExperimentRecommendationCampaignStatus.hpp:202:struct RecommendationCampaignStatusSnapshot
Sources/ExperimentRecommendationCampaignStatus.hpp:204:    RecommendationCampaignStatusRequest request;
Sources/ExperimentRecommendationCampaignStatus.hpp:212:    RecommendationCampaignStatusConsistency consistency =
Sources/ExperimentRecommendationCampaignStatus.hpp:213:        RecommendationCampaignStatusConsistency::consistent;
Sources/ExperimentRecommendationCampaignStatus.hpp:237:    std::vector<RecommendationCampaignStatusMember> members;
Sources/ExperimentRecommendationCampaignStatus.hpp:240:RecommendationCampaignStatusRequest NormalizeRecommendationCampaignStatusRequest(
Sources/ExperimentRecommendationCampaignStatus.hpp:241:    const RecommendationCampaignStatusRequest& request);
Sources/ExperimentRecommendationCampaignStatus.hpp:242:RecommendationCampaignStatusSnapshot BuildRecommendationCampaignStatusSnapshot(
Sources/ExperimentRecommendationCampaignStatus.hpp:243:    const RecommendationCampaignStatusRequest& request,
Sources/ExperimentRecommendationCampaignStatus.hpp:244:    RecommendationCampaignStatusInput input);
Sources/ExperimentRecommendationCampaignStatus.hpp:246:std::string RecommendationCampaignStatusExecutionStateText(
Sources/ExperimentRecommendationCampaignStatus.hpp:247:    RecommendationCampaignStatusExecutionState value);
Sources/ExperimentRecommendationCampaignStatus.hpp:248:std::string RecommendationCampaignStatusActivationStateText(
Sources/ExperimentRecommendationCampaignStatus.hpp:249:    RecommendationCampaignStatusActivationState value);
Sources/ExperimentRecommendationCampaignStatus.hpp:250:std::string RecommendationCampaignStatusOperationalStateText(
Sources/ExperimentRecommendationCampaignStatus.hpp:251:    RecommendationCampaignStatusOperationalState value);
Sources/ExperimentRecommendationCampaignStatus.hpp:252:std::string RecommendationCampaignStatusTerminalResultText(
Sources/ExperimentRecommendationCampaignStatus.hpp:253:    RecommendationCampaignStatusTerminalResult value);
Sources/ExperimentRecommendationCampaignStatus.hpp:254:std::string RecommendationCampaignStatusConsistencyText(
Sources/ExperimentRecommendationCampaignStatus.hpp:255:    RecommendationCampaignStatusConsistency value);
Sources/ExperimentRecommendationCampaignMaterialization.hpp:19:    kRecommendationCampaignMaterializationIdentityCanonicalMaximum =
Sources/ExperimentRecommendationCampaignStatus.cpp:1:#include "ExperimentRecommendationCampaignStatus.hpp"
Sources/ExperimentRecommendationCampaignStatus.cpp:17:void Diagnose(RecommendationCampaignStatusMember& member, std::string code)
Sources/ExperimentRecommendationCampaignStatus.cpp:22:    member.consistency = RecommendationCampaignStatusConsistency::inconsistent;
Sources/ExperimentRecommendationCampaignStatus.cpp:23:    member.operationalState = RecommendationCampaignStatusOperationalState::inconsistent;
Sources/ExperimentRecommendationCampaignStatus.cpp:24:    member.terminalResult = RecommendationCampaignStatusTerminalResult::unknown;
Sources/ExperimentRecommendationCampaignStatus.cpp:39:RecommendationCampaignStatusMember ClassifyMember(
Sources/ExperimentRecommendationCampaignStatus.cpp:40:    const RecommendationCampaignStatusMemberInput& input)
Sources/ExperimentRecommendationCampaignStatus.cpp:42:    RecommendationCampaignStatusMember member;
Sources/ExperimentRecommendationCampaignStatus.cpp:59:        member.executionState = RecommendationCampaignStatusExecutionState::conflicting;
Sources/ExperimentRecommendationCampaignStatus.cpp:61:        member.executionState = RecommendationCampaignStatusExecutionState::executed;
Sources/ExperimentRecommendationCampaignStatus.cpp:63:        member.activationState = RecommendationCampaignStatusActivationState::conflicting;
Sources/ExperimentRecommendationCampaignStatus.cpp:65:        member.activationState = RecommendationCampaignStatusActivationState::activated;
Sources/ExperimentRecommendationCampaignStatus.cpp:70:            member.executionState = RecommendationCampaignStatusExecutionState::malformed;
Sources/ExperimentRecommendationCampaignStatus.cpp:72:            member.activationState = RecommendationCampaignStatusActivationState::malformed;
Sources/ExperimentRecommendationCampaignStatus.cpp:173:    if (member.consistency == RecommendationCampaignStatusConsistency::inconsistent)
Sources/ExperimentRecommendationCampaignStatus.cpp:183:            member.operationalState = RecommendationCampaignStatusOperationalState::paused;
Sources/ExperimentRecommendationCampaignStatus.cpp:185:            Diagnose(member, "campaign_status_unactivated_lifecycle_invalid");
Sources/ExperimentRecommendationCampaignStatus.cpp:195:            member.operationalState = RecommendationCampaignStatusOperationalState::pending;
Sources/ExperimentRecommendationCampaignStatus.cpp:205:            member.operationalState = RecommendationCampaignStatusOperationalState::runningTrain;
Sources/ExperimentRecommendationCampaignStatus.cpp:207:            member.operationalState = RecommendationCampaignStatusOperationalState::runningInfer;
Sources/ExperimentRecommendationCampaignStatus.cpp:209:            member.operationalState = RecommendationCampaignStatusOperationalState::runningAnalyze;
Sources/ExperimentRecommendationCampaignStatus.cpp:217:                RecommendationCampaignStatusOperationalState::pausedAfterProgress;
Sources/ExperimentRecommendationCampaignStatus.cpp:232:            member.operationalState = RecommendationCampaignStatusOperationalState::completed;
Sources/ExperimentRecommendationCampaignStatus.cpp:233:            member.terminalResult = RecommendationCampaignStatusTerminalResult::succeeded;
Sources/ExperimentRecommendationCampaignStatus.cpp:243:            member.operationalState = RecommendationCampaignStatusOperationalState::failed;
Sources/ExperimentRecommendationCampaignStatus.cpp:244:            member.terminalResult = RecommendationCampaignStatusTerminalResult::failed;
Sources/ExperimentRecommendationCampaignStatus.cpp:253:            member.operationalState = RecommendationCampaignStatusOperationalState::cancelled;
Sources/ExperimentRecommendationCampaignStatus.cpp:254:            member.terminalResult = RecommendationCampaignStatusTerminalResult::cancelled;
Sources/ExperimentRecommendationCampaignStatus.cpp:259:        Diagnose(member, "campaign_status_experiment_lifecycle_invalid");
Sources/ExperimentRecommendationCampaignStatus.cpp:280:    const RecommendationCampaignStatusInput& input,
Sources/ExperimentRecommendationCampaignStatus.cpp:281:    const std::vector<RecommendationCampaignStatusMember>& members)
Sources/ExperimentRecommendationCampaignStatus.cpp:286:        << ";status_contract_version=" << kRecommendationCampaignStatusContractVersion
Sources/ExperimentRecommendationCampaignStatus.cpp:305:            << RecommendationCampaignStatusExecutionStateText(member.executionState)
Sources/ExperimentRecommendationCampaignStatus.cpp:306:            << ',' << RecommendationCampaignStatusActivationStateText(member.activationState)
Sources/ExperimentRecommendationCampaignStatus.cpp:307:            << ',' << RecommendationCampaignStatusOperationalStateText(member.operationalState)
Sources/ExperimentRecommendationCampaignStatus.cpp:308:            << ',' << RecommendationCampaignStatusTerminalResultText(member.terminalResult)
Sources/ExperimentRecommendationCampaignStatus.cpp:332:            << RecommendationCampaignStatusConsistencyText(member.consistency);
Sources/ExperimentRecommendationCampaignStatus.cpp:340:    const RecommendationCampaignStatusSnapshot& result)
Sources/ExperimentRecommendationCampaignStatus.cpp:372:RecommendationCampaignStatusRequest NormalizeRecommendationCampaignStatusRequest(
Sources/ExperimentRecommendationCampaignStatus.cpp:373:    const RecommendationCampaignStatusRequest& request)
Sources/ExperimentRecommendationCampaignStatus.cpp:380:RecommendationCampaignStatusSnapshot BuildRecommendationCampaignStatusSnapshot(
Sources/ExperimentRecommendationCampaignStatus.cpp:381:    const RecommendationCampaignStatusRequest& request,
Sources/ExperimentRecommendationCampaignStatus.cpp:382:    RecommendationCampaignStatusInput input)
Sources/ExperimentRecommendationCampaignStatus.cpp:384:    RecommendationCampaignStatusSnapshot result;
Sources/ExperimentRecommendationCampaignStatus.cpp:385:    result.request = NormalizeRecommendationCampaignStatusRequest(request);
Sources/ExperimentRecommendationCampaignStatus.cpp:398:        static_cast<std::size_t>(kMaximumRecommendationCampaignStatusMembers))
Sources/ExperimentRecommendationCampaignStatus.cpp:435:        if (member.executionState == RecommendationCampaignStatusExecutionState::executed)
Sources/ExperimentRecommendationCampaignStatus.cpp:437:        if (member.activationState == RecommendationCampaignStatusActivationState::activated)
Sources/ExperimentRecommendationCampaignStatus.cpp:441:            case RecommendationCampaignStatusOperationalState::proposalOnly:
Sources/ExperimentRecommendationCampaignStatus.cpp:443:            case RecommendationCampaignStatusOperationalState::paused:
Sources/ExperimentRecommendationCampaignStatus.cpp:444:            case RecommendationCampaignStatusOperationalState::pausedAfterProgress:
Sources/ExperimentRecommendationCampaignStatus.cpp:446:            case RecommendationCampaignStatusOperationalState::pending:
Sources/ExperimentRecommendationCampaignStatus.cpp:448:            case RecommendationCampaignStatusOperationalState::runningTrain:
Sources/ExperimentRecommendationCampaignStatus.cpp:450:            case RecommendationCampaignStatusOperationalState::runningInfer:
Sources/ExperimentRecommendationCampaignStatus.cpp:452:            case RecommendationCampaignStatusOperationalState::runningAnalyze:
Sources/ExperimentRecommendationCampaignStatus.cpp:454:            case RecommendationCampaignStatusOperationalState::completed:
Sources/ExperimentRecommendationCampaignStatus.cpp:456:            case RecommendationCampaignStatusOperationalState::failed:
Sources/ExperimentRecommendationCampaignStatus.cpp:458:            case RecommendationCampaignStatusOperationalState::cancelled:
Sources/ExperimentRecommendationCampaignStatus.cpp:460:            case RecommendationCampaignStatusOperationalState::inconsistent:
Sources/ExperimentRecommendationCampaignStatus.cpp:463:        if (member.terminalResult != RecommendationCampaignStatusTerminalResult::notTerminal &&
Sources/ExperimentRecommendationCampaignStatus.cpp:464:            member.terminalResult != RecommendationCampaignStatusTerminalResult::unknown)
Sources/ExperimentRecommendationCampaignStatus.cpp:468:                 RecommendationCampaignStatusOperationalState::runningTrain ||
Sources/ExperimentRecommendationCampaignStatus.cpp:470:                 RecommendationCampaignStatusOperationalState::runningInfer ||
Sources/ExperimentRecommendationCampaignStatus.cpp:472:                 RecommendationCampaignStatusOperationalState::runningAnalyze))
Sources/ExperimentRecommendationCampaignStatus.cpp:497:        result.consistency = RecommendationCampaignStatusConsistency::inconsistent;
Sources/ExperimentRecommendationCampaignStatus.cpp:511:EA_STATUS_TEXT_FUNCTION(RecommendationCampaignStatusExecutionStateText,
Sources/ExperimentRecommendationCampaignStatus.cpp:512:    RecommendationCampaignStatusExecutionState,
Sources/ExperimentRecommendationCampaignStatus.cpp:513:    case RecommendationCampaignStatusExecutionState::notExecuted: return "not_executed";
Sources/ExperimentRecommendationCampaignStatus.cpp:514:    case RecommendationCampaignStatusExecutionState::executed: return "executed";
Sources/ExperimentRecommendationCampaignStatus.cpp:515:    case RecommendationCampaignStatusExecutionState::malformed: return "malformed";
Sources/ExperimentRecommendationCampaignStatus.cpp:516:    case RecommendationCampaignStatusExecutionState::conflicting: return "conflicting";)
Sources/ExperimentRecommendationCampaignStatus.cpp:517:EA_STATUS_TEXT_FUNCTION(RecommendationCampaignStatusActivationStateText,
Sources/ExperimentRecommendationCampaignStatus.cpp:518:    RecommendationCampaignStatusActivationState,
Sources/ExperimentRecommendationCampaignStatus.cpp:519:    case RecommendationCampaignStatusActivationState::notActivated: return "not_activated";
Sources/ExperimentRecommendationCampaignStatus.cpp:520:    case RecommendationCampaignStatusActivationState::activated: return "activated";
Sources/ExperimentRecommendationCampaignStatus.cpp:521:    case RecommendationCampaignStatusActivationState::malformed: return "malformed";
Sources/ExperimentRecommendationCampaignStatus.cpp:522:    case RecommendationCampaignStatusActivationState::conflicting: return "conflicting";)
Sources/ExperimentRecommendationCampaignStatus.cpp:523:EA_STATUS_TEXT_FUNCTION(RecommendationCampaignStatusOperationalStateText,
Sources/ExperimentRecommendationCampaignStatus.cpp:524:    RecommendationCampaignStatusOperationalState,
Sources/ExperimentRecommendationCampaignStatus.cpp:525:    case RecommendationCampaignStatusOperationalState::proposalOnly: return "proposal_only";
Sources/ExperimentRecommendationCampaignStatus.cpp:526:    case RecommendationCampaignStatusOperationalState::paused: return "paused";
Sources/ExperimentRecommendationCampaignStatus.cpp:527:    case RecommendationCampaignStatusOperationalState::pending: return "pending";
Sources/ExperimentRecommendationCampaignStatus.cpp:528:    case RecommendationCampaignStatusOperationalState::runningTrain: return "running_train";
Sources/ExperimentRecommendationCampaignStatus.cpp:529:    case RecommendationCampaignStatusOperationalState::runningInfer: return "running_infer";
Sources/ExperimentRecommendationCampaignStatus.cpp:530:    case RecommendationCampaignStatusOperationalState::runningAnalyze: return "running_analyze";
Sources/ExperimentRecommendationCampaignStatus.cpp:531:    case RecommendationCampaignStatusOperationalState::completed: return "completed";
Sources/ExperimentRecommendationCampaignStatus.cpp:532:    case RecommendationCampaignStatusOperationalState::failed: return "failed";
Sources/ExperimentRecommendationCampaignStatus.cpp:533:    case RecommendationCampaignStatusOperationalState::cancelled: return "cancelled";
Sources/ExperimentRecommendationCampaignStatus.cpp:534:    case RecommendationCampaignStatusOperationalState::pausedAfterProgress: return "paused_after_progress";
Sources/ExperimentRecommendationCampaignStatus.cpp:535:    case RecommendationCampaignStatusOperationalState::inconsistent: return "inconsistent";)
Sources/ExperimentRecommendationCampaignStatus.cpp:536:EA_STATUS_TEXT_FUNCTION(RecommendationCampaignStatusTerminalResultText,
Sources/ExperimentRecommendationCampaignStatus.cpp:537:    RecommendationCampaignStatusTerminalResult,
Sources/ExperimentRecommendationCampaignStatus.cpp:538:    case RecommendationCampaignStatusTerminalResult::notTerminal: return "not_terminal";
Sources/ExperimentRecommendationCampaignStatus.cpp:539:    case RecommendationCampaignStatusTerminalResult::succeeded: return "succeeded";
Sources/ExperimentRecommendationCampaignStatus.cpp:540:    case RecommendationCampaignStatusTerminalResult::failed: return "failed";
Sources/ExperimentRecommendationCampaignStatus.cpp:541:    case RecommendationCampaignStatusTerminalResult::cancelled: return "cancelled";
Sources/ExperimentRecommendationCampaignStatus.cpp:542:    case RecommendationCampaignStatusTerminalResult::unknown: return "unknown";)
Sources/ExperimentRecommendationCampaignStatus.cpp:543:EA_STATUS_TEXT_FUNCTION(RecommendationCampaignStatusConsistencyText,
Sources/ExperimentRecommendationCampaignStatus.cpp:544:    RecommendationCampaignStatusConsistency,
Sources/ExperimentRecommendationCampaignStatus.cpp:545:    case RecommendationCampaignStatusConsistency::consistent: return "consistent";
Sources/ExperimentRecommendationCampaignStatus.cpp:546:    case RecommendationCampaignStatusConsistency::inconsistent: return "inconsistent";)
rg: README*: No such file or directory (os error 2)
rg: .github: No such file or directory (os error 2)

exec
/bin/bash -lc "sed -n '1,290p' Sources/ExperimentRecommendationCampaignMaterialization.hpp && sed -n '1,310p' Sources/ExperimentRecommendationCampaignMaterialization.cpp && sed -n '1,280p' Sources/ExperimentRecommendationCampaignStatus.hpp && sed -n '1,560p' Sources/ExperimentRecommendationCampaignStatus.cpp" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
#pragma once

#include "ExperimentRecommendationCampaignApproval.hpp"
#include "ExperimentRecommendationConversion.hpp"

#include <cstddef>
#include <string>
#include <vector>

namespace EA::ExperimentRecommendation
{

inline constexpr int kRecommendationCampaignMaterializationContractVersion = 1;
inline constexpr std::size_t
    kRecommendationCampaignMaterializationOperatorMaximum = 200;
inline constexpr std::size_t
    kRecommendationCampaignMaterializationReasonMaximum = 2000;
inline constexpr std::size_t
    kRecommendationCampaignMaterializationIdentityCanonicalMaximum =
        64 * 1024 * 1024;

struct RecommendationCampaignMaterializationRequest
{
    long long campaignApprovalId = -1;
    std::string operatorIdentity;
    std::string reasonText;
};

struct RecommendationCampaignMaterializationSelectedMember
{
    int memberOrdinal = 0;
    long long rankingMemberId = -1;
    long long recommendationId = -1;
    long long sourceExperimentId = -1;
    int rankingPosition = 0;
    std::string selectedMemberIdentityCanonical;
    std::string selectedMemberIdentityHash;
    ProposedExperimentSpecification proposal;
};

struct RecommendationCampaignMaterializationEvidence
{
    int materializationContractVersion =
        kRecommendationCampaignMaterializationContractVersion;
    long long campaignApprovalId = -1;
    RecommendationCampaignApprovalEvidence approval;
    std::string operatorIdentity;
    std::string reasonText;
    int selectedMemberCount = 0;
    std::vector<RecommendationCampaignMaterializationSelectedMember> members;
    std::string materializationIdentityCanonical;
    std::string materializationIdentityHash;
};

RecommendationCampaignMaterializationRequest
NormalizeRecommendationCampaignMaterializationRequest(
    const RecommendationCampaignMaterializationRequest& request);

RecommendationCampaignMaterializationEvidence
BuildRecommendationCampaignMaterializationEvidence(
    const RecommendationCampaignMaterializationRequest& request,
    const RecommendationCampaignApprovalEvidence& approval,
    const RecommendationCampaignPlan& plan,
    const std::vector<ProposedExperimentSpecification>& proposals);

void ValidateRecommendationCampaignMaterializationEvidence(
    const RecommendationCampaignMaterializationEvidence& evidence);

} // namespace EA::ExperimentRecommendation
#include "ExperimentRecommendationCampaignMaterialization.hpp"

#include "ExperimentRecommendation.hpp"

#include <algorithm>
#include <locale>
#include <set>
#include <sstream>
#include <stdexcept>

namespace EA::ExperimentRecommendation
{
namespace
{

std::string LengthText(const std::string& value)
{
    return std::to_string(value.size()) + ":" + value;
}

bool HasControlOrNul(const std::string& value)
{
    return std::any_of(value.begin(), value.end(), [](unsigned char value) {
        return value == 0 || value < 0x20 || value == 0x7f;
    });
}

std::string TrimAscii(const std::string& value)
{
    const auto blank = [](unsigned char character) {
        return character == ' ' || character == '\t' || character == '\n' ||
               character == '\r' || character == '\f' || character == '\v';
    };
    std::size_t begin = 0;
    while (begin < value.size() && blank(value[begin])) ++begin;
    std::size_t end = value.size();
    while (end > begin && blank(value[end - 1])) --end;
    return value.substr(begin, end - begin);
}

std::string SelectedMemberCanonical(
    int ordinal,
    const RecommendationCampaignPlanCandidate& candidate,
    const ProposedExperimentSpecification& proposal)
{
    std::ostringstream out;
    out.imbue(std::locale::classic());
    out << "experiment_recommendation_campaign_materialization_member_v1"
        << ";ordinal=" << ordinal
        << ";ranking_member_id=" << candidate.input.rankingMemberId
        << ";ranking_position=" << candidate.input.rankingPosition
        << ";recommendation_id=" << candidate.input.recommendationId
        << ";source_experiment_id=" << candidate.input.sourceExperimentId
        << ";recommendation_semantic="
        << LengthText(candidate.input.recommendationSemanticCanonical)
        << ";recommendation_semantic_hash="
        << LengthText(candidate.input.recommendationSemanticHash)
        << ";recommendation_invocation="
        << LengthText(candidate.input.recommendationInvocationCanonical)
        << ";recommendation_invocation_hash="
        << LengthText(candidate.input.recommendationInvocationHash)
        << ";proposal_identity="
        << LengthText(proposal.conversionIdentityCanonical)
        << ";proposal_identity_hash="
        << LengthText(proposal.conversionIdentityHash);
    return out.str();
}

std::string MaterializationCanonical(
    const RecommendationCampaignMaterializationEvidence& evidence)
{
    std::ostringstream out;
    out.imbue(std::locale::classic());
    out << "experiment_recommendation_campaign_materialization_v1"
        << ";contract_version=" << evidence.materializationContractVersion
        << ";campaign_approval_id=" << evidence.campaignApprovalId
        << ";campaign_approval_identity="
        << LengthText(evidence.approval.approvalIdentityCanonical)
        << ";campaign_approval_identity_hash="
        << LengthText(evidence.approval.approvalIdentityHash)
        << ";ranking_snapshot_id=" << evidence.approval.rankingSnapshotId
        << ";ranking_snapshot_identity="
        << LengthText(evidence.approval.rankingSnapshotIdentityCanonical)
        << ";ranking_snapshot_identity_hash="
        << LengthText(evidence.approval.rankingSnapshotIdentityHash)
        << ";planning_policy="
        << LengthText(evidence.approval.planningPolicyCanonical)
        << ";planning_policy_hash="
        << LengthText(evidence.approval.planningPolicyHash)
        << ";planning_scope="
        << LengthText(evidence.approval.planningScopeCanonical)
        << ";campaign_plan_identity="
        << LengthText(evidence.approval.campaignPlanIdentityCanonical)
        << ";campaign_plan_identity_hash="
        << LengthText(evidence.approval.campaignPlanIdentityHash)
        << ";campaign_review_identity="
        << LengthText(evidence.approval.campaignReviewIdentityCanonical)
        << ";campaign_review_identity_hash="
        << LengthText(evidence.approval.campaignReviewIdentityHash)
        << ";approval_decision="
        << RecommendationCampaignApprovalDecisionText(
               evidence.approval.decision)
        << ";approval_reviewer="
        << LengthText(evidence.approval.reviewerIdentity)
        << ";approval_reason=" << LengthText(evidence.approval.reasonText)
        << ";materialized_by=" << LengthText(evidence.operatorIdentity)
        << ";materialization_reason=" << LengthText(evidence.reasonText)
        << ";selected_member_count=" << evidence.members.size();
    for (std::size_t i = 0; i < evidence.members.size(); ++i)
    {
        const auto& member = evidence.members[i];
        out << ";member[" << i << "].identity="
            << LengthText(member.selectedMemberIdentityCanonical)
            << ";member[" << i << "].identity_hash="
            << LengthText(member.selectedMemberIdentityHash)
            << ";member[" << i << "].proposal_identity="
            << LengthText(member.proposal.conversionIdentityCanonical)
            << ";member[" << i << "].proposal_identity_hash="
            << LengthText(member.proposal.conversionIdentityHash);
    }
    return out.str();
}

} // namespace

RecommendationCampaignMaterializationRequest
NormalizeRecommendationCampaignMaterializationRequest(
    const RecommendationCampaignMaterializationRequest& request)
{
    RecommendationCampaignMaterializationRequest normalized = request;
    normalized.operatorIdentity = TrimAscii(request.operatorIdentity);
    normalized.reasonText = TrimAscii(request.reasonText);
    if (normalized.campaignApprovalId <= 0 ||
        normalized.operatorIdentity.empty() || normalized.reasonText.empty() ||
        normalized.operatorIdentity.size() >
            kRecommendationCampaignMaterializationOperatorMaximum ||
        normalized.reasonText.size() >
            kRecommendationCampaignMaterializationReasonMaximum ||
        HasControlOrNul(normalized.operatorIdentity) ||
        HasControlOrNul(normalized.reasonText))
        throw std::invalid_argument(
            "recommendation_campaign_materialization_request_invalid");
    return normalized;
}

RecommendationCampaignMaterializationEvidence
BuildRecommendationCampaignMaterializationEvidence(
    const RecommendationCampaignMaterializationRequest& request,
    const RecommendationCampaignApprovalEvidence& approval,
    const RecommendationCampaignPlan& plan,
    const std::vector<ProposedExperimentSpecification>& proposals)
{
    const auto normalized =
        NormalizeRecommendationCampaignMaterializationRequest(request);
    ValidateRecommendationCampaignApprovalEvidence(approval);
    if (approval.decision != RecommendationCampaignApprovalDecision::approved)
        throw std::invalid_argument(
            "recommendation_campaign_materialization_approval_rejected");
    if (approval.campaignPlanIdentityCanonical != plan.identityCanonical ||
        approval.campaignPlanIdentityHash != plan.identityHash ||
        approval.summary.selectedCount <= 0 ||
        approval.summary.selectedCount != plan.summary.selectedCount)
        throw std::invalid_argument(
            "recommendation_campaign_materialization_approval_stale");

    std::vector<const RecommendationCampaignPlanCandidate*> selected;
    for (const auto& candidate : plan.candidates)
        if (candidate.decision == RecommendationCampaignDecision::include)
            selected.push_back(&candidate);
    if (selected.size() != proposals.size() ||
        selected.size() != static_cast<std::size_t>(approval.summary.selectedCount))
        throw std::invalid_argument(
            "recommendation_campaign_materialization_member_count_mismatch");

    RecommendationCampaignMaterializationEvidence evidence;
    evidence.campaignApprovalId = normalized.campaignApprovalId;
    evidence.approval = approval;
    evidence.operatorIdentity = normalized.operatorIdentity;
    evidence.reasonText = normalized.reasonText;
    evidence.selectedMemberCount = static_cast<int>(selected.size());
    std::set<long long> rankingMembers;
    std::set<std::string> proposalCanonicals;
    for (std::size_t index = 0; index < selected.size(); ++index)
    {
        const auto& candidate = *selected[index];
        const auto& proposal = proposals[index];
        if (candidate.input.recommendationId != proposal.recommendationId ||
            candidate.input.sourceExperimentId != proposal.sourceExperimentId ||
            !rankingMembers.insert(candidate.input.rankingMemberId).second ||
            !proposalCanonicals.insert(
                proposal.conversionIdentityCanonical).second)
            throw std::invalid_argument(
                "recommendation_campaign_materialization_member_invalid");
        RecommendationCampaignMaterializationSelectedMember member;
        member.memberOrdinal = static_cast<int>(index) + 1;
        member.rankingMemberId = candidate.input.rankingMemberId;
        member.recommendationId = candidate.input.recommendationId;
        member.sourceExperimentId = candidate.input.sourceExperimentId;
        member.rankingPosition = candidate.input.rankingPosition;
        member.proposal = proposal;
        member.selectedMemberIdentityCanonical = SelectedMemberCanonical(
            member.memberOrdinal, candidate, proposal);
        member.selectedMemberIdentityHash = RecommendationCanonicalHash(
            member.selectedMemberIdentityCanonical);
        evidence.members.push_back(std::move(member));
    }
    evidence.materializationIdentityCanonical =
        MaterializationCanonical(evidence);
    evidence.materializationIdentityHash = RecommendationCanonicalHash(
        evidence.materializationIdentityCanonical);
    ValidateRecommendationCampaignMaterializationEvidence(evidence);
    return evidence;
}

void ValidateRecommendationCampaignMaterializationEvidence(
    const RecommendationCampaignMaterializationEvidence& evidence)
{
    if (evidence.materializationContractVersion !=
            kRecommendationCampaignMaterializationContractVersion ||
        evidence.campaignApprovalId <= 0 ||
        evidence.approval.decision !=
            RecommendationCampaignApprovalDecision::approved ||
        evidence.selectedMemberCount <= 0 ||
        evidence.selectedMemberCount !=
            static_cast<int>(evidence.members.size()) ||
        evidence.operatorIdentity.empty() || evidence.reasonText.empty() ||
        evidence.operatorIdentity.size() >
            kRecommendationCampaignMaterializationOperatorMaximum ||
        evidence.reasonText.size() >
            kRecommendationCampaignMaterializationReasonMaximum ||
        HasControlOrNul(evidence.operatorIdentity) ||
        HasControlOrNul(evidence.reasonText) ||
        evidence.materializationIdentityCanonical.empty() ||
        evidence.materializationIdentityCanonical.size() >
            kRecommendationCampaignMaterializationIdentityCanonicalMaximum ||
        evidence.materializationIdentityHash != RecommendationCanonicalHash(
            evidence.materializationIdentityCanonical) ||
        MaterializationCanonical(evidence) !=
            evidence.materializationIdentityCanonical)
        throw std::invalid_argument(
            "recommendation_campaign_materialization_evidence_invalid");
    ValidateRecommendationCampaignApprovalEvidence(evidence.approval);
    std::set<int> ordinals;
    std::set<long long> rankingMembers;
    std::set<std::string> proposalCanonicals;
    for (const auto& member : evidence.members)
    {
        if (member.memberOrdinal <= 0 ||
            member.memberOrdinal > evidence.selectedMemberCount ||
            member.rankingMemberId <= 0 || member.recommendationId <= 0 ||
            member.sourceExperimentId <= 0 || member.rankingPosition <= 0 ||
            member.proposal.recommendationId != member.recommendationId ||
            member.proposal.sourceExperimentId != member.sourceExperimentId ||
            member.selectedMemberIdentityCanonical.empty() ||
            member.selectedMemberIdentityHash != RecommendationCanonicalHash(
                member.selectedMemberIdentityCanonical) ||
            member.proposal.conversionIdentityHash != RecommendationCanonicalHash(
                member.proposal.conversionIdentityCanonical) ||
            !ordinals.insert(member.memberOrdinal).second ||
            !rankingMembers.insert(member.rankingMemberId).second ||
            !proposalCanonicals.insert(
                member.proposal.conversionIdentityCanonical).second)
            throw std::invalid_argument(
                "recommendation_campaign_materialization_member_invalid");
    }
    for (int ordinal = 1; ordinal <= evidence.selectedMemberCount; ++ordinal)
        if (!ordinals.contains(ordinal))
            throw std::invalid_argument(
                "recommendation_campaign_materialization_member_ordinal_invalid");
}

} // namespace EA::ExperimentRecommendation
#pragma once

#include <optional>
#include <string>
#include <vector>

namespace EA::ExperimentRecommendation
{

inline constexpr int kRecommendationCampaignStatusContractVersion = 1;
inline constexpr int kMaximumRecommendationCampaignStatusMembers = 1000;

struct RecommendationCampaignStatusRequest
{
    long long materializationId = -1;
};

enum class RecommendationCampaignStatusExecutionState
{
    notExecuted,
    executed,
    malformed,
    conflicting
};

enum class RecommendationCampaignStatusActivationState
{
    notActivated,
    activated,
    malformed,
    conflicting
};

enum class RecommendationCampaignStatusOperationalState
{
    proposalOnly,
    paused,
    pending,
    runningTrain,
    runningInfer,
    runningAnalyze,
    completed,
    failed,
    cancelled,
    pausedAfterProgress,
    inconsistent
};

enum class RecommendationCampaignStatusTerminalResult
{
    notTerminal,
    succeeded,
    failed,
    cancelled,
    unknown
};

enum class RecommendationCampaignStatusConsistency
{
    consistent,
    inconsistent
};

enum class RecommendationCampaignAggregateStatus
{
    notExecuted,
    partiallyExecuted,
    executedPaused,
    partiallyActivated,
    queued,
    inProgress,
    completed,
    completedWithFailures,
    cancelled,
    mixedTerminal,
    inconsistent
};

struct RecommendationCampaignStatusExperimentEvidence
{
    long long experimentId = -1;
    std::string symbol;
    int predictionHorizon = 0;
    std::string status;
    std::string phase;
    int targetEpochs = 0;
    std::optional<int> currentEpoch;
    std::optional<int> workerPid;
    std::optional<std::string> currentOperation;
    std::optional<std::string> workerStartedAt;
    std::optional<std::string> startedAt;
    std::optional<std::string> completedAt;
    std::optional<int> exitCode;
    std::optional<std::string> errorMessage;
    std::optional<long long> modelId;
    std::string invocationMode;
    long long duplicateNonce = 0;
    std::string invocationIdentityCanonical;
    bool invocationProvenanceValid = false;
    bool inferStartPresent = false;
    bool inferEndPresent = false;
    bool inferConfigured = false;
    int modelLinkCount = 0;
    int inferenceResultCount = 0;
    int completedInferenceResultCount = 0;
    int failedInferenceResultCount = 0;
    int analysisResultCount = 0;
    int completedAnalysisResultCount = 0;
    int failedAnalysisResultCount = 0;
};

struct RecommendationCampaignStatusMemberInput
{
    int memberOrdinal = 0;
    long long materializationMemberId = -1;
    long long recommendationId = -1;
    long long sourceExperimentId = -1;
    long long rankingMemberId = -1;
    long long proposalId = -1;
    std::optional<long long> currentReviewDecisionId;
    std::optional<std::string> currentReviewDecision;
    std::optional<long long> authorizationReviewDecisionId;
    std::optional<long long> executionId;
    std::optional<long long> activationId;
    std::optional<long long> experimentId;
    long long executionEvidenceCount = 0;
    long long activationEvidenceCount = 0;
    long long executionCount = 0;
    long long activationCount = 0;
    bool workflowConsistent = true;
    std::vector<std::string> workflowDiagnostics;
    std::optional<RecommendationCampaignStatusExperimentEvidence> experiment;
};

struct RecommendationCampaignStatusInput
{
    long long materializationId = -1;
    int materializationContractVersion = 0;
    std::string materializationIdentityCanonical;
    std::string materializationIdentityHash;
    int selectedMemberCount = 0;
    std::string observedAt;
    std::vector<RecommendationCampaignStatusMemberInput> members;
};

struct RecommendationCampaignStatusMember
{
    int memberOrdinal = 0;
    long long materializationMemberId = -1;
    long long recommendationId = -1;
    long long sourceExperimentId = -1;
    long long rankingMemberId = -1;
    long long proposalId = -1;
    std::optional<long long> currentReviewDecisionId;
    std::optional<std::string> currentReviewDecision;
    std::optional<long long> authorizationReviewDecisionId;
    std::optional<long long> executionId;
    std::optional<long long> activationId;
    std::optional<long long> experimentId;
    long long executionEvidenceCount = 0;
    long long activationEvidenceCount = 0;
    std::optional<std::string> symbol;
    std::optional<int> predictionHorizon;
    RecommendationCampaignStatusExecutionState executionState =
        RecommendationCampaignStatusExecutionState::notExecuted;
    RecommendationCampaignStatusActivationState activationState =
        RecommendationCampaignStatusActivationState::notActivated;
    RecommendationCampaignStatusOperationalState operationalState =
        RecommendationCampaignStatusOperationalState::proposalOnly;
    RecommendationCampaignStatusTerminalResult terminalResult =
        RecommendationCampaignStatusTerminalResult::notTerminal;
    RecommendationCampaignStatusConsistency consistency =
        RecommendationCampaignStatusConsistency::consistent;
    std::optional<std::string> status;
    std::optional<std::string> phase;
    std::optional<int> currentEpoch;
    std::optional<int> targetEpochs;
    std::optional<int> workerPid;
    std::optional<std::string> currentOperation;
    std::optional<std::string> workerStartedAt;
    std::optional<std::string> startedAt;
    std::optional<std::string> completedAt;
    std::optional<int> exitCode;
    std::optional<std::string> errorMessage;
    std::optional<long long> modelId;
    std::optional<std::string> invocationIdentityHash;
    int modelLinkCount = 0;
    int finalInferenceResultCount = 0;
    int completedFinalInferenceResultCount = 0;
    int failedFinalInferenceResultCount = 0;
    int finalAnalysisResultCount = 0;
    int completedFinalAnalysisResultCount = 0;
    int failedFinalAnalysisResultCount = 0;
    bool trainComplete = false;
    bool inferConfigured = false;
    bool inferComplete = false;
    bool analyzeConfigured = false;
    bool analyzeComplete = false;
    std::vector<std::string> diagnosticCodes;
};

struct RecommendationCampaignStatusSnapshot
{
    RecommendationCampaignStatusRequest request;
    int materializationContractVersion = 0;
    std::string materializationIdentityHash;
    std::string observedAt;
    std::string snapshotIdentityCanonical;
    std::string snapshotIdentityHash;
    RecommendationCampaignAggregateStatus aggregateStatus =
        RecommendationCampaignAggregateStatus::notExecuted;
    RecommendationCampaignStatusConsistency consistency =
        RecommendationCampaignStatusConsistency::consistent;
    int proposalOnlyCount = 0;
    int executedCount = 0;
    int activatedCount = 0;
    int pausedCount = 0;
    int pendingCount = 0;
    int runningCount = 0;
    int runningTrainCount = 0;
    int runningInferCount = 0;
    int runningAnalyzeCount = 0;
    int completedCount = 0;
    int failedCount = 0;
    int cancelledCount = 0;
    int inconsistentCount = 0;
    int terminalCount = 0;
    int nonterminalCount = 0;
    int activeWorkerCount = 0;
    std::optional<int> minimumCurrentEpoch;
    std::optional<int> maximumCurrentEpoch;
    std::optional<long long> totalCurrentEpochs;
    std::optional<long long> totalTargetEpochs;
    std::optional<double> completedPercent;
    std::optional<double> terminalPercent;
    std::vector<std::string> diagnosticCodes;
    std::vector<RecommendationCampaignStatusMember> members;
};

RecommendationCampaignStatusRequest NormalizeRecommendationCampaignStatusRequest(
    const RecommendationCampaignStatusRequest& request);
RecommendationCampaignStatusSnapshot BuildRecommendationCampaignStatusSnapshot(
    const RecommendationCampaignStatusRequest& request,
    RecommendationCampaignStatusInput input);

std::string RecommendationCampaignStatusExecutionStateText(
    RecommendationCampaignStatusExecutionState value);
std::string RecommendationCampaignStatusActivationStateText(
    RecommendationCampaignStatusActivationState value);
std::string RecommendationCampaignStatusOperationalStateText(
    RecommendationCampaignStatusOperationalState value);
std::string RecommendationCampaignStatusTerminalResultText(
    RecommendationCampaignStatusTerminalResult value);
std::string RecommendationCampaignStatusConsistencyText(
    RecommendationCampaignStatusConsistency value);
std::string RecommendationCampaignAggregateStatusText(
    RecommendationCampaignAggregateStatus value);

} // namespace EA::ExperimentRecommendation
#include "ExperimentRecommendationCampaignStatus.hpp"

#include "ExperimentRecommendation.hpp"
#include "ExperimentRecommendationCampaignMaterialization.hpp"

#include <algorithm>
#include <locale>
#include <set>
#include <sstream>
#include <stdexcept>

namespace EA::ExperimentRecommendation
{
namespace
{

void Diagnose(RecommendationCampaignStatusMember& member, std::string code)
{
    if (std::find(member.diagnosticCodes.begin(), member.diagnosticCodes.end(), code) ==
        member.diagnosticCodes.end())
        member.diagnosticCodes.push_back(std::move(code));
    member.consistency = RecommendationCampaignStatusConsistency::inconsistent;
    member.operationalState = RecommendationCampaignStatusOperationalState::inconsistent;
    member.terminalResult = RecommendationCampaignStatusTerminalResult::unknown;
}

bool IsKnownStatus(const std::string& value)
{
    return value == "pending" || value == "paused" || value == "running" ||
        value == "completed" || value == "failed" || value == "cancelled";
}

bool IsKnownPhase(const std::string& value)
{
    return value == "train" || value == "infer" || value == "analyze" ||
        value == "done";
}

RecommendationCampaignStatusMember ClassifyMember(
    const RecommendationCampaignStatusMemberInput& input)
{
    RecommendationCampaignStatusMember member;
    member.memberOrdinal = input.memberOrdinal;
    member.materializationMemberId = input.materializationMemberId;
    member.recommendationId = input.recommendationId;
    member.sourceExperimentId = input.sourceExperimentId;
    member.rankingMemberId = input.rankingMemberId;
    member.proposalId = input.proposalId;
    member.currentReviewDecisionId = input.currentReviewDecisionId;
    member.currentReviewDecision = input.currentReviewDecision;
    member.authorizationReviewDecisionId = input.authorizationReviewDecisionId;
    member.executionId = input.executionId;
    member.activationId = input.activationId;
    member.experimentId = input.experimentId;
    member.executionEvidenceCount = input.executionCount;
    member.activationEvidenceCount = input.activationCount;

    if (input.executionCount > 1)
        member.executionState = RecommendationCampaignStatusExecutionState::conflicting;
    else if (input.executionId)
        member.executionState = RecommendationCampaignStatusExecutionState::executed;
    if (input.activationCount > 1)
        member.activationState = RecommendationCampaignStatusActivationState::conflicting;
    else if (input.activationId)
        member.activationState = RecommendationCampaignStatusActivationState::activated;

    if (!input.workflowConsistent)
    {
        if (input.executionCount == 1 && input.executionId)
            member.executionState = RecommendationCampaignStatusExecutionState::malformed;
        if (input.activationCount == 1 && input.activationId)
            member.activationState = RecommendationCampaignStatusActivationState::malformed;
        for (const auto& code : input.workflowDiagnostics) Diagnose(member, code);
        if (input.workflowDiagnostics.empty())
            Diagnose(member, "campaign_status_workflow_inconsistent");
    }
    if (input.executionCount < 0 || input.executionCount > 1 ||
        (input.executionId.has_value() != (input.executionCount == 1)))
        Diagnose(member, "campaign_status_execution_cardinality_invalid");
    if (input.activationCount < 0 || input.activationCount > 1 ||
        (input.activationId.has_value() != (input.activationCount == 1)))
        Diagnose(member, "campaign_status_activation_cardinality_invalid");
    if (input.activationId && !input.executionId)
        Diagnose(member, "campaign_status_activation_without_execution");
    if (!input.executionId)
    {
        if (input.experiment || input.experimentId)
            Diagnose(member, "campaign_status_experiment_without_execution");
        return member;
    }
    if (!input.experiment || !input.experimentId)
    {
        Diagnose(member, "campaign_status_execution_experiment_missing");
        return member;
    }

    const auto& experiment = *input.experiment;
    member.symbol = experiment.symbol;
    member.predictionHorizon = experiment.predictionHorizon;
    member.status = experiment.status;
    member.phase = experiment.phase;
    member.currentEpoch = experiment.currentEpoch;
    member.targetEpochs = experiment.targetEpochs;
    member.workerPid = experiment.workerPid;
    member.currentOperation = experiment.currentOperation;
    member.workerStartedAt = experiment.workerStartedAt;
    member.startedAt = experiment.startedAt;
    member.completedAt = experiment.completedAt;
    member.exitCode = experiment.exitCode;
    member.errorMessage = experiment.errorMessage;
    member.modelId = experiment.modelId;
    if (!experiment.invocationIdentityCanonical.empty())
        member.invocationIdentityHash = RecommendationCanonicalHash(
            experiment.invocationIdentityCanonical);
    member.modelLinkCount = experiment.modelLinkCount;
    member.finalInferenceResultCount = experiment.inferenceResultCount;
    member.completedFinalInferenceResultCount =
        experiment.completedInferenceResultCount;
    member.failedFinalInferenceResultCount =
        experiment.failedInferenceResultCount;
    member.finalAnalysisResultCount = experiment.analysisResultCount;
    member.completedFinalAnalysisResultCount =
        experiment.completedAnalysisResultCount;
    member.failedFinalAnalysisResultCount = experiment.failedAnalysisResultCount;
    member.inferConfigured = experiment.inferConfigured;
    member.analyzeConfigured = experiment.inferConfigured;
    member.inferComplete = experiment.completedInferenceResultCount > 0;
    member.analyzeComplete = experiment.completedAnalysisResultCount > 0;
    member.trainComplete = experiment.modelId.has_value() &&
        experiment.modelLinkCount == 1;

    if (experiment.experimentId != *input.experimentId)
        Diagnose(member, "campaign_status_experiment_provenance_mismatch");
    if (!experiment.invocationProvenanceValid)
        Diagnose(member, "campaign_status_experiment_invocation_mismatch");
    if (experiment.symbol.empty() || experiment.predictionHorizon <= 0 ||
        experiment.targetEpochs <= 0 || experiment.duplicateNonce != 0 ||
        experiment.invocationMode != "recommendation_conversion")
        Diagnose(member, "campaign_status_experiment_identity_invalid");
    if (!IsKnownStatus(experiment.status))
        Diagnose(member, "campaign_status_experiment_status_unknown");
    if (!IsKnownPhase(experiment.phase))
        Diagnose(member, "campaign_status_experiment_phase_unknown");
    if (experiment.currentEpoch &&
        (*experiment.currentEpoch < 0 || *experiment.currentEpoch > experiment.targetEpochs))
        Diagnose(member, "campaign_status_current_epoch_invalid");
    if (experiment.inferStartPresent != experiment.inferEndPresent)
        Diagnose(member, "campaign_status_inference_configuration_invalid");
    if (experiment.modelLinkCount < 0 || experiment.modelLinkCount > 1)
        Diagnose(member, "campaign_status_model_link_conflict");
    if (experiment.inferenceResultCount < 0 ||
        experiment.completedInferenceResultCount < 0 ||
        experiment.failedInferenceResultCount < 0 ||
        experiment.completedInferenceResultCount +
                experiment.failedInferenceResultCount !=
            experiment.inferenceResultCount ||
        experiment.completedInferenceResultCount > 1 ||
        (experiment.completedInferenceResultCount > 0 &&
         experiment.failedInferenceResultCount > 0))
        Diagnose(member, "campaign_status_inference_evidence_invalid");
    if (experiment.analysisResultCount < 0 ||
        experiment.completedAnalysisResultCount < 0 ||
        experiment.failedAnalysisResultCount < 0 ||
        experiment.completedAnalysisResultCount +
                experiment.failedAnalysisResultCount !=
            experiment.analysisResultCount ||
        experiment.completedAnalysisResultCount > 1 ||
        experiment.failedAnalysisResultCount > 1 ||
        (experiment.completedAnalysisResultCount > 0 &&
         experiment.failedAnalysisResultCount > 0))
        Diagnose(member, "campaign_status_analysis_evidence_invalid");

    if (member.consistency == RecommendationCampaignStatusConsistency::inconsistent)
        return member;

    if (!input.activationId)
    {
        if (experiment.status == "paused" && experiment.phase == "train" &&
            !experiment.workerPid && !experiment.currentOperation &&
            !experiment.currentEpoch && !experiment.workerStartedAt &&
            !experiment.startedAt && !experiment.completedAt &&
            !experiment.exitCode && !experiment.errorMessage && !experiment.modelId)
            member.operationalState = RecommendationCampaignStatusOperationalState::paused;
        else
            Diagnose(member, "campaign_status_unactivated_lifecycle_invalid");
        return member;
    }

    if (experiment.status == "pending")
    {
        if (experiment.workerPid)
            Diagnose(member, "campaign_status_pending_worker_invalid");
        else if (experiment.phase == "train" || experiment.phase == "infer" ||
                 experiment.phase == "analyze")
            member.operationalState = RecommendationCampaignStatusOperationalState::pending;
        else
            Diagnose(member, "campaign_status_pending_phase_invalid");
    }
    else if (experiment.status == "running")
    {
        if (!experiment.workerStartedAt || !experiment.currentOperation ||
            *experiment.currentOperation != experiment.phase)
            Diagnose(member, "campaign_status_running_worker_evidence_invalid");
        else if (experiment.phase == "train")
            member.operationalState = RecommendationCampaignStatusOperationalState::runningTrain;
        else if (experiment.phase == "infer")
            member.operationalState = RecommendationCampaignStatusOperationalState::runningInfer;
        else if (experiment.phase == "analyze")
            member.operationalState = RecommendationCampaignStatusOperationalState::runningAnalyze;
        else
            Diagnose(member, "campaign_status_running_phase_invalid");
    }
    else if (experiment.status == "paused")
    {
        if (experiment.startedAt || experiment.currentEpoch || experiment.modelId)
            member.operationalState =
                RecommendationCampaignStatusOperationalState::pausedAfterProgress;
        else
            Diagnose(member, "campaign_status_activated_experiment_still_paused");
    }
    else if (experiment.status == "completed")
    {
        if (experiment.phase != "done" || !experiment.completedAt ||
            experiment.workerPid || !member.trainComplete ||
            !member.inferConfigured || !member.inferComplete ||
            !member.analyzeConfigured || !member.analyzeComplete ||
            (experiment.errorMessage && !experiment.errorMessage->empty()) ||
            (experiment.exitCode && *experiment.exitCode != 0))
            Diagnose(member, "campaign_status_completed_evidence_invalid");
        else
        {
            member.operationalState = RecommendationCampaignStatusOperationalState::completed;
            member.terminalResult = RecommendationCampaignStatusTerminalResult::succeeded;
        }
    }
    else if (experiment.status == "failed")
    {
        if (!experiment.completedAt || !experiment.errorMessage ||
            experiment.errorMessage->empty() || experiment.workerPid)
            Diagnose(member, "campaign_status_failed_evidence_invalid");
        else
        {
            member.operationalState = RecommendationCampaignStatusOperationalState::failed;
            member.terminalResult = RecommendationCampaignStatusTerminalResult::failed;
        }
    }
    else if (experiment.status == "cancelled")
    {
        if (!experiment.completedAt || experiment.workerPid)
            Diagnose(member, "campaign_status_cancelled_evidence_invalid");
        else
        {
            member.operationalState = RecommendationCampaignStatusOperationalState::cancelled;
            member.terminalResult = RecommendationCampaignStatusTerminalResult::cancelled;
        }
    }
    else if (experiment.status != "pending" && experiment.status != "running" &&
             experiment.status != "paused")
        Diagnose(member, "campaign_status_experiment_lifecycle_invalid");

    return member;
}

std::string OptionalLongLong(const std::optional<long long>& value)
{
    return value ? std::to_string(*value) : "null";
}

std::string OptionalInt(const std::optional<int>& value)
{
    return value ? std::to_string(*value) : "null";
}

std::string OptionalText(const std::optional<std::string>& value)
{
    return value ? std::to_string(value->size()) + ":" + *value : "null";
}

std::string SnapshotCanonical(
    const RecommendationCampaignStatusInput& input,
    const std::vector<RecommendationCampaignStatusMember>& members)
{
    std::ostringstream out;
    out.imbue(std::locale::classic());
    out << "experiment_recommendation_campaign_status_snapshot_v1"
        << ";status_contract_version=" << kRecommendationCampaignStatusContractVersion
        << ";materialization_id=" << input.materializationId
        << ";materialization_contract_version=" << input.materializationContractVersion
        << ";materialization_identity_hash=" << input.materializationIdentityHash
        << ";member_count=" << members.size();
    for (const auto& member : members)
    {
        out << ";member=" << member.memberOrdinal << ','
            << member.materializationMemberId << ',' << member.recommendationId << ','
            << member.sourceExperimentId << ',' << member.rankingMemberId << ','
            << member.proposalId << ','
            << OptionalLongLong(member.currentReviewDecisionId) << ','
            << OptionalText(member.currentReviewDecision) << ','
            << OptionalLongLong(member.authorizationReviewDecisionId) << ','
            << OptionalLongLong(member.executionId) << ','
            << OptionalLongLong(member.activationId) << ','
            << OptionalLongLong(member.experimentId) << ','
            << member.executionEvidenceCount << ','
            << member.activationEvidenceCount << ','
            << RecommendationCampaignStatusExecutionStateText(member.executionState)
            << ',' << RecommendationCampaignStatusActivationStateText(member.activationState)
            << ',' << RecommendationCampaignStatusOperationalStateText(member.operationalState)
            << ',' << RecommendationCampaignStatusTerminalResultText(member.terminalResult)
            << ',' << OptionalText(member.symbol) << ','
            << OptionalInt(member.predictionHorizon) << ','
            << OptionalText(member.status) << ',' << OptionalText(member.phase)
            << ',' << OptionalInt(member.currentEpoch) << ','
            << OptionalInt(member.targetEpochs) << ',' << OptionalInt(member.workerPid)
            << ',' << OptionalText(member.currentOperation) << ','
            << OptionalText(member.workerStartedAt) << ','
            << OptionalText(member.startedAt) << ','
            << OptionalText(member.completedAt) << ','
            << OptionalInt(member.exitCode) << ','
            << OptionalText(member.errorMessage) << ','
            << OptionalLongLong(member.modelId) << ','
            << OptionalText(member.invocationIdentityHash) << ','
            << member.modelLinkCount << ','
            << member.finalInferenceResultCount << ','
            << member.completedFinalInferenceResultCount << ','
            << member.failedFinalInferenceResultCount << ','
            << member.finalAnalysisResultCount << ','
            << member.completedFinalAnalysisResultCount << ','
            << member.failedFinalAnalysisResultCount << ','
            << member.trainComplete << ',' << member.inferConfigured << ','
            << member.inferComplete << ',' << member.analyzeConfigured << ','
            << member.analyzeComplete << ','
            << RecommendationCampaignStatusConsistencyText(member.consistency);
        for (const auto& code : member.diagnosticCodes)
            out << ',' << code.size() << ':' << code;
    }
    return out.str();
}

RecommendationCampaignAggregateStatus AggregateState(
    const RecommendationCampaignStatusSnapshot& result)
{
    const int count = static_cast<int>(result.members.size());
    if (result.inconsistentCount > 0)
        return RecommendationCampaignAggregateStatus::inconsistent;
    if (result.executedCount == 0)
        return RecommendationCampaignAggregateStatus::notExecuted;
    if (result.executedCount < count)
        return RecommendationCampaignAggregateStatus::partiallyExecuted;
    if (result.activatedCount < count)
        return result.activatedCount == 0
            ? RecommendationCampaignAggregateStatus::executedPaused
            : RecommendationCampaignAggregateStatus::partiallyActivated;
    if (result.terminalCount == count)
    {
        if (result.completedCount == count)
            return RecommendationCampaignAggregateStatus::completed;
        if (result.cancelledCount == count)
            return RecommendationCampaignAggregateStatus::cancelled;
        if (result.failedCount > 0 && result.cancelledCount == 0)
            return RecommendationCampaignAggregateStatus::completedWithFailures;
        return RecommendationCampaignAggregateStatus::mixedTerminal;
    }
    if (result.runningCount > 0)
        return RecommendationCampaignAggregateStatus::inProgress;
    if (result.pendingCount > 0)
        return RecommendationCampaignAggregateStatus::queued;
    return RecommendationCampaignAggregateStatus::executedPaused;
}

} // namespace

RecommendationCampaignStatusRequest NormalizeRecommendationCampaignStatusRequest(
    const RecommendationCampaignStatusRequest& request)
{
    if (request.materializationId <= 0)
        throw std::invalid_argument("campaign_status_materialization_id_invalid");
    return request;
}

RecommendationCampaignStatusSnapshot BuildRecommendationCampaignStatusSnapshot(
    const RecommendationCampaignStatusRequest& request,
    RecommendationCampaignStatusInput input)
{
    RecommendationCampaignStatusSnapshot result;
    result.request = NormalizeRecommendationCampaignStatusRequest(request);
    if (input.materializationId != result.request.materializationId ||
        input.materializationContractVersion !=
            kRecommendationCampaignMaterializationContractVersion ||
        input.materializationIdentityCanonical.empty() ||
        input.materializationIdentityHash !=
            RecommendationCanonicalHash(input.materializationIdentityCanonical))
        throw std::invalid_argument("campaign_status_materialization_identity_invalid");
    if (input.selectedMemberCount <= 0 || input.members.empty())
        throw std::invalid_argument("campaign_status_zero_members");
    if (input.selectedMemberCount != static_cast<int>(input.members.size()))
        throw std::invalid_argument("campaign_status_member_count_mismatch");
    if (input.members.size() >
        static_cast<std::size_t>(kMaximumRecommendationCampaignStatusMembers))
        throw std::invalid_argument("campaign_status_member_limit_exceeded");
    if (input.observedAt.empty())
        throw std::invalid_argument("campaign_status_observed_at_required");

    std::sort(input.members.begin(), input.members.end(),
        [](const auto& left, const auto& right)
        {
            return left.memberOrdinal < right.memberOrdinal;
        });
    std::set<long long> memberIds;
    std::set<long long> proposalIds;
    std::set<long long> rankingIds;
    result.members.reserve(input.members.size());
    for (std::size_t index = 0; index < input.members.size(); ++index)
    {
        const auto& member = input.members[index];
        if (member.memberOrdinal != static_cast<int>(index) + 1 ||
            member.materializationMemberId <= 0 || member.recommendationId <= 0 ||
            member.sourceExperimentId <= 0 || member.rankingMemberId <= 0 ||
            member.proposalId <= 0 ||
            !memberIds.insert(member.materializationMemberId).second ||
            !proposalIds.insert(member.proposalId).second ||
            !rankingIds.insert(member.rankingMemberId).second)
            throw std::invalid_argument("campaign_status_materialization_members_invalid");
        result.members.push_back(ClassifyMember(member));
    }

    result.materializationContractVersion = input.materializationContractVersion;
    result.materializationIdentityHash = input.materializationIdentityHash;
    result.observedAt = input.observedAt;
    long long totalCurrent = 0;
    long long totalTarget = 0;
    bool allCurrentEpochsAvailable = true;
    bool allTargetEpochsAvailable = true;
    for (const auto& member : result.members)
    {
        if (member.executionState == RecommendationCampaignStatusExecutionState::executed)
            ++result.executedCount;
        if (member.activationState == RecommendationCampaignStatusActivationState::activated)
            ++result.activatedCount;
        switch (member.operationalState)
        {
            case RecommendationCampaignStatusOperationalState::proposalOnly:
                ++result.proposalOnlyCount; break;
            case RecommendationCampaignStatusOperationalState::paused:
            case RecommendationCampaignStatusOperationalState::pausedAfterProgress:
                ++result.pausedCount; break;
            case RecommendationCampaignStatusOperationalState::pending:
                ++result.pendingCount; break;
            case RecommendationCampaignStatusOperationalState::runningTrain:
                ++result.runningCount; ++result.runningTrainCount; break;
            case RecommendationCampaignStatusOperationalState::runningInfer:
                ++result.runningCount; ++result.runningInferCount; break;
            case RecommendationCampaignStatusOperationalState::runningAnalyze:
                ++result.runningCount; ++result.runningAnalyzeCount; break;
            case RecommendationCampaignStatusOperationalState::completed:
                ++result.completedCount; break;
            case RecommendationCampaignStatusOperationalState::failed:
                ++result.failedCount; break;
            case RecommendationCampaignStatusOperationalState::cancelled:
                ++result.cancelledCount; break;
            case RecommendationCampaignStatusOperationalState::inconsistent:
                ++result.inconsistentCount; break;
        }
        if (member.terminalResult != RecommendationCampaignStatusTerminalResult::notTerminal &&
            member.terminalResult != RecommendationCampaignStatusTerminalResult::unknown)
            ++result.terminalCount;
        if (member.workerPid &&
            (member.operationalState ==
                 RecommendationCampaignStatusOperationalState::runningTrain ||
             member.operationalState ==
                 RecommendationCampaignStatusOperationalState::runningInfer ||
             member.operationalState ==
                 RecommendationCampaignStatusOperationalState::runningAnalyze))
            ++result.activeWorkerCount;
        if (member.currentEpoch)
        {
            totalCurrent += *member.currentEpoch;
            result.minimumCurrentEpoch = result.minimumCurrentEpoch
                ? std::min(*result.minimumCurrentEpoch, *member.currentEpoch)
                : member.currentEpoch;
            result.maximumCurrentEpoch = result.maximumCurrentEpoch
                ? std::max(*result.maximumCurrentEpoch, *member.currentEpoch)
                : member.currentEpoch;
        }
        else allCurrentEpochsAvailable = false;
        if (member.targetEpochs) totalTarget += *member.targetEpochs;
        else allTargetEpochsAvailable = false;
    }
    result.nonterminalCount = static_cast<int>(result.members.size()) -
        result.terminalCount;
    if (allCurrentEpochsAvailable) result.totalCurrentEpochs = totalCurrent;
    if (allTargetEpochsAvailable) result.totalTargetEpochs = totalTarget;
    const double denominator = static_cast<double>(result.members.size());
    result.completedPercent = 100.0 * result.completedCount / denominator;
    result.terminalPercent = 100.0 * result.terminalCount / denominator;
    if (result.inconsistentCount > 0)
    {
        result.consistency = RecommendationCampaignStatusConsistency::inconsistent;
        result.diagnosticCodes.push_back("campaign_status_member_inconsistent");
    }
    else result.diagnosticCodes.push_back("campaign_status_snapshot_consistent");
    result.aggregateStatus = AggregateState(result);
    result.snapshotIdentityCanonical = SnapshotCanonical(input, result.members);
    result.snapshotIdentityHash = RecommendationCanonicalHash(
        result.snapshotIdentityCanonical);
    return result;
}

#define EA_STATUS_TEXT_FUNCTION(name, type, cases) \
std::string name(type value) { switch (value) { cases } throw std::invalid_argument(#name "_invalid"); }

EA_STATUS_TEXT_FUNCTION(RecommendationCampaignStatusExecutionStateText,
    RecommendationCampaignStatusExecutionState,
    case RecommendationCampaignStatusExecutionState::notExecuted: return "not_executed";
    case RecommendationCampaignStatusExecutionState::executed: return "executed";
    case RecommendationCampaignStatusExecutionState::malformed: return "malformed";
    case RecommendationCampaignStatusExecutionState::conflicting: return "conflicting";)
EA_STATUS_TEXT_FUNCTION(RecommendationCampaignStatusActivationStateText,
    RecommendationCampaignStatusActivationState,
    case RecommendationCampaignStatusActivationState::notActivated: return "not_activated";
    case RecommendationCampaignStatusActivationState::activated: return "activated";
    case RecommendationCampaignStatusActivationState::malformed: return "malformed";
    case RecommendationCampaignStatusActivationState::conflicting: return "conflicting";)
EA_STATUS_TEXT_FUNCTION(RecommendationCampaignStatusOperationalStateText,
    RecommendationCampaignStatusOperationalState,
    case RecommendationCampaignStatusOperationalState::proposalOnly: return "proposal_only";
    case RecommendationCampaignStatusOperationalState::paused: return "paused";
    case RecommendationCampaignStatusOperationalState::pending: return "pending";
    case RecommendationCampaignStatusOperationalState::runningTrain: return "running_train";
    case RecommendationCampaignStatusOperationalState::runningInfer: return "running_infer";
    case RecommendationCampaignStatusOperationalState::runningAnalyze: return "running_analyze";
    case RecommendationCampaignStatusOperationalState::completed: return "completed";
    case RecommendationCampaignStatusOperationalState::failed: return "failed";
    case RecommendationCampaignStatusOperationalState::cancelled: return "cancelled";
    case RecommendationCampaignStatusOperationalState::pausedAfterProgress: return "paused_after_progress";
    case RecommendationCampaignStatusOperationalState::inconsistent: return "inconsistent";)
EA_STATUS_TEXT_FUNCTION(RecommendationCampaignStatusTerminalResultText,
    RecommendationCampaignStatusTerminalResult,
    case RecommendationCampaignStatusTerminalResult::notTerminal: return "not_terminal";
    case RecommendationCampaignStatusTerminalResult::succeeded: return "succeeded";
    case RecommendationCampaignStatusTerminalResult::failed: return "failed";
    case RecommendationCampaignStatusTerminalResult::cancelled: return "cancelled";
    case RecommendationCampaignStatusTerminalResult::unknown: return "unknown";)
EA_STATUS_TEXT_FUNCTION(RecommendationCampaignStatusConsistencyText,
    RecommendationCampaignStatusConsistency,
    case RecommendationCampaignStatusConsistency::consistent: return "consistent";
    case RecommendationCampaignStatusConsistency::inconsistent: return "inconsistent";)
EA_STATUS_TEXT_FUNCTION(RecommendationCampaignAggregateStatusText,
    RecommendationCampaignAggregateStatus,
    case RecommendationCampaignAggregateStatus::notExecuted: return "not_executed";
    case RecommendationCampaignAggregateStatus::partiallyExecuted: return "partially_executed";
    case RecommendationCampaignAggregateStatus::executedPaused: return "executed_paused";
    case RecommendationCampaignAggregateStatus::partiallyActivated: return "partially_activated";
    case RecommendationCampaignAggregateStatus::queued: return "queued";
    case RecommendationCampaignAggregateStatus::inProgress: return "in_progress";
    case RecommendationCampaignAggregateStatus::completed: return "completed";
    case RecommendationCampaignAggregateStatus::completedWithFailures: return "completed_with_failures";
    case RecommendationCampaignAggregateStatus::cancelled: return "cancelled";
    case RecommendationCampaignAggregateStatus::mixedTerminal: return "mixed_terminal";
    case RecommendationCampaignAggregateStatus::inconsistent: return "inconsistent";)


exec
/bin/bash -lc "rg -n \"ContractVersion|contractVersion|_v2|assessment_v\" Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst Sources/ExperimentRecommendationCampaignOutcomeAssessment.* && nl -ba Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp | sed -n '200,500p' && nl -ba Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp | sed -n '500,780p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:11:    kRecommendationCampaignOutcomeAssessmentContractVersion = 2;
Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:37:    const int contractVersion;
Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:45:        int contractVersion,
Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:435:    const int contractVersion;
Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:448:        int contractVersion,
Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:115:    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp:348:            "binary_forward_return_v2", "2026-02-01T00:00:00Z",
Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:289:    out << "experiment_recommendation_campaign_outcome_assessment_v2"
Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:291:        << kRecommendationCampaignOutcomeAssessmentContractVersion
Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:296:        << materialization.contractVersion
Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:531:        int contractVersionValue,
Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:537:      contractVersion(contractVersionValue),
Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:865:        int contractVersionValue,
Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:868:    : contractVersion(contractVersionValue),
Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:872:    if (contractVersion !=
Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:873:            kRecommendationCampaignOutcomeAssessmentContractVersion ||
Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1032:        if (materializationIdentity.contractVersion !=
Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1033:            kRecommendationCampaignMaterializationContractVersion)
Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1212:            kRecommendationCampaignOutcomeAssessmentContractVersion,
   200	std::vector<Metric> BaseSourceMetrics()
   201	{
   202	    return {NumericMetric("inference_accuracy", 0.70),
   203	        NumericMetric("leader_score", 0.80)};
   204	}
   205	
   206	std::vector<Metric> BaseResultMetrics()
   207	{
   208	    return {NumericMetric("inference_accuracy", 0.75),
   209	        NumericMetric("leader_score", 0.78)};
   210	}
   211	
   212	MemberEvidence Succeeded(
   213	    int ordinal,
   214	    std::vector<Metric> sourceMetrics = BaseSourceMetrics(),
   215	    std::vector<Metric> resultMetrics = BaseResultMetrics(),
   216	    Context sourceContext = ComparableContext(),
   217	    Context resultContext = ComparableContext())
   218	{
   219	    return {Identity(ordinal, ResultExperiment(ordinal)),
   220	        Lifecycle::Succeeded,
   221	        Source(ordinal, std::move(sourceMetrics), std::move(sourceContext)),
   222	        Result(ordinal, std::move(resultMetrics), std::move(resultContext))};
   223	}
   224	
   225	MemberEvidence LifecycleOnly(
   226	    int ordinal,
   227	    Lifecycle lifecycle,
   228	    std::optional<long long> expectedExperimentId = std::nullopt)
   229	{
   230	    return {Identity(ordinal, expectedExperimentId), lifecycle, std::nullopt,
   231	        std::nullopt};
   232	}
   233	
   234	Assessment Assess(
   235	    std::vector<MemberEvidence> members,
   236	    std::string observedAt = "2026-07-19T22:00:00Z")
   237	{
   238	    return BuildRecommendationCampaignOutcomeAssessment(Campaign(),
   239	        Materialization(static_cast<int>(members.size())), observedAt,
   240	        members);
   241	}
   242	
   243	const MetricComparison& ComparisonFor(
   244	    const Member& member,
   245	    const std::string& identity)
   246	{
   247	    const auto found = std::find_if(member.comparisons.begin(),
   248	        member.comparisons.end(), [&](const MetricComparison& comparison)
   249	    {
   250	        return comparison.metricIdentity == identity;
   251	    });
   252	    assert(found != member.comparisons.end());
   253	    return *found;
   254	}
   255	
   256	bool HasDiagnostic(const Member& member, Diagnostic diagnostic)
   257	{
   258	    return std::find(member.diagnostics.begin(), member.diagnostics.end(),
   259	               diagnostic) != member.diagnostics.end();
   260	}
   261	
   262	} // namespace
   263	
   264	int main()
   265	{
   266	    // Multiple metrics, including a future metric, are canonicalized by metric
   267	    // identity and compared independently.
   268	    const std::vector<Metric> sourceMetrics{
   269	        NumericMetric("leader_score", 0.80),
   270	        NumericMetric("future_calibration", 0.50),
   271	        NumericMetric("inference_accuracy", 0.70)};
   272	    const std::vector<Metric> resultMetrics{
   273	        NumericMetric("inference_accuracy", 0.75),
   274	        NumericMetric("leader_score", 0.78),
   275	        NumericMetric("future_calibration", 0.55)};
   276	    const auto multiMetric = Assess(
   277	        {Succeeded(1, sourceMetrics, resultMetrics)});
   278	    const auto& multiMember = multiMetric.members[0];
   279	    assert(multiMember.outcome == MemberOutcome::SucceededComparable);
   280	    assert(multiMember.consistency == Consistency::Consistent);
   281	    assert(multiMember.sourceEvidence->metrics.metrics.size() == 3);
   282	    assert(multiMember.resultEvidence->metrics.metrics.size() == 3);
   283	    assert(multiMember.comparisons.size() == 3);
   284	    assert(multiMember.sourceEvidence->metrics.metrics[0].identity ==
   285	        "future_calibration");
   286	    assert(multiMember.sourceEvidence->metrics.metrics[1].identity ==
   287	        "inference_accuracy");
   288	    assert(multiMember.sourceEvidence->metrics.metrics[2].identity ==
   289	        "leader_score");
   290	    assert(multiMember.comparisons[0].metricIdentity == "future_calibration");
   291	    assert(multiMember.comparisons[1].metricIdentity == "inference_accuracy");
   292	    assert(multiMember.comparisons[2].metricIdentity == "leader_score");
   293	    assert(ComparisonFor(multiMember, "inference_accuracy").delta &&
   294	        *ComparisonFor(multiMember, "inference_accuracy").delta > 0.0);
   295	    assert(ComparisonFor(multiMember, "leader_score").delta &&
   296	        *ComparisonFor(multiMember, "leader_score").delta < 0.0);
   297	    assert(ComparisonFor(multiMember, "future_calibration").delta &&
   298	        *ComparisonFor(multiMember, "future_calibration").delta > 0.0);
   299	
   300	    const auto reorderedMetrics = Assess({Succeeded(1,
   301	        {sourceMetrics[2], sourceMetrics[0], sourceMetrics[1]},
   302	        {resultMetrics[2], resultMetrics[1], resultMetrics[0]})});
   303	    assert(reorderedMetrics.members == multiMetric.members);
   304	    assert(reorderedMetrics.summary == multiMetric.summary);
   305	    assert(reorderedMetrics.identity == multiMetric.identity);
   306	
   307	    AssertInvalidArgument([&]
   308	    {
   309	        (void)Metrics({NumericMetric("leader_score", 0.1),
   310	            NumericMetric("leader_score", 0.2)});
   311	    });
   312	    AssertInvalidArgument([]
   313	    {
   314	        (void)ResultEvidence(901, 1001,
   315	            {ResultIdentity("analysis", 1),
   316	                ResultIdentity("analysis", 1)},
   317	            ComparableContext(), Metrics({}));
   318	    });
   319	
   320	    // Successful members distinguish comparable, changed-context, and metric
   321	    // gap outcomes without mixing those scientific results into lifecycle.
   322	    const auto comparable = Assess({Succeeded(1)});
   323	    assert(comparable.members[0].lifecycle == Lifecycle::Succeeded);
   324	    assert(comparable.members[0].outcome ==
   325	        MemberOutcome::SucceededComparable);
   326	    assert(comparable.summary.succeededComparableMemberCount == 1);
   327	    assert(comparable.summary.comparableMetricCount == 2);
   328	    assert(comparable.summary.positiveDeltaCount == 1);
   329	    assert(comparable.summary.negativeDeltaCount == 1);
   330	
   331	    const auto changedContext = Assess({Succeeded(1, BaseSourceMetrics(),
   332	        BaseResultMetrics(), ComparableContext(), ComparableContext("gbpusd"))});
   333	    assert(changedContext.members[0].outcome ==
   334	        MemberOutcome::SucceededContextChanged);
   335	    assert(changedContext.members[0].lifecycle == Lifecycle::Succeeded);
   336	    assert(changedContext.members[0].consistency == Consistency::Consistent);
   337	    for (const auto& comparison : changedContext.members[0].comparisons)
   338	    {
   339	        assert(comparison.classification == Classification::ContextChanged);
   340	        assert(comparison.contextDifferences ==
   341	            std::vector<Difference>{Difference::Symbol});
   342	        assert(!comparison.delta);
   343	    }
   344	
   345	    const auto allContextDifferences = Assess({Succeeded(1,
   346	        BaseSourceMetrics(), BaseResultMetrics(), ComparableContext(),
   347	        ComparableContext("gbpusd", 24, 0.001, 192,
   348	            "binary_forward_return_v2", "2026-02-01T00:00:00Z",
   349	            "2026-04-30T23:59:59Z"))});
   350	    assert(allContextDifferences.members[0].comparisons[0].contextDifferences ==
   351	        std::vector<Difference>({Difference::Symbol,
   352	            Difference::PredictionHorizon, Difference::Threshold,
   353	            Difference::WindowSize, Difference::LabelDefinition,
   354	            Difference::InferenceRange}));
   355	
   356	    const auto metricGap = Assess({Succeeded(1,
   357	        {NumericMetric("inference_accuracy", 0.70),
   358	            NumericMetric("leader_score", 0.80)},
   359	        {NumericMetric("future_calibration", 0.55),
   360	            NumericMetric("inference_accuracy", 0.75)})});
   361	    assert(metricGap.members[0].outcome == MemberOutcome::SucceededMetricGap);
   362	    assert(ComparisonFor(metricGap.members[0], "future_calibration")
   363	               .classification == Classification::MissingSourceMetric);
   364	    assert(ComparisonFor(metricGap.members[0], "leader_score")
   365	               .classification == Classification::MissingResultMetric);
   366	    assert(metricGap.summary.succeededMetricGapMemberCount == 1);
   367	    assert(metricGap.summary.metricComparisonCount == 3);
   368	    assert(metricGap.summary.comparableMetricCount == 1);
   369	    assert(metricGap.summary.missingSourceMetricCount == 1);
   370	    assert(metricGap.summary.missingResultMetricCount == 1);
   371	
   372	    const auto missingValue = Assess({Succeeded(1,
   373	        {NumericMetric("inference_accuracy", std::nullopt)},
   374	        {NumericMetric("inference_accuracy", 0.75)})});
   375	    assert(missingValue.members[0].outcome ==
   376	        MemberOutcome::SucceededMetricGap);
   377	    assert(missingValue.members[0].comparisons[0].classification ==
   378	        Classification::MetricValueUnavailable);
   379	    const auto unsupported = Assess({Succeeded(1,
   380	        {UnsupportedMetric("inference_accuracy", 0.70)},
   381	        {NumericMetric("inference_accuracy", 0.75)})});
   382	    assert(unsupported.members[0].comparisons[0].classification ==
   383	        Classification::Unsupported);
   384	    const auto supportedSingleMetric = Assess({Succeeded(1,
   385	        {NumericMetric("inference_accuracy", 0.70)},
   386	        {NumericMetric("inference_accuracy", 0.75)})});
   387	    assert(supportedSingleMetric.members[0].comparisons[0].classification ==
   388	        Classification::Comparable);
   389	    assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
   390	
   391	    // Lifecycle states do not require result payload. Proposal-only is simply
   392	    // not ready, never a missing-result scientific classification.
   393	    const auto failed = Assess(
   394	        {LifecycleOnly(1, Lifecycle::Failed, ResultExperiment(1))});
   395	    assert(failed.members[0].outcome == MemberOutcome::TerminalFailed);
   396	    assert(failed.members[0].consistency == Consistency::Consistent);
   397	    assert(!failed.members[0].resultEvidence);
   398	    assert(failed.members[0].comparisons.empty());
   399	
   400	    const auto cancelled = Assess(
   401	        {LifecycleOnly(1, Lifecycle::Cancelled, ResultExperiment(1))});
   402	    assert(cancelled.members[0].outcome == MemberOutcome::TerminalCancelled);
   403	    assert(!cancelled.members[0].resultEvidence);
   404	
   405	    const auto nonterminal = Assess(
   406	        {LifecycleOnly(1, Lifecycle::NotTerminal, ResultExperiment(1))});
   407	    assert(nonterminal.members[0].outcome == MemberOutcome::NotReady);
   408	    assert(nonterminal.members[0].diagnostics.empty());
   409	    assert(!nonterminal.members[0].resultEvidence);
   410	
   411	    const auto unknown = Assess({LifecycleOnly(1, Lifecycle::Unknown)});
   412	    assert(unknown.members[0].lifecycle == Lifecycle::Unknown);
   413	    assert(unknown.members[0].outcome == MemberOutcome::NotReady);
   414	
   415	    const auto proposalOnly = Assess(
   416	        {LifecycleOnly(1, Lifecycle::NotTerminal)});
   417	    assert(!proposalOnly.members[0].identity.expectedExperimentId);
   418	    assert(!proposalOnly.members[0].sourceEvidence);
   419	    assert(!proposalOnly.members[0].resultEvidence);
   420	    assert(proposalOnly.members[0].outcome == MemberOutcome::NotReady);
   421	    assert(proposalOnly.members[0].diagnostics.empty());
   422	    assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
   423	        std::string::npos);
   424	
   425	    AssertInvalidArgument([]
   426	    {
   427	        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
   428	            Lifecycle::Failed, std::nullopt,
   429	            Result(1, BaseResultMetrics()));
   430	    });
   431	
   432	    // Malformed downstream evidence is retained when member identity remains
   433	    // trustworthy, with fixed-order diagnostics and no scientific comparison.
   434	    const MemberEvidence incompleteResult(
   435	        Identity(1, std::nullopt), Lifecycle::Succeeded,
   436	        Source(1, {}, ComparableContext(), 999),
   437	        ResultEvidence(901, std::nullopt, {}, std::nullopt, Metrics({})));
   438	    const auto inconsistent = Assess({incompleteResult});
   439	    assert(inconsistent.members[0].lifecycle == Lifecycle::Succeeded);
   440	    assert(inconsistent.members[0].consistency == Consistency::Inconsistent);
   441	    assert(inconsistent.members[0].outcome == MemberOutcome::Inconsistent);
   442	    assert(inconsistent.members[0].resultEvidence);
   443	    assert(inconsistent.members[0].comparisons.empty());
   444	    assert(inconsistent.members[0].diagnostics ==
   445	        std::vector<Diagnostic>({
   446	            Diagnostic::SourceExperimentIdentityMismatch,
   447	            Diagnostic::ExpectedExperimentIdentityMissing,
   448	            Diagnostic::ResultModelIdentityMissing,
   449	            Diagnostic::ResultIdentityMissing,
   450	            Diagnostic::ResultContextMissing}));
   451	    assert(inconsistent.summary.inconsistentMemberCount == 1);
   452	    assert(inconsistent.summary.metricComparisonCount == 0);
   453	
   454	    const MemberEvidence noResult(Identity(1, ResultExperiment(1)),
   455	        Lifecycle::Succeeded, Source(1, BaseSourceMetrics()), std::nullopt);
   456	    const auto noResultAssessment = Assess({noResult});
   457	    assert(noResultAssessment.members[0].diagnostics ==
   458	        std::vector<Diagnostic>{Diagnostic::ResultEvidenceMissing});
   459	    assert(noResultAssessment.members[0].outcome ==
   460	        MemberOutcome::Inconsistent);
   461	
   462	    const MemberEvidence missingResultExperiment(
   463	        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
   464	        Source(1, BaseSourceMetrics()),
   465	        ResultEvidence(std::nullopt, 1001,
   466	            {ResultIdentity("scientific_result", ResultRecord(1))},
   467	            ComparableContext(), Metrics(BaseResultMetrics())));
   468	    const auto missingExperiment = Assess({missingResultExperiment});
   469	    assert(missingExperiment.members[0].diagnostics ==
   470	        std::vector<Diagnostic>{
   471	            Diagnostic::ResultExperimentIdentityMissing});
   472	
   473	    const MemberEvidence provenanceMismatch(
   474	        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
   475	        Source(1, BaseSourceMetrics()),
   476	        Result(1, BaseResultMetrics(), ComparableContext(), 999));
   477	    const auto malformedProvenance = Assess({provenanceMismatch});
   478	    assert(malformedProvenance.members[0].diagnostics ==
   479	        std::vector<Diagnostic>{
   480	            Diagnostic::ResultExperimentIdentityMismatch});
   481	    assert(malformedProvenance.members[0].resultEvidence->experimentId == 999);
   482	    assert(malformedProvenance.members[0].consistency ==
   483	        Consistency::Inconsistent);
   484	
   485	    const MemberEvidence missingModel(
   486	        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
   487	        Source(1, BaseSourceMetrics()),
   488	        ResultEvidence(ResultExperiment(1), std::nullopt,
   489	            {ResultIdentity("scientific_result", ResultRecord(1))},
   490	            ComparableContext(), Metrics(BaseResultMetrics())));
   491	    const auto missingModelAssessment = Assess({missingModel});
   492	    assert(missingModelAssessment.members[0].diagnostics ==
   493	        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityMissing});
   494	    assert(missingModelAssessment.members[0].consistency ==
   495	        Consistency::Inconsistent);
   496	    assert(missingModelAssessment.identity.hash !=
   497	        malformedProvenance.identity.hash);
   498	
   499	    // Cross-member experiment reuse marks every affected member, and the
   500	    // mismatched member additionally retains its exact mismatch diagnostic.
   500	    // mismatched member additionally retains its exact mismatch diagnostic.
   501	    const MemberEvidence reusedExperimentOne(
   502	        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
   503	        Source(1, BaseSourceMetrics()), Result(1, BaseResultMetrics()));
   504	    const MemberEvidence reusedExperimentTwo(
   505	        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
   506	        Source(2, BaseSourceMetrics()), Result(2, BaseResultMetrics(),
   507	            ComparableContext(), ResultExperiment(1)));
   508	    const auto experimentReuse =
   509	        Assess({reusedExperimentTwo, reusedExperimentOne});
   510	    assert(HasDiagnostic(experimentReuse.members[0],
   511	        Diagnostic::ResultExperimentReused));
   512	    assert(HasDiagnostic(experimentReuse.members[1],
   513	        Diagnostic::ResultExperimentReused));
   514	    assert(!HasDiagnostic(experimentReuse.members[0],
   515	        Diagnostic::ResultExperimentIdentityMismatch));
   516	    assert(HasDiagnostic(experimentReuse.members[1],
   517	        Diagnostic::ResultExperimentIdentityMismatch));
   518	    assert(experimentReuse.members[0].outcome == MemberOutcome::Inconsistent);
   519	    assert(experimentReuse.members[1].outcome == MemberOutcome::Inconsistent);
   520	
   521	    const MemberEvidence reusedResultOne(
   522	        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
   523	        Source(1, BaseSourceMetrics()), Result(1, BaseResultMetrics(),
   524	            ComparableContext(), std::nullopt, 8888));
   525	    const MemberEvidence reusedResultTwo(
   526	        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
   527	        Source(2, BaseSourceMetrics()), Result(2, BaseResultMetrics(),
   528	            ComparableContext(), std::nullopt, 8888));
   529	    const auto resultReuse = Assess({reusedResultTwo, reusedResultOne});
   530	    assert(resultReuse.members[0].diagnostics ==
   531	        std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
   532	    assert(resultReuse.members[1].diagnostics ==
   533	        std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
   534	
   535	    // Aggregate precedence is deterministic: inconsistency, failure,
   536	    // cancellation, not-ready, then successful context/gap/comparable states.
   537	    const auto allPrecedence = Assess({Succeeded(5),
   538	        LifecycleOnly(2, Lifecycle::Failed, ResultExperiment(2)),
   539	        provenanceMismatch,
   540	        LifecycleOnly(4, Lifecycle::NotTerminal, ResultExperiment(4)),
   541	        LifecycleOnly(3, Lifecycle::Cancelled, ResultExperiment(3))});
   542	    assert(allPrecedence.summary.outcome == AggregateOutcome::Inconsistent);
   543	    assert(Assess({Succeeded(3),
   544	               LifecycleOnly(1, Lifecycle::Failed, ResultExperiment(1)),
   545	               LifecycleOnly(2, Lifecycle::Cancelled, ResultExperiment(2))})
   546	               .summary.outcome == AggregateOutcome::HasFailures);
   547	    assert(Assess({Succeeded(2),
   548	               LifecycleOnly(1, Lifecycle::Cancelled, ResultExperiment(1))})
   549	               .summary.outcome == AggregateOutcome::HasCancellations);
   550	    assert(Assess({Succeeded(2),
   551	               LifecycleOnly(1, Lifecycle::NotTerminal,
   552	                   ResultExperiment(1))})
   553	               .summary.outcome == AggregateOutcome::NotReady);
   554	    assert(Assess({Succeeded(1, BaseSourceMetrics(), BaseResultMetrics(),
   555	                       ComparableContext(), ComparableContext("gbpusd")),
   556	               Succeeded(2, {NumericMetric("a", 0.1)}, {})})
   557	               .summary.outcome ==
   558	        AggregateOutcome::SucceededContextChanged);
   559	    assert(Assess({Succeeded(1, {NumericMetric("a", 0.1)}, {}),
   560	               Succeeded(2)})
   561	               .summary.outcome == AggregateOutcome::SucceededMetricGap);
   562	    assert(comparable.summary.outcome ==
   563	        AggregateOutcome::SucceededComparable);
   564	
   565	    // Member outcomes and per-metric/delta counts remain separate.
   566	    assert(multiMetric.summary.memberCount == 1);
   567	    assert(multiMetric.summary.succeededLifecycleCount == 1);
   568	    assert(multiMetric.summary.succeededComparableMemberCount == 1);
   569	    assert(multiMetric.summary.metricComparisonCount == 3);
   570	    assert(multiMetric.summary.comparableMetricCount == 3);
   571	    assert(multiMetric.summary.positiveDeltaCount == 2);
   572	    assert(multiMetric.summary.negativeDeltaCount == 1);
   573	    assert(multiMetric.summary.zeroDeltaCount == 0);
   574	
   575	    const auto zeroDelta = Assess({Succeeded(1,
   576	        {NumericMetric("a", -0.0)}, {NumericMetric("a", 0.0)})});
   577	    assert(zeroDelta.members[0].comparisons[0].delta);
   578	    assert(*zeroDelta.members[0].comparisons[0].delta == 0.0);
   579	    assert(!std::signbit(*zeroDelta.members[0].comparisons[0].delta));
   580	    assert(zeroDelta.summary.zeroDeltaCount == 1);
   581	
   582	    // Every contract dimension is bound into identity. observed_at alone is
   583	    // intentionally excluded.
   584	    const auto laterObservation = Assess({Succeeded(1)},
   585	        "2026-07-19T22:05:00Z");
   586	    assert(laterObservation.observedAt != comparable.observedAt);
   587	    assert(laterObservation.identity == comparable.identity);
   588	
   589	    const auto lifecycleNotTerminal =
   590	        Assess({LifecycleOnly(1, Lifecycle::NotTerminal)});
   591	    const auto lifecycleUnknown =
   592	        Assess({LifecycleOnly(1, Lifecycle::Unknown)});
   593	    assert(lifecycleNotTerminal.members[0].outcome ==
   594	        lifecycleUnknown.members[0].outcome);
   595	    assert(lifecycleNotTerminal.identity.hash !=
   596	        lifecycleUnknown.identity.hash);
   597	
   598	    assert(comparable.identity.hash != malformedProvenance.identity.hash);
   599	    assert(malformedProvenance.identity.hash !=
   600	        noResultAssessment.identity.hash);
   601	    const auto changedMetricValue = Assess({Succeeded(1,
   602	        BaseSourceMetrics(),
   603	        {NumericMetric("inference_accuracy", 0.76),
   604	            NumericMetric("leader_score", 0.78)})});
   605	    assert(changedMetricValue.identity.hash != comparable.identity.hash);
   606	    assert(metricGap.identity.hash != comparable.identity.hash);
   607	    assert(changedContext.identity.hash != comparable.identity.hash);
   608	    assert(unsupported.identity.hash != supportedSingleMetric.identity.hash);
   609	
   610	    const MemberEvidence changedProvenance(
   611	        Identity(1, 1901), Lifecycle::Succeeded,
   612	        Source(1, BaseSourceMetrics()),
   613	        ResultEvidence(1901, 1001,
   614	            {ResultIdentity("scientific_result", 2901)},
   615	            ComparableContext(), Metrics(BaseResultMetrics())));
   616	    const auto changedProvenanceAssessment = Assess({changedProvenance});
   617	    assert(changedProvenanceAssessment.members[0].outcome ==
   618	        MemberOutcome::SucceededComparable);
   619	    assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
   620	    assert(comparable.identity.hash == RecommendationCanonicalHash(
   621	        comparable.identity.canonicalText));
   622	    assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
   623	        std::string::npos);
   624	    assert(malformedProvenance.identity.canonicalText.find(
   625	               "result_experiment_identity_mismatch") != std::string::npos);
   626	
   627	    // Member order, result identity order, diagnostics, metrics, comparisons,
   628	    // canonical text, and hash are deterministic.
   629	    const MemberEvidence twoResultIdentities(
   630	        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
   631	        Source(1, BaseSourceMetrics()),
   632	        ResultEvidence(ResultExperiment(1), 1001,
   633	            {ResultIdentity("z_future", 12),
   634	                ResultIdentity("analysis", 11)},
   635	            ComparableContext(), Metrics(BaseResultMetrics())));
   636	    const MemberEvidence twoResultIdentitiesReordered(
   637	        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
   638	        Source(1, {BaseSourceMetrics()[1], BaseSourceMetrics()[0]}),
   639	        ResultEvidence(ResultExperiment(1), 1001,
   640	            {ResultIdentity("analysis", 11),
   641	                ResultIdentity("z_future", 12)},
   642	            ComparableContext(),
   643	            Metrics({BaseResultMetrics()[1], BaseResultMetrics()[0]})));
   644	    const auto canonicalOne = Assess({twoResultIdentities});
   645	    const auto canonicalTwo = Assess({twoResultIdentitiesReordered});
   646	    assert(canonicalOne.members == canonicalTwo.members);
   647	    assert(canonicalOne.identity == canonicalTwo.identity);
   648	    assert(canonicalOne.members[0].resultEvidence->resultIdentities[0].kind ==
   649	        "analysis");
   650	    assert(canonicalOne.members[0].resultEvidence->resultIdentities[1].kind ==
   651	        "z_future");
   652	
   653	    const auto orderedMembers = Assess({Succeeded(1), Succeeded(2)});
   654	    const auto shuffledMembers = Assess({Succeeded(2), Succeeded(1)});
   655	    assert(orderedMembers.members == shuffledMembers.members);
   656	    assert(orderedMembers.summary == shuffledMembers.summary);
   657	    assert(orderedMembers.identity == shuffledMembers.identity);
   658	    assert(shuffledMembers.members[0].identity.memberOrdinal == 1);
   659	    assert(shuffledMembers.members[1].identity.memberOrdinal == 2);
   660	
   661	    // Invalid scalar input and untrustworthy top-level identity fail fast.
   662	    AssertInvalidArgument([]
   663	    {
   664	        (void)Metric("inference_accuracy",
   665	            std::numeric_limits<double>::quiet_NaN(),
   666	            MetricSupport::NumericDelta);
   667	    });
   668	    AssertInvalidArgument([]
   669	    {
   670	        (void)ComparableContext("eurusd", 12,
   671	            std::numeric_limits<double>::infinity());
   672	    });
   673	    AssertInvalidArgument([]
   674	    {
   675	        (void)ResultIdentity("analysis", 0);
   676	    });
   677	    AssertInvalidArgument([]
   678	    {
   679	        (void)Identity(1, 0);
   680	    });
   681	    AssertInvalidArgument([]
   682	    {
   683	        (void)Metric(std::string("bad\0identity", 12), 0.1,
   684	            MetricSupport::NumericDelta);
   685	    });
   686	    AssertInvalidArgument([]
   687	    {
   688	        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
   689	            Materialization(2), "2026-07-19T22:00:00Z",
   690	            {Succeeded(1), Succeeded(1)});
   691	    });
   692	    AssertInvalidArgument([]
   693	    {
   694	        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
   695	            std::nullopt, std::nullopt);
   696	        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
   697	            std::nullopt, std::nullopt);
   698	        (void)Assess({first, second});
   699	    });
   700	    AssertInvalidArgument([]
   701	    {
   702	        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
   703	            Materialization(1), "", {Succeeded(1)});
   704	    });
   705	
   706	    const MemberEvidence overflowingDelta(
   707	        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
   708	        Source(1, {NumericMetric("wide",-
   709	            std::numeric_limits<double>::max())}),
   710	        Result(1, {NumericMetric("wide",
   711	            std::numeric_limits<double>::max())}));
   712	    const auto overflowAssessment = Assess({overflowingDelta});
   713	    assert(overflowAssessment.members[0].diagnostics ==
   714	        std::vector<Diagnostic>{Diagnostic::NumericDeltaNonFinite});
   715	    assert(overflowAssessment.members[0].comparisons.empty());
   716	    assert(overflowAssessment.members[0].outcome == MemberOutcome::Inconsistent);
   717	
   718	    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
   719	               Lifecycle::NotTerminal) == "not_terminal");
   720	    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
   721	               Lifecycle::Succeeded) == "succeeded");
   722	    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
   723	               Lifecycle::Failed) == "failed");
   724	    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
   725	               Lifecycle::Cancelled) == "cancelled");
   726	    assert(RecommendationCampaignOutcomeAssessmentLifecycleStateText(
   727	               Lifecycle::Unknown) == "unknown");
   728	    assert(RecommendationCampaignOutcomeAssessmentConsistencyStateText(
   729	               Consistency::Consistent) == "consistent");
   730	    assert(RecommendationCampaignOutcomeAssessmentConsistencyStateText(
   731	               Consistency::Inconsistent) == "inconsistent");
   732	    assert(RecommendationCampaignOutcomeAssessmentMemberOutcomeText(
   733	               MemberOutcome::SucceededMetricGap) == "succeeded_metric_gap");
   734	    assert(RecommendationCampaignOutcomeAssessmentAggregateOutcomeText(
   735	               AggregateOutcome::HasFailures) == "has_failures");
   736	
   737	    return 0;
   738	}

exec
/bin/bash -lc "rg -n \"set<long long>.*recommend|recommendationIds|sourceExperimentIds|proposalIds|rankingMemberIds|duplicate\" Sources/ExperimentRecommendationCampaignPlanning.cpp Sources/ExperimentRecommendationRanking.cpp Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp Sources/ExperimentRecommendationCampaignHandoff* Sources/ExperimentRecommendationCampaignStatusRepository.cpp | sed -n '1,320p' && rg -n \"sourceExperimentId|recommendationId\" Sources/ExperimentRecommendationRanking.cpp | sed -n '1,260p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:47:        throw std::runtime_error("campaign_status_duplicate_experiment_link");
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:65:       e.duplicate_nonce,e.c_next_threshold,e.core_lr_mult,e.head_lr_mult,
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:171:        value.duplicateNonce = row["duplicate_nonce"].as<long long>();
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:210:            throw std::runtime_error("campaign_status_duplicate_experiment_row");
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:228:    std::vector<long long> proposalIds;
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:229:    proposalIds.reserve(materialization.members.size());
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:231:        proposalIds.push_back(member.conversionProposalId);
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:233:        transaction, proposalIds);
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:235:        transaction, proposalIds);
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:245:            throw std::runtime_error("campaign_status_duplicate_workflow");
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:385:       ('experiment','duplicate_nonce'),('experiment','infer_start'),
Sources/ExperimentRecommendationRanking.cpp:426:                "duplicate_recommendation_ranking_evaluation_result");
Sources/ExperimentRecommendationRanking.cpp:487:                "duplicate_recommendation_ranking_evaluation_result");
Sources/ExperimentRecommendationCampaignHandoffRepository.cpp:26:    std::vector<long long> proposalIds;
Sources/ExperimentRecommendationCampaignHandoffRepository.cpp:27:    proposalIds.reserve(materialization.members.size());
Sources/ExperimentRecommendationCampaignHandoffRepository.cpp:29:        proposalIds.push_back(member.conversionProposalId);
Sources/ExperimentRecommendationCampaignHandoffRepository.cpp:31:        transaction, proposalIds);
Sources/ExperimentRecommendationCampaignHandoffRepository.cpp:37:                "recommendation_campaign_handoff_duplicate_proposal_projection");
Sources/ExperimentRecommendationCampaignPlanning.cpp:716:        case RecommendationCampaignReason::duplicateConversionIdentity: return "duplicate_conversion_identity";
Sources/ExperimentRecommendationCampaignPlanning.cpp:875:                          duplicateConversionIdentity);
193:        value.recommendationId <= 0 || value.recommendationScanId <= 0 ||
194:        value.sourceExperimentId <= 0 || value.horizon <= 0 ||

exec
/bin/bash -lc "sed -n '150,240p' Sources/ExperimentRecommendationRanking.cpp && sed -n '600,740p' Sources/ExperimentRecommendationCampaignPlanning.cpp && sed -n '200,280p' Sources/ExperimentRecommendationCampaignStatusRepository.cpp && rg -n \"struct RecommendationCampaignMaterialization|conversionProposalId|materializationMemberId\" Sources/ExperimentRecommendationCampaignMaterializationRepository.* Sources/ExperimentRecommendationCampaignHandoff.hpp" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
        case RecommendationRankingBucket::blocked: return 1;
        case RecommendationRankingBucket::nonActionable: return 2;
    }
    throw std::invalid_argument("invalid_recommendation_ranking_bucket");
}

bool ValidScopeText(const std::string& value)
{
    if (value.empty() || value.size() > 128) return false;
    const auto asciiWhitespace = [](unsigned char byte) {
        return byte == ' ' || byte == '\t' || byte == '\n' || byte == '\r' ||
               byte == '\f' || byte == '\v';
    };
    if (asciiWhitespace(static_cast<unsigned char>(value.front())) ||
        asciiWhitespace(static_cast<unsigned char>(value.back())))
        return false;
    return std::none_of(value.begin(), value.end(), [](unsigned char byte) {
        return byte < 0x20 || byte == 0x7f;
    });
}

std::optional<std::string> TopComponent(
    const std::vector<RecommendationScoreComponent>& components,
    bool penalty)
{
    const RecommendationScoreComponent* best = nullptr;
    for (const auto& component : components)
    {
        if (component.penalty != penalty) continue;
        if (!std::isfinite(component.weightedContribution))
            throw std::invalid_argument("invalid_recommendation_ranking_component");
        if (best == nullptr ||
            component.weightedContribution > best->weightedContribution ||
            (component.weightedContribution == best->weightedContribution &&
             component.componentName < best->componentName))
            best = &component;
    }
    return best ? std::optional<std::string>{best->componentName} : std::nullopt;
}

void ValidateEvaluation(const RecommendationRankingEvaluation& value)
{
    if (value.evaluationResultId <= 0 || value.evaluationRunId <= 0 ||
        value.recommendationId <= 0 || value.recommendationScanId <= 0 ||
        value.sourceExperimentId <= 0 || value.horizon <= 0 ||
        value.symbol.empty() || value.family.empty() ||
        value.recommendationSemanticHash.empty() ||
        value.evaluationIdentityCanonical.empty() ||
        value.evaluationIdentityHash.empty() ||
        value.evaluationPolicyCanonical.empty() ||
        value.evaluationPolicyHash.empty() || value.evaluationVersion <= 0 ||
        value.evaluatorVersion <= 0 || value.scoringPolicyCanonical.empty() ||
        value.scoringPolicyHash.empty() || value.scoringVersion <= 0 ||
        value.reasonCode.empty() || value.explanation.empty() ||
        value.componentCount < 0 || value.missingEvidenceCount < 0 ||
        value.componentCount != static_cast<int>(value.components.size()))
        throw std::invalid_argument("invalid_recommendation_ranking_evidence");
    const bool ready = value.disposition ==
        RecommendationEvaluationDisposition::advisoryReady;
    if (ready != value.finalScore.has_value() ||
        ready != (value.eligibility == RecommendationEligibility::eligible) ||
        ready != !value.components.empty())
        throw std::invalid_argument("invalid_recommendation_ranking_result_shape");
    if (value.finalScore && (!std::isfinite(*value.finalScore) ||
        *value.finalScore < 0.0 || *value.finalScore > 1.0))
        throw std::invalid_argument("nonfinite_recommendation_ranking_score");
    if (value.components.size() >
        static_cast<std::size_t>(kMaximumRecommendationComparisonComponents))
        throw std::invalid_argument("recommendation_ranking_component_limit_exceeded");
    std::set<std::string> componentNames;
    for (const auto& component : value.components)
    {
        if (component.componentName.empty() ||
            !componentNames.insert(component.componentName).second ||
            !std::isfinite(component.normalizedValue) ||
            !std::isfinite(component.weight) ||
            !std::isfinite(component.weightedContribution) ||
            component.normalizedValue < 0.0 ||
            component.normalizedValue > 1.0 || component.weight < 0.0 ||
            component.weightedContribution < 0.0)
            throw std::invalid_argument("invalid_recommendation_ranking_component");
    }
}

std::string ScoreTieBreak(const RecommendationRankingEvaluation& value)
{
    if (!value.finalScore)
        return "priority:" + std::to_string(DispositionPriority(value.disposition));
    return "score:" + CanonicalRecommendationDouble(*value.finalScore);
}

    policy.maximumPerSymbol = ParseOptionalInt(reader.Token());
    reader.Expect(";maximum_per_horizon=");
    policy.maximumPerHorizon = ParseOptionalInt(reader.Token());
    reader.Expect(";maximum_per_source=");
    policy.maximumPerSourceExperiment = ParseOptionalInt(reader.Token());
    reader.Expect(";reconsider_rejected=");
    policy.reconsiderRejectedWorkflows = ParseBool(reader.Token());
    reader.Expect(";reconsider_failed=");
    policy.reconsiderFailedWorkflows = ParseBool(reader.Token());
    reader.Expect(";reconsider_cancelled=");
    policy.reconsiderCancelledWorkflows = ParseBool(reader.Token());
    reader.Expect(";completed_excludes=");
    policy.completedWorkflowsExcludeSelection = ParseBool(reader.Token());
    reader.Expect(";inconsistent_excludes=");
    policy.inconsistentWorkflowsAlwaysExclude = ParseBool(reader.Token());
    reader.Expect(";tie_breaking=");
    policy.tieBreaking = reader.Framed();
    if (!reader.AtEnd() ||
        RecommendationCampaignPlanningPolicyCanonicalText(policy) != canonical)
        throw std::invalid_argument(
            "recommendation_campaign_policy_canonical_invalid");
    return policy;
}

std::optional<std::string> ValidateRecommendationCampaignPlanningScope(
    const RecommendationCampaignPlanningScope& scope)
{
    if (scope.rankingSnapshotId <= 0)
        return "recommendation_campaign_ranking_snapshot_id_invalid";
    if (scope.symbol)
    {
        const auto canonical = EA::CanonicalSymbol::TryNormalize(*scope.symbol);
        if (scope.symbol->size() > 64 || !canonical ||
            *canonical != *scope.symbol)
            return "recommendation_campaign_symbol_invalid";
    }
    if (scope.horizon && *scope.horizon <= 0)
        return "recommendation_campaign_horizon_invalid";
    if (scope.recommendationId && *scope.recommendationId <= 0)
        return "recommendation_campaign_recommendation_id_invalid";
    return std::nullopt;
}

std::string RecommendationCampaignPlanningScopeCanonicalText(
    const RecommendationCampaignPlanningScope& scope)
{
    if (const auto error = ValidateRecommendationCampaignPlanningScope(scope))
        throw std::invalid_argument(*error);
    std::ostringstream out;
    out.imbue(std::locale::classic());
    out << "experiment_recommendation_campaign_scope_v1"
        << ";ranking_snapshot_id=" << scope.rankingSnapshotId
        << ";symbol=" << OptionalFramedText(scope.symbol)
        << ";horizon=" << OptionalIntText(scope.horizon)
        << ";recommendation_id="
        << OptionalLongText(scope.recommendationId);
    return out.str();
}

RecommendationCampaignPlanningScope
ParseRecommendationCampaignPlanningScopeCanonicalText(
    const std::string& canonical)
{
    CanonicalReader reader{canonical};
    RecommendationCampaignPlanningScope scope;
    reader.Expect("experiment_recommendation_campaign_scope_v1;");
    reader.Expect("ranking_snapshot_id=");
    scope.rankingSnapshotId = ParseLong(reader.Token());
    reader.Expect(";symbol=");
    scope.symbol = reader.OptionalFramed();
    reader.Expect(";horizon=");
    scope.horizon = ParseOptionalInt(reader.Token());
    reader.Expect(";recommendation_id=");
    scope.recommendationId = ParseOptionalLong(reader.Token());
    if (!reader.AtEnd() ||
        RecommendationCampaignPlanningScopeCanonicalText(scope) != canonical)
        throw std::invalid_argument(
            "recommendation_campaign_scope_canonical_invalid");
    return scope;
}

std::string RecommendationCampaignDecisionText(
    RecommendationCampaignDecision decision)
{
    switch (decision)
    {
        case RecommendationCampaignDecision::include: return "include";
        case RecommendationCampaignDecision::exclude: return "exclude";
    }
    throw std::invalid_argument("recommendation_campaign_decision_invalid");
}

std::string RecommendationCampaignReasonText(RecommendationCampaignReason reason)
{
    switch (reason)
    {
        case RecommendationCampaignReason::selected: return "selected";
        case RecommendationCampaignReason::planningDisabled: return "planning_disabled";
        case RecommendationCampaignReason::recommendationNotRanked: return "recommendation_not_ranked";
        case RecommendationCampaignReason::rankingNotAdvisoryReady: return "ranking_not_advisory_ready";
        case RecommendationCampaignReason::belowMinimumLeaderScore: return "below_minimum_leader_score";
        case RecommendationCampaignReason::belowMinimumInferenceAccuracy: return "below_minimum_inference_accuracy";
        case RecommendationCampaignReason::predictedNeutralMetricUnavailable: return "predicted_neutral_metric_unavailable";
        case RecommendationCampaignReason::predictedNeutralAboveMaximum: return "predicted_neutral_above_maximum";
        case RecommendationCampaignReason::profitabilityMetricUnavailable: return "profitability_metric_unavailable";
        case RecommendationCampaignReason::belowMinimumProfitability: return "below_minimum_profitability";
        case RecommendationCampaignReason::proposalAlreadyExists: return "proposal_already_exists";
        case RecommendationCampaignReason::workflowPendingReview: return "workflow_pending_review";
        case RecommendationCampaignReason::workflowRejectedNotReconsiderable: return "workflow_rejected_not_reconsiderable";
        case RecommendationCampaignReason::workflowApprovedNotExecuted: return "workflow_approved_not_executed";
        case RecommendationCampaignReason::workflowExecutedPaused: return "workflow_executed_paused";
        case RecommendationCampaignReason::workflowActivatedOrSchedulerOwned: return "workflow_activated_or_scheduler_owned";
        case RecommendationCampaignReason::workflowCompleted: return "workflow_completed";
        case RecommendationCampaignReason::workflowFailedNotReconsiderable: return "workflow_failed_not_reconsiderable";
        case RecommendationCampaignReason::workflowCancelledNotReconsiderable: return "workflow_cancelled_not_reconsiderable";
        case RecommendationCampaignReason::workflowInconsistent: return "workflow_inconsistent";
        case RecommendationCampaignReason::duplicateConversionIdentity: return "duplicate_conversion_identity";
        case RecommendationCampaignReason::sourceExperimentConflict: return "source_experiment_conflict";
        case RecommendationCampaignReason::symbolLimitReached: return "symbol_limit_reached";
        case RecommendationCampaignReason::horizonLimitReached: return "horizon_limit_reached";
        case RecommendationCampaignReason::sourceExperimentLimitReached: return "source_experiment_limit_reached";
        case RecommendationCampaignReason::campaignLimitReached: return "campaign_limit_reached";
        case RecommendationCampaignReason::candidateLimitReached: return "candidate_limit_reached";
        case RecommendationCampaignReason::unsupportedContractVersion: return "unsupported_contract_version";
        case RecommendationCampaignReason::identityValidationFailed: return "identity_validation_failed";
    }
    throw std::invalid_argument("recommendation_campaign_reason_invalid");
}

std::string RecommendationCampaignReasonExplanation(
    RecommendationCampaignReason reason)
{
    switch (reason)
    {
        case RecommendationCampaignReason::selected:
            return "The candidate is included in this read-only plan.";
        case RecommendationCampaignReason::planningDisabled:
            return "Campaign planning is disabled by policy.";
        case RecommendationCampaignReason::profitabilityMetricUnavailable:
            return "No authoritative profitability metric exists in the current schema.";
        case RecommendationCampaignReason::workflowInconsistent:
        value.completedInferenceResultCount =
            row["completed_inference_result_count"].as<int>();
        value.failedInferenceResultCount =
            row["failed_inference_result_count"].as<int>();
        value.analysisResultCount = row["analysis_result_count"].as<int>();
        value.completedAnalysisResultCount =
            row["completed_analysis_result_count"].as<int>();
        value.failedAnalysisResultCount =
            row["failed_analysis_result_count"].as<int>();
        if (!result.emplace(value.experimentId, std::move(value)).second)
            throw std::runtime_error("campaign_status_duplicate_experiment_row");
    }
    return result;
}

RecommendationCampaignStatusInput LoadInput(
    pqxx::transaction_base& transaction,
    const PersistedRecommendationCampaignMaterialization& materialization)
{
    RecommendationCampaignStatusInput input;
    input.materializationId = materialization.materializationId;
    input.materializationContractVersion = materialization.contractVersion;
    input.materializationIdentityCanonical = materialization.identityCanonical;
    input.materializationIdentityHash = materialization.identityHash;
    input.selectedMemberCount = materialization.selectedMemberCount;
    input.observedAt = transaction.exec(
        "SELECT transaction_timestamp()::text;").one_row()[0].as<std::string>();

    std::vector<long long> proposalIds;
    proposalIds.reserve(materialization.members.size());
    for (const auto& member : materialization.members)
        proposalIds.push_back(member.conversionProposalId);
    const auto workflows = ListRecommendationConversionWorkflowsForProposals(
        transaction, proposalIds);
    const auto proposals = ListRecommendationConversionProposalsByIds(
        transaction, proposalIds);
    std::map<long long, const PersistedRecommendationConversionProposal*> byProposalEvidence;
    for (const auto& proposal : proposals)
        byProposalEvidence.emplace(proposal.proposalId, &proposal);
    std::map<long long, const RecommendationConversionWorkflowView*> byProposal;
    std::vector<long long> experimentIds;
    std::map<long long, std::string> expectedInvocationByExperiment;
    for (const auto& workflow : workflows)
    {
        if (!byProposal.emplace(workflow.proposalId, &workflow).second)
            throw std::runtime_error("campaign_status_duplicate_workflow");
        if (workflow.execution)
        {
            experimentIds.push_back(workflow.execution->experimentId);
            const auto proposal = byProposalEvidence.find(workflow.proposalId);
            if (proposal != byProposalEvidence.end())
                expectedInvocationByExperiment.emplace(
                    workflow.execution->experimentId,
                    proposal->second->proposal.proposedInvocationCanonical);
        }
    }
    const auto experiments = LoadExperimentEvidence(
        transaction, experimentIds, expectedInvocationByExperiment);

    input.members.reserve(materialization.members.size());
    for (const auto& stored : materialization.members)
    {
        RecommendationCampaignStatusMemberInput member;
        member.memberOrdinal = stored.memberOrdinal;
        member.materializationMemberId = stored.materializationMemberId;
        member.recommendationId = stored.recommendationId;
        member.sourceExperimentId = stored.sourceExperimentId;
        member.rankingMemberId = stored.rankingMemberId;
        member.proposalId = stored.conversionProposalId;
        const auto found = byProposal.find(stored.conversionProposalId);
        if (found == byProposal.end())
        {
            member.workflowConsistent = false;
            member.workflowDiagnostics.push_back("campaign_status_proposal_missing");
            input.members.push_back(std::move(member));
            continue;
        }
        const auto& workflow = *found->second;
        const auto proposal = byProposalEvidence.find(stored.conversionProposalId);
        if (workflow.recommendationId != stored.recommendationId ||
            workflow.sourceExperimentId != stored.sourceExperimentId ||
Sources/ExperimentRecommendationCampaignHandoff.hpp:75:    long long conversionProposalId = -1;
Sources/ExperimentRecommendationCampaignHandoff.hpp:99:    long long conversionProposalId = -1;
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:80:    value.materializationMemberId = row[0].as<long long>();
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:88:    value.conversionProposalId = row[8].as<long long>();
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:94:        value.rankingPosition <= 0 || value.conversionProposalId <= 0 ||
Sources/ExperimentRecommendationCampaignMaterializationRepository.hpp:20:    long long materializationMemberId = -1;
Sources/ExperimentRecommendationCampaignMaterializationRepository.hpp:28:    long long conversionProposalId = -1;
Sources/ExperimentRecommendationCampaignMaterializationRepository.hpp:62:struct RecommendationCampaignMaterializationPersistResult

exec
/bin/bash -lc "rg -n \"Step 5b|5b|OutcomeAssessment|outcome assessment|campaign outcome\" docs Sources Tests -g '"'!ExperimentRecommendationCampaignOutcomeAssessment.*'"' -g '"'!ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp'"' | sed -n '1,360p' && git show --stat --oneline HEAD && git show --stat --oneline HEAD~1" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
8b9939e Phase 5 Step 4: Add recommendation campaign operational status
 Database/README.md                                 |   6 +
 ExpertAdvisor.xcodeproj/project.pbxproj            |  24 +
 Sources/ExperimentRecommendationCampaignStatus.cpp | 563 ++++++++++++++
 Sources/ExperimentRecommendationCampaignStatus.hpp | 259 +++++++
 ...imentRecommendationCampaignStatusRepository.cpp | 547 +++++++++++++
 ...imentRecommendationCampaignStatusRepository.hpp |  21 +
 ...perimentRecommendationCampaignStatusService.cpp | 264 +++++++
 ...perimentRecommendationCampaignStatusService.hpp |  17 +
 ...RecommendationConversionExecutionRepository.cpp |  13 +-
 ...RecommendationConversionExecutionRepository.hpp |   6 +
 ...xperimentRecommendationConversionRepository.cpp |  39 +
 ...xperimentRecommendationConversionRepository.hpp |   8 +
 ...tRecommendationConversionWorkflowRepository.cpp |   2 +
 ...tRecommendationConversionWorkflowRepository.hpp |   2 +
 Sources/ExperimentScheduler.cpp                    |  56 ++
 ...RecommendationCampaignStatusRepositoryTests.cpp | 858 +++++++++++++++++++++
 ...ExperimentRecommendationCampaignStatusTests.cpp | 277 +++++++
 ...se4DExperimentRecommendationCampaignHandoff.rst |   5 +-
 ...5ExperimentRecommendationCampaignActivation.rst |   2 +
 ...e5ExperimentRecommendationCampaignExecution.rst |   2 +
 ...hase5ExperimentRecommendationCampaignLaunch.rst |   4 +
 ...hase5ExperimentRecommendationCampaignStatus.rst | 107 +++
 .../Volume_VIII_Recommendation_Engine.md           |  16 +-
 23 files changed, 3091 insertions(+), 7 deletions(-)
05383ad Phase 5 Step 3: Add atomic campaign launch
 Database/README.md                                 |    8 +
 ExpertAdvisor.xcodeproj/project.pbxproj            |   24 +
 Sources/ExperimentRecommendationCampaignLaunch.cpp |  338 +++++
 Sources/ExperimentRecommendationCampaignLaunch.hpp |  120 ++
 ...imentRecommendationCampaignLaunchRepository.cpp |  162 +++
 ...imentRecommendationCampaignLaunchRepository.hpp |   45 +
 ...perimentRecommendationCampaignLaunchService.cpp |  329 +++++
 ...perimentRecommendationCampaignLaunchService.hpp |   17 +
 Sources/ExperimentScheduler.cpp                    |   63 +
 ...RecommendationCampaignLaunchRepositoryTests.cpp | 1412 ++++++++++++++++++++
 ...ExperimentRecommendationCampaignLaunchTests.cpp |  255 ++++
 ...se4DExperimentRecommendationCampaignHandoff.rst |    5 +-
 ...erimentRecommendationCampaignProposalReview.rst |    6 +-
 ...5ExperimentRecommendationCampaignActivation.rst |    4 +
 ...e5ExperimentRecommendationCampaignExecution.rst |    4 +
 ...hase5ExperimentRecommendationCampaignLaunch.rst |  136 ++
 .../Volume_VIII_Recommendation_Engine.md           |   34 +-
 17 files changed, 2953 insertions(+), 9 deletions(-)

exec
/bin/bash -lc "ls -la && find . -maxdepth 3 -type f \\( -name '*.sh' -o -name 'Makefile' -o -name 'CMakeLists.txt' -o -name '*.xcscheme' \\) -print | sort && rg -n \"std=c\\+\\+|CLANG_CXX_LANGUAGE_STANDARD|OTHER_CPLUSPLUSFLAGS|GCC_TREAT_WARNINGS_AS_ERRORS\" ExpertAdvisor.xcodeproj/project.pbxproj && rg -n \"RecommendationCanonicalHash|CanonicalRecommendationDouble\" Sources/ExperimentRecommendation.cpp Sources/ExperimentRecommendation.hpp" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
total 6931184
drwxr-xr-x  179 vjp  staff       5728 Jul 19 23:05 .
drwxr-xr-x@  22 vjp  staff        704 Jul 17 12:20 ..
-rw-r--r--@   1 vjp  staff      14340 Jun 12 21:36 .DS_Store
-rw-r--r--    1 vjp  staff     422131 Jul 19 22:26 .derived-data-log-0CA5RPJ1
drwxr-xr-x   19 vjp  staff        608 Jul 19 23:06 .git
-rw-r-----    1 vjp  staff         60 Jul  5 16:31 .gitattributes
-rw-r--r--    1 vjp  staff        433 Jul 19 17:39 .gitignore
-rw-r--r--    1 vjp  staff         79 Feb 18 21:05 .gitmodules
drwxr-xr-x    8 vjp  staff        256 Apr 10 20:24 .venv
drwxr-xr-x    8 vjp  staff        256 May  2 20:28 Build
drwxr-xr-x    5 vjp  staff        160 Jul  4 11:38 Common
drwxr-xr-x    6 vjp  staff        192 Jul 19 17:39 Database
drwxr-xr-x    8 vjp  staff        256 May  9 20:59 DerivedData
drwxr-xr-x@   6 vjp  staff        192 Jul 19 17:39 ExpertAdvisor.xcodeproj
-rw-r--r--    1 vjp  staff       8917 Jul  2 06:14 H6_GBPUSD_core120_epoch240_resume.txt
-rw-r--r--    1 vjp  staff      44002 Jul  3 11:20 H6_GBPUSD_core120_epoch240_resume_from398.txt
-rw-r--r--    1 vjp  staff  598829222 Jun 30 09:40 H6_USDJPY_core120_epoch120_ckpt20.txt
-rw-r--r--    1 vjp  staff       4898 Jun 25 23:40 H6_accuracy_acceptance_summary.txt
-rw-r--r--    1 vjp  staff  334733585 Jun 21 04:15 H6_core120_head50_bias5.txt
-rw-r--r--    1 vjp  staff  335384135 Jun 21 04:09 H6_core160_head25_bias2p5.txt
-rw-r--r--    1 vjp  staff  335693720 Jun 21 04:11 H6_core80_head25_bias2p5.txt
-rw-r--r--    1 vjp  staff       4898 Jun 25 23:40 H6_detailed_acceptance_summary.txt
-rw-r--r--    1 vjp  staff      19424 Jul  2 06:39 H6_finished_4_key_results.txt
-rw-r--r--    1 vjp  staff       8246 Jun 21 09:58 H6_infer_summary_compare.txt
-rw-r--r--    1 vjp  staff  362702610 Jun 25 23:40 H6_training_loss_raw.txt
-rw-r--r--    1 vjp  staff      23289 Jun 25 23:41 H6_training_loss_tail_summary.txt
-rw-r--r--    1 vjp  staff      65745 Jul  2 00:15 H8_AUDCAD_core120_epoch120_ckpt20.txt
-rw-r--r--    1 vjp  staff      44002 Jul  3 14:09 H8_AUDCAD_core120_epoch240_resume.txt
-rw-r--r--    1 vjp  staff      65422 Jul  1 23:52 H8_AUDUSD_core120_epoch120_ckpt20.txt
-rw-r--r--    1 vjp  staff      43719 Jul  3 14:09 H8_AUDUSD_core120_epoch240_resume.txt
-rw-r--r--    1 vjp  staff      65792 Jul  2 01:04 H8_GBPUSD_core120_epoch120_ckpt20.txt
-rw-r--r--    1 vjp  staff        983 Jul  3 21:33 H8_GBPUSD_core120_epoch240.txt
-rw-r--r--    1 vjp  staff        901 Jul  3 21:36 H8_GBPUSD_core120_epoch240_rerun.txt
-rw-r--r--    1 vjp  staff      44038 Jul  3 14:24 H8_GBPUSD_core120_epoch240_resume.txt
-rw-r--r--    1 vjp  staff      82073 Jul  2 01:00 H8_USDJPY_core120_epoch120_ckpt20.txt
-rw-r--r--    1 vjp  staff      60407 Jul  3 14:07 H8_USDJPY_core120_epoch240_resume.txt
-rw-r--r--    1 vjp  staff       4532 Jul  2 07:13 H8_finished_4_key_results.txt
drwxr-xr-x   18 vjp  staff        576 Jul  4 16:07 Headers
drwxr-xr-x    7 vjp  staff        224 Jul  7 05:20 LSTM
drwxr-xr-x   16 vjp  staff        512 May  2 20:28 MetaNN
-rw-------@   1 vjp  staff      16027 Jul 15 17:52 Phase4B_Step1_Advisory_Evaluation_Foundation_Prompt.txt
-rw-------@   1 vjp  staff      29373 Jul 15 18:49 Phase4B_Step2_Advisory_Ranking_Comparison_Prompt.txt
-rw-------@   1 vjp  staff      14092 Jul 15 21:04 Phase4B_Step2_Canonicalization_Comparison_Review_Prompt.txt
-rw-------@   1 vjp  staff      18836 Jul 18 18:20 Phase4B_Step2_Consolidated_Verification_and_Hardening_Prompt.txt
-rw-------@   1 vjp  staff      17080 Jul 15 20:18 Phase4B_Step2_Independent_Review_Prompt.txt
-rw-------@   1 vjp  staff       3044 Jul 15 20:38 Phase4B_Step2_Trigger_Review_Prompt.txt
-rw-------@   1 vjp  staff       3576 Jul 18 21:41 Phase4C_Step1_Manual_Conversion_Domain_Prompt.txt
-rw-------@   1 vjp  staff      14470 Jul 18 22:03 Phase4C_Step2_Durable_Conversion_Proposal_Persistence_Prompt.txt
-rw-------@   1 vjp  staff      11919 Jul 18 22:27 Phase4C_Step2_Focused_Persistence_Design_Review_Prompt.txt
-rw-------@   1 vjp  staff       2059 Jul 19 00:01 Phase4C_Step4_Prompt_and_Reasoning.txt
-rw-------@   1 vjp  staff      11335 Jul 19 17:21 Phase4D_Completion_Audit_Prompt_and_Reasoning.txt
-rw-------@   1 vjp  staff       1639 Jul 19 08:57 Phase4D_Step2_Prompt_and_Reasoning.txt
-rw-r--r--    1 vjp  staff          0 Jul 19 21:28 Phase5Step4PreStageRegressionOutput.txt
-rw-------@   1 vjp  staff       8482 Jul 19 21:07 Phase5Step4PreStageRegressionPrompt.txt
-rw-r--r--    1 vjp  staff    1270185 Jul 19 21:52 Phase5Step5ArchitecturePlanningOutput.txt
-rw-------@   1 vjp  staff       7522 Jul 19 21:34 Phase5Step5ArchitecturePlanningPrompt.txt
-rw-r--r--    1 vjp  staff    3229672 Jul 19 22:21 Phase5Step5aOutput.txt
-rw-r--r--    1 vjp  staff       3800 Jul 19 22:03 Phase5Step5aPrompt.txt
-rw-------@   1 vjp  staff       8123 Jul 19 22:39 Phase5Step5a_ContractCorrection_Input.txt
-rw-r--r--    1 vjp  staff    5571021 Jul 19 23:00 Phase5Step5a_ContractCorrection_Output.txt
-rw-------@   1 vjp  staff       1502 Jul 19 22:28 Phase5Step5a_IndependentArchitecturalReview_Input.txt
-rw-r--r--    1 vjp  staff     476748 Jul 19 22:34 Phase5Step5a_IndependentArchitecturalReview_Output.txt
-rw-------@   1 vjp  staff       2678 Jul 19 17:41 Phase5_Prompt_and_Reasoning.txt
drwxr-xr-x  123 vjp  staff       3936 Jul 19 22:51 Sources
-rw-r--r--    1 vjp  staff       2241 Jul 19 23:05 Step5a_IndependentReview_Input.txt
-rw-r--r--    1 vjp  staff     361534 Jul 19 23:06 Step5a_IndependentReview_Output.txt
drwxr-xr-x   58 vjp  staff       1856 Jul 19 22:54 Tests
-rw-r--r--    1 vjp  staff          0 Jul 18 11:48 after_restart.txt
-rw-r--r--    1 vjp  staff     329257 Jul 18 11:50 after_restart_0m.txt
-rw-r--r--    1 vjp  staff     329392 Jul 18 12:00 after_restart_10m.txt
-rw-r--r--    1 vjp  staff     329274 Jul 18 17:24 after_restart_later.txt
-rw-r--r--@   1 vjp  staff        413 May 28  2025 alter_index.plpgsql
-rw-r--r--    1 vjp  staff        663 Feb 11 04:16 alter_table.plpgsql
-rw-r--r--    1 vjp  staff     167583 Jun 30 06:05 analysis.txt
-rw-r--r--@   1 vjp  staff        937 Aug 13  2025 backtest_report.plpgsql
-rw-r--r--    1 vjp  staff  102609593 Jul 17 20:38 before.txt
-rw-r--r--    1 vjp  staff       4269 Feb 11 04:16 bollinger_band.plpgsql
-rw-r--r--@   1 vjp  staff       6021 Jun 29 00:00 candlestick.plpgsql
-rw-r--r--    1 vjp  staff        697 Feb 11 04:16 chande_momentum.plpgsql
-rw-r--r--    1 vjp  staff        388 Feb 11 04:16 check_indexes.plpgsql
-rw-r--r--    1 vjp  staff        658 Jun 28 12:26 check_tables.plpgsql
-rw-r--r--    1 vjp  staff  333981239 Jun 20 12:44 core_lr120_neutral1p0_threshold0008_horizon2_epoch60.txt
-rw-r--r--    1 vjp  staff  333758068 Jun 20 12:58 core_lr120_neutral1p0_threshold0008_horizon4_epoch60.txt
-rw-r--r--    1 vjp  staff  333701863 Jun 20 13:31 core_lr120_neutral1p0_threshold0008_horizon6_epoch60.txt
-rw-r--r--    1 vjp  staff  333576256 Jun 20 13:21 core_lr120_neutral1p0_threshold0008_horizon8_epoch60.txt
-rw-r--r--    1 vjp  staff        415 Feb 11 04:16 count_from.plpgsql
-rw-r--r--    1 vjp  staff       2298 Feb 11 04:16 create_bollinger_band_materialized_view.plpgsql
-rw-r--r--    1 vjp  staff       4253 Mar  6 20:31 create_candlestick_materialized_view.plpgsql
-rw-r--r--    1 vjp  staff        577 Mar  6 20:57 create_dtr_view.plpgsql
-rw-r--r--    1 vjp  staff      13238 Feb 11 04:16 create_ema_materialized_view.plpgsql
-rw-r--r--    1 vjp  staff       3150 Feb 11 04:16 create_fractal_materialized_view.plpgsql
-rw-r--r--    1 vjp  staff        469 Feb 13 16:36 create_index.plpgsql
-rw-r--r--    1 vjp  staff        469 Feb 13 16:36 create_index_a.plpgsql
-rw-r--r--    1 vjp  staff        469 Feb 13 16:36 create_index_c.plpgsql
-rw-r--r--    1 vjp  staff        469 Feb 13 16:36 create_index_e.plpgsql
-rw-r--r--    1 vjp  staff        468 Feb 13 16:36 create_index_g.plpgsql
-rw-r--r--    1 vjp  staff        469 Feb 13 16:36 create_index_n.plpgsql
-rw-r--r--    1 vjp  staff        469 Feb 13 16:36 create_index_u.plpgsql
-rw-r--r--    1 vjp  staff       4089 Feb 13 16:36 create_new_partitioned_table.plpgsql
-rw-r--r--    1 vjp  staff      54827 Feb 11 04:16 create_partitioned_tables.plpgsql
-rw-r--r--    1 vjp  staff       1863 Feb 11 04:16 create_results_table.plpgsql
-rw-r--r--    1 vjp  staff       5769 Feb 11 04:16 database_report.plpgsql
-rw-r--r--    1 vjp  staff      84087 Jul 18 20:38 debug_build.txt
-rw-r--r--    1 vjp  staff        365 Feb 11 04:16 delete_from.plpgsql
-rw-r--r--    1 vjp  staff     127604 Jul 19 17:03 diff.txt
drwxr-xr-x   26 vjp  staff        832 Jul 19 22:56 docs
-rw-r--r--    1 vjp  staff        764 Feb 11 04:16 donchian_channel.plpgsql
-rw-r--r--    1 vjp  staff        371 Feb 11 04:16 drop_bollinger_materialized_view.plpgsql
-rw-r--r--    1 vjp  staff        375 Mar  6 20:31 drop_candlestick_materialized_view.plpgsql
-rw-r--r--    1 vjp  staff        344 Feb 11 04:16 drop_dtr_view.plpgsql
-rw-r--r--    1 vjp  staff        367 Feb 11 04:16 drop_ema_materialized_view.plpgsql
-rw-r--r--    1 vjp  staff        371 Feb 11 04:16 drop_fractal_materialized_view.plpgsql
-rw-r--r--    1 vjp  staff        369 Feb 11 04:16 drop_index.plpgsql
-rw-r--r--    1 vjp  staff       1578 Feb 11 04:16 elder_ray.plpgsql
-rw-r--r--    1 vjp  staff       2005 Mar 20 20:33 ema.plpgsql
drwxr-xr-x  491 vjp  staff      15712 Jul 19 23:06 experiment_logs
drwxr-xr-x   12 vjp  staff        384 Jul  6 22:42 experiment_reports
-rwxr-xr-x    1 vjp  staff       1277 Jun 30 06:19 extract_lstm_analysis.sh
-rw-r--r--    1 vjp  staff       2510 Feb 11 04:16 fractal.plpgsql
-rw-r--r--    1 vjp  staff    7915665 Jun 20 14:55 h6_lr120_head25_bias2p5_epoch120.txt
-rw-r--r--    1 vjp  staff       7063 Jun 30 19:28 h6_results.csv
-rw-r--r--    1 vjp  staff        813 Feb 11 04:16 import_forex_data_a.plpgsql
-rw-r--r--    1 vjp  staff        813 Feb 11 04:16 import_forex_data_c.plpgsql
-rw-r--r--    1 vjp  staff        812 Feb 11 04:16 import_forex_data_e.plpgsql
-rw-r--r--    1 vjp  staff        812 Feb 11 04:16 import_forex_data_g.plpgsql
-rw-r--r--    1 vjp  staff        812 Feb 11 04:16 import_forex_data_n.plpgsql
-rw-r--r--    1 vjp  staff        812 Feb 11 04:16 import_forex_data_u.plpgsql
-rw-r--r--    1 vjp  staff       1978 Feb 13 16:36 importrmp.plpgsql
-rw-r--r--    1 vjp  staff     462279 Jun 30 02:08 infer_all_H6_AUDCAD_epoch020_to_latest.txt
-rw-r--r--    1 vjp  staff     461779 Jun 30 02:08 infer_all_H6_AUDUSD_epoch020_to_latest.txt
-rw-r--r--    1 vjp  staff     584031 Jun 30 10:59 infer_all_H6_EURUSD_epoch020_to_latest_H6_corrected.txt
-rw-r--r--    1 vjp  staff     461723 Jun 30 02:08 infer_all_H6_USDCAD_epoch020_to_latest.txt
-rw-r--r--    1 vjp  staff      51322 Jun 29 20:53 infer_eval_H6_AUDCAD_core120_epoch220.txt
-rw-r--r--    1 vjp  staff      51407 Jun 29 20:53 infer_eval_H6_AUDUSD_core120_epoch220.txt
-rw-r--r--    1 vjp  staff      51271 Jun 29 20:53 infer_eval_H6_EURUSD_core120_epoch220.txt
-rw-r--r--    1 vjp  staff      51251 Jun 29 20:53 infer_eval_H6_GBPUSD_core120_epoch220.txt
-rw-r--r--    1 vjp  staff      52035 Jun 29 20:53 infer_eval_H6_USDCAD_core120_epoch220.txt
-rw-r--r--    1 vjp  staff   31068005 Jun 20 18:02 input15m.txt
-rw-r--r--    1 vjp  staff    2047836 Jun 20 18:03 input4hr.txt
-rw-------@   1 vjp  staff      10881 Jul 18 21:36 investigate_child_exit_127_prompt_and_reasoning.txt
-rw-r--r--    1 vjp  staff   83124209 Jul 17 09:43 lstm_vmmap.txt
-rwxr-xr-x    1 vjp  staff        570 Jun 30 19:27 make_lstm_results_csv.sh
-rw-r--r--    1 vjp  staff        436 Jun 28 12:13 max_time.plpgsql
-rw-r--r--    1 vjp  staff      17439 Jul  4 16:17 meta_analysis_dashboard.md
-rw-r--r--    1 vjp  staff      11586 Jul  4 16:11 meta_analysis_report.md
-rwxr-xr-x    1 vjp  staff       2338 Jun 30 20:24 migrate_lstm_db.sh
-rw-------@   1 vjp  staff       9374 Jul 18 21:04 phase4b_post_commit_verification_prompt.txt
-rw-------@   1 vjp  staff      22036 Jul 18 23:40 phase4c_step3_code_review_prompt_and_reasoning.txt
-rw-------@   1 vjp  staff      16709 Jul 18 23:12 phase4c_step3_prompt_and_reasoning.txt
-rw-------@   1 vjp  staff      12556 Jul 19 06:39 phase4c_step5_prompt_and_reasoning.txt
-rw-------@   1 vjp  staff      13741 Jul 19 07:12 phase4c_step6_prompt_and_reasoning.txt
-rw-------@   1 vjp  staff      17700 Jul 19 07:41 phase4d_step1_prompt_and_reasoning.txt
-rw-------@   1 vjp  staff      15607 Jul 19 09:25 phase4d_step3_campaign_approval_prompt.txt
-rw-------@   1 vjp  staff      14147 Jul 19 10:02 phase4d_step3_review_prompt.txt
-rw-------@   1 vjp  staff      13972 Jul 19 11:15 phase4d_step4_high_reasoning_review_prompt.txt
-rw-------@   1 vjp  staff      20690 Jul 19 10:35 phase4d_step4_prompt_and_reasoning.txt
-rw-------@   1 vjp  staff      14302 Jul 19 11:52 phase4d_step5_handoff_status_prompt_and_reasoning.txt
-rw-------@   1 vjp  staff      22860 Jul 19 12:32 phase4d_step5_review_prompt_and_reasoning.txt
-rw-------@   1 vjp  staff      16921 Jul 19 14:10 phase4d_step6_independent_review_prompt.txt
-rw-r--r--    1 vjp  staff     195923 Jul 19 18:40 phase5-2.txt
-rw-r--r--    1 vjp  staff     153295 Jul 19 18:00 phase5.txt
-rw-r--r--    1 vjp  staff       1220 Feb 11 04:16 pip.csv
-rw-r--r--    1 vjp  staff       2409 Feb 11 04:16 pocket.plpgsql
-rw-r--r--    1 vjp  staff       3174 Feb 11 04:16 refresh_bollinger_band_materialized_view.plpgsql
-rw-r--r--    1 vjp  staff       3217 Jun 29 00:06 refresh_candlestick_materialized_view.plpgsql
-rw-r--r--    1 vjp  staff      13567 Feb 11 04:16 refresh_ema_materialized_view.plpgsql
-rw-r--r--    1 vjp  staff       3134 Feb 11 04:16 refresh_fractal_materialized_view.plpgsql
-rw-r--r--    1 vjp  staff        455 Feb 11 04:16 reindex.plpgsql
-rw-r--r--    1 vjp  staff       2885 Feb 11 04:16 remove_duplicates.plpgsql
-rw-r--r--    1 vjp  staff     159150 Jul 19 20:59 response.txt
drwxr-xr-x    3 vjp  staff         96 Jul 18 20:18 review_backups
-rw-------@   1 vjp  staff       7529 Mar 20 20:37 rsi.plpgsql
-rw-r--r--    1 vjp  staff       1510 Jul  4 18:17 scheduler.log
-rw-r--r--    1 vjp  staff       4287 Jul 10 19:52 scheduler_loop.txt
-rw-r--r--@   1 vjp  staff        439 Jun 28 07:17 select.plpgsql
-rw-r--r--    1 vjp  staff        376 Feb 13 16:36 set_index_tablespace.plpgsql
-rw-r--r--@   1 vjp  staff        366 Feb 12 17:28 set_tablespace.plpgsql
drwxr-xr-x    3 vjp  staff         96 Apr 19 22:40 training_output
-rw-r--r--    1 vjp  staff       8367 Jun 28 12:18 view_report.plpgsql
./Database/repair_usdpjyrmp_symbol.sh
./extract_lstm_analysis.sh
./make_lstm_results_csv.sh
./migrate_lstm_db.sh
928:				CLANG_CXX_LANGUAGE_STANDARD = "c++20";
961:				OTHER_CPLUSPLUSFLAGS = (
989:				CLANG_CXX_LANGUAGE_STANDARD = "c++20";
1024:				OTHER_CPLUSPLUSFLAGS = (
1054:				CLANG_CXX_LANGUAGE_STANDARD = "c++17";
1111:				OTHER_CPLUSPLUSFLAGS = "$(OTHER_CFLAGS)";
1127:				CLANG_CXX_LANGUAGE_STANDARD = "c++17";
1178:				OTHER_CPLUSPLUSFLAGS = "$(OTHER_CFLAGS)";
1191:				CLANG_CXX_LANGUAGE_STANDARD = "c++20";
1224:				OTHER_CPLUSPLUSFLAGS = (
1253:				CLANG_CXX_LANGUAGE_STANDARD = "c++20";
1288:				OTHER_CPLUSPLUSFLAGS = (
Sources/ExperimentRecommendation.hpp:127:std::string CanonicalRecommendationDouble(double value);
Sources/ExperimentRecommendation.hpp:130:std::string RecommendationCanonicalHash(const std::string& canonicalText);
Sources/ExperimentRecommendation.cpp:184:    return value ? CanonicalRecommendationDouble(*value) : "NULL";
Sources/ExperimentRecommendation.cpp:435:std::string CanonicalRecommendationDouble(double value)
Sources/ExperimentRecommendation.cpp:449:std::string RecommendationCanonicalHash(const std::string& canonicalText)
Sources/ExperimentRecommendation.cpp:464:        << CanonicalRecommendationDouble(policy.minimumLeaderScore)
Sources/ExperimentRecommendation.cpp:466:        << CanonicalRecommendationDouble(policy.minimumInferenceAccuracy)
Sources/ExperimentRecommendation.cpp:478:        << SortedJoin(policy.coreLrOffsets, CanonicalRecommendationDouble)
Sources/ExperimentRecommendation.cpp:480:        << SortedJoin(policy.headLrOffsets, CanonicalRecommendationDouble)
Sources/ExperimentRecommendation.cpp:482:        << SortedJoin(policy.labelThresholdOffsets, CanonicalRecommendationDouble)
Sources/ExperimentRecommendation.cpp:499:    return RecommendationCanonicalHash(
Sources/ExperimentRecommendation.cpp:547:        << CanonicalRecommendationDouble(configuration.labelThreshold)
Sources/ExperimentRecommendation.cpp:565:    return RecommendationCanonicalHash(
Sources/ExperimentRecommendation.cpp:588:    identity.hash = RecommendationCanonicalHash(identity.canonicalText);
Sources/ExperimentRecommendation.cpp:618:    return RecommendationCanonicalHash(
Sources/ExperimentRecommendation.cpp:632:    identity.hash = RecommendationCanonicalHash(identity.canonicalText);

exec
/bin/bash -lc "sed -n '420,465p' Sources/ExperimentRecommendation.cpp && xcodebuild -project ExpertAdvisor.xcodeproj -list" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 1686ms:
Command line invocation:
    /Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project ExpertAdvisor.xcodeproj -list

Information about project "ExpertAdvisor":
    Targets:
        LSTM Debug
        LSTM Release

    Build Configurations:
        Debug
        Release

    If no build configuration is specified and -scheme is not passed then "Release" is used.

    Schemes:
        DataOpTest
        LayerTest
        libMetaNN
        LSTM Debug
        LSTM Release
        MetalBuffer
        MetalBufferTest
        MetalSwift
        MetalSwiftTest Debug
        MetalSwiftTest Release
        MetalSwiftTests
        MetaNN_metal
        OtherTests


exec
/bin/bash -lc 'clang++ -std=c++20 -Wall -Wextra -Wpedantic -Wconversion -I Sources Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp Sources/ExperimentRecommendation.cpp -o /tmp/step5a-review-test && /tmp/step5a-review-test' in /Volumes/Developer SSD/ExpertAdvisor
 exited 1 in 2855ms:
Sources/ExperimentRecommendation.cpp:3:10: fatal error: 'CanonicalSymbol.hpp' file not found
    3 | #include "CanonicalSymbol.hpp"
      |          ^~~~~~~~~~~~~~~~~~~~~
1 error generated.

exec
/bin/bash -lc "sed -n '940,1040p' ExpertAdvisor.xcodeproj/project.pbxproj && clang++ -std=c++20 -Wall -Wextra -Wpedantic -Wconversion -I Sources -I Headers -I Common Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp Sources/ExperimentRecommendation.cpp -o /tmp/step5a-review-test && /tmp/step5a-review-test" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 3608ms:
Sources/ExperimentRecommendation.cpp:166:35: warning: implicit conversion changes signedness: 'const char' to 'const unsigned char' [-Wsign-conversion]
  166 |     for (const unsigned char byte : canonicalText)
      |                              ~~~~ ^
1 warning generated.

exec
/bin/bash -lc "rg -n \"kMaximum.*(Metric|Result|Identity|Canonical|Members)|Metric.*Maximum|maximum.*metric|metric.*limit|IdentityCanonicalMaximum\" Sources Tests | sed -n '1,360p' && rg -n \"inference_accuracy|leader_score|analysis.*metric|metric\" Sources/ExperimentRecommendationEvaluation* Sources/ExperimentRecommendationScoring* | sed -n '1,280p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
Tests/ExperimentRecommendationCampaignActivationTests.cpp:194:    auto oversized = Input(kMaximumRecommendationCampaignActivationMembers + 1);
Tests/ExperimentRecommendationCampaignExecutionTests.cpp:183:    auto oversized = Input(kMaximumRecommendationCampaignExecutionMembers + 1);
Tests/ExperimentRecommendationCampaignProposalReviewTests.cpp:252:        kMaximumRecommendationCampaignProposalReviewMembers + 1);
Sources/ExperimentRecommendationCampaignMaterialization.cpp:235:            kRecommendationCampaignMaterializationIdentityCanonicalMaximum ||
Tests/ExperimentRecommendationRankingTests.cpp:172:        policy, {}, kMaximumRecommendationRankingMembers);
Tests/ExperimentRecommendationRankingTests.cpp:412:                                              kMaximumRecommendationRankingMembers);
Sources/ExperimentRecommendationCampaignExecution.cpp:93:            kMaximumRecommendationCampaignExecutionMembers))
Sources/ExperimentRecommendationRankingService.cpp:258:    if (request.limit <= 0 || request.limit > kMaximumRecommendationRankingMembers)
Sources/ExperimentRecommendationCampaignStatus.hpp:11:inline constexpr int kMaximumRecommendationCampaignStatusMembers = 1000;
Sources/ExperimentRecommendationCampaignActivation.hpp:13:inline constexpr int kMaximumRecommendationCampaignActivationMembers = 1000;
Sources/ExperimentRecommendationCampaignProposalReview.cpp:161:            kMaximumRecommendationCampaignProposalReviewMembers))
Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp:13:    kMaximumRecommendationCampaignOutcomeAssessmentMembers = 1000;
Sources/ExperimentRecommendationCampaignProposalReviewService.cpp:228:                kMaximumRecommendationCampaignProposalReviewMembers))
Sources/ExperimentRecommendationCampaignLaunch.hpp:14:inline constexpr int kMaximumRecommendationCampaignLaunchMembers = 1000;
Sources/ExperimentRecommendationCampaignApproval.hpp:27:    kRecommendationCampaignApprovalIdentityCanonicalMaximum = 64 * 1024 * 1024;
Sources/ExperimentRecommendationCampaignMaterialization.hpp:19:    kRecommendationCampaignMaterializationIdentityCanonicalMaximum =
Sources/ExperimentRecommendationCampaignExecutionService.cpp:171:                kMaximumRecommendationCampaignExecutionMembers))
Sources/ExperimentRecommendationRanking.hpp:13:inline constexpr int kMaximumRecommendationRankingMembers = 1000;
Sources/ExperimentRecommendationCampaignApproval.cpp:334:            kRecommendationCampaignApprovalIdentityCanonicalMaximum,
Sources/ExperimentRecommendationCampaignExecution.hpp:13:inline constexpr int kMaximumRecommendationCampaignExecutionMembers = 1000;
Sources/ExperimentRecommendationCampaignActivationService.cpp:192:                kMaximumRecommendationCampaignActivationMembers))
Sources/ExperimentRecommendationRanking.cpp:413:    if (outputLimit <= 0 || outputLimit > kMaximumRecommendationRankingMembers)
Sources/ExperimentRecommendationRanking.cpp:510:    if (outputLimit <= 0 || outputLimit > kMaximumRecommendationRankingMembers)
Sources/ExperimentRecommendationRanking.cpp:527:    if (outputLimit <= 0 || outputLimit > kMaximumRecommendationRankingMembers)
Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:546:        memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp:1042:                                 kMaximumRecommendationCampaignOutcomeAssessmentMembers))
Sources/ExperimentRecommendationCampaignActivation.cpp:72:            kMaximumRecommendationCampaignActivationMembers))
Sources/ExperimentRecommendationCampaignStatus.cpp:398:        static_cast<std::size_t>(kMaximumRecommendationCampaignStatusMembers))
Sources/ExperimentRecommendationCampaignProposalReview.hpp:13:inline constexpr int kMaximumRecommendationCampaignProposalReviewMembers = 1000;
Sources/ExperimentRecommendationCampaignLaunchService.cpp:308:                kMaximumRecommendationCampaignLaunchMembers))
Sources/ExperimentRecommendationCampaignLaunch.cpp:81:            kMaximumRecommendationCampaignLaunchMembers))
Sources/ExperimentRecommendationRankingRepository.cpp:402:    if (request.limit <= 0 || request.limit > kMaximumRecommendationRankingMembers ||
Sources/ExperimentRecommendationRankingRepository.cpp:483:        static_cast<std::size_t>(kMaximumRecommendationRankingMembers))
Sources/ExperimentRecommendationEvaluation.cpp:277:        << ";leader_score=" << EvidenceDouble(score.sourceLeaderScore)
Sources/ExperimentRecommendationEvaluation.cpp:278:        << ";inference_accuracy="
Sources/ExperimentRecommendationScoring.cpp:174:        else if (key == "leader_score_weight")
Sources/ExperimentRecommendationScoring.cpp:176:        else if (key == "inference_accuracy_weight")
Sources/ExperimentRecommendationScoring.cpp:290:        << ";leader_score_weight=" << CanonicalRecommendationDouble(policy.leaderScoreWeight)
Sources/ExperimentRecommendationScoring.cpp:291:        << ";inference_accuracy_weight=" << CanonicalRecommendationDouble(policy.inferenceAccuracyWeight)
Sources/ExperimentRecommendationScoring.cpp:338:        return InvalidResult(policy, "nonfinite_source_metric");
Sources/ExperimentRecommendationScoring.cpp:341:        return InvalidResult(policy, "source_inference_accuracy_out_of_range");
Sources/ExperimentRecommendationScoring.cpp:370:    AddComponent(result, "leader_quality", "leader_score_clamped_unit",
Sources/ExperimentRecommendationScoring.cpp:374:    AddComponent(result, "inference_accuracy", "inference_accuracy_unit",
Sources/ExperimentRecommendationEvaluationRepository.cpp:108:    score.sourceLeaderScore = row["source_leader_score"].as<double>();
Sources/ExperimentRecommendationEvaluationRepository.cpp:286:        "r.source_rank,r.source_leader_score,r.source_infer_accuracy,"

exec
/bin/bash -lc "nl -ba Sources/ExperimentRecommendationCampaignStatus.cpp | sed -n '35,275p' && nl -ba Sources/ExperimentRecommendationCampaignStatus.hpp | sed -n '105,245p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
    35	    return value == "train" || value == "infer" || value == "analyze" ||
    36	        value == "done";
    37	}
    38	
    39	RecommendationCampaignStatusMember ClassifyMember(
    40	    const RecommendationCampaignStatusMemberInput& input)
    41	{
    42	    RecommendationCampaignStatusMember member;
    43	    member.memberOrdinal = input.memberOrdinal;
    44	    member.materializationMemberId = input.materializationMemberId;
    45	    member.recommendationId = input.recommendationId;
    46	    member.sourceExperimentId = input.sourceExperimentId;
    47	    member.rankingMemberId = input.rankingMemberId;
    48	    member.proposalId = input.proposalId;
    49	    member.currentReviewDecisionId = input.currentReviewDecisionId;
    50	    member.currentReviewDecision = input.currentReviewDecision;
    51	    member.authorizationReviewDecisionId = input.authorizationReviewDecisionId;
    52	    member.executionId = input.executionId;
    53	    member.activationId = input.activationId;
    54	    member.experimentId = input.experimentId;
    55	    member.executionEvidenceCount = input.executionCount;
    56	    member.activationEvidenceCount = input.activationCount;
    57	
    58	    if (input.executionCount > 1)
    59	        member.executionState = RecommendationCampaignStatusExecutionState::conflicting;
    60	    else if (input.executionId)
    61	        member.executionState = RecommendationCampaignStatusExecutionState::executed;
    62	    if (input.activationCount > 1)
    63	        member.activationState = RecommendationCampaignStatusActivationState::conflicting;
    64	    else if (input.activationId)
    65	        member.activationState = RecommendationCampaignStatusActivationState::activated;
    66	
    67	    if (!input.workflowConsistent)
    68	    {
    69	        if (input.executionCount == 1 && input.executionId)
    70	            member.executionState = RecommendationCampaignStatusExecutionState::malformed;
    71	        if (input.activationCount == 1 && input.activationId)
    72	            member.activationState = RecommendationCampaignStatusActivationState::malformed;
    73	        for (const auto& code : input.workflowDiagnostics) Diagnose(member, code);
    74	        if (input.workflowDiagnostics.empty())
    75	            Diagnose(member, "campaign_status_workflow_inconsistent");
    76	    }
    77	    if (input.executionCount < 0 || input.executionCount > 1 ||
    78	        (input.executionId.has_value() != (input.executionCount == 1)))
    79	        Diagnose(member, "campaign_status_execution_cardinality_invalid");
    80	    if (input.activationCount < 0 || input.activationCount > 1 ||
    81	        (input.activationId.has_value() != (input.activationCount == 1)))
    82	        Diagnose(member, "campaign_status_activation_cardinality_invalid");
    83	    if (input.activationId && !input.executionId)
    84	        Diagnose(member, "campaign_status_activation_without_execution");
    85	    if (!input.executionId)
    86	    {
    87	        if (input.experiment || input.experimentId)
    88	            Diagnose(member, "campaign_status_experiment_without_execution");
    89	        return member;
    90	    }
    91	    if (!input.experiment || !input.experimentId)
    92	    {
    93	        Diagnose(member, "campaign_status_execution_experiment_missing");
    94	        return member;
    95	    }
    96	
    97	    const auto& experiment = *input.experiment;
    98	    member.symbol = experiment.symbol;
    99	    member.predictionHorizon = experiment.predictionHorizon;
   100	    member.status = experiment.status;
   101	    member.phase = experiment.phase;
   102	    member.currentEpoch = experiment.currentEpoch;
   103	    member.targetEpochs = experiment.targetEpochs;
   104	    member.workerPid = experiment.workerPid;
   105	    member.currentOperation = experiment.currentOperation;
   106	    member.workerStartedAt = experiment.workerStartedAt;
   107	    member.startedAt = experiment.startedAt;
   108	    member.completedAt = experiment.completedAt;
   109	    member.exitCode = experiment.exitCode;
   110	    member.errorMessage = experiment.errorMessage;
   111	    member.modelId = experiment.modelId;
   112	    if (!experiment.invocationIdentityCanonical.empty())
   113	        member.invocationIdentityHash = RecommendationCanonicalHash(
   114	            experiment.invocationIdentityCanonical);
   115	    member.modelLinkCount = experiment.modelLinkCount;
   116	    member.finalInferenceResultCount = experiment.inferenceResultCount;
   117	    member.completedFinalInferenceResultCount =
   118	        experiment.completedInferenceResultCount;
   119	    member.failedFinalInferenceResultCount =
   120	        experiment.failedInferenceResultCount;
   121	    member.finalAnalysisResultCount = experiment.analysisResultCount;
   122	    member.completedFinalAnalysisResultCount =
   123	        experiment.completedAnalysisResultCount;
   124	    member.failedFinalAnalysisResultCount = experiment.failedAnalysisResultCount;
   125	    member.inferConfigured = experiment.inferConfigured;
   126	    member.analyzeConfigured = experiment.inferConfigured;
   127	    member.inferComplete = experiment.completedInferenceResultCount > 0;
   128	    member.analyzeComplete = experiment.completedAnalysisResultCount > 0;
   129	    member.trainComplete = experiment.modelId.has_value() &&
   130	        experiment.modelLinkCount == 1;
   131	
   132	    if (experiment.experimentId != *input.experimentId)
   133	        Diagnose(member, "campaign_status_experiment_provenance_mismatch");
   134	    if (!experiment.invocationProvenanceValid)
   135	        Diagnose(member, "campaign_status_experiment_invocation_mismatch");
   136	    if (experiment.symbol.empty() || experiment.predictionHorizon <= 0 ||
   137	        experiment.targetEpochs <= 0 || experiment.duplicateNonce != 0 ||
   138	        experiment.invocationMode != "recommendation_conversion")
   139	        Diagnose(member, "campaign_status_experiment_identity_invalid");
   140	    if (!IsKnownStatus(experiment.status))
   141	        Diagnose(member, "campaign_status_experiment_status_unknown");
   142	    if (!IsKnownPhase(experiment.phase))
   143	        Diagnose(member, "campaign_status_experiment_phase_unknown");
   144	    if (experiment.currentEpoch &&
   145	        (*experiment.currentEpoch < 0 || *experiment.currentEpoch > experiment.targetEpochs))
   146	        Diagnose(member, "campaign_status_current_epoch_invalid");
   147	    if (experiment.inferStartPresent != experiment.inferEndPresent)
   148	        Diagnose(member, "campaign_status_inference_configuration_invalid");
   149	    if (experiment.modelLinkCount < 0 || experiment.modelLinkCount > 1)
   150	        Diagnose(member, "campaign_status_model_link_conflict");
   151	    if (experiment.inferenceResultCount < 0 ||
   152	        experiment.completedInferenceResultCount < 0 ||
   153	        experiment.failedInferenceResultCount < 0 ||
   154	        experiment.completedInferenceResultCount +
   155	                experiment.failedInferenceResultCount !=
   156	            experiment.inferenceResultCount ||
   157	        experiment.completedInferenceResultCount > 1 ||
   158	        (experiment.completedInferenceResultCount > 0 &&
   159	         experiment.failedInferenceResultCount > 0))
   160	        Diagnose(member, "campaign_status_inference_evidence_invalid");
   161	    if (experiment.analysisResultCount < 0 ||
   162	        experiment.completedAnalysisResultCount < 0 ||
   163	        experiment.failedAnalysisResultCount < 0 ||
   164	        experiment.completedAnalysisResultCount +
   165	                experiment.failedAnalysisResultCount !=
   166	            experiment.analysisResultCount ||
   167	        experiment.completedAnalysisResultCount > 1 ||
   168	        experiment.failedAnalysisResultCount > 1 ||
   169	        (experiment.completedAnalysisResultCount > 0 &&
   170	         experiment.failedAnalysisResultCount > 0))
   171	        Diagnose(member, "campaign_status_analysis_evidence_invalid");
   172	
   173	    if (member.consistency == RecommendationCampaignStatusConsistency::inconsistent)
   174	        return member;
   175	
   176	    if (!input.activationId)
   177	    {
   178	        if (experiment.status == "paused" && experiment.phase == "train" &&
   179	            !experiment.workerPid && !experiment.currentOperation &&
   180	            !experiment.currentEpoch && !experiment.workerStartedAt &&
   181	            !experiment.startedAt && !experiment.completedAt &&
   182	            !experiment.exitCode && !experiment.errorMessage && !experiment.modelId)
   183	            member.operationalState = RecommendationCampaignStatusOperationalState::paused;
   184	        else
   185	            Diagnose(member, "campaign_status_unactivated_lifecycle_invalid");
   186	        return member;
   187	    }
   188	
   189	    if (experiment.status == "pending")
   190	    {
   191	        if (experiment.workerPid)
   192	            Diagnose(member, "campaign_status_pending_worker_invalid");
   193	        else if (experiment.phase == "train" || experiment.phase == "infer" ||
   194	                 experiment.phase == "analyze")
   195	            member.operationalState = RecommendationCampaignStatusOperationalState::pending;
   196	        else
   197	            Diagnose(member, "campaign_status_pending_phase_invalid");
   198	    }
   199	    else if (experiment.status == "running")
   200	    {
   201	        if (!experiment.workerStartedAt || !experiment.currentOperation ||
   202	            *experiment.currentOperation != experiment.phase)
   203	            Diagnose(member, "campaign_status_running_worker_evidence_invalid");
   204	        else if (experiment.phase == "train")
   205	            member.operationalState = RecommendationCampaignStatusOperationalState::runningTrain;
   206	        else if (experiment.phase == "infer")
   207	            member.operationalState = RecommendationCampaignStatusOperationalState::runningInfer;
   208	        else if (experiment.phase == "analyze")
   209	            member.operationalState = RecommendationCampaignStatusOperationalState::runningAnalyze;
   210	        else
   211	            Diagnose(member, "campaign_status_running_phase_invalid");
   212	    }
   213	    else if (experiment.status == "paused")
   214	    {
   215	        if (experiment.startedAt || experiment.currentEpoch || experiment.modelId)
   216	            member.operationalState =
   217	                RecommendationCampaignStatusOperationalState::pausedAfterProgress;
   218	        else
   219	            Diagnose(member, "campaign_status_activated_experiment_still_paused");
   220	    }
   221	    else if (experiment.status == "completed")
   222	    {
   223	        if (experiment.phase != "done" || !experiment.completedAt ||
   224	            experiment.workerPid || !member.trainComplete ||
   225	            !member.inferConfigured || !member.inferComplete ||
   226	            !member.analyzeConfigured || !member.analyzeComplete ||
   227	            (experiment.errorMessage && !experiment.errorMessage->empty()) ||
   228	            (experiment.exitCode && *experiment.exitCode != 0))
   229	            Diagnose(member, "campaign_status_completed_evidence_invalid");
   230	        else
   231	        {
   232	            member.operationalState = RecommendationCampaignStatusOperationalState::completed;
   233	            member.terminalResult = RecommendationCampaignStatusTerminalResult::succeeded;
   234	        }
   235	    }
   236	    else if (experiment.status == "failed")
   237	    {
   238	        if (!experiment.completedAt || !experiment.errorMessage ||
   239	            experiment.errorMessage->empty() || experiment.workerPid)
   240	            Diagnose(member, "campaign_status_failed_evidence_invalid");
   241	        else
   242	        {
   243	            member.operationalState = RecommendationCampaignStatusOperationalState::failed;
   244	            member.terminalResult = RecommendationCampaignStatusTerminalResult::failed;
   245	        }
   246	    }
   247	    else if (experiment.status == "cancelled")
   248	    {
   249	        if (!experiment.completedAt || experiment.workerPid)
   250	            Diagnose(member, "campaign_status_cancelled_evidence_invalid");
   251	        else
   252	        {
   253	            member.operationalState = RecommendationCampaignStatusOperationalState::cancelled;
   254	            member.terminalResult = RecommendationCampaignStatusTerminalResult::cancelled;
   255	        }
   256	    }
   257	    else if (experiment.status != "pending" && experiment.status != "running" &&
   258	             experiment.status != "paused")
   259	        Diagnose(member, "campaign_status_experiment_lifecycle_invalid");
   260	
   261	    return member;
   262	}
   263	
   264	std::string OptionalLongLong(const std::optional<long long>& value)
   265	{
   266	    return value ? std::to_string(*value) : "null";
   267	}
   268	
   269	std::string OptionalInt(const std::optional<int>& value)
   270	{
   271	    return value ? std::to_string(*value) : "null";
   272	}
   273	
   274	std::string OptionalText(const std::optional<std::string>& value)
   275	{
   105	    int completedInferenceResultCount = 0;
   106	    int failedInferenceResultCount = 0;
   107	    int analysisResultCount = 0;
   108	    int completedAnalysisResultCount = 0;
   109	    int failedAnalysisResultCount = 0;
   110	};
   111	
   112	struct RecommendationCampaignStatusMemberInput
   113	{
   114	    int memberOrdinal = 0;
   115	    long long materializationMemberId = -1;
   116	    long long recommendationId = -1;
   117	    long long sourceExperimentId = -1;
   118	    long long rankingMemberId = -1;
   119	    long long proposalId = -1;
   120	    std::optional<long long> currentReviewDecisionId;
   121	    std::optional<std::string> currentReviewDecision;
   122	    std::optional<long long> authorizationReviewDecisionId;
   123	    std::optional<long long> executionId;
   124	    std::optional<long long> activationId;
   125	    std::optional<long long> experimentId;
   126	    long long executionEvidenceCount = 0;
   127	    long long activationEvidenceCount = 0;
   128	    long long executionCount = 0;
   129	    long long activationCount = 0;
   130	    bool workflowConsistent = true;
   131	    std::vector<std::string> workflowDiagnostics;
   132	    std::optional<RecommendationCampaignStatusExperimentEvidence> experiment;
   133	};
   134	
   135	struct RecommendationCampaignStatusInput
   136	{
   137	    long long materializationId = -1;
   138	    int materializationContractVersion = 0;
   139	    std::string materializationIdentityCanonical;
   140	    std::string materializationIdentityHash;
   141	    int selectedMemberCount = 0;
   142	    std::string observedAt;
   143	    std::vector<RecommendationCampaignStatusMemberInput> members;
   144	};
   145	
   146	struct RecommendationCampaignStatusMember
   147	{
   148	    int memberOrdinal = 0;
   149	    long long materializationMemberId = -1;
   150	    long long recommendationId = -1;
   151	    long long sourceExperimentId = -1;
   152	    long long rankingMemberId = -1;
   153	    long long proposalId = -1;
   154	    std::optional<long long> currentReviewDecisionId;
   155	    std::optional<std::string> currentReviewDecision;
   156	    std::optional<long long> authorizationReviewDecisionId;
   157	    std::optional<long long> executionId;
   158	    std::optional<long long> activationId;
   159	    std::optional<long long> experimentId;
   160	    long long executionEvidenceCount = 0;
   161	    long long activationEvidenceCount = 0;
   162	    std::optional<std::string> symbol;
   163	    std::optional<int> predictionHorizon;
   164	    RecommendationCampaignStatusExecutionState executionState =
   165	        RecommendationCampaignStatusExecutionState::notExecuted;
   166	    RecommendationCampaignStatusActivationState activationState =
   167	        RecommendationCampaignStatusActivationState::notActivated;
   168	    RecommendationCampaignStatusOperationalState operationalState =
   169	        RecommendationCampaignStatusOperationalState::proposalOnly;
   170	    RecommendationCampaignStatusTerminalResult terminalResult =
   171	        RecommendationCampaignStatusTerminalResult::notTerminal;
   172	    RecommendationCampaignStatusConsistency consistency =
   173	        RecommendationCampaignStatusConsistency::consistent;
   174	    std::optional<std::string> status;
   175	    std::optional<std::string> phase;
   176	    std::optional<int> currentEpoch;
   177	    std::optional<int> targetEpochs;
   178	    std::optional<int> workerPid;
   179	    std::optional<std::string> currentOperation;
   180	    std::optional<std::string> workerStartedAt;
   181	    std::optional<std::string> startedAt;
   182	    std::optional<std::string> completedAt;
   183	    std::optional<int> exitCode;
   184	    std::optional<std::string> errorMessage;
   185	    std::optional<long long> modelId;
   186	    std::optional<std::string> invocationIdentityHash;
   187	    int modelLinkCount = 0;
   188	    int finalInferenceResultCount = 0;
   189	    int completedFinalInferenceResultCount = 0;
   190	    int failedFinalInferenceResultCount = 0;
   191	    int finalAnalysisResultCount = 0;
   192	    int completedFinalAnalysisResultCount = 0;
   193	    int failedFinalAnalysisResultCount = 0;
   194	    bool trainComplete = false;
   195	    bool inferConfigured = false;
   196	    bool inferComplete = false;
   197	    bool analyzeConfigured = false;
   198	    bool analyzeComplete = false;
   199	    std::vector<std::string> diagnosticCodes;
   200	};
   201	
   202	struct RecommendationCampaignStatusSnapshot
   203	{
   204	    RecommendationCampaignStatusRequest request;
   205	    int materializationContractVersion = 0;
   206	    std::string materializationIdentityHash;
   207	    std::string observedAt;
   208	    std::string snapshotIdentityCanonical;
   209	    std::string snapshotIdentityHash;
   210	    RecommendationCampaignAggregateStatus aggregateStatus =
   211	        RecommendationCampaignAggregateStatus::notExecuted;
   212	    RecommendationCampaignStatusConsistency consistency =
   213	        RecommendationCampaignStatusConsistency::consistent;
   214	    int proposalOnlyCount = 0;
   215	    int executedCount = 0;
   216	    int activatedCount = 0;
   217	    int pausedCount = 0;
   218	    int pendingCount = 0;
   219	    int runningCount = 0;
   220	    int runningTrainCount = 0;
   221	    int runningInferCount = 0;
   222	    int runningAnalyzeCount = 0;
   223	    int completedCount = 0;
   224	    int failedCount = 0;
   225	    int cancelledCount = 0;
   226	    int inconsistentCount = 0;
   227	    int terminalCount = 0;
   228	    int nonterminalCount = 0;
   229	    int activeWorkerCount = 0;
   230	    std::optional<int> minimumCurrentEpoch;
   231	    std::optional<int> maximumCurrentEpoch;
   232	    std::optional<long long> totalCurrentEpochs;
   233	    std::optional<long long> totalTargetEpochs;
   234	    std::optional<double> completedPercent;
   235	    std::optional<double> terminalPercent;
   236	    std::vector<std::string> diagnosticCodes;
   237	    std::vector<RecommendationCampaignStatusMember> members;
   238	};
   239	
   240	RecommendationCampaignStatusRequest NormalizeRecommendationCampaignStatusRequest(
   241	    const RecommendationCampaignStatusRequest& request);
   242	RecommendationCampaignStatusSnapshot BuildRecommendationCampaignStatusSnapshot(
   243	    const RecommendationCampaignStatusRequest& request,
   244	    RecommendationCampaignStatusInput input);
   245	

exec
/bin/bash -lc "sed -n '1,260p' Phase5Step5ArchitecturePlanningPrompt.txt && sed -n '1,260p' Phase5Step5aPrompt.txt && sed -n '1,300p' Phase5Step5a_ContractCorrection_Input.txt && sed -n '1,220p' Phase5Step5a_IndependentArchitecturalReview_Input.txt" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
Using xhigh reasoning, define the next coherent Phase 5 step for the ExpertAdvisor recommendation-campaign subsystem after Phase 5 Step 4.

This is an architecture and implementation-planning task only.

Do not modify files.
Do not stage, commit, push, pull, fetch, merge, rebase, reset, switch branches, or open a pull request.
Do not build or run tests unless a lightweight read-only source inspection genuinely requires it.
Do not connect to PostgreSQL.
Do not use or modify the active LSTM database.
Do not signal, poll, stop, restart, or otherwise affect the scheduler or workers.
Do not implement Phase 5 Step 5.

REPOSITORY STATE

Expected branch:
phase5

Expected HEAD:
8b9939e

Expected commit:
Phase 5 Step 4: Add recommendation campaign operational status

Begin by verifying:

git branch --show-current
git rev-parse HEAD
git log -1 --oneline
git status --short
git diff --check
git diff --cached --check

If branch or HEAD differs, report the difference and continue read-only. Do not alter repository state.

CURRENT IMPLEMENTED CAMPAIGN PATH

Phase 4D provides:

1. deterministic read-only campaign planning;
2. deterministic read-only campaign review;
3. explicit persisted approval/rejection;
4. exact approved-campaign materialization into Phase 4C proposals;
5. read-only campaign handoff;
6. explicit atomic campaign-wide Phase 4C proposal review.

Phase 5 provides:

1. atomic execution of exact materialized members into paused experiments;
2. atomic activation of exact executed members into pending/train;
3. atomic execution-and-activation launch;
4. repeatable-read, read-only operational status across exact campaign membership and downstream experiment/result evidence.

The existing scheduler independently processes ordinary pending experiments.

AUTHORITATIVE SOURCES TO INSPECT

At minimum inspect:

docs/Phase4DExperimentRecommendationCampaignHandoff.rst
docs/Phase5ExperimentRecommendationCampaignExecution.rst
docs/Phase5ExperimentRecommendationCampaignActivation.rst
docs/Phase5ExperimentRecommendationCampaignLaunch.rst
docs/Phase5ExperimentRecommendationCampaignStatus.rst
docs/architecture/Volume_VIII_Recommendation_Engine.md

Relevant production and test code for:

- campaign materialization;
- campaign review;
- campaign execution;
- campaign activation;
- campaign launch;
- campaign operational status;
- Phase 4C conversion workflow;
- scheduler experiment lifecycle and operational controls;
- continuation automation;
- recommendation/campaign CLI dispatch and conflict validation.

GOAL

Recommend exactly one next Phase 5 step that provides real operational or research value and forms a coherent, bounded extension of Steps 1–4.

Do not assume that “Step 5” must be autonomous campaign management.

Evaluate candidate directions such as:

1. explicit campaign-wide pause, resume, cancel, retry, or stop controls;
2. an explicit campaign reconciliation or repair command;
3. persisted campaign lifecycle authority;
4. scheduler-aware campaign monitoring or admission control;
5. bounded automatic campaign progression;
6. campaign completion/failure summary persistence;
7. no additional Phase 5 step yet, with a recommendation to move to another project phase.

These are candidates, not requirements.

ARCHITECTURAL CONSTRAINTS

Preserve existing authority boundaries unless the selected step explicitly and convincingly justifies changing one.

Do not casually introduce:

- hidden automation;
- background scans;
- automatic proposal review;
- automatic campaign approval;
- automatic experiment creation outside existing Phase 4C primitives;
- a second experiment lifecycle authority;
- duplicated scheduler state;
- mutable campaign membership;
- profitability claims;
- score-based authorization;
- process-liveness inference from persisted worker_pid;
- unbounded database scans;
- scheduler polling from read-only status code;
- broad locks over unrelated campaigns or experiments.

Any proposed mutation must be:

- explicit;
- separately confirmed where destructive or lifecycle-changing;
- transactionally bounded;
- idempotent or fail-closed;
- auditable;
- limited to exact immutable materialized membership;
- compatible with existing per-experiment operational controls;
- safe under partial progress and concurrent scheduler activity.

ANALYSIS REQUIRED

1. Reconstruct the exact current authority and data-flow boundaries.

2. Identify the most important operational gap remaining after Step 4.

3. Compare at least four plausible next-step candidates.

For each candidate report:

- user value;
- architectural fit;
- required schema changes;
- scheduler coupling;
- concurrency and locking risk;
- interaction with partially progressed campaigns;
- interaction with terminal, failed, cancelled, paused, and inconsistent members;
- idempotency/retry semantics;
- implementation size;
- test burden;
- risk of creating duplicate lifecycle authority;
- whether it should be Phase 5, deferred, or rejected.

4. Select one preferred next step.

The preferred step must have a narrow one-sentence purpose and a clear reason it is more valuable than the alternatives.

5. Define the complete proposed contract:

- command name and both accepted CLI forms;
- required IDs and options;
- confirmation behavior;
- dry-run behavior, if appropriate;
- exact membership authority;
- eligibility rules;
- per-member outcomes;
- aggregate outcomes;
- transaction boundaries;
- locking order;
- concurrency behavior;
- retry and duplicate behavior;
- failure and rollback semantics;
- scheduler interaction;
- worker interaction;
- process-liveness boundary;
- database schema/migration requirements;
- runtime privileges;
- machine-readable output;
- documentation changes;
- explicit non-goals.

6. Determine whether the step should be:

- one implementation step;
- split into multiple smaller steps;
- preceded by a schema-only or pure-domain step;
- postponed until additional experiment evidence exists.

7. Produce an implementation sequence with independently reviewable increments.

For every increment include:

- exact goal;
- likely files to add;
- likely files to modify;
- domain/repository/service/CLI boundaries;
- tests required;
- database safety requirements;
- regression suites required;
- completion criteria;
- proposed commit message.

8. Identify all unresolved questions. Resolve them from current code and documentation wherever possible. Only leave questions unresolved when the repository genuinely supplies no answer.

9. State whether implementation should begin now.

Do not recommend implementation merely to keep the scheduler busy. The next step must provide durable value.

REQUIRED FINAL RESPONSE

Use this structure:

## Repository state

## Current Phase 5 boundary

## Remaining operational gap

## Candidate comparison

Use a concise comparison table.

## Recommended Phase 5 Step 5

Include:

- title;
- one-sentence purpose;
- why this direction wins;
- why it belongs in Phase 5;
- why it is safe to implement now.

## Proposed contract

## Transaction and concurrency model

## Scheduler and worker boundary

## Schema and privilege impact

## CLI and machine output

## Test and regression plan

## Implementation increments

## Deferred and rejected alternatives

## Remaining risks and unresolved questions

## Recommendation

End with exactly one of:

“Proceed with Phase 5 Step 5 implementation planning.”

“Do not add Phase 5 Step 5 yet; move to <specific next activity>.”

Do not edit the repository.
Using xhigh reasoning, implement Phase 5 Step 5a only.

This is the first implementation increment of the Recommendation Campaign Outcome Assessment feature.

Repository state

Expected branch:
phase5

Expected worktree:
clean

Begin by verifying:

git branch --show-current
git rev-parse HEAD
git status --short
git diff --check
git diff --cached --check

If the repository is not clean, stop and report why.

Do not fetch.
Do not pull.
Do not merge.
Do not rebase.
Do not switch branches.

Purpose

Implement only the pure domain contract and deterministic assessment model.

Do NOT implement:

- PostgreSQL repository access
- CLI parsing
- scheduler integration
- worker interaction
- service layer
- persistence
- documentation beyond comments required for the new types

Architecture

This feature is a read-only assessment.

It compares:

frozen recommendation evidence

↓

exact materialized experiment

↓

final experiment analysis evidence

↓

deterministic comparison

It does NOT:

- evaluate recommendation policy
- determine whether the recommendation was scientifically good
- decide whether another experiment should be run
- score campaigns
- modify database state
- communicate with the scheduler
- infer process liveness
- persist assessment results

Terminology

Use the term:

Recommendation Campaign Outcome Assessment

Do not use simply "Outcome" for externally visible names.

Future CLI (not implemented in this step) will be:

--recommendation-campaign-outcome-assessment

Domain requirements

Design immutable domain structures representing:

assessment identity

campaign identity

materialization identity

member identity

source evidence

result evidence

comparison context

comparison classification

assessment summary

member assessment

Define explicit comparison classifications including at minimum:

Comparable

ContextChanged

MissingSourceEvidence

MissingResultEvidence

ResultNotTerminal

MetricUnavailable

UnsupportedComparison

Requirements

Every comparison must be deterministic.

A comparison is Comparable only if all scientifically relevant context matches.

At minimum compare:

symbol

prediction horizon

threshold

window size

label definition

metric identity

inference range

If any differ:

classification becomes ContextChanged

no numeric delta is produced

Delta values are optional.

Never compute a delta when contexts differ.

Metric identities

Metrics are identified by both:

metric name

metric value

Never compare metrics solely because both contain floating-point values.

Example:

infer_accuracy

leader_score

validation_accuracy

must never be mixed.

Assessment semantics

Assessment is:

read-only

point-in-time

non-persistent

non-authoritative

The assessment does not declare a campaign successful or unsuccessful.

It reports only evidence.

Testing

Add comprehensive pure unit tests covering:

identical contexts

different symbols

different horizons

different thresholds

different metric names

missing source evidence

missing result evidence

non-terminal result

unsupported comparisons

deterministic equality

deterministic ordering

null delta generation

context mismatch

No PostgreSQL tests.

No scheduler tests.

No CLI tests.

No integration tests.

No filesystem access.

Constraints

No database schema changes.

No repository changes.

No services.

No CLI.

No scheduler.

No process inspection.

No documentation edits except comments directly associated with new types.

Deliverables

Implement:

pure domain types

comparison rules

comparison classification

deterministic assessment model
# Phase 5 Step 5a Contract Correction

Use model_reasoning_effort="xhigh".

Implement the required architectural corrections to the current uncommitted Phase 5 Step 5a outcome-assessment domain model.

Do not fetch, pull, rebase, merge, reset, switch branches, push, or open a pull request. Work only with the current local checkout and current uncommitted Step 5a files.

Use incremental builds by default. Do not clean the Xcode build folder unless a concrete build failure indicates that a clean is necessary. If a clean becomes necessary, explain the reason in the final response.

CURRENT STATE

Expected branch:
phase5

Expected committed HEAD:
8b9939e

Expected uncommitted files include:

- Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
- Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
- Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
- ExpertAdvisor.xcodeproj/project.pbxproj

Begin by verifying, without changing repository state:

git branch --show-current
git rev-parse HEAD
git log -1 --oneline
git status --short
git diff --check
git diff --cached --check

If branch or HEAD differs, report the difference and continue cautiously using the current local checkout. Do not synchronize with any remote.

GOAL

Correct the Step 5a public domain contract before Step 5b begins.

The implementation must remain a pure, immutable, deterministic domain layer. Do not add repository, database, service, CLI, scheduler, worker, filesystem, or persistence behavior.

BLOCKING ARCHITECTURAL CORRECTIONS

1. Separate lifecycle, consistency, and scientific comparison.

Represent terminal/lifecycle state explicitly, including at minimum:

- not_terminal
- succeeded
- failed
- cancelled
- unknown

Represent consistency independently:

- consistent
- inconsistent

Preserve deterministic diagnostic codes for inconsistent downstream evidence.

Do not use MissingResultEvidence as a substitute for lifecycle state.

Derive explicit member-level outcomes sufficient to distinguish, at minimum:

- not ready / nonterminal
- succeeded and comparable
- succeeded with context change
- succeeded with metric gap
- terminal failed
- terminal cancelled
- inconsistent

Names may differ if the resulting contract is clearer and internally consistent.

2. Replace singular metrics with deterministic metric collections.

Source evidence and result evidence must support multiple scientific metrics for the same campaign member.

Requirements:

- one member can carry inference accuracy and leader score simultaneously;
- future metrics can be added without redesigning member cardinality;
- reject duplicate metric identities within one evidence collection;
- canonicalize and order metrics deterministically by metric identity;
- derive one comparison per metric;
- keep member outcome counts separate from per-metric comparison and delta counts;
- metric input order must not affect identity or output ordering.

3. Make successful-result payload conditional on lifecycle.

Failed, cancelled, inconsistent, proposal-only, and otherwise nonterminal members must not require fabricated:

- experiment IDs
- model IDs
- result IDs
- context
- metric values

A proposal-only member must be representable as nonterminal/not ready rather than being mislabeled MissingResultEvidence.

4. Add exact result provenance validation.

Bind result evidence to the exact materialized campaign member and its expected downstream experiment identity.

Requirements:

- validate result experiment identity against the member's expected experiment evidence when present;
- reject cross-member reuse of the same result experiment or result identity;
- reject provenance mismatches deterministically;
- preserve malformed downstream evidence as an inconsistent member with diagnostics when exact top-level materialization membership remains trustworthy;
- only fail the entire assessment when the materialization/member identity itself cannot be trusted.

5. Protect output invariants.

Derived member and aggregate outputs must be valid by construction or completely validated at construction.

Prevent callers from creating contradictory combinations of:

- lifecycle
- consistency
- diagnostics
- comparisons
- summaries
- counts
- canonical identities

Restrict constructors/factories as appropriate, or perform complete cross-field validation.

6. Preserve Step 5a scope and existing strengths.

Retain:

- immutable output objects;
- finite-number validation;
- positive-ID validation where IDs are present and required;
- contiguous ordinal validation;
- uniqueness validation;
- deterministic member ordering;
- deterministic diagnostic ordering;
- deterministic metric/comparison ordering;
- locale-independent canonicalization and hashing;
- observed_at excluded from evidence identity;
- no external I/O or persistence.

IDENTITY REQUIREMENTS

Update canonical identity and hashing so that they bind:

- explicit lifecycle state;
- independent consistency state;
- deterministic diagnostics;
- all canonicalized source metrics;
- all canonicalized result metrics;
- all derived per-metric comparisons;
- exact member/result provenance;
- aggregate member and metric summaries.

Input metric order must not change identity.

Changing any lifecycle state, consistency state, diagnostic, metric value, metric presence, comparison result, or provenance must change identity where scientifically or operationally meaningful.

TEST REQUIREMENTS

Expand focused tests to cover at minimum:

1. inference accuracy and leader score on the same member;
2. a future third metric;
3. shuffled source and result metric order producing identical canonical output and identity;
4. duplicate metric identities rejected;
5. successful comparable member;
6. successful member with changed context;
7. successful member with a metric gap;
8. failed member;
9. cancelled member;
10. nonterminal member;
11. proposal-only member represented as nonterminal/not ready, not MissingResultEvidence;
12. inconsistent downstream evidence retained with deterministic diagnostics;
13. malformed result provenance;
14. cross-member result experiment reuse;
15. cross-member result identity reuse;
16. aggregate precedence for inconsistency, failure, cancellation, nonterminal, and successful outcomes;
17. member counts separated from per-metric comparison counts;
18. identity changes for every lifecycle, consistency, diagnostic, metric, comparison, and provenance change;
19. public construction cannot produce contradictory aggregates;
20. deterministic ordering and canonicalization.

VERIFICATION

Run focused unit tests for the Step 5a domain model with warnings treated as errors.

Run the relevant existing recommendation-campaign pure regression tests.

Run an incremental Xcode Debug build.

Run an incremental Xcode Release build if the Debug build and focused tests pass.

Do not clean before building unless required by a specific failure.

Run:

git diff --check
git diff --cached --check

Do not stage or commit.

Do not connect to PostgreSQL or run database-backed tests because Step 5a is a pure domain step.

Do not modify unrelated files.

DOCUMENTATION

If Step 5a documentation already exists in the uncommitted work, update it to match the corrected contract.

If no Step 5a documentation exists, add only the minimal focused document necessary to describe:

- purpose and boundary;
- lifecycle/consistency/comparison separation;
- multi-metric model;
- provenance validation;
- deterministic identity;
- explicit non-goals.

Do not prematurely document Step 5b implementation details.

FINAL RESPONSE

Provide:

## Repository state

## Changes made

## Corrected public contract

## Multi-metric behavior

## Lifecycle and consistency behavior

## Provenance and invariant protection

## Tests added or changed

## Verification results

Include exact commands and pass/fail outcomes.

## Files changed

## Remaining risks

## Recommendation

End with exactly one of:

“Phase 5 Step 5a is ready for independent review.”

“Phase 5 Step 5a is not ready for independent review.”

Do not commit.
Phase 5 Step 5a Independent Architectural Review

Pull the latest HEAD on the current branch before reviewing.

Use model_reasoning_effort=“xhigh”.

Perform a read-only architectural review of the current unstaged
implementation of Phase 5 Step 5a.

Do NOT modify any files.

Focus on these questions:

1.  Does the Step 5a domain model support multiple scientific metrics
    per campaign member (for example inference accuracy, leader score,
    and future metrics) without requiring a redesign in Step 5b?

2.  Does the current contract correctly distinguish:

    -   successful terminal completion
    -   failed experiments
    -   cancelled experiments
    -   inconsistent or malformed evidence
    -   non-terminal experiments

    Ensure these are not unintentionally collapsed into
    MissingResultEvidence or a generic Terminal state.

3.  Verify that the implementation remains within the intended Step 5a
    boundary:

    -   pure immutable domain model
    -   validation
    -   identity
    -   deterministic ordering
    -   no repository
    -   no database access
    -   no CLI
    -   no scheduler changes
    -   no persistence

4.  Identify any contract changes that should be made BEFORE Step 5b
    begins.

5.  Do not suggest stylistic refactors unless they materially improve
    the architecture.

Provide: - Executive summary - Findings - Risks - Recommended changes
(if any) - Final recommendation: - Ready to commit - Commit after minor
fixes - Do not commit yet

exec
/bin/bash -lc "nl -ba Sources/ExperimentRecommendationRanking.cpp | sed -n '380,545p' && nl -ba Sources/ExperimentRecommendationCampaignPlanning.cpp | sed -n '800,930p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
   380	    if (value == "advisory_ready") return RecommendationRankingBucket::advisoryReady;
   381	    if (value == "blocked") return RecommendationRankingBucket::blocked;
   382	    if (value == "non_actionable") return RecommendationRankingBucket::nonActionable;
   383	    return std::nullopt;
   384	}
   385	
   386	RecommendationRankingBucket RecommendationRankingBucketForDisposition(
   387	    RecommendationEvaluationDisposition disposition)
   388	{
   389	    switch (disposition)
   390	    {
   391	        case RecommendationEvaluationDisposition::advisoryReady:
   392	            return RecommendationRankingBucket::advisoryReady;
   393	        case RecommendationEvaluationDisposition::blockedPendingDuplicate:
   394	        case RecommendationEvaluationDisposition::blockedActiveDuplicate:
   395	        case RecommendationEvaluationDisposition::completedDuplicate:
   396	            return RecommendationRankingBucket::blocked;
   397	        case RecommendationEvaluationDisposition::insufficientEvidence:
   398	        case RecommendationEvaluationDisposition::staleSourceEvidence:
   399	        case RecommendationEvaluationDisposition::unsupportedRecommendationFamily:
   400	        case RecommendationEvaluationDisposition::invalidPersistedEvidence:
   401	            return RecommendationRankingBucket::nonActionable;
   402	    }
   403	    throw std::invalid_argument("invalid_recommendation_ranking_disposition");
   404	}
   405	
   406	std::vector<RecommendationRankingMember> RankRecommendationEvaluationEvidence(
   407	    const RecommendationRankingPolicy& policy,
   408	    const std::vector<RecommendationRankingEvaluation>& evaluations,
   409	    int outputLimit)
   410	{
   411	    if (const auto error = ValidateRecommendationRankingPolicy(policy))
   412	        throw std::invalid_argument(*error);
   413	    if (outputLimit <= 0 || outputLimit > kMaximumRecommendationRankingMembers)
   414	        throw std::invalid_argument("recommendation_ranking_limit_invalid");
   415	    if (evaluations.size() >
   416	        static_cast<std::size_t>(kMaximumRecommendationRankingInputs))
   417	        throw std::invalid_argument("recommendation_ranking_input_limit_exceeded");
   418	    std::vector<RecommendationRankingMember> members;
   419	    members.reserve(evaluations.size());
   420	    std::set<long long> evaluationResultIds;
   421	    for (const auto& evaluation : evaluations)
   422	    {
   423	        ValidateEvaluation(evaluation);
   424	        if (!evaluationResultIds.insert(evaluation.evaluationResultId).second)
   425	            throw std::invalid_argument(
   426	                "duplicate_recommendation_ranking_evaluation_result");
   427	        RecommendationRankingMember member;
   428	        member.evaluation = evaluation;
   429	        member.bucket = RecommendationRankingBucketForDisposition(
   430	            evaluation.disposition);
   431	        member.tieBreakPrimary = ScoreTieBreak(evaluation);
   432	        member.tieBreakSemanticHash = evaluation.recommendationSemanticHash;
   433	        member.tieBreakEvaluationHash = evaluation.evaluationIdentityHash;
   434	        member.inclusionReason = member.bucket ==
   435	            RecommendationRankingBucket::advisoryReady
   436	            ? "included_advisory_ready" : "included_for_advisory_inspection";
   437	        if (member.bucket != RecommendationRankingBucket::advisoryReady)
   438	            member.blockReason = evaluation.reasonCode;
   439	        member.topPositiveComponent = TopComponent(evaluation.components, false);
   440	        member.topPenaltyComponent = TopComponent(evaluation.components, true);
   441	        members.push_back(std::move(member));
   442	    }
   443	    std::sort(members.begin(), members.end(), [](const auto& left,
   444	                                                  const auto& right) {
   445	        if (left.bucket != right.bucket)
   446	            return BucketPriority(left.bucket) < BucketPriority(right.bucket);
   447	        if (left.bucket == RecommendationRankingBucket::advisoryReady &&
   448	            left.evaluation.finalScore != right.evaluation.finalScore)
   449	            return *left.evaluation.finalScore > *right.evaluation.finalScore;
   450	        if (left.bucket != RecommendationRankingBucket::advisoryReady &&
   451	            left.evaluation.disposition != right.evaluation.disposition)
   452	            return DispositionPriority(left.evaluation.disposition) <
   453	                   DispositionPriority(right.evaluation.disposition);
   454	        return std::tie(left.evaluation.recommendationSemanticHash,
   455	                        left.evaluation.evaluationIdentityHash,
   456	                        left.evaluation.evaluationResultId) <
   457	               std::tie(right.evaluation.recommendationSemanticHash,
   458	                        right.evaluation.evaluationIdentityHash,
   459	                        right.evaluation.evaluationResultId);
   460	    });
   461	    if (members.size() > static_cast<std::size_t>(outputLimit))
   462	        members.resize(static_cast<std::size_t>(outputLimit));
   463	    int bucketRanks[3] = {0, 0, 0};
   464	    for (std::size_t index = 0; index < members.size(); ++index)
   465	    {
   466	        members[index].globalOrdinal = static_cast<int>(index) + 1;
   467	        const int bucket = BucketPriority(members[index].bucket);
   468	        members[index].bucketRank = ++bucketRanks[bucket];
   469	    }
   470	    return members;
   471	}
   472	
   473	std::string RecommendationRankingMembershipCanonicalText(
   474	    const std::vector<RecommendationRankingEvaluation>& evaluations)
   475	{
   476	    if (evaluations.size() >
   477	        static_cast<std::size_t>(kMaximumRecommendationRankingInputs))
   478	        throw std::invalid_argument("recommendation_ranking_input_limit_exceeded");
   479	    std::vector<std::pair<std::string, long long>> identities;
   480	    identities.reserve(evaluations.size());
   481	    std::set<long long> evaluationResultIds;
   482	    for (const auto& value : evaluations)
   483	    {
   484	        ValidateEvaluation(value);
   485	        if (!evaluationResultIds.insert(value.evaluationResultId).second)
   486	            throw std::invalid_argument(
   487	                "duplicate_recommendation_ranking_evaluation_result");
   488	        identities.emplace_back(value.evaluationIdentityCanonical,
   489	                                value.evaluationResultId);
   490	    }
   491	    std::sort(identities.begin(), identities.end());
   492	    std::ostringstream out;
   493	    out.imbue(std::locale::classic());
   494	    out << "experiment_recommendation_ranking_membership_v1;count="
   495	        << identities.size();
   496	    for (std::size_t index = 0; index < identities.size(); ++index)
   497	        out << ";member[" << index << "].evaluation_identity="
   498	            << LengthText(identities[index].first)
   499	            << ";member[" << index << "].evaluation_result_id="
   500	            << identities[index].second;
   501	    return out.str();
   502	}
   503	
   504	std::string RecommendationRankingSnapshotIdentityCanonicalText(
   505	    const RecommendationRankingPolicy& policy,
   506	    const RecommendationRankingScope& scope,
   507	    int outputLimit,
   508	    const std::vector<RecommendationRankingEvaluation>& evaluations)
   509	{
   510	    if (outputLimit <= 0 || outputLimit > kMaximumRecommendationRankingMembers)
   511	        throw std::invalid_argument("recommendation_ranking_limit_invalid");
   512	    const std::string membership =
   513	        RecommendationRankingMembershipCanonicalText(evaluations);
   514	    return RecommendationRankingSnapshotIdentityCanonicalTextFromMembership(
   515	        policy, scope, outputLimit, membership);
   516	}
   517	
   518	std::string RecommendationRankingSnapshotIdentityCanonicalTextFromMembership(
   519	    const RecommendationRankingPolicy& policy,
   520	    const RecommendationRankingScope& scope,
   521	    int outputLimit,
   522	    const std::string& membershipCanonical)
   523	{
   524	    if (!ValidRecommendationRankingMembershipCanonicalText(membershipCanonical))
   525	        throw std::invalid_argument(
   526	            "invalid_recommendation_ranking_membership_canonical");
   527	    if (outputLimit <= 0 || outputLimit > kMaximumRecommendationRankingMembers)
   528	        throw std::invalid_argument("recommendation_ranking_limit_invalid");
   529	    const std::string policyText =
   530	        RecommendationRankingPolicyCanonicalText(policy);
   531	    const std::string scopeText = RecommendationRankingScopeCanonicalText(scope);
   532	    return "experiment_recommendation_ranking_snapshot_identity_v1;policy=" +
   533	        LengthText(policyText) + ";scope=" + LengthText(scopeText) +
   534	        ";limit=" + std::to_string(outputLimit) + ";membership=" +
   535	        LengthText(membershipCanonical);
   536	}
   537	
   538	std::string RecommendationComparisonStateText(
   539	    RecommendationComparisonState value)
   540	{
   541	    switch (value)
   542	    {
   543	        case RecommendationComparisonState::comparable: return "comparable";
   544	        case RecommendationComparisonState::incomparablePolicyVersion:
   545	            return "incomparable_policy_version";
   800	            });
   801	    }
   802	
   803	    std::set<std::string> selectedInvocationCanonicals;
   804	    std::map<std::string, int> symbolCounts;
   805	    std::map<int, int> horizonCounts;
   806	    std::map<long long, int> sourceCounts;
   807	
   808	    for (std::size_t index = 0; index < ordered.size(); ++index)
   809	    {
   810	        RecommendationCampaignPlanCandidate candidate;
   811	        candidate.ordinal = static_cast<int>(index + 1);
   812	        candidate.input = std::move(ordered[index]);
   813	
   814	        if (!input.policy.enabled)
   815	            AddReason(candidate, RecommendationCampaignReason::planningDisabled);
   816	        else
   817	        {
   818	            if (candidate.ordinal > input.policy.maximumCandidatesConsidered)
   819	                AddReason(candidate,
   820	                          RecommendationCampaignReason::candidateLimitReached);
   821	            if (candidate.input.rankingSnapshotId !=
   822	                    input.scope.rankingSnapshotId ||
   823	                candidate.input.rankingPosition <= 0)
   824	                AddReason(candidate,
   825	                          RecommendationCampaignReason::recommendationNotRanked);
   826	            if (candidate.input.rankingBucket != "advisory_ready" ||
   827	                !candidate.input.rankingScore)
   828	                AddReason(candidate,
   829	                          RecommendationCampaignReason::rankingNotAdvisoryReady);
   830	            if (candidate.input.rankingVersion != 1)
   831	                AddReason(candidate,
   832	                          RecommendationCampaignReason::unsupportedContractVersion);
   833	            if (!rankingPolicyConsistent ||
   834	                !CandidateIdentityValid(candidate.input) ||
   835	                candidate.input.rankingSnapshotIdentityCanonical !=
   836	                    input.rankingSnapshotIdentityCanonical ||
   837	                candidate.input.rankingSnapshotIdentityHash !=
   838	                    input.rankingSnapshotIdentityHash)
   839	                AddReason(candidate,
   840	                          RecommendationCampaignReason::identityValidationFailed);
   841	            if (std::isfinite(candidate.input.leaderScore) &&
   842	                candidate.input.leaderScore < input.policy.minimumLeaderScore)
   843	                AddReason(candidate, RecommendationCampaignReason::
   844	                          belowMinimumLeaderScore);
   845	            if (std::isfinite(candidate.input.inferenceAccuracy) &&
   846	                candidate.input.inferenceAccuracy <
   847	                    input.policy.minimumInferenceAccuracy)
   848	                AddReason(candidate, RecommendationCampaignReason::
   849	                          belowMinimumInferenceAccuracy);
   850	            if (!candidate.input.predictedNeutralProportion)
   851	                AddReason(candidate, RecommendationCampaignReason::
   852	                          predictedNeutralMetricUnavailable);
   853	            else if (*candidate.input.predictedNeutralProportion >
   854	                     input.policy.maximumPredictedNeutralProportion)
   855	                AddReason(candidate, RecommendationCampaignReason::
   856	                          predictedNeutralAboveMaximum);
   857	            if (input.policy.minimumProfitability)
   858	            {
   859	                if (!candidate.input.profitabilityMetric)
   860	                    AddReason(candidate, RecommendationCampaignReason::
   861	                              profitabilityMetricUnavailable);
   862	                else if (*candidate.input.profitabilityMetric <
   863	                         *input.policy.minimumProfitability)
   864	                    AddReason(candidate, RecommendationCampaignReason::
   865	                              belowMinimumProfitability);
   866	            }
   867	            AddWorkflowReasons(candidate, input.policy);
   868	        }
   869	
   870	        if (candidate.reasons.empty())
   871	        {
   872	            if (selectedInvocationCanonicals.contains(
   873	                    candidate.input.recommendationInvocationCanonical))
   874	                AddReason(candidate, RecommendationCampaignReason::
   875	                          duplicateConversionIdentity);
   876	            const int sourceCount = sourceCounts[candidate.input.sourceExperimentId];
   877	            if (input.policy.maximumPerSourceExperiment && sourceCount > 0 &&
   878	                *input.policy.maximumPerSourceExperiment == 1)
   879	                AddReason(candidate, RecommendationCampaignReason::
   880	                          sourceExperimentConflict);
   881	            else if (input.policy.maximumPerSourceExperiment &&
   882	                     sourceCount >= *input.policy.maximumPerSourceExperiment)
   883	                AddReason(candidate, RecommendationCampaignReason::
   884	                          sourceExperimentLimitReached);
   885	            if (input.policy.maximumPerSymbol &&
   886	                symbolCounts[candidate.input.symbol] >=
   887	                    *input.policy.maximumPerSymbol)
   888	                AddReason(candidate,
   889	                          RecommendationCampaignReason::symbolLimitReached);
   890	            if (input.policy.maximumPerHorizon &&
   891	                horizonCounts[candidate.input.predictionHorizon] >=
   892	                    *input.policy.maximumPerHorizon)
   893	                AddReason(candidate,
   894	                          RecommendationCampaignReason::horizonLimitReached);
   895	            if (plan.summary.selectedCount >=
   896	                input.policy.maximumSelectedRecommendations)
   897	                AddReason(candidate,
   898	                          RecommendationCampaignReason::campaignLimitReached);
   899	        }
   900	
   901	        if (candidate.reasons.empty())
   902	        {
   903	            candidate.decision = RecommendationCampaignDecision::include;
   904	            candidate.reasons.push_back(RecommendationCampaignReason::selected);
   905	            ++plan.summary.selectedCount;
   906	            selectedInvocationCanonicals.insert(
   907	                candidate.input.recommendationInvocationCanonical);
   908	            ++symbolCounts[candidate.input.symbol];
   909	            ++horizonCounts[candidate.input.predictionHorizon];
   910	            ++sourceCounts[candidate.input.sourceExperimentId];
   911	        }
   912	        else
   913	        {
   914	            candidate.decision = RecommendationCampaignDecision::exclude;
   915	            ++plan.summary.excludedCount;
   916	        }
   917	        plan.candidates.push_back(std::move(candidate));
   918	    }
   919	
   920	    plan.summary.candidateCount = static_cast<int>(plan.candidates.size());
   921	    plan.identityCanonical = PlanIdentityCanonicalText(
   922	        plan, input.rankingSnapshotIdentityCanonical,
   923	        input.rankingSnapshotIdentityHash);
   924	    plan.identityHash = RecommendationCanonicalHash(plan.identityCanonical);
   925	    return plan;
   926	}
   927	
   928	bool RecommendationCampaignPlanOrderingIsDeterministic(
   929	    const RecommendationCampaignPlan& plan)
   930	{

exec
/bin/bash -lc "nl -ba Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp | sed -n '1,165p' && nl -ba Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp | sed -n '300,470p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
     1	#include "ExperimentRecommendationCampaignMaterializationRepository.hpp"
     2	
     3	#include "ExperimentRecommendationEvaluation.hpp"
     4	
     5	#include <algorithm>
     6	#include <locale>
     7	#include <sstream>
     8	#include <stdexcept>
     9	#include <tuple>
    10	
    11	namespace EA::ExperimentRecommendation
    12	{
    13	namespace
    14	{
    15	
    16	template <typename Value>
    17	std::optional<Value> OptionalValue(const pqxx::row& row, const char* column)
    18	{
    19	    const auto field = row[column];
    20	    if (field.is_null()) return std::nullopt;
    21	    return field.as<Value>();
    22	}
    23	
    24	std::string LengthText(const std::string& value)
    25	{
    26	    return std::to_string(value.size()) + ":" + value;
    27	}
    28	
    29	std::string AuthorizationCanonical(const pqxx::row& row)
    30	{
    31	    std::ostringstream out;
    32	    out.imbue(std::locale::classic());
    33	    out << "experiment_recommendation_conversion_review_authorization_v1"
    34	        << ";review_event_id="
    35	        << row["recommendation_review_event_id"].as<long long>()
    36	        << ";recommendation_id=" << row["recommendation_id"].as<long long>()
    37	        << ";action=" << LengthText(row["review_action"].as<std::string>())
    38	        << ";resulting_status="
    39	        << LengthText(row["review_resulting_status"].as<std::string>())
    40	        << ";recommendation_semantic="
    41	        << LengthText(row["review_semantic_canonical"].as<std::string>())
    42	        << ";recommendation_semantic_hash="
    43	        << LengthText(row["review_semantic_hash"].as<std::string>())
    44	        << ";source_experiment_id="
    45	        << row["review_source_experiment_id"].as<long long>();
    46	    return out.str();
    47	}
    48	
    49	std::string MaterializationColumns()
    50	{
    51	    return
    52	        "recommendation_campaign_materialization_id,"
    53	        "recommendation_campaign_approval_id,"
    54	        "materialization_contract_version,approval_identity_hash,"
    55	        "recommendation_ranking_snapshot_id,ranking_snapshot_identity_hash,"
    56	        "planning_policy_hash,campaign_plan_identity_hash,"
    57	        "campaign_review_identity_hash,materialized_by,"
    58	        "materialization_reason_text,selected_member_count,"
    59	        "initially_created_proposal_count,initially_reused_proposal_count,"
    60	        "materialization_identity_canonical,materialization_identity_hash,"
    61	        "created_at::text AS created_at";
    62	}
    63	
    64	std::string MemberColumns()
    65	{
    66	    return
    67	        "recommendation_campaign_materialization_member_id,member_ordinal,"
    68	        "recommendation_ranking_member_id,recommendation_id,"
    69	        "source_experiment_id,ranking_position,"
    70	        "selected_member_identity_canonical,selected_member_identity_hash,"
    71	        "recommendation_conversion_proposal_id,"
    72	        "proposal_identity_canonical,proposal_identity_hash,"
    73	        "created_at::text AS created_at";
    74	}
    75	
    76	PersistedRecommendationCampaignMaterializationMember MapMember(
    77	    const pqxx::row& row)
    78	{
    79	    PersistedRecommendationCampaignMaterializationMember value;
    80	    value.materializationMemberId = row[0].as<long long>();
    81	    value.memberOrdinal = row[1].as<int>();
    82	    value.rankingMemberId = row[2].as<long long>();
    83	    value.recommendationId = row[3].as<long long>();
    84	    value.sourceExperimentId = row[4].as<long long>();
    85	    value.rankingPosition = row[5].as<int>();
    86	    value.selectedMemberIdentityCanonical = row[6].as<std::string>();
    87	    value.selectedMemberIdentityHash = row[7].as<std::string>();
    88	    value.conversionProposalId = row[8].as<long long>();
    89	    value.proposalIdentityCanonical = row[9].as<std::string>();
    90	    value.proposalIdentityHash = row[10].as<std::string>();
    91	    value.createdAt = row[11].as<std::string>();
    92	    if (value.memberOrdinal <= 0 || value.rankingMemberId <= 0 ||
    93	        value.recommendationId <= 0 || value.sourceExperimentId <= 0 ||
    94	        value.rankingPosition <= 0 || value.conversionProposalId <= 0 ||
    95	        value.selectedMemberIdentityHash != RecommendationCanonicalHash(
    96	            value.selectedMemberIdentityCanonical) ||
    97	        value.proposalIdentityHash != RecommendationCanonicalHash(
    98	            value.proposalIdentityCanonical))
    99	        throw std::runtime_error(
   100	            "invalid_persisted_recommendation_campaign_materialization_member");
   101	    return value;
   102	}
   103	
   104	PersistedRecommendationCampaignMaterialization MapMaterialization(
   105	    pqxx::transaction_base& transaction,
   106	    const pqxx::row& row)
   107	{
   108	    PersistedRecommendationCampaignMaterialization value;
   109	    value.materializationId = row[0].as<long long>();
   110	    value.campaignApprovalId = row[1].as<long long>();
   111	    value.contractVersion = row[2].as<int>();
   112	    value.approvalIdentityHash = row[3].as<std::string>();
   113	    value.rankingSnapshotId = row[4].as<long long>();
   114	    value.rankingSnapshotIdentityHash = row[5].as<std::string>();
   115	    value.planningPolicyHash = row[6].as<std::string>();
   116	    value.campaignPlanIdentityHash = row[7].as<std::string>();
   117	    value.campaignReviewIdentityHash = row[8].as<std::string>();
   118	    value.materializedBy = row[9].as<std::string>();
   119	    value.reasonText = row[10].as<std::string>();
   120	    value.selectedMemberCount = row[11].as<int>();
   121	    value.initiallyCreatedProposalCount = row[12].as<int>();
   122	    value.initiallyReusedProposalCount = row[13].as<int>();
   123	    value.identityCanonical = row[14].as<std::string>();
   124	    value.identityHash = row[15].as<std::string>();
   125	    value.createdAt = row[16].as<std::string>();
   126	    if (value.materializationId <= 0 || value.campaignApprovalId <= 0 ||
   127	        value.contractVersion !=
   128	            kRecommendationCampaignMaterializationContractVersion ||
   129	        value.selectedMemberCount <= 0 ||
   130	        value.identityHash != RecommendationCanonicalHash(
   131	            value.identityCanonical))
   132	        throw std::runtime_error(
   133	            "invalid_persisted_recommendation_campaign_materialization");
   134	    const pqxx::result members = transaction.exec(
   135	        "SELECT " + MemberColumns() + " FROM "
   136	        "experiment_recommendation_campaign_materialization_member WHERE "
   137	        "recommendation_campaign_materialization_id=$1 "
   138	        "ORDER BY member_ordinal ASC;",
   139	        pqxx::params{value.materializationId});
   140	    value.members.reserve(members.size());
   141	    for (const auto& member : members) value.members.push_back(MapMember(member));
   142	    if (static_cast<int>(value.members.size()) != value.selectedMemberCount)
   143	        throw std::runtime_error(
   144	            "incomplete_persisted_recommendation_campaign_materialization");
   145	    for (std::size_t index = 0; index < value.members.size(); ++index)
   146	        if (value.members[index].memberOrdinal != static_cast<int>(index) + 1)
   147	            throw std::runtime_error(
   148	                "invalid_persisted_recommendation_campaign_materialization_order");
   149	    return value;
   150	}
   151	
   152	bool Matches(
   153	    const PersistedRecommendationCampaignMaterialization& persisted,
   154	    const RecommendationCampaignMaterializationEvidence& evidence)
   155	{
   156	    if (persisted.campaignApprovalId != evidence.campaignApprovalId ||
   157	        persisted.contractVersion != evidence.materializationContractVersion ||
   158	        persisted.identityCanonical != evidence.materializationIdentityCanonical ||
   159	        persisted.identityHash != evidence.materializationIdentityHash ||
   160	        persisted.materializedBy != evidence.operatorIdentity ||
   161	        persisted.reasonText != evidence.reasonText ||
   162	        persisted.selectedMemberCount != evidence.selectedMemberCount ||
   163	        persisted.members.size() != evidence.members.size())
   164	        return false;
   165	    for (std::size_t index = 0; index < persisted.members.size(); ++index)
   300	    if (!status) throw std::runtime_error("invalid_persisted_recommendation_status");
   301	    request.recommendationStatus = *status;
   302	    request.sourceExperimentId = row["source_experiment_id"].as<long long>();
   303	    request.recommendationSourceExperimentId = request.sourceExperimentId;
   304	    request.recommendationSemanticCanonical =
   305	        row["semantic_configuration_canonical"].as<std::string>();
   306	    request.recommendationSemanticHash = row["semantic_hash"].as<std::string>();
   307	    request.recommendationInvocationCanonical =
   308	        row["invocation_configuration_canonical"].as<std::string>();
   309	    request.recommendationInvocationHash = row["invocation_hash"].as<std::string>();
   310	    if (row["ranking_bucket"].as<std::string>() != "advisory_ready" ||
   311	        row["ranking_semantic_hash"].as<std::string>() !=
   312	            request.recommendationSemanticHash ||
   313	        row["ranking_source_experiment_id"].as<long long>() !=
   314	            request.sourceExperimentId ||
   315	        row["evaluation_recommendation_id"].as<long long>() !=
   316	            request.recommendationId ||
   317	        row["evaluation_semantic_canonical"].as<std::string>() !=
   318	            request.recommendationSemanticCanonical ||
   319	        row["evaluation_semantic_hash"].as<std::string>() !=
   320	            request.recommendationSemanticHash)
   321	        throw std::runtime_error(
   322	            "inconsistent_persisted_recommendation_campaign_member_provenance");
   323	    if (!row["recommendation_review_event_id"].is_null())
   324	    {
   325	        if (row["review_semantic_canonical"].as<std::string>() !=
   326	                request.recommendationSemanticCanonical ||
   327	            row["review_semantic_hash"].as<std::string>() !=
   328	                request.recommendationSemanticHash ||
   329	            row["review_source_experiment_id"].as<long long>() !=
   330	                request.sourceExperimentId)
   331	            throw std::runtime_error(
   332	                "inconsistent_persisted_recommendation_review_provenance");
   333	        auto& authorization = request.reviewAuthorization;
   334	        authorization.present = true;
   335	        authorization.recommendationId = request.recommendationId;
   336	        const auto action = ParseRecommendationReviewAction(
   337	            row["review_action"].as<std::string>());
   338	        const auto resulting = ParseRecommendationStatus(
   339	            row["review_resulting_status"].as<std::string>());
   340	        if (!action || !resulting)
   341	            throw std::runtime_error("invalid_persisted_recommendation_review");
   342	        authorization.latestAction = *action;
   343	        authorization.resultingStatus = *resulting;
   344	        authorization.latestActionEffective = true;
   345	        authorization.superseded = false;
   346	        authorization.authorizationCanonical = AuthorizationCanonical(row);
   347	        authorization.authorizationHash = RecommendationCanonicalHash(
   348	            authorization.authorizationCanonical);
   349	    }
   350	    auto& evaluation = request.evaluation;
   351	    evaluation.state = row["evaluation_run_status"].as<std::string>() == "completed"
   352	        ? RecommendationConversionEvidenceState::completed
   353	        : RecommendationConversionEvidenceState::pending;
   354	    evaluation.valid = evaluation.state ==
   355	                           RecommendationConversionEvidenceState::completed;
   356	    evaluation.recommendationId = request.recommendationId;
   357	    evaluation.sourceExperimentId =
   358	        row["evaluation_source_experiment_id"].as<long long>();
   359	    evaluation.eligibility = row["eligibility"].as<std::string>() == "eligible"
   360	        ? RecommendationEligibility::eligible : RecommendationEligibility::ineligible;
   361	    const auto disposition = ParseRecommendationEvaluationDisposition(
   362	        row["disposition"].as<std::string>());
   363	    if (!disposition)
   364	        throw std::runtime_error("invalid_persisted_recommendation_disposition");
   365	    evaluation.disposition = *disposition;
   366	    evaluation.evaluationIdentityCanonical =
   367	        row["evaluation_identity_canonical"].as<std::string>();
   368	    evaluation.evaluationIdentityHash =
   369	        row["evaluation_identity_hash"].as<std::string>();
   370	    evaluation.evaluationPolicyCanonical =
   371	        row["evaluation_policy_canonical"].as<std::string>();
   372	    evaluation.evaluationPolicyHash =
   373	        row["evaluation_policy_hash"].as<std::string>();
   374	    evaluation.scoringPolicyHash =
   375	        row["evaluation_scoring_hash"].as<std::string>();
   376	    auto& score = request.score;
   377	    if (!row["score_recommendation_id"].is_null())
   378	    {
   379	        if (row["score_semantic_canonical"].as<std::string>() !=
   380	                request.recommendationSemanticCanonical ||
   381	            row["score_source_experiment_id"].as<long long>() !=
   382	                request.sourceExperimentId)
   383	            throw std::runtime_error(
   384	                "inconsistent_persisted_recommendation_score_provenance");
   385	        score.state = row["score_run_status"].as<std::string>() == "completed"
   386	            ? RecommendationConversionEvidenceState::completed
   387	            : RecommendationConversionEvidenceState::pending;
   388	        score.valid = score.state == RecommendationConversionEvidenceState::completed &&
   389	                      row["score_status"].as<std::string>() == "scored";
   390	        score.recommendationId =
   391	            row["score_recommendation_id"].as<long long>();
   392	        score.finalScore = row["final_score"].as<double>();
   393	        score.scoringPolicyCanonical =
   394	            row["scoring_policy_canonical"].as<std::string>();
   395	        score.scoringPolicyHash =
   396	            row["scoring_policy_hash"].as<std::string>();
   397	    }
   398	    auto& invocation = request.sourceInvocation;
   399	    if (!row["date_mapping_valid"].as<bool>())
   400	        throw std::runtime_error(
   401	            "invalid_persisted_recommendation_source_date_mapping");
   402	    invocation.configuration.symbol = row["symbol"].as<std::string>();
   403	    invocation.configuration.predictionHorizon =
   404	        row["prediction_horizon"].as<int>();
   405	    invocation.configuration.labelThreshold =
   406	        row["c_next_threshold"].as<double>();
   407	    invocation.configuration.coreLrMult = OptionalValue<double>(row, "core_lr_mult");
   408	    invocation.configuration.headLrMult = OptionalValue<double>(row, "head_lr_mult");
   409	    invocation.configuration.targetEpochs = row["target_epochs"].as<int>();
   410	    invocation.configuration.trainStartDate = row["train_start_date"].as<std::string>();
   411	    invocation.configuration.trainEndDate = row["train_end_date"].as<std::string>();
   412	    invocation.configuration.inferStartDate = OptionalValue<std::string>(row, "infer_start_date");
   413	    invocation.configuration.inferEndDate = OptionalValue<std::string>(row, "infer_end_date");
   414	    invocation.checkpointInterval = row["checkpoint_interval"].as<int>();
   415	    invocation.resumeModelId = OptionalValue<long long>(row, "resume_model_id");
   416	    request.mutations.push_back({
   417	        row["changed_parameter"].as<std::string>(),
   418	        row["source_value_canonical"].as<std::string>(),
   419	        row["proposed_value_canonical"].as<std::string>()});
   420	    return request;
   421	}
   422	
   423	std::optional<PersistedRecommendationCampaignMaterialization>
   424	FindRecommendationCampaignMaterializationByApproval(
   425	    pqxx::transaction_base& transaction,
   426	    long long campaignApprovalId)
   427	{
   428	    if (campaignApprovalId <= 0)
   429	        throw std::invalid_argument(
   430	            "recommendation_campaign_materialization_approval_id_invalid");
   431	    const auto rows = transaction.exec(
   432	        "SELECT " + MaterializationColumns() + " FROM "
   433	        "experiment_recommendation_campaign_materialization WHERE "
   434	        "recommendation_campaign_approval_id=$1;",
   435	        pqxx::params{campaignApprovalId});
   436	    if (rows.empty()) return std::nullopt;
   437	    return MapMaterialization(transaction, rows.one_row());
   438	}
   439	
   440	RecommendationCampaignMaterializationPersistResult
   441	PersistRecommendationCampaignMaterialization(
   442	    pqxx::transaction_base& transaction,
   443	    const RecommendationCampaignMaterializationEvidence& evidence)
   444	{
   445	    ValidateRecommendationCampaignMaterializationEvidence(evidence);
   446	    transaction.exec(
   447	        "SELECT pg_advisory_xact_lock(hashtextextended($1, "
   448	        "-7046029254386353131));",
   449	        pqxx::params{evidence.approval.approvalIdentityHash});
   450	    if (auto existing = FindRecommendationCampaignMaterializationByApproval(
   451	            transaction, evidence.campaignApprovalId))
   452	    {
   453	        if (!Matches(*existing, evidence))
   454	            throw std::runtime_error(
   455	                "recommendation_campaign_materialization_conflict");
   456	        return {RecommendationCampaignMaterializationPersistOutcome::existingIdentical,
   457	                std::move(*existing), 0, evidence.selectedMemberCount};
   458	    }
   459	
   460	    std::vector<std::size_t> order(evidence.members.size());
   461	    for (std::size_t i = 0; i < order.size(); ++i) order[i] = i;
   462	    std::sort(order.begin(), order.end(), [&](std::size_t left, std::size_t right) {
   463	        const auto& a = evidence.members[left].proposal;
   464	        const auto& b = evidence.members[right].proposal;
   465	        return std::tie(a.conversionIdentityHash, a.conversionIdentityCanonical) <
   466	               std::tie(b.conversionIdentityHash, b.conversionIdentityCanonical);
   467	    });
   468	    std::vector<RecommendationConversionProposalPersistResult> proposals(
   469	        evidence.members.size());
   470	    int created = 0;

exec
/bin/bash -lc 'tail -n 140 Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp && clang++ -std=c++20 -Wall -Wextra -Wpedantic -Wconversion -Werror -I Sources -I Headers -I Common Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp Sources/ExperimentRecommendation.cpp -Wno-error=sign-conversion -o /tmp/step5a-review-test-werror && /tmp/step5a-review-test-werror' in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 3166ms:
Sources/ExperimentRecommendation.cpp:166:35: warning: implicit conversion changes signedness: 'const char' to 'const unsigned char' [-Wsign-conversion]
  166 |     for (const unsigned char byte : canonicalText)
      |                              ~~~~ ^
1 warning generated.

exec
/bin/bash -lc "rg -n \"kMaximumRecommendationComparisonComponents|kMaximumRecommendationRankingInputs|constexpr.*Maximum\" Sources/ExperimentRecommendation*.hpp | sed -n '1,240p' && rg -n \"IdentityCanonicalMaximum|size\\(\\) >.*Maximum|text.*maximum\" Sources/ExperimentRecommendation*.cpp | sed -n '1,280p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
Sources/ExperimentRecommendationCampaignActivation.hpp:13:inline constexpr int kMaximumRecommendationCampaignActivationMembers = 1000;
Sources/ExperimentRecommendationCampaignApprovalRepository.hpp:14:inline constexpr int kMaximumRecommendationCampaignApprovalListLimit = 1000;
Sources/ExperimentRecommendationCampaignApproval.hpp:13:inline constexpr std::size_t kRecommendationCampaignApprovalReviewerMaximum = 200;
Sources/ExperimentRecommendationCampaignApproval.hpp:14:inline constexpr std::size_t kRecommendationCampaignApprovalReasonMaximum = 2000;
Sources/ExperimentRecommendationCampaignApproval.hpp:15:inline constexpr std::size_t kRecommendationCampaignApprovalHashMaximum = 256;
Sources/ExperimentRecommendationCampaignExecution.hpp:13:inline constexpr int kMaximumRecommendationCampaignExecutionMembers = 1000;
Sources/ExperimentRecommendationCampaignHandoff.hpp:13:inline constexpr int kMaximumRecommendationCampaignHandoffListLimit = 1000;
Sources/ExperimentRecommendationCampaignLaunch.hpp:14:inline constexpr int kMaximumRecommendationCampaignLaunchMembers = 1000;
Sources/ExperimentRecommendationCampaignProposalReview.hpp:13:inline constexpr int kMaximumRecommendationCampaignProposalReviewMembers = 1000;
Sources/ExperimentRecommendationCampaignMaterializationRepository.hpp:15:inline constexpr int kMaximumRecommendationCampaignMaterializationListLimit =
Sources/ExperimentRecommendationCampaignPlanning.hpp:13:inline constexpr int kDefaultRecommendationCampaignMaximumSelected = 10;
Sources/ExperimentRecommendationCampaignPlanning.hpp:14:inline constexpr int kDefaultRecommendationCampaignMaximumCandidates = 100;
Sources/ExperimentRecommendationCampaignPlanning.hpp:15:inline constexpr int kMaximumRecommendationCampaignCandidates = 1000;
Sources/ExperimentRecommendationCampaignStatus.hpp:11:inline constexpr int kMaximumRecommendationCampaignStatusMembers = 1000;
Sources/ExperimentRecommendationConversionWorkflow.hpp:11:inline constexpr int kMaximumRecommendationConversionWorkflowListLimit = 1000;
Sources/ExperimentRecommendationRanking.hpp:12:inline constexpr int kMaximumRecommendationRankingInputs = 1000;
Sources/ExperimentRecommendationRanking.hpp:13:inline constexpr int kMaximumRecommendationRankingMembers = 1000;
Sources/ExperimentRecommendationRanking.hpp:14:inline constexpr int kMaximumRecommendationRankingListLimit = 1000;
Sources/ExperimentRecommendationRanking.hpp:15:inline constexpr int kMaximumRecommendationComparisonComponents = 100;
Sources/ExperimentRecommendationReview.hpp:12:inline constexpr std::size_t kRecommendationReviewReasonCodeMaximum = 64;
Sources/ExperimentRecommendationReview.hpp:13:inline constexpr std::size_t kRecommendationReviewReasonMaximum = 2000;
Sources/ExperimentRecommendationReview.hpp:14:inline constexpr std::size_t kRecommendationReviewerMaximum = 200;
Sources/ExperimentRecommendationReview.hpp:15:inline constexpr std::size_t kRecommendationReviewNoteMaximum = 2000;
Sources/ExperimentRecommendationConversionRepository.hpp:14:inline constexpr int kMaximumRecommendationConversionProposalListLimit = 1000;
Sources/ExperimentRecommendationCampaignApproval.cpp:334:            kRecommendationCampaignApprovalIdentityCanonicalMaximum,
Sources/ExperimentRecommendationCampaignMaterialization.cpp:235:            kRecommendationCampaignMaterializationIdentityCanonicalMaximum ||
Sources/ExperimentRecommendationCampaignPlanningRepository.cpp:180:    if (rows.size() > kMaximumRecommendationCampaignCandidates)
Sources/ExperimentRecommendationReview.cpp:127:    if (normalized.reasonCode.size() > kRecommendationReviewReasonCodeMaximum)

codex
I found three contract-level issues worth correcting before Step 5b:

- A failed or cancelled member can currently omit its downstream experiment identity and still be reported as consistent, even though those terminal states are impossible without an executed experiment.
- Reused expected experiment links are treated as corruption of materialized membership and abort the whole assessment. That ownership is wrong: the materialization can remain trustworthy while downstream execution provenance is inconsistent, so affected members should be retained and diagnosed.
- Step 5b has no way to carry a trustworthy member identity plus an already-known inconsistent lifecycle/workflow snapshot into Step 5a; mapping it to `Unknown` would incorrectly produce a consistent/not-ready result.

I’m correcting those boundaries, retaining unexpected lifecycle/result payloads as diagnosed member evidence instead of throwing them away, adding model-reuse validation, and hardening metric summary cardinalities against signed overflow.
The independent review identified downstream-provenance ownership and lifecycle-consistency holes that need narrow contract fixes before verification.
  ✓ Inspect repository rules, worktree state, and identify the Phase 5 Step 5a implementation/test surface
  ✓ Review architecture, lifecycle/domain invariants, validation, determinism, serialization, and Step 5b compatibility
  → Implement narrowly scoped corrections and add missing tests
  • Run focused tests, sanitizers, nearby regressions, and incremental Xcode Release build
  • Audit final diff and deliver commit-readiness assessment
apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..74d16af1635e29d91d9db1a26306f4008590505f
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -202,6 +203,8 @@
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,7 +216,11 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
@@ -227,12 +234,16 @@
 
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..74d16af1635e29d91d9db1a26306f4008590505f
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -202,6 +203,8 @@
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,7 +216,11 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
@@ -227,12 +234,16 @@
 
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 

exec
/bin/bash -lc "nl -ba Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp | sed -n '185,275p' && nl -ba Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp | sed -n '320,395p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
   185	        std::optional<RecommendationCampaignOutcomeAssessmentComparisonContext>
   186	            context,
   187	        RecommendationCampaignOutcomeAssessmentMetricCollection metrics);
   188	    bool operator==(
   189	        const RecommendationCampaignOutcomeAssessmentResultEvidence&) const =
   190	        default;
   191	};
   192	
   193	enum class RecommendationCampaignOutcomeAssessmentLifecycleState
   194	{
   195	    NotTerminal,
   196	    Succeeded,
   197	    Failed,
   198	    Cancelled,
   199	    Unknown
   200	};
   201	
   202	enum class RecommendationCampaignOutcomeAssessmentConsistencyState
   203	{
   204	    Consistent,
   205	    Inconsistent
   206	};
   207	
   208	struct RecommendationCampaignOutcomeAssessmentMemberEvidence
   209	{
   210	    const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
   211	    const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
   212	    const RecommendationCampaignOutcomeAssessmentConsistencyState
   213	        inputConsistency;
   214	    const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
   215	        sourceEvidence;
   216	    const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
   217	        resultEvidence;
   218	
   219	    RecommendationCampaignOutcomeAssessmentMemberEvidence(
   220	        RecommendationCampaignOutcomeAssessmentMemberIdentity identity,
   221	        RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle,
   222	        std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
   223	            sourceEvidence,
   224	        std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
   225	            resultEvidence,
   226	        RecommendationCampaignOutcomeAssessmentConsistencyState
   227	            inputConsistency =
   228	                RecommendationCampaignOutcomeAssessmentConsistencyState::
   229	                    Consistent);
   230	    bool operator==(
   231	        const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
   232	        default;
   233	};
   234	
   235	enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
   236	{
   237	    InputEvidenceInconsistent,
   238	    SourceExperimentIdentityMismatch,
   239	    ExpectedExperimentIdentityMissing,
   240	    ExpectedExperimentIdentityReused,
   241	    ResultEvidenceUnexpectedForLifecycle,
   242	    ResultEvidenceMissing,
   243	    ResultExperimentIdentityMissing,
   244	    ResultExperimentIdentityMismatch,
   245	    ResultModelIdentityMissing,
   246	    ResultModelIdentityReused,
   247	    ResultIdentityMissing,
   248	    ResultContextMissing,
   249	    ResultExperimentReused,
   250	    ResultIdentityReused,
   251	    NumericDeltaNonFinite
   252	};
   253	
   254	enum class RecommendationCampaignOutcomeAssessmentMemberOutcome
   255	{
   256	    NotReady,
   257	    SucceededComparable,
   258	    SucceededContextChanged,
   259	    SucceededMetricGap,
   260	    TerminalFailed,
   261	    TerminalCancelled,
   262	    Inconsistent
   263	};
   264	
   265	enum class RecommendationCampaignOutcomeAssessmentMetricComparisonClassification
   266	{
   267	    Comparable,
   268	    ContextChanged,
   269	    MissingSourceMetric,
   270	    MissingResultMetric,
   271	    MetricValueUnavailable,
   272	    Unsupported
   273	};
   274	
   275	enum class RecommendationCampaignOutcomeAssessmentContextDifference
   320	
   321	enum class RecommendationCampaignOutcomeAssessmentAggregateOutcome
   322	{
   323	    Inconsistent,
   324	    HasFailures,
   325	    HasCancellations,
   326	    NotReady,
   327	    SucceededContextChanged,
   328	    SucceededMetricGap,
   329	    SucceededComparable
   330	};
   331	
   332	struct RecommendationCampaignOutcomeAssessmentSummary
   333	{
   334	    const RecommendationCampaignOutcomeAssessmentAggregateOutcome outcome;
   335	
   336	    const int memberCount;
   337	    const int notTerminalLifecycleCount;
   338	    const int succeededLifecycleCount;
   339	    const int failedLifecycleCount;
   340	    const int cancelledLifecycleCount;
   341	    const int unknownLifecycleCount;
   342	    const int consistentMemberCount;
   343	    const int inconsistentMemberCount;
   344	
   345	    const int notReadyMemberCount;
   346	    const int succeededComparableMemberCount;
   347	    const int succeededContextChangedMemberCount;
   348	    const int succeededMetricGapMemberCount;
   349	    const int terminalFailedMemberCount;
   350	    const int terminalCancelledMemberCount;
   351	    const int inconsistentOutcomeMemberCount;
   352	
   353	    const std::size_t metricComparisonCount;
   354	    const std::size_t comparableMetricCount;
   355	    const std::size_t contextChangedMetricCount;
   356	    const std::size_t missingSourceMetricCount;
   357	    const std::size_t missingResultMetricCount;
   358	    const std::size_t metricValueUnavailableCount;
   359	    const std::size_t unsupportedMetricCount;
   360	    const std::size_t positiveDeltaCount;
   361	    const std::size_t zeroDeltaCount;
   362	    const std::size_t negativeDeltaCount;
   363	
   364	    RecommendationCampaignOutcomeAssessmentSummary(
   365	        const RecommendationCampaignOutcomeAssessmentSummary&) = default;
   366	    RecommendationCampaignOutcomeAssessmentSummary(
   367	        RecommendationCampaignOutcomeAssessmentSummary&&) = default;
   368	    bool operator==(
   369	        const RecommendationCampaignOutcomeAssessmentSummary&) const = default;
   370	
   371	private:
   372	    RecommendationCampaignOutcomeAssessmentSummary(
   373	        RecommendationCampaignOutcomeAssessmentAggregateOutcome outcome,
   374	        int memberCount,
   375	        int notTerminalLifecycleCount,
   376	        int succeededLifecycleCount,
   377	        int failedLifecycleCount,
   378	        int cancelledLifecycleCount,
   379	        int unknownLifecycleCount,
   380	        int consistentMemberCount,
   381	        int inconsistentMemberCount,
   382	        int notReadyMemberCount,
   383	        int succeededComparableMemberCount,
   384	        int succeededContextChangedMemberCount,
   385	        int succeededMetricGapMemberCount,
   386	        int terminalFailedMemberCount,
   387	        int terminalCancelledMemberCount,
   388	        int inconsistentOutcomeMemberCount,
   389	        std::size_t metricComparisonCount,
   390	        std::size_t comparableMetricCount,
   391	        std::size_t contextChangedMetricCount,
   392	        std::size_t missingSourceMetricCount,
   393	        std::size_t missingResultMetricCount,
   394	        std::size_t metricValueUnavailableCount,
   395	        std::size_t unsupportedMetricCount,

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..7131552573320d12b9c7adf0dbe1babedf0de1cb
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..7131552573320d12b9c7adf0dbe1babedf0de1cb
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..96d6a5da31521972c49150947642658d7e66c52f
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..96d6a5da31521972c49150947642658d7e66c52f
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..17643542c7669f347f6be22c952b2ee1452d55ad
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..17643542c7669f347f6be22c952b2ee1452d55ad
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b7e4710f473ddb70e9a89518fdb10d2d9985ed36
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b7e4710f473ddb70e9a89518fdb10d2d9985ed36
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..1e6bd3e9796fe43da66b09609c2efa54e1e0f241
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1306,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1325,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..1e6bd3e9796fe43da66b09609c2efa54e1e0f241
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1306,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1325,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..1e6bd3e9796fe43da66b09609c2efa54e1e0f241
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1306,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1325,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..71b9248f611cd2d23b61a0f322f1ad70f9a32e84
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +432,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..1e6bd3e9796fe43da66b09609c2efa54e1e0f241
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1306,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1325,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..71b9248f611cd2d23b61a0f322f1ad70f9a32e84
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +432,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..1e6bd3e9796fe43da66b09609c2efa54e1e0f241
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1306,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1325,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..9a6e5c2f30d9af986128f90d11bbfd3ebffa6f74
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +432,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +566,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..1e6bd3e9796fe43da66b09609c2efa54e1e0f241
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1306,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1325,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..9a6e5c2f30d9af986128f90d11bbfd3ebffa6f74
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +432,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +566,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..1e6bd3e9796fe43da66b09609c2efa54e1e0f241
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1306,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1325,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..40a184dc2cdd9e9ebb469c37e8ed7c66925e8cca
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +432,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +566,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -689,18 +741,29 @@
             Materialization(2), "2026-07-19T22:00:00Z",
             {Succeeded(1), Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            Materialization(1), "", {Succeeded(1)});
     });
     AssertInvalidArgument([]
     {
-        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
-            Materialization(1), "", {Succeeded(1)});
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
     });
 
     const MemberEvidence overflowingDelta(

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..1e6bd3e9796fe43da66b09609c2efa54e1e0f241
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1306,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1325,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..40a184dc2cdd9e9ebb469c37e8ed7c66925e8cca
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +432,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +566,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -689,18 +741,29 @@
             Materialization(2), "2026-07-19T22:00:00Z",
             {Succeeded(1), Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            Materialization(1), "", {Succeeded(1)});
     });
     AssertInvalidArgument([]
     {
-        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
-            Materialization(1), "", {Succeeded(1)});
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
     });
 
     const MemberEvidence overflowingDelta(

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..1e6bd3e9796fe43da66b09609c2efa54e1e0f241
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1306,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1325,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..40a184dc2cdd9e9ebb469c37e8ed7c66925e8cca
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +432,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +566,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -689,18 +741,29 @@
             Materialization(2), "2026-07-19T22:00:00Z",
             {Succeeded(1), Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            Materialization(1), "", {Succeeded(1)});
     });
     AssertInvalidArgument([]
     {
-        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
-            Materialization(1), "", {Succeeded(1)});
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
     });
 
     const MemberEvidence overflowingDelta(
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..e1814c70b4d42a3194478cc7a36deec26b0ca5ca
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,13 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..1e6bd3e9796fe43da66b09609c2efa54e1e0f241
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1306,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1325,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..40a184dc2cdd9e9ebb469c37e8ed7c66925e8cca
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +432,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +566,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -689,18 +741,29 @@
             Materialization(2), "2026-07-19T22:00:00Z",
             {Succeeded(1), Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            Materialization(1), "", {Succeeded(1)});
     });
     AssertInvalidArgument([]
     {
-        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
-            Materialization(1), "", {Succeeded(1)});
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
     });
 
     const MemberEvidence overflowingDelta(
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..e1814c70b4d42a3194478cc7a36deec26b0ca5ca
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,13 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

exec
/bin/bash -lc 'clang++ -std=c++20 -Wall -Wextra -Wpedantic -Werror -I Sources -I Headers -I Common Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp Sources/ExperimentRecommendation.cpp -o /tmp/step5a-review-test && /tmp/step5a-review-test' in /Volumes/Developer SSD/ExpertAdvisor
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..1e6bd3e9796fe43da66b09609c2efa54e1e0f241
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1306,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1325,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..40a184dc2cdd9e9ebb469c37e8ed7c66925e8cca
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +432,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +566,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -689,18 +741,29 @@
             Materialization(2), "2026-07-19T22:00:00Z",
             {Succeeded(1), Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            Materialization(1), "", {Succeeded(1)});
     });
     AssertInvalidArgument([]
     {
-        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
-            Materialization(1), "", {Succeeded(1)});
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
     });
 
     const MemberEvidence overflowingDelta(
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..e1814c70b4d42a3194478cc7a36deec26b0ca5ca
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,13 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

 succeeded in 3000ms:
exec
/bin/bash -lc "git diff --check && git diff --cached --check && git status --short && git diff --stat && git diff -- Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst | sed -n '1,1200p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
 M ExpertAdvisor.xcodeproj/project.pbxproj
?? Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
?? Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
?? Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
?? docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
 ExpertAdvisor.xcodeproj/project.pbxproj | 8 ++++++++
 1 file changed, 8 insertions(+)

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..1e6bd3e9796fe43da66b09609c2efa54e1e0f241
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1306,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1325,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..40a184dc2cdd9e9ebb469c37e8ed7c66925e8cca
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +432,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +566,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -689,18 +741,29 @@
             Materialization(2), "2026-07-19T22:00:00Z",
             {Succeeded(1), Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            Materialization(1), "", {Succeeded(1)});
     });
     AssertInvalidArgument([]
     {
-        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
-            Materialization(1), "", {Succeeded(1)});
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
     });
 
     const MemberEvidence overflowingDelta(
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..e1814c70b4d42a3194478cc7a36deec26b0ca5ca
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,13 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

exec
/bin/bash -lc "sed -n '130,180p' Sources/ExperimentRecommendation.cpp && sed -n '1,90p' Sources/ExperimentRecommendationCampaignMaterializationRepository.hpp && rg -n \"approvalIdentityHash\" Sources/ExperimentRecommendationCampaign* | sed -n '1,200p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
    values.erase(std::unique(values.begin(), values.end()), values.end());
}

template <typename T, typename Formatter>
std::string SortedJoin(std::vector<T> values, Formatter formatter)
{
    SortAndDeduplicate(values);
    std::ostringstream out;
    out.imbue(std::locale::classic());
    for (size_t index = 0; index < values.size(); ++index)
    {
        if (index != 0) out << ':';
        out << formatter(values[index]);
    }
    return out.str();
}

std::string SortedStringSetText(const std::set<std::string>& values)
{
    std::ostringstream out;
    out.imbue(std::locale::classic());
    bool first = true;
    for (const std::string& value : values)
    {
        if (!first) out << ':';
        first = false;
        out << value;
    }
    return out.str();
}

std::string StableRecommendationHash(const std::string& canonicalText)
{
    // Canonical text, not this compact accelerator, is authoritative identity.
    // Future persistence must store and compare both after a hash match.
    std::uint64_t hash = 14695981039346656037ULL;
    for (const unsigned char byte : canonicalText)
    {
        hash ^= byte;
        hash *= 1099511628211ULL;
    }
    constexpr char digits[] = "0123456789abcdef";
    std::array<char, 16> encoded{};
    for (size_t index = 0; index < encoded.size(); ++index)
    {
        const unsigned shift = static_cast<unsigned>(
            (encoded.size() - 1 - index) * 4);
        encoded[index] = digits[(hash >> shift) & 0x0fU];
    }
    return "fnv1a64:" + std::string(encoded.begin(), encoded.end());
}
#pragma once

#include "ExperimentRecommendationCampaignMaterialization.hpp"
#include "ExperimentRecommendationConversionRepository.hpp"

#include <optional>
#include <string>
#include <vector>

#include <pqxx/pqxx>

namespace EA::ExperimentRecommendation
{

inline constexpr int kMaximumRecommendationCampaignMaterializationListLimit =
    1000;

struct PersistedRecommendationCampaignMaterializationMember
{
    long long materializationMemberId = -1;
    int memberOrdinal = 0;
    long long rankingMemberId = -1;
    long long recommendationId = -1;
    long long sourceExperimentId = -1;
    int rankingPosition = 0;
    std::string selectedMemberIdentityCanonical;
    std::string selectedMemberIdentityHash;
    long long conversionProposalId = -1;
    std::string proposalIdentityCanonical;
    std::string proposalIdentityHash;
    std::string createdAt;
};

struct PersistedRecommendationCampaignMaterialization
{
    long long materializationId = -1;
    long long campaignApprovalId = -1;
    int contractVersion = 0;
    std::string approvalIdentityHash;
    long long rankingSnapshotId = -1;
    std::string rankingSnapshotIdentityHash;
    std::string planningPolicyHash;
    std::string campaignPlanIdentityHash;
    std::string campaignReviewIdentityHash;
    std::string materializedBy;
    std::string reasonText;
    int selectedMemberCount = 0;
    int initiallyCreatedProposalCount = 0;
    int initiallyReusedProposalCount = 0;
    std::string identityCanonical;
    std::string identityHash;
    std::string createdAt;
    std::vector<PersistedRecommendationCampaignMaterializationMember> members;
};

enum class RecommendationCampaignMaterializationPersistOutcome
{
    recorded,
    existingIdentical
};

struct RecommendationCampaignMaterializationPersistResult
{
    RecommendationCampaignMaterializationPersistOutcome outcome =
        RecommendationCampaignMaterializationPersistOutcome::recorded;
    PersistedRecommendationCampaignMaterialization materialization;
    int newlyCreatedProposalCount = 0;
    int reusedProposalCount = 0;
};

bool RecommendationCampaignMaterializationSchemaExists(
    pqxx::connection& connection);
bool RecommendationCampaignMaterializationSchemaExists(
    pqxx::transaction_base& transaction);

RecommendationConversionRequest
LoadRecommendationCampaignConversionRequest(
    pqxx::transaction_base& transaction,
    long long rankingSnapshotId,
    long long rankingMemberId,
    long long recommendationId);

RecommendationCampaignMaterializationPersistResult
PersistRecommendationCampaignMaterialization(
    pqxx::transaction_base& transaction,
    const RecommendationCampaignMaterializationEvidence& evidence);

std::optional<PersistedRecommendationCampaignMaterialization>
FindRecommendationCampaignMaterialization(
    pqxx::connection& connection,
Sources/ExperimentRecommendationCampaignApproval.hpp:70:    std::string approvalIdentityHash;
Sources/ExperimentRecommendationCampaignApprovalRepository.cpp:92:    value.approvalIdentityHash =
Sources/ExperimentRecommendationCampaignApprovalRepository.cpp:159:           left.approvalIdentityHash == right.approvalIdentityHash;
Sources/ExperimentRecommendationCampaignApprovalRepository.cpp:263:            evidence.approvalIdentityHash}).one_row();
Sources/ExperimentRecommendationCampaignApproval.cpp:273:    evidence.approvalIdentityHash = RecommendationCanonicalHash(
Sources/ExperimentRecommendationCampaignApproval.cpp:337:            evidence.approvalIdentityHash,
Sources/ExperimentRecommendationCampaignApproval.cpp:340:        evidence.approvalIdentityHash != RecommendationCanonicalHash(
Sources/ExperimentRecommendationCampaignApprovalService.cpp:82:           << RecommendationMachineText(value.approvalIdentityHash)
Sources/ExperimentRecommendationCampaignMaterializationRepository.hpp:39:    std::string approvalIdentityHash;
Sources/ExperimentRecommendationCampaignMaterialization.cpp:80:        << LengthText(evidence.approval.approvalIdentityHash)
Sources/ExperimentRecommendationCampaignMaterializationService.cpp:100:           << RecommendationMachineText(value.approvalIdentityHash)
Sources/ExperimentRecommendationCampaignMaterializationService.cpp:210:            reconstructedApproval.approvalIdentityHash !=
Sources/ExperimentRecommendationCampaignMaterializationService.cpp:211:                approval->evidence.approvalIdentityHash ||
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:112:    value.approvalIdentityHash = row[3].as<std::string>();
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:449:        pqxx::params{evidence.approval.approvalIdentityHash});
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:500:            approval.approvalIdentityCanonical, approval.approvalIdentityHash,

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..1e6bd3e9796fe43da66b09609c2efa54e1e0f241
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1306,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1325,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..563e4d8adb412131b05fc5961807ea9653d305a2
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -198,10 +199,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +222,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +350,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +386,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..40a184dc2cdd9e9ebb469c37e8ed7c66925e8cca
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +432,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +566,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -689,18 +741,29 @@
             Materialization(2), "2026-07-19T22:00:00Z",
             {Succeeded(1), Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            Materialization(1), "", {Succeeded(1)});
     });
     AssertInvalidArgument([]
     {
-        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
-            Materialization(1), "", {Succeeded(1)});
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
     });
 
     const MemberEvidence overflowingDelta(
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..e1814c70b4d42a3194478cc7a36deec26b0ca5ca
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,13 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

codex
A second canonical-identity issue surfaced while tracing the persisted materialization shape: the materialization already stores the approval identity hash, but Step 5a’s materialization identity discards it and cross-checks only the numeric approval ID. I’m binding that stored hash into the input type and requiring both approval ID and approval identity hash to match; this prevents two self-consistent but unrelated canonical identities from being combined under the same claimed database ID.
apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..1e6bd3e9796fe43da66b09609c2efa54e1e0f241
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1306,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1325,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..40a184dc2cdd9e9ebb469c37e8ed7c66925e8cca
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +432,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +566,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -689,18 +741,29 @@
             Materialization(2), "2026-07-19T22:00:00Z",
             {Succeeded(1), Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            Materialization(1), "", {Succeeded(1)});
     });
     AssertInvalidArgument([]
     {
-        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
-            Materialization(1), "", {Succeeded(1)});
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
     });
 
     const MemberEvidence overflowingDelta(
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..e1814c70b4d42a3194478cc7a36deec26b0ca5ca
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,13 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..1e6bd3e9796fe43da66b09609c2efa54e1e0f241
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -699,16 +711,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +780,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +827,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1077,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1090,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1141,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1176,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1306,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1325,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..40a184dc2cdd9e9ebb469c37e8ed7c66925e8cca
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +432,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +566,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -689,18 +741,29 @@
             Materialization(2), "2026-07-19T22:00:00Z",
             {Succeeded(1), Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            Materialization(1), "", {Succeeded(1)});
     });
     AssertInvalidArgument([]
     {
-        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
-            Materialization(1), "", {Succeeded(1)});
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
     });
 
     const MemberEvidence overflowingDelta(
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..e1814c70b4d42a3194478cc7a36deec26b0ca5ca
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,13 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..d0ffcbb514eacbaa642790083005c5813f7af81a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1081,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1094,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1145,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1180,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1310,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1329,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..40a184dc2cdd9e9ebb469c37e8ed7c66925e8cca
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +432,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +566,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -689,18 +741,29 @@
             Materialization(2), "2026-07-19T22:00:00Z",
             {Succeeded(1), Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            Materialization(1), "", {Succeeded(1)});
     });
     AssertInvalidArgument([]
     {
-        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
-            Materialization(1), "", {Succeeded(1)});
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
     });
 
     const MemberEvidence overflowingDelta(
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..e1814c70b4d42a3194478cc7a36deec26b0ca5ca
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,13 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..d0ffcbb514eacbaa642790083005c5813f7af81a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1065,7 +1081,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1094,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1145,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1180,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1310,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1329,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..40a184dc2cdd9e9ebb469c37e8ed7c66925e8cca
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +432,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +566,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -689,18 +741,29 @@
             Materialization(2), "2026-07-19T22:00:00Z",
             {Succeeded(1), Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            Materialization(1), "", {Succeeded(1)});
     });
     AssertInvalidArgument([]
     {
-        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
-            Materialization(1), "", {Succeeded(1)});
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
     });
 
     const MemberEvidence overflowingDelta(
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..e1814c70b4d42a3194478cc7a36deec26b0ca5ca
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,13 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..40a184dc2cdd9e9ebb469c37e8ed7c66925e8cca
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +432,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +566,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -689,18 +741,29 @@
             Materialization(2), "2026-07-19T22:00:00Z",
             {Succeeded(1), Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            Materialization(1), "", {Succeeded(1)});
     });
     AssertInvalidArgument([]
     {
-        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
-            Materialization(1), "", {Succeeded(1)});
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
     });
 
     const MemberEvidence overflowingDelta(
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..e1814c70b4d42a3194478cc7a36deec26b0ca5ca
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,13 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..40a184dc2cdd9e9ebb469c37e8ed7c66925e8cca
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -388,6 +388,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +432,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +566,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -689,18 +741,29 @@
             Materialization(2), "2026-07-19T22:00:00Z",
             {Succeeded(1), Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            Materialization(1), "", {Succeeded(1)});
     });
     AssertInvalidArgument([]
     {
-        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
-            Materialization(1), "", {Succeeded(1)});
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
     });
 
     const MemberEvidence overflowingDelta(
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..e1814c70b4d42a3194478cc7a36deec26b0ca5ca
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,13 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..27fd98f7149d661ecc91cb7255bc91e00ee561b4
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -689,18 +742,29 @@
             Materialization(2), "2026-07-19T22:00:00Z",
             {Succeeded(1), Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            Materialization(1), "", {Succeeded(1)});
     });
     AssertInvalidArgument([]
     {
-        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
-            Materialization(1), "", {Succeeded(1)});
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
     });
 
     const MemberEvidence overflowingDelta(
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..e1814c70b4d42a3194478cc7a36deec26b0ca5ca
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,13 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..27fd98f7149d661ecc91cb7255bc91e00ee561b4
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -689,18 +742,29 @@
             Materialization(2), "2026-07-19T22:00:00Z",
             {Succeeded(1), Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            Materialization(1), "", {Succeeded(1)});
     });
     AssertInvalidArgument([]
     {
-        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
-            Materialization(1), "", {Succeeded(1)});
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
     });
 
     const MemberEvidence overflowingDelta(
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..e1814c70b4d42a3194478cc7a36deec26b0ca5ca
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,13 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..e1814c70b4d42a3194478cc7a36deec26b0ca5ca
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,13 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..e1814c70b4d42a3194478cc7a36deec26b0ca5ca
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,13 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..0a426c2e97600e1989a5640b96f41de812f545a7
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,15 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..0a426c2e97600e1989a5640b96f41de812f545a7
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,15 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

exec
/bin/bash -lc 'clang++ -std=c++20 -Wall -Wextra -Wpedantic -Werror -I Sources -I Headers -I Common Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp Sources/ExperimentRecommendation.cpp -o /tmp/step5a-review-test && /tmp/step5a-review-test && git diff --check && git diff --cached --check' in /Volumes/Developer SSD/ExpertAdvisor
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..0a426c2e97600e1989a5640b96f41de812f545a7
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,15 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

 succeeded in 2573ms:
exec
/bin/bash -lc "for f in Tests/ExperimentRecommendationCampaignExecutionTests.cpp Tests/ExperimentRecommendationCampaignActivationTests.cpp Tests/ExperimentRecommendationCampaignLaunchTests.cpp Tests/ExperimentRecommendationCampaignStatusTests.cpp Tests/ExperimentRecommendationCampaignMaterializationTests.cpp; do printf '%s\\n' \""'$f"; sed -n '"'1,18p' \""'$f"; done' in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
Tests/ExperimentRecommendationCampaignExecutionTests.cpp
#include "../Sources/ExperimentRecommendationCampaignExecution.hpp"

#include "../Sources/ExperimentRecommendation.hpp"

#include <cassert>
#include <functional>
#include <iostream>
#include <stdexcept>
#include <string>

using namespace EA::ExperimentRecommendation;

namespace
{

RecommendationCampaignExecutionRequest Request(bool dryRun = false)
{
    return {17, dryRun};
Tests/ExperimentRecommendationCampaignActivationTests.cpp
#include "../Sources/ExperimentRecommendationCampaignActivation.hpp"

#include "../Sources/ExperimentRecommendation.hpp"
#include "../Sources/ExperimentRecommendationConversionActivation.hpp"

#include <cassert>
#include <functional>
#include <iostream>
#include <stdexcept>
#include <string>

using namespace EA::ExperimentRecommendation;

namespace
{

RecommendationCampaignActivationRequest Request(bool dryRun = false)
{
Tests/ExperimentRecommendationCampaignLaunchTests.cpp
#include "../Sources/ExperimentRecommendationCampaignLaunch.hpp"
#include "../Sources/ExperimentRecommendation.hpp"
#include "../Sources/ExperimentRecommendationConversionActivation.hpp"

#include <cassert>
#include <functional>
#include <iostream>
#include <stdexcept>
#include <string>

using namespace EA::ExperimentRecommendation;

namespace
{

RecommendationCampaignLaunchInput BaseInput(bool existingExecutions)
{
    RecommendationCampaignLaunchInput input;
Tests/ExperimentRecommendationCampaignStatusTests.cpp
#include "../Sources/ExperimentRecommendationCampaignStatus.hpp"
#include "../Sources/ExperimentRecommendation.hpp"

#include <algorithm>
#include <cassert>
#include <stdexcept>

using namespace EA::ExperimentRecommendation;

namespace
{

RecommendationCampaignStatusMemberInput Member(int ordinal)
{
    RecommendationCampaignStatusMemberInput member;
    member.memberOrdinal = ordinal;
    member.materializationMemberId = 100 + ordinal;
    member.recommendationId = 200 + ordinal;
Tests/ExperimentRecommendationCampaignMaterializationTests.cpp
#include "../Sources/ExperimentRecommendationCampaignMaterialization.hpp"

#include "../Sources/ExperimentRecommendation.hpp"

#include <cassert>
#include <stdexcept>

using namespace EA::ExperimentRecommendation;

namespace
{

RecommendationCampaignCandidateInput Candidate(long long id, int position)
{
    RecommendationCampaignCandidateInput value;
    value.rankingSnapshotId = 7;
    value.rankingSnapshotIdentityCanonical = "materialization-snapshot";
    value.rankingSnapshotIdentityHash = RecommendationCanonicalHash(

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..0a426c2e97600e1989a5640b96f41de812f545a7
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,15 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

exec
/bin/bash -lc "for f in Sources/ExperimentRecommendationCampaignExecution.cpp Sources/ExperimentRecommendationCampaignActivation.cpp Sources/ExperimentRecommendationCampaignLaunch.cpp Sources/ExperimentRecommendationCampaignStatus.cpp Sources/ExperimentRecommendationCampaignMaterialization.cpp; do printf '%s\\n' \""'$f"; sed -n '"'1,16p' \""'$f"; done' in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
Sources/ExperimentRecommendationCampaignExecution.cpp
#include "ExperimentRecommendationCampaignExecution.hpp"

#include "ExperimentRecommendation.hpp"

#include <locale>
#include <set>
#include <sstream>
#include <stdexcept>

namespace EA::ExperimentRecommendation
{
namespace
{

std::string LengthText(const std::string& value)
{
Sources/ExperimentRecommendationCampaignActivation.cpp
#include "ExperimentRecommendationCampaignActivation.hpp"

#include "ExperimentRecommendation.hpp"
#include "ExperimentRecommendationConversionActivation.hpp"

#include <locale>
#include <set>
#include <sstream>
#include <stdexcept>

namespace EA::ExperimentRecommendation
{
namespace
{

std::string LengthText(const std::string& value)
Sources/ExperimentRecommendationCampaignLaunch.cpp
#include "ExperimentRecommendationCampaignLaunch.hpp"

#include "ExperimentRecommendation.hpp"
#include "ExperimentRecommendationConversionActivation.hpp"

#include <locale>
#include <map>
#include <set>
#include <sstream>
#include <stdexcept>

namespace EA::ExperimentRecommendation
{
namespace
{

Sources/ExperimentRecommendationCampaignStatus.cpp
#include "ExperimentRecommendationCampaignStatus.hpp"

#include "ExperimentRecommendation.hpp"
#include "ExperimentRecommendationCampaignMaterialization.hpp"

#include <algorithm>
#include <locale>
#include <set>
#include <sstream>
#include <stdexcept>

namespace EA::ExperimentRecommendation
{
namespace
{

Sources/ExperimentRecommendationCampaignMaterialization.cpp
#include "ExperimentRecommendationCampaignMaterialization.hpp"

#include "ExperimentRecommendation.hpp"

#include <algorithm>
#include <locale>
#include <set>
#include <sstream>
#include <stdexcept>

namespace EA::ExperimentRecommendation
{
namespace
{

std::string LengthText(const std::string& value)

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..0a426c2e97600e1989a5640b96f41de812f545a7
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,15 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

exec
/bin/bash -lc "rg -o \"[A-Za-z0-9_]+\\(\" Sources/ExperimentRecommendationCampaignActivation.cpp Sources/ExperimentRecommendationCampaignLaunch.cpp | rg \"Activation|Canonical|Campaign\" | sort -u | sed -n '1,240p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
Sources/ExperimentRecommendationCampaignActivation.cpp:BuildRecommendationCampaignActivationPlan(
Sources/ExperimentRecommendationCampaignActivation.cpp:LengthText(
Sources/ExperimentRecommendationCampaignActivation.cpp:NormalizeRecommendationCampaignActivationRequest(
Sources/ExperimentRecommendationCampaignActivation.cpp:OperationCanonical(
Sources/ExperimentRecommendationCampaignActivation.cpp:RecommendationCanonicalHash(
Sources/ExperimentRecommendationCampaignActivation.cpp:classic(
Sources/ExperimentRecommendationCampaignActivation.cpp:empty(
Sources/ExperimentRecommendationCampaignActivation.cpp:imbue(
Sources/ExperimentRecommendationCampaignActivation.cpp:insert(
Sources/ExperimentRecommendationCampaignActivation.cpp:invalid_argument(
Sources/ExperimentRecommendationCampaignActivation.cpp:move(
Sources/ExperimentRecommendationCampaignActivation.cpp:push_back(
Sources/ExperimentRecommendationCampaignActivation.cpp:reserve(
Sources/ExperimentRecommendationCampaignActivation.cpp:size(
Sources/ExperimentRecommendationCampaignActivation.cpp:str(
Sources/ExperimentRecommendationCampaignActivation.cpp:to_string(
Sources/ExperimentRecommendationCampaignLaunch.cpp:ActivationMembersByOrdinal(
Sources/ExperimentRecommendationCampaignLaunch.cpp:BuildRecommendationCampaignLaunchPlan(
Sources/ExperimentRecommendationCampaignLaunch.cpp:LengthText(
Sources/ExperimentRecommendationCampaignLaunch.cpp:NormalizeRecommendationCampaignLaunchRequest(
Sources/ExperimentRecommendationCampaignLaunch.cpp:OperationCanonical(
Sources/ExperimentRecommendationCampaignLaunch.cpp:RecommendationCampaignLaunchActivationDispositionText(
Sources/ExperimentRecommendationCampaignLaunch.cpp:RecommendationCampaignLaunchExecutionDispositionText(
Sources/ExperimentRecommendationCampaignLaunch.cpp:RecommendationCampaignLaunchPlanStateText(
Sources/ExperimentRecommendationCampaignLaunch.cpp:RecommendationCanonicalHash(
Sources/ExperimentRecommendationCampaignLaunch.cpp:classic(
Sources/ExperimentRecommendationCampaignLaunch.cpp:emplace(
Sources/ExperimentRecommendationCampaignLaunch.cpp:empty(
Sources/ExperimentRecommendationCampaignLaunch.cpp:end(
Sources/ExperimentRecommendationCampaignLaunch.cpp:find(
Sources/ExperimentRecommendationCampaignLaunch.cpp:imbue(
Sources/ExperimentRecommendationCampaignLaunch.cpp:insert(
Sources/ExperimentRecommendationCampaignLaunch.cpp:invalid_argument(
Sources/ExperimentRecommendationCampaignLaunch.cpp:move(
Sources/ExperimentRecommendationCampaignLaunch.cpp:push_back(
Sources/ExperimentRecommendationCampaignLaunch.cpp:reserve(
Sources/ExperimentRecommendationCampaignLaunch.cpp:size(
Sources/ExperimentRecommendationCampaignLaunch.cpp:str(
Sources/ExperimentRecommendationCampaignLaunch.cpp:to_string(

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..0a426c2e97600e1989a5640b96f41de812f545a7
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,15 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

exec
/bin/bash -lc 'clang++ -std=c++20 -Wall -Wextra -Wpedantic -Werror -I Sources -I Headers -I Common Tests/ExperimentRecommendationCampaignExecutionTests.cpp Sources/ExperimentRecommendationCampaignExecution.cpp Sources/ExperimentRecommendation.cpp -o /tmp/step5a-execution-regression && /tmp/step5a-execution-regression' in /Volumes/Developer SSD/ExpertAdvisor
exec
/bin/bash -lc 'clang++ -std=c++20 -Wall -Wextra -Wpedantic -Werror -I Sources -I Headers -I Common Tests/ExperimentRecommendationCampaignActivationTests.cpp Sources/ExperimentRecommendationCampaignActivation.cpp Sources/ExperimentRecommendation.cpp -o /tmp/step5a-activation-regression && /tmp/step5a-activation-regression' in /Volumes/Developer SSD/ExpertAdvisor
exec
/bin/bash -lc 'clang++ -std=c++20 -Wall -Wextra -Wpedantic -Werror -I Sources -I Headers -I Common Tests/ExperimentRecommendationCampaignLaunchTests.cpp Sources/ExperimentRecommendationCampaignLaunch.cpp Sources/ExperimentRecommendation.cpp -o /tmp/step5a-launch-regression && /tmp/step5a-launch-regression' in /Volumes/Developer SSD/ExpertAdvisor
exec
/bin/bash -lc 'clang++ -std=c++20 -Wall -Wextra -Wpedantic -Werror -I Sources -I Headers -I Common Tests/ExperimentRecommendationCampaignStatusTests.cpp Sources/ExperimentRecommendationCampaignStatus.cpp Sources/ExperimentRecommendation.cpp -o /tmp/step5a-status-regression && /tmp/step5a-status-regression' in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 2396ms:
 succeeded in 2580ms:
Experiment recommendation campaign execution tests passed

 succeeded in 2743ms:
Experiment recommendation campaign activation tests passed

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..0a426c2e97600e1989a5640b96f41de812f545a7
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,15 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

 succeeded in 2887ms:
Experiment recommendation campaign launch tests passed

exec
/bin/bash -lc 'clang++ -std=c++20 -Wall -Wextra -Wpedantic -Werror -I Sources -I Headers -I Common Tests/ExperimentRecommendationCampaignMaterializationTests.cpp Sources/ExperimentRecommendationCampaignMaterialization.cpp Sources/ExperimentRecommendationCampaignApproval.cpp Sources/ExperimentRecommendationCampaignReview.cpp Sources/ExperimentRecommendationCampaignPlanning.cpp Sources/ExperimentRecommendationConversion.cpp Sources/ExperimentRecommendation.cpp -o /tmp/step5a-materialization-regression && /tmp/step5a-materialization-regression' in /Volumes/Developer SSD/ExpertAdvisor
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..0a426c2e97600e1989a5640b96f41de812f545a7
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,15 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

 exited 1 in 3636ms:
Undefined symbols for architecture arm64:
  "EA::ExperimentRecommendation::RecommendationReviewActionText(EA::ExperimentRecommendation::RecommendationReviewAction)", referenced from:
      EA::ExperimentRecommendation::(anonymous namespace)::AuthorizationCanonical(EA::ExperimentRecommendation::RecommendationConversionReviewAuthorization const&) in ExperimentRecommendationConversion-a1c82c.o
  "EA::ExperimentRecommendation::RecommendationRankingBucketText(EA::ExperimentRecommendation::RecommendationRankingBucket)", referenced from:
      EA::ExperimentRecommendation::(anonymous namespace)::ValidOptionalRanking(std::__1::optional<EA::ExperimentRecommendation::RecommendationConversionRankingProvenance> const&) in ExperimentRecommendationConversion-a1c82c.o
  "EA::ExperimentRecommendation::RecommendationMutationParameterText(EA::ExperimentRecommendation::RecommendationMutationParameter)", referenced from:
      EA::ExperimentRecommendation::(anonymous namespace)::ConversionIdentityCanonical(EA::ExperimentRecommendation::RecommendationConversionRequest const&, EA::ExperimentRecommendation::RecommendationMutationParameter, std::__1::basic_string<char, std::__1::char_traits<char>, std::__1::allocator<char>> const&, std::__1::basic_string<char, std::__1::char_traits<char>, std::__1::allocator<char>> const&, std::__1::basic_string<char, std::__1::char_traits<char>, std::__1::allocator<char>> const&, std::__1::basic_string<char, std::__1::char_traits<char>, std::__1::allocator<char>> const&) in ExperimentRecommendationConversion-a1c82c.o
  "EA::ExperimentRecommendation::RecommendationConversionWorkflowStateText(EA::ExperimentRecommendation::RecommendationConversionWorkflowState)", referenced from:
      EA::ExperimentRecommendation::(anonymous namespace)::WorkflowCanonicalText(EA::ExperimentRecommendation::RecommendationCampaignWorkflowEvidence const&) in ExperimentRecommendationCampaignPlanning-70c9e1.o
  "EA::ExperimentRecommendation::RecommendationConversionWorkflowIntegrityText(EA::ExperimentRecommendation::RecommendationConversionWorkflowIntegrity)", referenced from:
      EA::ExperimentRecommendation::(anonymous namespace)::WorkflowCanonicalText(EA::ExperimentRecommendation::RecommendationCampaignWorkflowEvidence const&) in ExperimentRecommendationCampaignPlanning-70c9e1.o
ld: symbol(s) not found for architecture arm64
clang++: error: linker command failed with exit code 1 (use -v to see invocation)

exec
/bin/bash -lc 'rg -n "std::string RecommendationReviewActionText|std::string RecommendationRankingBucketText|std::string RecommendationMutationParameterText|std::string RecommendationConversionWorkflowStateText|std::string RecommendationConversionWorkflowIntegrityText" Sources/*.cpp' in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
Sources/ExperimentRecommendationCandidateGenerator.cpp:189:std::string RecommendationMutationParameterText(
Sources/ExperimentRecommendationConversionWorkflow.cpp:29:std::string RecommendationConversionWorkflowStateText(
Sources/ExperimentRecommendationConversionWorkflow.cpp:83:std::string RecommendationConversionWorkflowIntegrityText(
Sources/ExperimentRecommendationRanking.cpp:366:std::string RecommendationRankingBucketText(RecommendationRankingBucket value)
Sources/ExperimentRecommendationReview.cpp:49:std::string RecommendationReviewActionText(RecommendationReviewAction action)

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..0a426c2e97600e1989a5640b96f41de812f545a7
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,15 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

exec
/bin/bash -lc 'clang++ -std=c++20 -Wall -Wextra -Wpedantic -Werror -I Sources -I Headers -I Common Tests/ExperimentRecommendationCampaignMaterializationTests.cpp Sources/ExperimentRecommendationCampaignMaterialization.cpp Sources/ExperimentRecommendationCampaignApproval.cpp Sources/ExperimentRecommendationCampaignReview.cpp Sources/ExperimentRecommendationCampaignPlanning.cpp Sources/ExperimentRecommendationConversion.cpp Sources/ExperimentRecommendationConversionWorkflow.cpp Sources/ExperimentRecommendationReview.cpp Sources/ExperimentRecommendationRanking.cpp Sources/ExperimentRecommendationCandidateGenerator.cpp Sources/ExperimentRecommendation.cpp -o /tmp/step5a-materialization-regression && /tmp/step5a-materialization-regression' in /Volumes/Developer SSD/ExpertAdvisor
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..bdbd85b21f66aa406a7d782e5492be4887fc7f63
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -390,16 +402,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +540,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +560,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +715,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +784,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +831,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1042,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1083,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1096,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1147,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1182,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1312,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1331,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..0a426c2e97600e1989a5640b96f41de812f545a7
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,15 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

 succeeded in 5457ms:
codex
One final identity hardening is necessary: the repository’s hash helper explicitly documents hashes as compact accelerators and canonical text as authoritative, but the assessment identity currently embeds only upstream hashes. I’m including the length-framed campaign and materialization canonical texts alongside their hashes, so the assessment’s authoritative canonical identity cannot collapse distinct upstream canonical identities merely because their compact hashes collide.
apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..0a426c2e97600e1989a5640b96f41de812f545a7
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,15 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..0a426c2e97600e1989a5640b96f41de812f545a7
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,11 +35,15 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..7e90177a6e53babb71cbfb5cc8ead146a7e6a177
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -691,17 +744,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

exec
/bin/bash -lc 'clang++ -std=c++20 -Wall -Wextra -Wpedantic -Werror -I Sources -I Headers -I Common Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp Sources/ExperimentRecommendation.cpp -o /tmp/step5a-review-test && /tmp/step5a-review-test' in /Volumes/Developer SSD/ExpertAdvisor
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..58596f26af25545d206a5296a844fa022d0b201a
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,28 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::
+                    Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +352,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +388,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

 succeeded in 2267ms:
apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

exec
/bin/bash -lc "nl -ba Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp | sed -n '1,130p' && nl -ba Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp | sed -n '275,475p' && nl -ba Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp | sed -n '520,735p' && nl -ba Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp | sed -n '1020,1215p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
     1	#include "ExperimentRecommendationCampaignOutcomeAssessment.hpp"
     2	
     3	#include "ExperimentRecommendation.hpp"
     4	#include "ExperimentRecommendationCampaignMaterialization.hpp"
     5	
     6	#include <algorithm>
     7	#include <cmath>
     8	#include <locale>
     9	#include <map>
    10	#include <set>
    11	#include <sstream>
    12	#include <stdexcept>
    13	#include <tuple>
    14	#include <utility>
    15	
    16	namespace EA::ExperimentRecommendation
    17	{
    18	namespace
    19	{
    20	
    21	using AggregateOutcome =
    22	    RecommendationCampaignOutcomeAssessmentAggregateOutcome;
    23	using Classification =
    24	    RecommendationCampaignOutcomeAssessmentMetricComparisonClassification;
    25	using Comparison =
    26	    RecommendationCampaignOutcomeAssessmentMetricComparison;
    27	using Consistency = RecommendationCampaignOutcomeAssessmentConsistencyState;
    28	using Context = RecommendationCampaignOutcomeAssessmentComparisonContext;
    29	using Diagnostic = RecommendationCampaignOutcomeAssessmentDiagnosticCode;
    30	using Difference = RecommendationCampaignOutcomeAssessmentContextDifference;
    31	using Lifecycle = RecommendationCampaignOutcomeAssessmentLifecycleState;
    32	using Member = RecommendationCampaignOutcomeAssessmentMember;
    33	using MemberEvidence = RecommendationCampaignOutcomeAssessmentMemberEvidence;
    34	using MemberOutcome = RecommendationCampaignOutcomeAssessmentMemberOutcome;
    35	using Metric = RecommendationCampaignOutcomeAssessmentMetric;
    36	using MetricCollection =
    37	    RecommendationCampaignOutcomeAssessmentMetricCollection;
    38	using MetricSupport =
    39	    RecommendationCampaignOutcomeAssessmentMetricComparisonSupport;
    40	using ResultIdentity =
    41	    RecommendationCampaignOutcomeAssessmentResultIdentity;
    42	
    43	void RequireText(const std::string& value, const char* error)
    44	{
    45	    if (value.empty() || value.find('\0') != std::string::npos)
    46	        throw std::invalid_argument(error);
    47	}
    48	
    49	void RequireOptionalPositive(
    50	    const std::optional<long long>& value,
    51	    const char* error)
    52	{
    53	    if (value && *value <= 0) throw std::invalid_argument(error);
    54	}
    55	
    56	void RequireValidMetricSupport(MetricSupport value)
    57	{
    58	    switch (value)
    59	    {
    60	        case MetricSupport::NumericDelta:
    61	        case MetricSupport::Unsupported:
    62	            return;
    63	    }
    64	    throw std::invalid_argument(
    65	        "recommendation_campaign_outcome_assessment_metric_support_invalid");
    66	}
    67	
    68	void RequireValidLifecycle(Lifecycle value)
    69	{
    70	    switch (value)
    71	    {
    72	        case Lifecycle::NotTerminal:
    73	        case Lifecycle::Succeeded:
    74	        case Lifecycle::Failed:
    75	        case Lifecycle::Cancelled:
    76	        case Lifecycle::Unknown:
    77	            return;
    78	    }
    79	    throw std::invalid_argument(
    80	        "recommendation_campaign_outcome_assessment_lifecycle_invalid");
    81	}
    82	
    83	void RequireValidConsistency(Consistency value)
    84	{
    85	    switch (value)
    86	    {
    87	        case Consistency::Consistent:
    88	        case Consistency::Inconsistent:
    89	            return;
    90	    }
    91	    throw std::invalid_argument(
    92	        "recommendation_campaign_outcome_assessment_consistency_invalid");
    93	}
    94	
    95	std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
    96	{
    97	    std::vector<const Metric*> ordered;
    98	    ordered.reserve(metrics.size());
    99	    for (const auto& metric : metrics) ordered.push_back(&metric);
   100	    std::sort(ordered.begin(), ordered.end(), [](const Metric* left,
   101	                                              const Metric* right)
   102	    {
   103	        return left->identity < right->identity;
   104	    });
   105	    for (std::size_t index = 1; index < ordered.size(); ++index)
   106	        if (ordered[index - 1]->identity == ordered[index]->identity)
   107	            throw std::invalid_argument(
   108	                "recommendation_campaign_outcome_assessment_metric_identity_duplicate");
   109	    std::vector<Metric> canonical;
   110	    canonical.reserve(ordered.size());
   111	    for (const auto* metric : ordered) canonical.push_back(*metric);
   112	    return canonical;
   113	}
   114	
   115	std::vector<ResultIdentity> CanonicalResultIdentities(
   116	    std::vector<ResultIdentity> identities)
   117	{
   118	    std::vector<const ResultIdentity*> ordered;
   119	    ordered.reserve(identities.size());
   120	    for (const auto& identity : identities) ordered.push_back(&identity);
   121	    std::sort(ordered.begin(), ordered.end(),
   122	        [](const ResultIdentity* left, const ResultIdentity* right)
   123	    {
   124	        return std::tie(left->kind, left->id) <
   125	            std::tie(right->kind, right->id);
   126	    });
   127	    for (std::size_t index = 1; index < ordered.size(); ++index)
   128	        if (*ordered[index - 1] == *ordered[index])
   129	            throw std::invalid_argument(
   130	                "recommendation_campaign_outcome_assessment_result_identity_duplicate");
   275	        << summary.inconsistentOutcomeMemberCount
   276	        << ";metric_comparison_count=" << summary.metricComparisonCount
   277	        << ";comparable_metric_count=" << summary.comparableMetricCount
   278	        << ";context_changed_metric_count="
   279	        << summary.contextChangedMetricCount
   280	        << ";missing_source_metric_count="
   281	        << summary.missingSourceMetricCount
   282	        << ";missing_result_metric_count="
   283	        << summary.missingResultMetricCount
   284	        << ";metric_value_unavailable_count="
   285	        << summary.metricValueUnavailableCount
   286	        << ";unsupported_metric_count=" << summary.unsupportedMetricCount
   287	        << ";positive_delta_count=" << summary.positiveDeltaCount
   288	        << ";zero_delta_count=" << summary.zeroDeltaCount
   289	        << ";negative_delta_count=" << summary.negativeDeltaCount;
   290	}
   291	
   292	std::string AssessmentCanonicalText(
   293	    const RecommendationCampaignOutcomeAssessmentCampaignIdentity& campaign,
   294	    const RecommendationCampaignOutcomeAssessmentMaterializationIdentity&
   295	        materialization,
   296	    const RecommendationCampaignOutcomeAssessmentSummary& summary,
   297	    const std::vector<Member>& members)
   298	{
   299	    std::ostringstream out;
   300	    out.imbue(std::locale::classic());
   301	    out << "experiment_recommendation_campaign_outcome_assessment_v2"
   302	        << ";assessment_contract_version="
   303	        << kRecommendationCampaignOutcomeAssessmentContractVersion
   304	        << ";campaign_approval_id=" << campaign.campaignApprovalId
   305	        << ";campaign_identity="
   306	        << LengthPrefixed(campaign.identityCanonical)
   307	        << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
   308	        << ";materialization_id=" << materialization.materializationId
   309	        << ";materialization_contract_version="
   310	        << materialization.contractVersion
   311	        << ";materialization_identity="
   312	        << LengthPrefixed(materialization.identityCanonical)
   313	        << ";materialization_identity_hash="
   314	        << LengthPrefixed(materialization.identityHash);
   315	    for (const auto& member : members)
   316	    {
   317	        out << ";member=" << member.identity.memberOrdinal << ','
   318	            << member.identity.materializationMemberId << ','
   319	            << member.identity.rankingMemberId << ','
   320	            << member.identity.recommendationId << ','
   321	            << member.identity.sourceExperimentId << ','
   322	            << member.identity.proposalId << ','
   323	            << OptionalLongLong(member.identity.expectedExperimentId)
   324	            << ",lifecycle="
   325	            << RecommendationCampaignOutcomeAssessmentLifecycleStateText(
   326	                   member.lifecycle)
   327	            << ",consistency="
   328	            << RecommendationCampaignOutcomeAssessmentConsistencyStateText(
   329	                   member.consistency)
   330	            << ",outcome="
   331	            << RecommendationCampaignOutcomeAssessmentMemberOutcomeText(
   332	                   member.outcome)
   333	            << ",diagnostic_count=" << member.diagnostics.size();
   334	        for (const auto diagnostic : member.diagnostics)
   335	            out << ','
   336	                << RecommendationCampaignOutcomeAssessmentDiagnosticCodeText(
   337	                       diagnostic);
   338	        out << ",source=";
   339	        AppendSourceEvidence(out, member.sourceEvidence);
   340	        out << ",result=";
   341	        AppendResultEvidence(out, member.resultEvidence);
   342	        out << ",comparison_count=" << member.comparisons.size();
   343	        for (const auto& comparison : member.comparisons)
   344	        {
   345	            out << ',';
   346	            AppendComparison(out, comparison);
   347	        }
   348	    }
   349	    AppendSummary(out, summary);
   350	    return out.str();
   351	}
   352	
   353	std::vector<Difference> ContextDifferences(
   354	    const Context& source,
   355	    const Context& result)
   356	{
   357	    std::vector<Difference> differences;
   358	    if (source.symbol != result.symbol)
   359	        differences.push_back(Difference::Symbol);
   360	    if (source.predictionHorizon != result.predictionHorizon)
   361	        differences.push_back(Difference::PredictionHorizon);
   362	    if (source.threshold != result.threshold)
   363	        differences.push_back(Difference::Threshold);
   364	    if (source.windowSize != result.windowSize)
   365	        differences.push_back(Difference::WindowSize);
   366	    if (source.labelDefinition != result.labelDefinition)
   367	        differences.push_back(Difference::LabelDefinition);
   368	    if (source.inferenceRangeStart != result.inferenceRangeStart ||
   369	        source.inferenceRangeEnd != result.inferenceRangeEnd)
   370	        differences.push_back(Difference::InferenceRange);
   371	    return differences;
   372	}
   373	
   374	void CanonicalizeDiagnostics(std::vector<Diagnostic>& diagnostics)
   375	{
   376	    std::sort(diagnostics.begin(), diagnostics.end(),
   377	        [](Diagnostic left, Diagnostic right)
   378	    {
   379	        return static_cast<int>(left) < static_cast<int>(right);
   380	    });
   381	    diagnostics.erase(
   382	        std::unique(diagnostics.begin(), diagnostics.end()),
   383	        diagnostics.end());
   384	}
   385	
   386	struct WorkingMember
   387	{
   388	    const MemberEvidence* evidence;
   389	    std::vector<Diagnostic> diagnostics;
   390	};
   391	
   392	struct MutableSummary
   393	{
   394	    int memberCount = 0;
   395	    int notTerminalLifecycleCount = 0;
   396	    int succeededLifecycleCount = 0;
   397	    int failedLifecycleCount = 0;
   398	    int cancelledLifecycleCount = 0;
   399	    int unknownLifecycleCount = 0;
   400	    int consistentMemberCount = 0;
   401	    int inconsistentMemberCount = 0;
   402	    int notReadyMemberCount = 0;
   403	    int succeededComparableMemberCount = 0;
   404	    int succeededContextChangedMemberCount = 0;
   405	    int succeededMetricGapMemberCount = 0;
   406	    int terminalFailedMemberCount = 0;
   407	    int terminalCancelledMemberCount = 0;
   408	    int inconsistentOutcomeMemberCount = 0;
   409	    std::size_t metricComparisonCount = 0;
   410	    std::size_t comparableMetricCount = 0;
   411	    std::size_t contextChangedMetricCount = 0;
   412	    std::size_t missingSourceMetricCount = 0;
   413	    std::size_t missingResultMetricCount = 0;
   414	    std::size_t metricValueUnavailableCount = 0;
   415	    std::size_t unsupportedMetricCount = 0;
   416	    std::size_t positiveDeltaCount = 0;
   417	    std::size_t zeroDeltaCount = 0;
   418	    std::size_t negativeDeltaCount = 0;
   419	};
   420	
   421	AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
   422	{
   423	    if (counts.inconsistentMemberCount > 0)
   424	        return AggregateOutcome::Inconsistent;
   425	    if (counts.terminalFailedMemberCount > 0)
   426	        return AggregateOutcome::HasFailures;
   427	    if (counts.terminalCancelledMemberCount > 0)
   428	        return AggregateOutcome::HasCancellations;
   429	    if (counts.notReadyMemberCount > 0)
   430	        return AggregateOutcome::NotReady;
   431	    if (counts.succeededContextChangedMemberCount > 0)
   432	        return AggregateOutcome::SucceededContextChanged;
   433	    if (counts.succeededMetricGapMemberCount > 0)
   434	        return AggregateOutcome::SucceededMetricGap;
   435	    return AggregateOutcome::SucceededComparable;
   436	}
   437	
   438	void CountMember(MutableSummary& counts, const Member& member)
   439	{
   440	    ++counts.memberCount;
   441	    switch (member.lifecycle)
   442	    {
   443	        case Lifecycle::NotTerminal:
   444	            ++counts.notTerminalLifecycleCount;
   445	            break;
   446	        case Lifecycle::Succeeded:
   447	            ++counts.succeededLifecycleCount;
   448	            break;
   449	        case Lifecycle::Failed:
   450	            ++counts.failedLifecycleCount;
   451	            break;
   452	        case Lifecycle::Cancelled:
   453	            ++counts.cancelledLifecycleCount;
   454	            break;
   455	        case Lifecycle::Unknown:
   456	            ++counts.unknownLifecycleCount;
   457	            break;
   458	    }
   459	    if (member.consistency == Consistency::Consistent)
   460	        ++counts.consistentMemberCount;
   461	    else
   462	        ++counts.inconsistentMemberCount;
   463	
   464	    switch (member.outcome)
   465	    {
   466	        case MemberOutcome::NotReady:
   467	            ++counts.notReadyMemberCount;
   468	            break;
   469	        case MemberOutcome::SucceededComparable:
   470	            ++counts.succeededComparableMemberCount;
   471	            break;
   472	        case MemberOutcome::SucceededContextChanged:
   473	            ++counts.succeededContextChangedMemberCount;
   474	            break;
   475	        case MemberOutcome::SucceededMetricGap:
   520	}
   521	
   522	} // namespace
   523	
   524	RecommendationCampaignOutcomeAssessmentCampaignIdentity::
   525	    RecommendationCampaignOutcomeAssessmentCampaignIdentity(
   526	        long long campaignApprovalIdValue,
   527	        std::string identityCanonicalValue,
   528	        std::string identityHashValue)
   529	    : campaignApprovalId(campaignApprovalIdValue),
   530	      identityCanonical(std::move(identityCanonicalValue)),
   531	      identityHash(std::move(identityHashValue))
   532	{
   533	    if (campaignApprovalId <= 0)
   534	        throw std::invalid_argument(
   535	            "recommendation_campaign_outcome_assessment_campaign_id_invalid");
   536	    RequireText(identityCanonical,
   537	        "recommendation_campaign_outcome_assessment_campaign_identity_missing");
   538	    if (identityHash != RecommendationCanonicalHash(identityCanonical))
   539	        throw std::invalid_argument(
   540	            "recommendation_campaign_outcome_assessment_campaign_identity_invalid");
   541	}
   542	
   543	RecommendationCampaignOutcomeAssessmentMaterializationIdentity::
   544	    RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
   545	        long long materializationIdValue,
   546	        long long campaignApprovalIdValue,
   547	        std::string campaignIdentityHashValue,
   548	        int contractVersionValue,
   549	        int memberCountValue,
   550	        std::string identityCanonicalValue,
   551	        std::string identityHashValue)
   552	    : materializationId(materializationIdValue),
   553	      campaignApprovalId(campaignApprovalIdValue),
   554	      campaignIdentityHash(std::move(campaignIdentityHashValue)),
   555	      contractVersion(contractVersionValue),
   556	      memberCount(memberCountValue),
   557	      identityCanonical(std::move(identityCanonicalValue)),
   558	      identityHash(std::move(identityHashValue))
   559	{
   560	    if (materializationId <= 0 || campaignApprovalId <= 0)
   561	        throw std::invalid_argument(
   562	            "recommendation_campaign_outcome_assessment_materialization_id_invalid");
   563	    if (memberCount <= 0 ||
   564	        memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
   565	        throw std::invalid_argument(
   566	            "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
   567	    RequireText(campaignIdentityHash,
   568	        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
   569	    RequireText(identityCanonical,
   570	        "recommendation_campaign_outcome_assessment_materialization_identity_missing");
   571	    if (identityHash != RecommendationCanonicalHash(identityCanonical))
   572	        throw std::invalid_argument(
   573	            "recommendation_campaign_outcome_assessment_materialization_identity_invalid");
   574	}
   575	
   576	RecommendationCampaignOutcomeAssessmentMemberIdentity::
   577	    RecommendationCampaignOutcomeAssessmentMemberIdentity(
   578	        int memberOrdinalValue,
   579	        long long materializationMemberIdValue,
   580	        long long rankingMemberIdValue,
   581	        long long recommendationIdValue,
   582	        long long sourceExperimentIdValue,
   583	        long long proposalIdValue,
   584	        std::optional<long long> expectedExperimentIdValue)
   585	    : memberOrdinal(memberOrdinalValue),
   586	      materializationMemberId(materializationMemberIdValue),
   587	      rankingMemberId(rankingMemberIdValue),
   588	      recommendationId(recommendationIdValue),
   589	      sourceExperimentId(sourceExperimentIdValue),
   590	      proposalId(proposalIdValue),
   591	      expectedExperimentId(expectedExperimentIdValue)
   592	{
   593	    if (memberOrdinal <= 0 || materializationMemberId <= 0 ||
   594	        rankingMemberId <= 0 || recommendationId <= 0 ||
   595	        sourceExperimentId <= 0 || proposalId <= 0)
   596	        throw std::invalid_argument(
   597	            "recommendation_campaign_outcome_assessment_member_identity_invalid");
   598	    RequireOptionalPositive(expectedExperimentId,
   599	        "recommendation_campaign_outcome_assessment_expected_experiment_id_invalid");
   600	}
   601	
   602	RecommendationCampaignOutcomeAssessmentMetric::
   603	    RecommendationCampaignOutcomeAssessmentMetric(
   604	        std::string identityValue,
   605	        std::optional<double> valueValue,
   606	        MetricSupport comparisonSupportValue)
   607	    : identity(std::move(identityValue)),
   608	      value(valueValue && *valueValue == 0.0
   609	              ? std::optional<double>(0.0)
   610	              : valueValue),
   611	      comparisonSupport(comparisonSupportValue)
   612	{
   613	    RequireText(identity,
   614	        "recommendation_campaign_outcome_assessment_metric_identity_invalid");
   615	    if (value && !std::isfinite(*value))
   616	        throw std::invalid_argument(
   617	            "recommendation_campaign_outcome_assessment_metric_value_nonfinite");
   618	    RequireValidMetricSupport(comparisonSupport);
   619	}
   620	
   621	RecommendationCampaignOutcomeAssessmentMetricCollection::
   622	    RecommendationCampaignOutcomeAssessmentMetricCollection(
   623	        std::vector<Metric> metricsValue)
   624	    : metrics(CanonicalMetrics(std::move(metricsValue)))
   625	{
   626	}
   627	
   628	RecommendationCampaignOutcomeAssessmentComparisonContext::
   629	    RecommendationCampaignOutcomeAssessmentComparisonContext(
   630	        std::string symbolValue,
   631	        int predictionHorizonValue,
   632	        double thresholdValue,
   633	        int windowSizeValue,
   634	        std::string labelDefinitionValue,
   635	        std::string inferenceRangeStartValue,
   636	        std::string inferenceRangeEndValue)
   637	    : symbol(std::move(symbolValue)),
   638	      predictionHorizon(predictionHorizonValue),
   639	      threshold(thresholdValue == 0.0 ? 0.0 : thresholdValue),
   640	      windowSize(windowSizeValue),
   641	      labelDefinition(std::move(labelDefinitionValue)),
   642	      inferenceRangeStart(std::move(inferenceRangeStartValue)),
   643	      inferenceRangeEnd(std::move(inferenceRangeEndValue))
   644	{
   645	    RequireText(symbol,
   646	        "recommendation_campaign_outcome_assessment_context_symbol_invalid");
   647	    RequireText(labelDefinition,
   648	        "recommendation_campaign_outcome_assessment_context_label_definition_invalid");
   649	    RequireText(inferenceRangeStart,
   650	        "recommendation_campaign_outcome_assessment_context_inference_range_invalid");
   651	    RequireText(inferenceRangeEnd,
   652	        "recommendation_campaign_outcome_assessment_context_inference_range_invalid");
   653	    if (predictionHorizon <= 0 || windowSize <= 0 || !std::isfinite(threshold))
   654	        throw std::invalid_argument(
   655	            "recommendation_campaign_outcome_assessment_context_invalid");
   656	}
   657	
   658	RecommendationCampaignOutcomeAssessmentSourceEvidence::
   659	    RecommendationCampaignOutcomeAssessmentSourceEvidence(
   660	        long long sourceExperimentIdValue,
   661	        std::optional<long long> sourceModelIdValue,
   662	        std::optional<long long> sourceAnalysisIdValue,
   663	        Context contextValue,
   664	        MetricCollection metricsValue)
   665	    : sourceExperimentId(sourceExperimentIdValue),
   666	      sourceModelId(sourceModelIdValue),
   667	      sourceAnalysisId(sourceAnalysisIdValue),
   668	      context(std::move(contextValue)),
   669	      metrics(std::move(metricsValue))
   670	{
   671	    if (sourceExperimentId <= 0)
   672	        throw std::invalid_argument(
   673	            "recommendation_campaign_outcome_assessment_source_experiment_id_invalid");
   674	    RequireOptionalPositive(sourceModelId,
   675	        "recommendation_campaign_outcome_assessment_source_model_id_invalid");
   676	    RequireOptionalPositive(sourceAnalysisId,
   677	        "recommendation_campaign_outcome_assessment_source_analysis_id_invalid");
   678	}
   679	
   680	RecommendationCampaignOutcomeAssessmentResultIdentity::
   681	    RecommendationCampaignOutcomeAssessmentResultIdentity(
   682	        std::string kindValue,
   683	        long long idValue)
   684	    : kind(std::move(kindValue)), id(idValue)
   685	{
   686	    RequireText(kind,
   687	        "recommendation_campaign_outcome_assessment_result_identity_kind_invalid");
   688	    if (id <= 0)
   689	        throw std::invalid_argument(
   690	            "recommendation_campaign_outcome_assessment_result_identity_id_invalid");
   691	}
   692	
   693	RecommendationCampaignOutcomeAssessmentResultEvidence::
   694	    RecommendationCampaignOutcomeAssessmentResultEvidence(
   695	        std::optional<long long> experimentIdValue,
   696	        std::optional<long long> modelIdValue,
   697	        std::vector<ResultIdentity> resultIdentitiesValue,
   698	        std::optional<Context> contextValue,
   699	        MetricCollection metricsValue)
   700	    : experimentId(experimentIdValue),
   701	      modelId(modelIdValue),
   702	      resultIdentities(
   703	          CanonicalResultIdentities(std::move(resultIdentitiesValue))),
   704	      context(std::move(contextValue)),
   705	      metrics(std::move(metricsValue))
   706	{
   707	    RequireOptionalPositive(experimentId,
   708	        "recommendation_campaign_outcome_assessment_result_experiment_id_invalid");
   709	    RequireOptionalPositive(modelId,
   710	        "recommendation_campaign_outcome_assessment_result_model_id_invalid");
   711	}
   712	
   713	RecommendationCampaignOutcomeAssessmentMemberEvidence::
   714	    RecommendationCampaignOutcomeAssessmentMemberEvidence(
   715	        RecommendationCampaignOutcomeAssessmentMemberIdentity identityValue,
   716	        Lifecycle lifecycleValue,
   717	        std::optional<
   718	            RecommendationCampaignOutcomeAssessmentSourceEvidence>
   719	            sourceEvidenceValue,
   720	        std::optional<
   721	            RecommendationCampaignOutcomeAssessmentResultEvidence>
   722	            resultEvidenceValue,
   723	        Consistency inputConsistencyValue)
   724	    : identity(std::move(identityValue)),
   725	      lifecycle(lifecycleValue),
   726	      inputConsistency(inputConsistencyValue),
   727	      sourceEvidence(std::move(sourceEvidenceValue)),
   728	      resultEvidence(std::move(resultEvidenceValue))
   729	{
   730	    RequireValidLifecycle(lifecycle);
   731	    RequireValidConsistency(inputConsistency);
   732	}
   733	
   734	RecommendationCampaignOutcomeAssessmentMetricComparison::
   735	    RecommendationCampaignOutcomeAssessmentMetricComparison(
  1020	                break;
  1021	        }
  1022	        if (std::any_of(comparisons.begin(), comparisons.end(),
  1023	                [](const Comparison& comparison)
  1024	        {
  1025	            return comparison.classification == Classification::ContextChanged;
  1026	        }))
  1027	            return MemberOutcome::SucceededContextChanged;
  1028	        if (comparisons.empty() ||
  1029	            std::any_of(comparisons.begin(), comparisons.end(),
  1030	                [](const Comparison& comparison)
  1031	        {
  1032	            return comparison.classification != Classification::Comparable;
  1033	        }))
  1034	            return MemberOutcome::SucceededMetricGap;
  1035	        return MemberOutcome::SucceededComparable;
  1036	    }
  1037	
  1038	    static RecommendationCampaignOutcomeAssessment Build(
  1039	        const RecommendationCampaignOutcomeAssessmentCampaignIdentity&
  1040	            campaignIdentity,
  1041	        const RecommendationCampaignOutcomeAssessmentMaterializationIdentity&
  1042	            materializationIdentity,
  1043	        const std::string& observedAt,
  1044	        const std::vector<MemberEvidence>& members)
  1045	    {
  1046	        RequireText(observedAt,
  1047	            "recommendation_campaign_outcome_assessment_observed_at_invalid");
  1048	        if (materializationIdentity.campaignApprovalId !=
  1049	                campaignIdentity.campaignApprovalId ||
  1050	            materializationIdentity.campaignIdentityHash !=
  1051	                campaignIdentity.identityHash)
  1052	            throw std::invalid_argument(
  1053	                "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
  1054	        if (materializationIdentity.contractVersion !=
  1055	            kRecommendationCampaignMaterializationContractVersion)
  1056	            throw std::invalid_argument(
  1057	                "recommendation_campaign_outcome_assessment_materialization_version_unsupported");
  1058	        if (materializationIdentity.memberCount !=
  1059	            static_cast<int>(members.size()))
  1060	            throw std::invalid_argument(
  1061	                "recommendation_campaign_outcome_assessment_member_count_mismatch");
  1062	        if (members.empty() ||
  1063	            members.size() > static_cast<std::size_t>(
  1064	                                 kMaximumRecommendationCampaignOutcomeAssessmentMembers))
  1065	            throw std::invalid_argument(
  1066	                "recommendation_campaign_outcome_assessment_member_count_invalid");
  1067	
  1068	        std::vector<const MemberEvidence*> ordered;
  1069	        ordered.reserve(members.size());
  1070	        for (const auto& member : members) ordered.push_back(&member);
  1071	        std::sort(ordered.begin(), ordered.end(),
  1072	            [](const MemberEvidence* left, const MemberEvidence* right)
  1073	        {
  1074	            return std::tie(left->identity.memberOrdinal,
  1075	                       left->identity.materializationMemberId,
  1076	                       left->identity.rankingMemberId,
  1077	                       left->identity.recommendationId,
  1078	                       left->identity.proposalId) <
  1079	                std::tie(right->identity.memberOrdinal,
  1080	                       right->identity.materializationMemberId,
  1081	                       right->identity.rankingMemberId,
  1082	                       right->identity.recommendationId,
  1083	                       right->identity.proposalId);
  1084	        });
  1085	
  1086	        std::set<long long> materializationMemberIds;
  1087	        std::set<long long> rankingMemberIds;
  1088	        std::set<long long> recommendationIds;
  1089	        std::set<long long> proposalIds;
  1090	        std::vector<WorkingMember> working;
  1091	        working.reserve(ordered.size());
  1092	        for (std::size_t index = 0; index < ordered.size(); ++index)
  1093	        {
  1094	            const auto& evidence = *ordered[index];
  1095	            if (evidence.identity.memberOrdinal != static_cast<int>(index) + 1 ||
  1096	                !materializationMemberIds
  1097	                     .insert(evidence.identity.materializationMemberId)
  1098	                     .second ||
  1099	                !rankingMemberIds.insert(evidence.identity.rankingMemberId)
  1100	                     .second ||
  1101	                !recommendationIds.insert(evidence.identity.recommendationId)
  1102	                     .second ||
  1103	                !proposalIds.insert(evidence.identity.proposalId).second)
  1104	                throw std::invalid_argument(
  1105	                    "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
  1106	
  1107	            WorkingMember item{&evidence, {}};
  1108	            if (evidence.inputConsistency == Consistency::Inconsistent)
  1109	                item.diagnostics.push_back(
  1110	                    Diagnostic::InputEvidenceInconsistent);
  1111	            if (evidence.sourceEvidence &&
  1112	                evidence.sourceEvidence->sourceExperimentId !=
  1113	                    evidence.identity.sourceExperimentId)
  1114	                item.diagnostics.push_back(
  1115	                    Diagnostic::SourceExperimentIdentityMismatch);
  1116	            if ((evidence.lifecycle == Lifecycle::Succeeded ||
  1117	                    evidence.lifecycle == Lifecycle::Failed ||
  1118	                    evidence.lifecycle == Lifecycle::Cancelled) &&
  1119	                !evidence.identity.expectedExperimentId)
  1120	                item.diagnostics.push_back(
  1121	                    Diagnostic::ExpectedExperimentIdentityMissing);
  1122	            if (evidence.lifecycle != Lifecycle::Succeeded &&
  1123	                evidence.resultEvidence)
  1124	                item.diagnostics.push_back(
  1125	                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
  1126	            if (evidence.lifecycle == Lifecycle::Succeeded)
  1127	            {
  1128	                if (!evidence.resultEvidence)
  1129	                    item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
  1130	                else
  1131	                {
  1132	                    if (!evidence.resultEvidence->experimentId)
  1133	                        item.diagnostics.push_back(
  1134	                            Diagnostic::ResultExperimentIdentityMissing);
  1135	                    else if (evidence.identity.expectedExperimentId &&
  1136	                        evidence.resultEvidence->experimentId !=
  1137	                            evidence.identity.expectedExperimentId)
  1138	                        item.diagnostics.push_back(
  1139	                            Diagnostic::ResultExperimentIdentityMismatch);
  1140	                    if (!evidence.resultEvidence->modelId)
  1141	                        item.diagnostics.push_back(
  1142	                            Diagnostic::ResultModelIdentityMissing);
  1143	                    if (evidence.resultEvidence->resultIdentities.empty())
  1144	                        item.diagnostics.push_back(
  1145	                            Diagnostic::ResultIdentityMissing);
  1146	                    if (!evidence.resultEvidence->context)
  1147	                        item.diagnostics.push_back(
  1148	                            Diagnostic::ResultContextMissing);
  1149	                }
  1150	            }
  1151	            working.push_back(std::move(item));
  1152	        }
  1153	
  1154	        std::map<long long, std::vector<std::size_t>> expectedExperiments;
  1155	        std::map<long long, std::vector<std::size_t>> resultExperiments;
  1156	        std::map<long long, std::vector<std::size_t>> resultModels;
  1157	        std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
  1158	            resultIdentities;
  1159	        for (std::size_t index = 0; index < working.size(); ++index)
  1160	        {
  1161	            const auto& expected =
  1162	                working[index].evidence->identity.expectedExperimentId;
  1163	            if (expected) expectedExperiments[*expected].push_back(index);
  1164	            const auto& result = working[index].evidence->resultEvidence;
  1165	            if (!result) continue;
  1166	            if (result->experimentId)
  1167	                resultExperiments[*result->experimentId].push_back(index);
  1168	            if (result->modelId)
  1169	                resultModels[*result->modelId].push_back(index);
  1170	            for (const auto& identity : result->resultIdentities)
  1171	                resultIdentities[{identity.kind, identity.id}].push_back(index);
  1172	        }
  1173	        for (const auto& [unused, indices] : expectedExperiments)
  1174	        {
  1175	            static_cast<void>(unused);
  1176	            if (indices.size() < 2) continue;
  1177	            for (const auto index : indices)
  1178	                working[index].diagnostics.push_back(
  1179	                    Diagnostic::ExpectedExperimentIdentityReused);
  1180	        }
  1181	        for (const auto& [unused, indices] : resultExperiments)
  1182	        {
  1183	            static_cast<void>(unused);
  1184	            if (indices.size() < 2) continue;
  1185	            for (const auto index : indices)
  1186	                working[index].diagnostics.push_back(
  1187	                    Diagnostic::ResultExperimentReused);
  1188	        }
  1189	        for (const auto& [unused, indices] : resultModels)
  1190	        {
  1191	            static_cast<void>(unused);
  1192	            if (indices.size() < 2) continue;
  1193	            for (const auto index : indices)
  1194	                working[index].diagnostics.push_back(
  1195	                    Diagnostic::ResultModelIdentityReused);
  1196	        }
  1197	        for (const auto& [unused, indices] : resultIdentities)
  1198	        {
  1199	            static_cast<void>(unused);
  1200	            if (indices.size() < 2) continue;
  1201	            for (const auto index : indices)
  1202	                working[index].diagnostics.push_back(
  1203	                    Diagnostic::ResultIdentityReused);
  1204	        }
  1205	
  1206	        std::vector<Member> assessedMembers;
  1207	        assessedMembers.reserve(working.size());
  1208	        for (auto& item : working)
  1209	        {
  1210	            CanonicalizeDiagnostics(item.diagnostics);
  1211	            std::vector<Comparison> comparisons;
  1212	            if (item.diagnostics.empty() &&
  1213	                item.evidence->lifecycle == Lifecycle::Succeeded)
  1214	            {
  1215	                bool nonFiniteDelta = false;

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

exec
/bin/bash -lc "rg -n \"recommendation_ranking_member|campaign_materialization_member\" Database Sources | sed -n '1,320p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
Sources/ExperimentRecommendationCampaignMaterialization.cpp:48:    out << "experiment_recommendation_campaign_materialization_member_v1"
Sources/ExperimentRecommendationCampaignMaterialization.cpp:173:            "recommendation_campaign_materialization_member_count_mismatch");
Sources/ExperimentRecommendationCampaignMaterialization.cpp:193:                "recommendation_campaign_materialization_member_invalid");
Sources/ExperimentRecommendationCampaignMaterialization.cpp:264:                "recommendation_campaign_materialization_member_invalid");
Sources/ExperimentRecommendationCampaignMaterialization.cpp:269:                "recommendation_campaign_materialization_member_ordinal_invalid");
Sources/ExperimentRecommendationCampaignMaterializationService.cpp:233:                    "recommendation_campaign_materialization_member_ineligible:" +
Database/README.md:54:  `experiment_recommendation_campaign_materialization_member`, with
Sources/ExperimentRecommendationCampaignHandoffRepository.cpp:92:   AND to_regclass('experiment_recommendation_campaign_materialization_member') IS NOT NULL
Sources/ExperimentRecommendationRanking.cpp:65:        "experiment_recommendation_ranking_membership_v1;count=";
Sources/ExperimentRecommendationRanking.cpp:494:    out << "experiment_recommendation_ranking_membership_v1;count="
Sources/ExperimentRecommendationRanking.cpp:526:            "invalid_recommendation_ranking_membership_canonical");
Sources/ExperimentRecommendationRankingRepository.cpp:256:    return "recommendation_ranking_member_id,"
Sources/ExperimentRecommendationRankingRepository.cpp:269:    return "rm.recommendation_ranking_member_id,"
Sources/ExperimentRecommendationRankingRepository.cpp:292:            row["recommendation_ranking_member_id"].as<long long>();
Sources/ExperimentRecommendationRankingRepository.cpp:350:           TableExists(transaction, "experiment_recommendation_ranking_member");
Sources/ExperimentRecommendationRankingRepository.cpp:484:        throw std::invalid_argument("invalid_recommendation_ranking_members_request");
Sources/ExperimentRecommendationRankingRepository.cpp:502:            "INSERT INTO experiment_recommendation_ranking_member ("
Sources/ExperimentRecommendationRankingRepository.cpp:530:            "RETURNING recommendation_ranking_member_id;",
Sources/ExperimentRecommendationRankingRepository.cpp:547:            " FROM experiment_recommendation_ranking_member "
Sources/ExperimentRecommendationRankingRepository.cpp:552:            throw std::runtime_error("recommendation_ranking_member_retry_mismatch");
Sources/ExperimentRecommendationRankingRepository.cpp:554:        item.memberId = row["recommendation_ranking_member_id"].as<long long>();
Sources/ExperimentRecommendationRankingRepository.cpp:561:        "SELECT count(*) FROM experiment_recommendation_ranking_member "
Sources/ExperimentRecommendationRankingRepository.cpp:565:        throw std::runtime_error("recommendation_ranking_membership_retry_mismatch");
Sources/ExperimentRecommendationRankingRepository.cpp:588:        "FROM experiment_recommendation_ranking_member "
Sources/ExperimentRecommendationRankingRepository.cpp:596:        throw std::runtime_error("recommendation_ranking_member_count_mismatch");
Sources/ExperimentRecommendationRankingRepository.cpp:632:        "SELECT count(*) FROM experiment_recommendation_ranking_member "
Sources/ExperimentRecommendationRankingRepository.cpp:699:        "experiment_recommendation_ranking_member WHERE "
Sources/ExperimentRecommendationRankingRepository.cpp:721:        throw std::invalid_argument("recommendation_ranking_member_id_invalid");
Sources/ExperimentRecommendationRankingRepository.cpp:725:        " FROM experiment_recommendation_ranking_member rm "
Sources/ExperimentRecommendationRankingRepository.cpp:733:        "WHERE rm.recommendation_ranking_member_id=$1 "
Sources/ExperimentRecommendationCampaignLaunchRepository.cpp:68:   AND to_regclass('experiment_recommendation_campaign_materialization_member') IS NOT NULL
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:366:   AND to_regclass('experiment_recommendation_campaign_materialization_member') IS NOT NULL
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:494:       ('experiment_recommendation_campaign_materialization_member',
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:495:        'recommendation_campaign_materialization_member_id'),
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:496:       ('experiment_recommendation_campaign_materialization_member',
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:498:       ('experiment_recommendation_campaign_materialization_member','member_ordinal'),
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:499:       ('experiment_recommendation_campaign_materialization_member',
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:500:        'recommendation_ranking_member_id'),
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:501:       ('experiment_recommendation_campaign_materialization_member','recommendation_id'),
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:502:       ('experiment_recommendation_campaign_materialization_member','source_experiment_id'),
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:503:       ('experiment_recommendation_campaign_materialization_member','ranking_position'),
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:504:       ('experiment_recommendation_campaign_materialization_member',
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:506:       ('experiment_recommendation_campaign_materialization_member',
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:508:       ('experiment_recommendation_campaign_materialization_member',
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:510:       ('experiment_recommendation_campaign_materialization_member',
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:512:       ('experiment_recommendation_campaign_materialization_member','proposal_identity_hash'),
Sources/ExperimentRecommendationCampaignStatusRepository.cpp:513:       ('experiment_recommendation_campaign_materialization_member','created_at')
Sources/ExperimentRecommendationCampaignPlanningRepository.cpp:78:   AND to_regclass('experiment_recommendation_ranking_member') IS NOT NULL
Sources/ExperimentRecommendationCampaignPlanningRepository.cpp:144:SELECT rm.recommendation_ranking_member_id,rm.global_ordinal,rm.bucket,
Sources/ExperimentRecommendationCampaignPlanningRepository.cpp:155:FROM experiment_recommendation_ranking_member rm
Sources/ExperimentRecommendationCampaignPlanningRepository.cpp:198:            row["recommendation_ranking_member_id"].as<long long>();
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:67:        "recommendation_campaign_materialization_member_id,member_ordinal,"
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:68:        "recommendation_ranking_member_id,recommendation_id,"
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:100:            "invalid_persisted_recommendation_campaign_materialization_member");
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:136:        "experiment_recommendation_campaign_materialization_member WHERE "
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:224:            "recommendation_campaign_materialization_member_id_invalid");
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:274:        "FROM experiment_recommendation_ranking_member m "
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:290:        "m.recommendation_ranking_member_id=$2 AND m.recommendation_id=$3;",
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:294:            "recommendation_campaign_materialization_member_not_found");
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:517:            "INSERT INTO experiment_recommendation_campaign_materialization_member ("
Sources/ExperimentRecommendationCampaignMaterializationRepository.cpp:519:            "recommendation_ranking_member_id,recommendation_id,"
Database/migrations/035_experiment_recommendation_ranking.sql:87:CREATE TABLE IF NOT EXISTS experiment_recommendation_ranking_member (
Database/migrations/035_experiment_recommendation_ranking.sql:88:    recommendation_ranking_member_id bigserial PRIMARY KEY,
Database/migrations/035_experiment_recommendation_ranking.sql:131:    CONSTRAINT recommendation_ranking_member_result_uidx UNIQUE
Database/migrations/035_experiment_recommendation_ranking.sql:133:    CONSTRAINT recommendation_ranking_member_bucket_rank_uidx UNIQUE
Database/migrations/035_experiment_recommendation_ranking.sql:135:    CONSTRAINT recommendation_ranking_member_ordinal_uidx UNIQUE
Database/migrations/035_experiment_recommendation_ranking.sql:137:    CONSTRAINT recommendation_ranking_member_shape_check CHECK (
Database/migrations/035_experiment_recommendation_ranking.sql:151:    CONSTRAINT recommendation_ranking_member_finite_check CHECK (
Database/migrations/035_experiment_recommendation_ranking.sql:157:CREATE OR REPLACE FUNCTION recommendation_ranking_membership_contains(
Database/migrations/035_experiment_recommendation_ranking.sql:173:        'experiment_recommendation_ranking_membership_v1;count=', 'UTF8');
Database/migrations/035_experiment_recommendation_ranking.sql:347:CREATE OR REPLACE FUNCTION enforce_recommendation_ranking_member_insert()
Database/migrations/035_experiment_recommendation_ranking.sql:366:              FROM experiment_recommendation_ranking_member existing
Database/migrations/035_experiment_recommendation_ranking.sql:403:      AND recommendation_ranking_membership_contains(
Database/migrations/035_experiment_recommendation_ranking.sql:410:              FROM experiment_recommendation_ranking_member existing
Database/migrations/035_experiment_recommendation_ranking.sql:418:              FROM experiment_recommendation_ranking_member existing
Database/migrations/035_experiment_recommendation_ranking.sql:468:                  CONSTRAINT = 'recommendation_ranking_member_authority_check';
Database/migrations/035_experiment_recommendation_ranking.sql:508:    FROM experiment_recommendation_ranking_member
Database/migrations/035_experiment_recommendation_ranking.sql:531:        'ALTER FUNCTION %I.enforce_recommendation_ranking_member_insert() '
Database/migrations/035_experiment_recommendation_ranking.sql:542:DROP TRIGGER IF EXISTS enforce_recommendation_ranking_member_insert_trigger
Database/migrations/035_experiment_recommendation_ranking.sql:543:    ON experiment_recommendation_ranking_member;
Database/migrations/035_experiment_recommendation_ranking.sql:544:CREATE TRIGGER enforce_recommendation_ranking_member_insert_trigger
Database/migrations/035_experiment_recommendation_ranking.sql:545:BEFORE INSERT ON experiment_recommendation_ranking_member
Database/migrations/035_experiment_recommendation_ranking.sql:546:FOR EACH ROW EXECUTE FUNCTION enforce_recommendation_ranking_member_insert();
Database/migrations/035_experiment_recommendation_ranking.sql:560:CREATE INDEX IF NOT EXISTS recommendation_ranking_member_result_idx
Database/migrations/035_experiment_recommendation_ranking.sql:561:    ON experiment_recommendation_ranking_member(
Database/migrations/035_experiment_recommendation_ranking.sql:569:REVOKE UPDATE, DELETE ON experiment_recommendation_ranking_member FROM pqxx;
Database/migrations/035_experiment_recommendation_ranking.sql:570:GRANT SELECT, INSERT ON experiment_recommendation_ranking_member TO pqxx;
Database/migrations/035_experiment_recommendation_ranking.sql:579:        pg_get_serial_sequence('experiment_recommendation_ranking_member',
Database/migrations/035_experiment_recommendation_ranking.sql:580:                               'recommendation_ranking_member_id')
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:10:CREATE UNIQUE INDEX IF NOT EXISTS recommendation_ranking_member_provenance_uidx
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:11:    ON experiment_recommendation_ranking_member(
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:12:        recommendation_ranking_member_id, recommendation_id,
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:97:experiment_recommendation_campaign_materialization_member (
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:98:    recommendation_campaign_materialization_member_id bigserial PRIMARY KEY
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:99:        CHECK (recommendation_campaign_materialization_member_id > 0),
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:104:    recommendation_ranking_member_id bigint NOT NULL,
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:122:    CONSTRAINT recommendation_campaign_materialization_member_rank_fkey
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:123:        FOREIGN KEY (recommendation_ranking_member_id, recommendation_id,
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:125:        REFERENCES experiment_recommendation_ranking_member(
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:126:            recommendation_ranking_member_id, recommendation_id,
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:128:    CONSTRAINT recommendation_campaign_materialization_member_proposal_fkey
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:134:    CONSTRAINT recommendation_campaign_materialization_member_ordinal_uidx
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:136:    CONSTRAINT recommendation_campaign_materialization_member_rank_uidx
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:138:                recommendation_ranking_member_id),
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:139:    CONSTRAINT recommendation_campaign_materialization_member_proposal_uidx
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:191:enforce_recommendation_campaign_materialization_member_insert()
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:199:    JOIN experiment_recommendation_ranking_member ranking_member
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:200:      ON ranking_member.recommendation_ranking_member_id =
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:201:             NEW.recommendation_ranking_member_id
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:222:                      'recommendation_campaign_materialization_member_authority_check';
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:241:    FROM experiment_recommendation_campaign_materialization_member
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:264:        'ALTER FUNCTION %I.enforce_recommendation_campaign_materialization_member_insert() '
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:283:enforce_recommendation_campaign_materialization_member_insert_trigger
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:284:ON experiment_recommendation_campaign_materialization_member;
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:286:enforce_recommendation_campaign_materialization_member_insert_trigger
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:287:BEFORE INSERT ON experiment_recommendation_campaign_materialization_member
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:289:    enforce_recommendation_campaign_materialization_member_insert();
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:303:COMMENT ON TABLE experiment_recommendation_campaign_materialization_member IS
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:309:    experiment_recommendation_campaign_materialization_member FROM PUBLIC;
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:313:    experiment_recommendation_campaign_materialization_member FROM pqxx;
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:318:    enforce_recommendation_campaign_materialization_member_insert()
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:324:GRANT SELECT ON experiment_recommendation_campaign_materialization_member TO pqxx;
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:340:    recommendation_ranking_member_id,recommendation_id,source_experiment_id,
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:344:ON experiment_recommendation_campaign_materialization_member TO pqxx;
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:354:            'experiment_recommendation_campaign_materialization_member',
Database/migrations/041_experiment_recommendation_campaign_materialization.sql:355:            'recommendation_campaign_materialization_member_id')]

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

exec
/bin/bash -lc "sed -n '87,160p' Database/migrations/035_experiment_recommendation_ranking.sql && sed -n '90,150p' Database/migrations/041_experiment_recommendation_campaign_materialization.sql" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
CREATE TABLE IF NOT EXISTS experiment_recommendation_ranking_member (
    recommendation_ranking_member_id bigserial PRIMARY KEY,
    recommendation_ranking_snapshot_id bigint NOT NULL REFERENCES
        experiment_recommendation_ranking_snapshot(
            recommendation_ranking_snapshot_id),
    recommendation_evaluation_result_id bigint NOT NULL REFERENCES
        experiment_recommendation_evaluation_result(
            recommendation_evaluation_result_id),
    recommendation_id bigint NOT NULL REFERENCES
        experiment_recommendation(recommendation_id),
    recommendation_semantic_hash text NOT NULL
        CHECK (btrim(recommendation_semantic_hash)<>''),
    evaluation_identity_hash text NOT NULL
        CHECK (btrim(evaluation_identity_hash)<>''),
    bucket text NOT NULL CHECK (bucket IN (
        'advisory_ready','blocked','non_actionable')),
    bucket_rank integer NOT NULL CHECK (bucket_rank > 0),
    global_ordinal integer NOT NULL CHECK (global_ordinal > 0),
    final_score double precision,
    disposition text NOT NULL CHECK (disposition IN (
        'advisory_ready','insufficient_evidence',
        'blocked_pending_duplicate','blocked_active_duplicate',
        'completed_duplicate','stale_source_evidence',
        'unsupported_recommendation_family','invalid_persisted_evidence')),
    tie_break_primary text NOT NULL CHECK (btrim(tie_break_primary)<>''),
    tie_break_semantic_hash text NOT NULL
        CHECK (btrim(tie_break_semantic_hash)<>''),
    tie_break_evaluation_hash text NOT NULL
        CHECK (btrim(tie_break_evaluation_hash)<>''),
    inclusion_reason text NOT NULL CHECK (btrim(inclusion_reason)<>''),
    block_reason text,
    top_positive_component text,
    top_penalty_component text,
    symbol text NOT NULL CHECK (btrim(symbol)<>''),
    horizon integer NOT NULL CHECK (horizon > 0),
    family text NOT NULL CHECK (btrim(family)<>''),
    source_value_canonical text NOT NULL,
    proposed_value_canonical text NOT NULL,
    source_experiment_id bigint NOT NULL REFERENCES experiment(experiment_id),
    source_model_id bigint REFERENCES model(model_id),
    source_analysis_id bigint REFERENCES experiment_analysis_result(analysis_id),
    recommendation_scan_id bigint NOT NULL REFERENCES
        experiment_recommendation_scan(recommendation_scan_id),
    created_at timestamptz NOT NULL DEFAULT now(),
    CONSTRAINT recommendation_ranking_member_result_uidx UNIQUE
        (recommendation_ranking_snapshot_id,recommendation_evaluation_result_id),
    CONSTRAINT recommendation_ranking_member_bucket_rank_uidx UNIQUE
        (recommendation_ranking_snapshot_id,bucket,bucket_rank),
    CONSTRAINT recommendation_ranking_member_ordinal_uidx UNIQUE
        (recommendation_ranking_snapshot_id,global_ordinal),
    CONSTRAINT recommendation_ranking_member_shape_check CHECK (
        (bucket='advisory_ready' AND disposition='advisory_ready'
            AND final_score IS NOT NULL AND final_score>=0.0 AND final_score<=1.0
            AND block_reason IS NULL)
        OR (bucket='blocked' AND disposition IN (
                'blocked_pending_duplicate','blocked_active_duplicate',
                'completed_duplicate') AND final_score IS NULL
            AND block_reason IS NOT NULL AND btrim(block_reason)<>'')
        OR (bucket='non_actionable' AND disposition IN (
                'insufficient_evidence','stale_source_evidence',
                'unsupported_recommendation_family','invalid_persisted_evidence')
            AND final_score IS NULL AND block_reason IS NOT NULL
            AND btrim(block_reason)<>'')
    ),
    CONSTRAINT recommendation_ranking_member_finite_check CHECK (
        final_score IS NULL OR final_score NOT IN (
            'NaN'::double precision,'Infinity'::double precision,
            '-Infinity'::double precision))
);

CREATE OR REPLACE FUNCTION recommendation_ranking_membership_contains(
    membership_canonical text,
    target_evaluation_identity text,
    target_evaluation_result_id bigint)
        ON DELETE RESTRICT,
    CONSTRAINT recommendation_campaign_materialization_counts_check CHECK (
        selected_member_count = initially_created_proposal_count +
            initially_reused_proposal_count)
);

CREATE TABLE IF NOT EXISTS
experiment_recommendation_campaign_materialization_member (
    recommendation_campaign_materialization_member_id bigserial PRIMARY KEY
        CHECK (recommendation_campaign_materialization_member_id > 0),
    recommendation_campaign_materialization_id bigint NOT NULL REFERENCES
        experiment_recommendation_campaign_materialization(
            recommendation_campaign_materialization_id) ON DELETE RESTRICT,
    member_ordinal integer NOT NULL CHECK (member_ordinal > 0),
    recommendation_ranking_member_id bigint NOT NULL,
    recommendation_id bigint NOT NULL REFERENCES
        experiment_recommendation(recommendation_id) ON DELETE RESTRICT,
    source_experiment_id bigint NOT NULL REFERENCES
        experiment(experiment_id) ON DELETE RESTRICT,
    ranking_position integer NOT NULL CHECK (ranking_position > 0),
    selected_member_identity_canonical text COLLATE "C" NOT NULL CHECK (
        btrim(selected_member_identity_canonical) <> '' AND
        octet_length(selected_member_identity_canonical) <= 4194304),
    selected_member_identity_hash text NOT NULL CHECK (
        selected_member_identity_hash ~ '^fnv1a64:[0-9a-f]{16}$'),
    recommendation_conversion_proposal_id bigint NOT NULL,
    proposal_identity_canonical text COLLATE "C" NOT NULL CHECK (
        btrim(proposal_identity_canonical) <> '' AND
        octet_length(proposal_identity_canonical) <= 1048576),
    proposal_identity_hash text NOT NULL CHECK (
        proposal_identity_hash ~ '^fnv1a64:[0-9a-f]{16}$'),
    created_at timestamptz NOT NULL DEFAULT now(),
    CONSTRAINT recommendation_campaign_materialization_member_rank_fkey
        FOREIGN KEY (recommendation_ranking_member_id, recommendation_id,
                     source_experiment_id)
        REFERENCES experiment_recommendation_ranking_member(
            recommendation_ranking_member_id, recommendation_id,
            source_experiment_id) ON DELETE RESTRICT,
    CONSTRAINT recommendation_campaign_materialization_member_proposal_fkey
        FOREIGN KEY (recommendation_conversion_proposal_id,
                     recommendation_id, source_experiment_id)
        REFERENCES experiment_recommendation_conversion_proposal(
            recommendation_conversion_proposal_id, recommendation_id,
            source_experiment_id) ON DELETE RESTRICT,
    CONSTRAINT recommendation_campaign_materialization_member_ordinal_uidx
        UNIQUE (recommendation_campaign_materialization_id, member_ordinal),
    CONSTRAINT recommendation_campaign_materialization_member_rank_uidx
        UNIQUE (recommendation_campaign_materialization_id,
                recommendation_ranking_member_id),
    CONSTRAINT recommendation_campaign_materialization_member_proposal_uidx
        UNIQUE (recommendation_campaign_materialization_id,
                recommendation_conversion_proposal_id)
);

CREATE OR REPLACE FUNCTION
enforce_recommendation_campaign_materialization_manifest_insert()
RETURNS trigger
LANGUAGE plpgsql
SECURITY INVOKER
AS $$
BEGIN

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

exec
/bin/bash -lc "rg -n \"evaluation_result.*UNIQUE|recommendation_evaluation_result.*uidx|recommendation_id\" Database/migrations/034_experiment_recommendation_evaluation.sql | sed -n '1,180p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
25:    recommendation_id_filter bigint
26:        REFERENCES experiment_recommendation(recommendation_id),
62:    recommendation_id bigint NOT NULL
63:        REFERENCES experiment_recommendation(recommendation_id),
100:    CONSTRAINT recommendation_evaluation_result_run_recommendation_uidx
101:        UNIQUE (recommendation_evaluation_run_id, recommendation_id),
102:    CONSTRAINT experiment_recommendation_evaluation_result_run_ordinal_uidx
104:    CONSTRAINT experiment_recommendation_evaluation_result_identity_uidx
181:CREATE INDEX IF NOT EXISTS experiment_recommendation_evaluation_result_recommendation_idx
183:        recommendation_id, recommendation_evaluation_run_id DESC);

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

codex
The corrected focused suite and all nearby pure Phase 5 regressions are passing with warnings treated as errors. The review surface remains pure—no repository, schema, scheduler, experiment, or CLI code changed. I’m moving into the required sanitizer and incremental Release verification now, then I’ll audit the final untracked-file diff and Xcode project membership.
Narrow lifecycle, provenance, identity, cardinality, documentation, and test corrections are implemented and pass the focused warning-clean test plus nearby pure regressions.
  ✓ Inspect repository rules, worktree state, and identify the Phase 5 Step 5a implementation/test surface
  ✓ Review architecture, lifecycle/domain invariants, validation, determinism, serialization, and Step 5b compatibility
  ✓ Implement narrowly scoped corrections and add missing tests
  → Run focused tests, sanitizers, nearby regressions, and incremental Xcode Release build
  • Audit final diff and deliver commit-readiness assessment
exec
/bin/bash -lc 'clang++ -std=c++20 -Wall -Wextra -Wpedantic -Werror -fsanitize=address,undefined -fno-omit-frame-pointer -g -I Sources -I Headers -I Common Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp Sources/ExperimentRecommendation.cpp -o /tmp/step5a-review-sanitized && ASAN_OPTIONS=detect_leaks=1 UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1 /tmp/step5a-review-sanitized' in /Volumes/Developer SSD/ExpertAdvisor
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

 exited 134 in 4800ms:
==53978==AddressSanitizer: detect_leaks is not supported on this platform.
/bin/bash: line 1: 53978 Abort trap: 6           ASAN_OPTIONS=detect_leaks=1 UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1 /tmp/step5a-review-sanitized

exec
/bin/bash -lc 'ASAN_OPTIONS=detect_leaks=0:halt_on_error=1 UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1 /tmp/step5a-review-sanitized' in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

exec
/bin/bash -lc "rg -n \"Command line invocation|xcodebuild|DerivedData|BUILD SUCCEEDED|Build settings\" .derived-data-log-0CA5RPJ1 | tail -n 40 && find DerivedData -maxdepth 2 -type d | sed -n '1,40p' && find Build -maxdepth 2 -type d | sed -n '1,40p'" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
DerivedData
DerivedData/SDKStatCaches.noindex
DerivedData/SymbolCache.noindex
DerivedData/SymbolCache.noindex/MacOSX26.5.sdk (25F70)
DerivedData/Logs
DerivedData/Logs/Test
DerivedData/Logs/Launch
DerivedData/Logs/Build
DerivedData/Logs/Package
DerivedData/Logs/Localization
DerivedData/ModuleCache.noindex
DerivedData/ModuleCache.noindex/BCSOQCRJUNHT
DerivedData/ModuleCache.noindex/109N4H1ENENN5
DerivedData/ModuleCache.noindex/2YROBP2PSKY4Q
DerivedData/ModuleCache.noindex/3J33XGCGE042C
DerivedData/ModuleCache.noindex/3NG36GI4CVYZN
DerivedData/CompilationCache.noindex
DerivedData/CompilationCache.noindex/generic
DerivedData/ExpertAdvisor
DerivedData/ExpertAdvisor/SDKStatCaches.noindex
DerivedData/ExpertAdvisor/Index.noindex
DerivedData/ExpertAdvisor/KeywordIndex
DerivedData/ExpertAdvisor/SnapshotsPreviewCache
DerivedData/ExpertAdvisor/SymbolCache
DerivedData/ExpertAdvisor/Logs
DerivedData/ExpertAdvisor/ModuleCache.noindex
DerivedData/ExpertAdvisor/TextIndex
DerivedData/ExpertAdvisor/CompilationCache.noindex
DerivedData/ExpertAdvisor/Build
DerivedData/ExpertAdvisor/IntelligenceCodePreviewCache
DerivedData/ExpertAdvisor/Localization
Build
Build/XCBuildData
Build/XCBuildData/e2fcf57aabc4216e7d9cecb5ef19c8e3.xcbuilddata
Build/XCBuildData/c7f96a0c0481da0a17468167a9f3dda3.xcbuilddata
Build/XCBuildData/ce9619ac6a66ee5c4aee70aacf41238c.xcbuilddata
Build/XCBuildData/b118bbc38ae523113a1a2c330341ff53.xcbuilddata
Build/Products
Build/Products/Debug
Build/EagerLinkingTBDs
Build/Intermediates
Build/Intermediates/XCBuildData
Build/Intermediates/EagerLinkingTBDs
Build/Intermediates/ExpertAdvisor.build
Build/ExpertAdvisor.build
Build/ExpertAdvisor.build/Release

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

exec
/bin/bash -lc "xcodebuild -project ExpertAdvisor.xcodeproj -scheme 'LSTM Release' -configuration Release -showBuildSettings | rg '("'^'"\\s*(BUILD_DIR|BUILD_ROOT|CONFIGURATION_BUILD_DIR|OBJROOT|SYMROOT|PROJECT_TEMP_DIR|DERIVED_DATA_DIR))'" in /Volumes/Developer SSD/ExpertAdvisor
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

 succeeded in 2188ms:
--- xcodebuild: WARNING: Using the first of multiple matching destinations:
{ platform:macOS, arch:arm64, id:00008132-001C503911C3001C, name:My Mac }
{ platform:macOS, arch:x86_64, id:00008132-001C503911C3001C, name:My Mac }
{ platform:macOS, name:Any Mac }
    BUILD_DIR = /Volumes/Developer SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products
    BUILD_ROOT = /Volumes/Developer SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products
    CONFIGURATION_BUILD_DIR = /Volumes/Developer SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products/Release
    OBJROOT = /Volumes/Developer SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex
    PROJECT_TEMP_DIR = /Volumes/Developer SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex/ExpertAdvisor.build
    SYMROOT = /Volumes/Developer SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products

exec
/bin/bash -lc "xcodebuild -project ExpertAdvisor.xcodeproj -scheme 'LSTM Release' -configuration Release -destination 'platform=macOS,arch=arm64' build" in /Volumes/Developer SSD/ExpertAdvisor
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

 succeeded in 5751ms:
Command line invocation:
    /Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project ExpertAdvisor.xcodeproj -scheme "LSTM Release" -configuration Release -destination platform=macOS,arch=arm64 build

/Users/vjp/Library/Developer/Toolchains/LLVM22.xctoolchain:1:1: warning: failed to load toolchain: could not find Info.plist in /Users/vjp/Library/Developer/Toolchains/LLVM22.xctoolchain
ComputePackagePrebuildTargetDependencyGraph

Prepare packages

CreateBuildRequest

SendProjectDescription

CreateBuildOperation

ComputeTargetDependencyGraph
note: Building targets in dependency order
note: Target dependency graph (4 targets)
    Target 'LSTM Release' in project 'ExpertAdvisor'
        ➜ Explicit dependency on target 'MetaNN_metal' in project 'MetaNNXC'
        ➜ Explicit dependency on target 'MetaNN' in project 'MetaNNXC'
        ➜ Implicit dependency on target 'MetalBuffer' in project 'MetalSwift' via file 'libMetalBuffer.a' in build phase 'Link Binary'
    Target 'MetaNN' in project 'MetaNNXC'
        ➜ Explicit dependency on target 'MetaNN_metal' in project 'MetaNNXC'
    Target 'MetaNN_metal' in project 'MetaNNXC'
        ➜ Explicit dependency on target 'MetalBuffer' in project 'MetalSwift'
    Target 'MetalBuffer' in project 'MetalSwift' (no dependencies)

GatherProvisioningInputs

CreateBuildDescription

ExecuteExternalTool /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang -v -E -dM -arch arm64 -isysroot /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk -x objective-c++ -c /dev/null

ExecuteExternalTool /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang -v -E -dM -isysroot /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk -x c -c /dev/null

ExecuteExternalTool /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang -v -E -dM -arch arm64 -isysroot /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk -x c++ -c /dev/null

ExecuteExternalTool /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang -v -E -dM -arch arm64 -isysroot /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk -x c -c /dev/null

ExecuteExternalTool /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/libtool -V

ExecuteExternalTool /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/ld -version_details

Build description signature: fd770b0874d9134ed50a839b174e2bfb
Build description path: /Volumes/Developer SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex/XCBuildData/fd770b0874d9134ed50a839b174e2bfb.xcbuilddata
ClangStatCache /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang-stat-cache /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/SDKStatCaches.noindex/macosx26.5-25F70-688ef53f1462e2c8f657fdc38a81448fe082c4a02f00227109f4ed75e425c832.sdkstatcache
    cd /Volumes/Developer\ SSD/ExpertAdvisor/ExpertAdvisor.xcodeproj
    /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang-stat-cache /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk -o /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/SDKStatCaches.noindex/macosx26.5-25F70-688ef53f1462e2c8f657fdc38a81448fe082c4a02f00227109f4ed75e425c832.sdkstatcache

CompileC /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex/ExpertAdvisor.build/Release/LSTM\ Release.build/Objects-normal/arm64/ExperimentRecommendationCampaignOutcomeAssessment.o /Volumes/Developer\ SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp normal arm64 c++ com.apple.compilers.llvm.clang.1_0.compiler (in target 'LSTM Release' from project 'ExpertAdvisor')
    cd /Volumes/Developer\ SSD/ExpertAdvisor
    
    Using response file: /Volumes/Developer SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex/ExpertAdvisor.build/Release/LSTM Release.build/Objects-normal/arm64/82b82416624d2658e5098eb0a28c15c5-common-args.resp
    
    /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang -x c++ -ivfsstatcache /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/SDKStatCaches.noindex/macosx26.5-25F70-688ef53f1462e2c8f657fdc38a81448fe082c4a02f00227109f4ed75e425c832.sdkstatcache -target arm64-apple-macos26.2 -fmessage-length\=0 -fdiagnostics-show-note-include-stack -fmacro-backtrace-limit\=0 -fno-color-diagnostics -fmodules-prune-interval\=86400 -fmodules-prune-after\=345600 -fbuild-session-file\=/Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ModuleCache.noindex/Session.modulevalidation -fmodules-validate-once-per-build-session -Wnon-modular-include-in-framework-module -Werror\=non-modular-include-in-framework-module -Wno-trigraphs -Wno-missing-field-initializers -Wno-missing-prototypes -Werror\=return-type -Wdocumentation -Wunreachable-code -Wquoted-include-in-framework-header -Werror\=deprecated-objc-isa-usage -Werror\=objc-root-class -Wno-non-virtual-dtor -Wno-overloaded-virtual -Wno-exit-time-destructors -Wno-missing-braces -Wparentheses -Wswitch -Wunused-function -Wno-unused-label -Wno-unused-parameter -Wunused-variable -Wunused-value -Wempty-body -Wuninitialized -Wconditional-uninitialized -Wno-unknown-pragmas -Wno-shadow -Wno-four-char-constants -Wno-conversion -Wconstant-conversion -Wint-conversion -Wbool-conversion -Wenum-conversion -Wno-float-conversion -Wnon-literal-null-conversion -Wobjc-literal-conversion -Wshorten-64-to-32 -Wno-newline-eof -Wno-c++11-extensions -Wno-implicit-fallthrough -isysroot /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk -fstrict-aliasing -Wdeprecated-declarations -Winvalid-offsetof -Wno-sign-conversion -Winfinite-recursion -Wmove -Wcomma -Wblock-capture-autoreleasing -Wstrict-prototypes -Wrange-loop-analysis -Wno-semicolon-before-method-body -Wunguarded-availability @/Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex/ExpertAdvisor.build/Release/LSTM\ Release.build/Objects-normal/arm64/82b82416624d2658e5098eb0a28c15c5-common-args.resp -MMD -MT dependencies -MF /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex/ExpertAdvisor.build/Release/LSTM\ Release.build/Objects-normal/arm64/ExperimentRecommendationCampaignOutcomeAssessment.d --serialize-diagnostics /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex/ExpertAdvisor.build/Release/LSTM\ Release.build/Objects-normal/arm64/ExperimentRecommendationCampaignOutcomeAssessment.dia -c /Volumes/Developer\ SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp -o /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex/ExpertAdvisor.build/Release/LSTM\ Release.build/Objects-normal/arm64/ExperimentRecommendationCampaignOutcomeAssessment.o

Ld /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products/Release/LSTM_Release normal (in target 'LSTM Release' from project 'ExpertAdvisor')
    cd /Volumes/Developer\ SSD/ExpertAdvisor
    /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang++ -Xlinker -reproducible -target arm64-apple-macos26.2 -isysroot /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk -O3 -L/Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex/EagerLinkingTBDs/Release -L/Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products/Release -L/opt/homebrew/opt/libpqxx@7.10.1/lib -L/opt/homebrew/opt/libpq/lib -L/opt/homebrew/lib -L/Volumes/Developer\ SSD/ExpertAdvisor -L/opt/homebrew/lib -L/opt/homebrew/lib/postgresql@17 -L/opt/homebrew/opt/libomp/lib -F/Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex/EagerLinkingTBDs/Release -F/Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products/Release -filelist /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex/ExpertAdvisor.build/Release/LSTM\ Release.build/Objects-normal/arm64/LSTM_Release.LinkFileList -Xlinker -dead_strip -Xlinker -object_path_lto -Xlinker /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex/ExpertAdvisor.build/Release/LSTM\ Release.build/Objects-normal/arm64/LSTM_Release_lto.o -Xlinker -dependency_info -Xlinker /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex/ExpertAdvisor.build/Release/LSTM\ Release.build/Objects-normal/arm64/LSTM_Release_dependency_info.dat -stdlib\=libc++ -lpq -lpqxx -lomp -framework Foundation -framework Metal -framework MetalPerformanceShaders -lMetalBuffer /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products/Release/libMetaNN.a -Xlinker -no_adhoc_codesign -o /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products/Release/LSTM_Release

GenerateDSYMFile /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products/Release/LSTM_Release.dSYM /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products/Release/LSTM_Release (in target 'LSTM Release' from project 'ExpertAdvisor')
    cd /Volumes/Developer\ SSD/ExpertAdvisor
    /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/dsymutil /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products/Release/LSTM_Release -o /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products/Release/LSTM_Release.dSYM

CodeSign /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products/Release/LSTM_Release (in target 'LSTM Release' from project 'ExpertAdvisor')
    cd /Volumes/Developer\ SSD/ExpertAdvisor
    
    Signing Identity:     "Sign to Run Locally"
    
    /usr/bin/codesign --force --sign - --entitlements /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex/ExpertAdvisor.build/Release/LSTM\ Release.build/LSTM_Release.xcent --timestamp\=none --generate-entitlement-der /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products/Release/LSTM_Release

RegisterExecutionPolicyException /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products/Release/LSTM_Release (in target 'LSTM Release' from project 'ExpertAdvisor')
    cd /Volumes/Developer\ SSD/ExpertAdvisor
    builtin-RegisterExecutionPolicyException /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Products/Release/LSTM_Release

** BUILD SUCCEEDED **


diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

exec
/bin/bash -lc 'for file in Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst; do git diff --no-index --check /dev/null "$file" 2>&1; done
git diff --check
git diff --cached --check
git status --short --branch
wc -l Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst' in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
## phase5...github/phase5
 M ExpertAdvisor.xcodeproj/project.pbxproj
 M refresh_candlestick_materialized_view.plpgsql
?? Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
?? Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
?? Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
?? docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
    1416 Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
     531 Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
     818 Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
      60 docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
    2825 total

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

codex
A new unrelated worktree modification appeared during verification: `refresh_candlestick_materialized_view.plpgsql` was clean at the start and is now modified. None of the review/build commands target that file, and I’m leaving it untouched; I’ll exclude it from the Step 5a change list and call it out explicitly in the handoff so the final commit scope remains clean.
exec
/bin/bash -lc "git diff -- refresh_candlestick_materialized_view.plpgsql && stat -f '%N | modified=%Sm | size=%z' -t '%Y-%m-%d %H:%M:%S %z' refresh_candlestick_materialized_view.plpgsql && git diff --numstat -- ExpertAdvisor.xcodeproj/project.pbxproj && git diff -- ExpertAdvisor.xcodeproj/project.pbxproj" in /Volumes/Developer SSD/ExpertAdvisor
 succeeded in 0ms:
diff --git a/refresh_candlestick_materialized_view.plpgsql b/refresh_candlestick_materialized_view.plpgsql
index 3fbd36e..5700876 100644
--- a/refresh_candlestick_materialized_view.plpgsql
+++ b/refresh_candlestick_materialized_view.plpgsql
@@ -9,36 +9,12 @@ declare
 	ispopulated boolean;
 	pair cursor(key text) for select tablename from pg_tables  where tablename like key  order by tablename;
 begin
-	FOR y in 2000..2030 LOOP
+	FOR y in 2026..2030 LOOP
 	select substring('''%' || y || '%''',2,6) into key;
 	FOR f in pair(key)
 	LOOP
 		select substring(f::text, 2, 11) into tbl;
 		raise info 'f: %, tbl: %', f, tbl;
-		qs = format('refresh materialized view %I_candlestick1m', tbl);
-		begin
-			qs2 = format('select ispopulated from pg_matviews where matviewname like ''%I_candlestick1m''', tbl);
-			execute qs2 into ispopulated;
-			raise info '%_candlestick1m ispopulated: %', tbl, ispopulated;	
-			if ispopulated is not true then
-				raise info 'executing: %', qs;
-				execute  qs;
-			end if;
-		exception when others then 
-			raise notice '% %', SQLERRM, SQLSTATE;
-		end;
-		qs = format('refresh materialized view %I_candlestick5m', tbl);
-		begin
-			qs2 = format('select ispopulated from pg_matviews where matviewname like ''%I_candlestick5m''', tbl);
-			execute qs2 into ispopulated;
-			raise info '%_candlestick5m ispopulated: %', tbl, ispopulated;	
-			if ispopulated is not true then
-				raise info 'executing: %', qs;
-				execute  qs;
-			end if;
-		exception when others then 
-			raise notice '% %', SQLERRM, SQLSTATE;
-		end;
 		qs = format('refresh materialized view %I_candlestick15m', tbl);
 		begin
 			qs2 = format('select ispopulated from pg_matviews where matviewname like ''%I_candlestick15m''', tbl);
refresh_candlestick_materialized_view.plpgsql | modified=2026-07-19 23:17:39 -0500 | size=2305
8	0	ExpertAdvisor.xcodeproj/project.pbxproj
diff --git a/ExpertAdvisor.xcodeproj/project.pbxproj b/ExpertAdvisor.xcodeproj/project.pbxproj
index b1fa83b..c425d3b 100644
--- a/ExpertAdvisor.xcodeproj/project.pbxproj
+++ b/ExpertAdvisor.xcodeproj/project.pbxproj
@@ -149,6 +149,8 @@
 		0B570009307C000100AAA001 /* ExperimentRecommendationCampaignStatusRepository.cpp in Sources */ = {isa = PBXBuildFile; fileRef = 0B570002307C000100AAA001 /* ExperimentRecommendationCampaignStatusRepository.cpp */; };
 		0B57000A307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp in Sources */ = {isa = PBXBuildFile; fileRef = 0B570004307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp */; };
 		0B57000B307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp in Sources */ = {isa = PBXBuildFile; fileRef = 0B570004307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp */; };
+		0B580002307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp in Sources */ = {isa = PBXBuildFile; fileRef = 0B580000307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp */; };
+		0B580003307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp in Sources */ = {isa = PBXBuildFile; fileRef = 0B580000307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp */; };
 /* End PBXBuildFile section */
 
 /* Begin PBXContainerItemProxy section */
@@ -378,6 +380,8 @@
 		0B570003307C000100AAA001 /* ExperimentRecommendationCampaignStatusRepository.hpp */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.cpp.h; name = ExperimentRecommendationCampaignStatusRepository.hpp; path = Sources/ExperimentRecommendationCampaignStatusRepository.hpp; sourceTree = SOURCE_ROOT; };
 		0B570004307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.cpp.cpp; name = ExperimentRecommendationCampaignStatusService.cpp; path = Sources/ExperimentRecommendationCampaignStatusService.cpp; sourceTree = SOURCE_ROOT; };
 		0B570005307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.hpp */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.cpp.h; name = ExperimentRecommendationCampaignStatusService.hpp; path = Sources/ExperimentRecommendationCampaignStatusService.hpp; sourceTree = SOURCE_ROOT; };
+		0B580000307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.cpp.cpp; name = ExperimentRecommendationCampaignOutcomeAssessment.cpp; path = Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp; sourceTree = SOURCE_ROOT; };
+		0B580001307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.hpp */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.cpp.h; name = ExperimentRecommendationCampaignOutcomeAssessment.hpp; path = Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp; sourceTree = SOURCE_ROOT; };
 /* End PBXFileReference section */
 
 /* Begin PBXFrameworksBuildPhase section */
@@ -575,6 +579,8 @@
 				0B570003307C000100AAA001 /* ExperimentRecommendationCampaignStatusRepository.hpp */,
 				0B570004307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp */,
 				0B570005307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.hpp */,
+				0B580000307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp */,
+				0B580001307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.hpp */,
 				0A5000082FC0000100AAA001 /* WorkerLifecycleDiagnostics.hpp */,
 				0A4000002FB0000100AAA001 /* RunMetadata.cpp */,
 				0890B6A42F1DC54900CEDECF /* LSTM.cpp */,
@@ -819,6 +825,7 @@
 				0B570006307C000100AAA001 /* ExperimentRecommendationCampaignStatus.cpp in Sources */,
 				0B570008307C000100AAA001 /* ExperimentRecommendationCampaignStatusRepository.cpp in Sources */,
 				0B57000A307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp in Sources */,
+				0B580002307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp in Sources */,
 				0A4000012FB0000100AAA001 /* RunMetadata.cpp in Sources */,
 				08395C292EDA4EC2005B70A0 /* db_cursor.cpp in Sources */,
 				0890B6A52F1DC54900CEDECF /* LSTM.cpp in Sources */,
@@ -892,6 +899,7 @@
 				0B570007307C000100AAA001 /* ExperimentRecommendationCampaignStatus.cpp in Sources */,
 				0B570009307C000100AAA001 /* ExperimentRecommendationCampaignStatusRepository.cpp in Sources */,
 				0B57000B307C000100AAA001 /* ExperimentRecommendationCampaignStatusService.cpp in Sources */,
+				0B580003307D000100AAA001 /* ExperimentRecommendationCampaignOutcomeAssessment.cpp in Sources */,
 				0A4000022FB0000100AAA001 /* RunMetadata.cpp in Sources */,
 				0A1000042F70000100AAA001 /* db_cursor.cpp in Sources */,
 				0A1000052F70000100AAA001 /* LSTM.cpp in Sources */,

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..c9b6726d3b32ddb054205c74f6e7d8d4b0d62265
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,13 +433,37 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
 
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
+
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
     const MemberEvidence incompleteResult(
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -619,6 +672,12 @@
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +750,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

apply patch
patch: completed
/Volumes/Developer SSD/ExpertAdvisor/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..89e8284501fc6f3e4f977bd75c544fb3175ca0dc
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -617,8 +670,19 @@
     assert(changedProvenanceAssessment.members[0].outcome ==
         MemberOutcome::SucceededComparable);
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
+    assert(comparable.identity.contractVersion ==
+        kRecommendationCampaignOutcomeAssessmentContractVersion);
+    assert(comparable.identity.canonicalText.starts_with(
+        "experiment_recommendation_campaign_outcome_assessment_v2;"
+        "assessment_contract_version=2;"));
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +755,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..89e8284501fc6f3e4f977bd75c544fb3175ca0dc
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -617,8 +670,19 @@
     assert(changedProvenanceAssessment.members[0].outcome ==
         MemberOutcome::SucceededComparable);
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
+    assert(comparable.identity.contractVersion ==
+        kRecommendationCampaignOutcomeAssessmentContractVersion);
+    assert(comparable.identity.canonicalText.starts_with(
+        "experiment_recommendation_campaign_outcome_assessment_v2;"
+        "assessment_contract_version=2;"));
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +755,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

exec
/bin/bash -lc 'clang++ -std=c++20 -Wall -Wextra -Wpedantic -Werror -I Sources -I Headers -I Common Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp Sources/ExperimentRecommendation.cpp -o /tmp/step5a-review-test && /tmp/step5a-review-test && clang++ -std=c++20 -Wall -Wextra -Wpedantic -Werror -fsanitize=address,undefined -fno-omit-frame-pointer -g -I Sources -I Headers -I Common Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp Sources/ExperimentRecommendation.cpp -o /tmp/step5a-review-sanitized && ASAN_OPTIONS=detect_leaks=0:halt_on_error=1 UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1 /tmp/step5a-review-sanitized' in /Volumes/Developer SSD/ExpertAdvisor
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..89e8284501fc6f3e4f977bd75c544fb3175ca0dc
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -617,8 +670,19 @@
     assert(changedProvenanceAssessment.members[0].outcome ==
         MemberOutcome::SucceededComparable);
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
+    assert(comparable.identity.contractVersion ==
+        kRecommendationCampaignOutcomeAssessmentContractVersion);
+    assert(comparable.identity.canonicalText.starts_with(
+        "experiment_recommendation_campaign_outcome_assessment_v2;"
+        "assessment_contract_version=2;"));
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +755,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

 succeeded in 6237ms:
exec
/bin/bash -lc "xcodebuild -project ExpertAdvisor.xcodeproj -scheme 'LSTM Release' -configuration Release -destination 'platform=macOS,arch=arm64' build" in /Volumes/Developer SSD/ExpertAdvisor
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..89e8284501fc6f3e4f977bd75c544fb3175ca0dc
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -617,8 +670,19 @@
     assert(changedProvenanceAssessment.members[0].outcome ==
         MemberOutcome::SucceededComparable);
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
+    assert(comparable.identity.contractVersion ==
+        kRecommendationCampaignOutcomeAssessmentContractVersion);
+    assert(comparable.identity.canonicalText.starts_with(
+        "experiment_recommendation_campaign_outcome_assessment_v2;"
+        "assessment_contract_version=2;"));
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +755,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

 succeeded in 1775ms:
Command line invocation:
    /Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project ExpertAdvisor.xcodeproj -scheme "LSTM Release" -configuration Release -destination platform=macOS,arch=arm64 build

/Users/vjp/Library/Developer/Toolchains/LLVM22.xctoolchain:1:1: warning: failed to load toolchain: could not find Info.plist in /Users/vjp/Library/Developer/Toolchains/LLVM22.xctoolchain
ComputePackagePrebuildTargetDependencyGraph

Prepare packages

CreateBuildRequest

SendProjectDescription

CreateBuildOperation

ComputeTargetDependencyGraph
note: Building targets in dependency order
note: Target dependency graph (4 targets)
    Target 'LSTM Release' in project 'ExpertAdvisor'
        ➜ Explicit dependency on target 'MetaNN_metal' in project 'MetaNNXC'
        ➜ Explicit dependency on target 'MetaNN' in project 'MetaNNXC'
        ➜ Implicit dependency on target 'MetalBuffer' in project 'MetalSwift' via file 'libMetalBuffer.a' in build phase 'Link Binary'
    Target 'MetaNN' in project 'MetaNNXC'
        ➜ Explicit dependency on target 'MetaNN_metal' in project 'MetaNNXC'
    Target 'MetaNN_metal' in project 'MetaNNXC'
        ➜ Explicit dependency on target 'MetalBuffer' in project 'MetalSwift'
    Target 'MetalBuffer' in project 'MetalSwift' (no dependencies)

GatherProvisioningInputs

CreateBuildDescription

ExecuteExternalTool /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang -v -E -dM -arch arm64 -isysroot /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk -x objective-c++ -c /dev/null

ExecuteExternalTool /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang -v -E -dM -isysroot /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk -x c -c /dev/null

ExecuteExternalTool /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang -v -E -dM -arch arm64 -isysroot /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk -x c++ -c /dev/null

ExecuteExternalTool /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang -v -E -dM -arch arm64 -isysroot /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk -x c -c /dev/null

ExecuteExternalTool /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/libtool -V

ExecuteExternalTool /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/ld -version_details

Build description signature: e00bb3bce1787c75d888e63cec7588e2
Build description path: /Volumes/Developer SSD/ExpertAdvisor/DerivedData/ExpertAdvisor/Build/Intermediates.noindex/XCBuildData/e00bb3bce1787c75d888e63cec7588e2.xcbuilddata
ClangStatCache /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang-stat-cache /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/SDKStatCaches.noindex/macosx26.5-25F70-688ef53f1462e2c8f657fdc38a81448fe082c4a02f00227109f4ed75e425c832.sdkstatcache
    cd /Volumes/Developer\ SSD/ExpertAdvisor/ExpertAdvisor.xcodeproj
    /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang-stat-cache /Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX26.5.sdk -o /Volumes/Developer\ SSD/ExpertAdvisor/DerivedData/SDKStatCaches.noindex/macosx26.5-25F70-688ef53f1462e2c8f657fdc38a81448fe082c4a02f00227109f4ed75e425c832.sdkstatcache

** BUILD SUCCEEDED **


diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..89e8284501fc6f3e4f977bd75c544fb3175ca0dc
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -617,8 +670,19 @@
     assert(changedProvenanceAssessment.members[0].outcome ==
         MemberOutcome::SucceededComparable);
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
+    assert(comparable.identity.contractVersion ==
+        kRecommendationCampaignOutcomeAssessmentContractVersion);
+    assert(comparable.identity.canonicalText.starts_with(
+        "experiment_recommendation_campaign_outcome_assessment_v2;"
+        "assessment_contract_version=2;"));
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +755,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

exec
/bin/bash -lc 'git branch --show-current && git rev-parse HEAD && git log -1 --oneline && git diff --check && git diff --cached --check && for file in Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst; do git diff --no-index --check /dev/null "$file" 2>&1; done && true' in /Volumes/Developer SSD/ExpertAdvisor
 exited 1 in 0ms:
phase5
8b9939e1f8862e49d7411453baecd43fbe5a7c82
8b9939e Phase 5 Step 4: Add recommendation campaign operational status

Focused warning-clean tests, combined AddressSanitizer/UndefinedBehaviorSanitizer, five nearby pure campaign regressions, untracked-file whitespace checks, and the incremental Xcode Release build all pass. Final scope and concurrent unrelated worktree change were audited.
  ✓ Inspect repository rules, worktree state, and identify the Phase 5 Step 5a implementation/test surface
  ✓ Review architecture, lifecycle/domain invariants, validation, determinism, serialization, and Step 5b compatibility
  ✓ Implement narrowly scoped corrections and add missing tests
  ✓ Run focused tests, sanitizers, nearby regressions, and incremental Xcode Release build
  ✓ Audit final diff and deliver commit-readiness assessment
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..89e8284501fc6f3e4f977bd75c544fb3175ca0dc
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -617,8 +670,19 @@
     assert(changedProvenanceAssessment.members[0].outcome ==
         MemberOutcome::SucceededComparable);
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
+    assert(comparable.identity.contractVersion ==
+        kRecommendationCampaignOutcomeAssessmentContractVersion);
+    assert(comparable.identity.canonicalText.starts_with(
+        "experiment_recommendation_campaign_outcome_assessment_v2;"
+        "assessment_contract_version=2;"));
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +755,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

codex
## Architectural assessment

Phase 5 Step 5a is architecturally sound after correction. It now provides:

- Separate lifecycle, consistency, diagnostics, and scientific comparison models.
- Immutable, builder-controlled derived outputs.
- Deterministic metric, result-identity, diagnostic, member, and comparison ordering.
- Complete canonical identity binding, including authoritative campaign/materialization canonical text.
- Clear downstream-provenance ownership suitable for Step 5b.
- No repository, database, scheduler, experiment, persistence, or CLI behavior.

## Defects corrected

- Terminal success, failure, and cancellation now require an expected experiment identity.
- Duplicate expected experiment links produce member diagnostics instead of aborting trustworthy materialized membership.
- Known inconsistent workflow/lifecycle evidence can be carried into the assessment explicitly.
- Unexpected result evidence is retained and diagnosed instead of being rejected during construction.
- Cross-member model reuse is detected.
- Campaign and materialization identities must match by approval ID and canonical identity hash.
- Assessment identity now embeds authoritative upstream canonical texts, not merely compact hashes.
- Metric summary cardinalities use `std::size_t`, eliminating signed count-overflow risk.
- Tests were expanded for all corrected invariants and serialization versioning.

## Remaining concerns

No blocking code concerns.

Step 5b must populate the materialization’s stored approval identity hash and propagate known workflow inconsistency through `inputConsistency`. LeakSanitizer is unsupported on this macOS platform, but AddressSanitizer and UndefinedBehaviorSanitizer passed.

An unrelated modification to `refresh_candlestick_materialized_view.plpgsql` appeared during verification. I did not touch it; exclude it from the Step 5a commit.

## Files in the Step 5a commit set

- [OutcomeAssessment.hpp](</Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp>)
- [OutcomeAssessment.cpp](</Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp>)
- [OutcomeAssessmentTests.cpp](</Volumes/Developer SSD/ExpertAdvisor/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp>)
- [Step 5a documentation](</Volumes/Developer SSD/ExpertAdvisor/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst>)
- [Xcode project](</Volumes/Developer SSD/ExpertAdvisor/ExpertAdvisor.xcodeproj/project.pbxproj>)

## Verification results

- Focused Step 5a test with `-Wall -Wextra -Wpedantic -Werror`: passed.
- AddressSanitizer + UndefinedBehaviorSanitizer: passed with leak detection disabled because unsupported.
- Nearby pure regression tests passed:

  - Campaign execution
  - Campaign activation
  - Campaign launch
  - Campaign status
  - Campaign materialization

- Incremental Xcode Release build:

  ```text
  xcodebuild -project ExpertAdvisor.xcodeproj \
    -scheme "LSTM Release" -configuration Release \
    -destination "platform=macOS,arch=arm64" build
  ```

  Result: `BUILD SUCCEEDED`. DerivedData was reused and never cleaned. A non-fatal warning remains for an invalid local `LLVM22.xctoolchain` installation; Xcode’s default toolchain completed the build.

- Tracked and untracked whitespace checks: passed.
- Branch remains `phase5` at `8b9939e`.
- No commit created.

## Recommendation

Stage only the five Step 5a files listed above and exclude the unrelated PL/pgSQL modification.

**READY TO COMMIT**
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
index cdcdc73f6d25e8e424464a86ece88f57cdcbeb5f..b85b7498790a55b09834667a02802035c5014dd1
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp
@@ -80,6 +80,18 @@
         "recommendation_campaign_outcome_assessment_lifecycle_invalid");
 }
 
+void RequireValidConsistency(Consistency value)
+{
+    switch (value)
+    {
+        case Consistency::Consistent:
+        case Consistency::Inconsistent:
+            return;
+    }
+    throw std::invalid_argument(
+        "recommendation_campaign_outcome_assessment_consistency_invalid");
+}
+
 std::vector<Metric> CanonicalMetrics(std::vector<Metric> metrics)
 {
     std::vector<const Metric*> ordered;
@@ -290,10 +302,14 @@
         << ";assessment_contract_version="
         << kRecommendationCampaignOutcomeAssessmentContractVersion
         << ";campaign_approval_id=" << campaign.campaignApprovalId
+        << ";campaign_identity="
+        << LengthPrefixed(campaign.identityCanonical)
         << ";campaign_identity_hash=" << LengthPrefixed(campaign.identityHash)
         << ";materialization_id=" << materialization.materializationId
         << ";materialization_contract_version="
         << materialization.contractVersion
+        << ";materialization_identity="
+        << LengthPrefixed(materialization.identityCanonical)
         << ";materialization_identity_hash="
         << LengthPrefixed(materialization.identityHash);
     for (const auto& member : members)
@@ -390,16 +406,16 @@
     int terminalFailedMemberCount = 0;
     int terminalCancelledMemberCount = 0;
     int inconsistentOutcomeMemberCount = 0;
-    int metricComparisonCount = 0;
-    int comparableMetricCount = 0;
-    int contextChangedMetricCount = 0;
-    int missingSourceMetricCount = 0;
-    int missingResultMetricCount = 0;
-    int metricValueUnavailableCount = 0;
-    int unsupportedMetricCount = 0;
-    int positiveDeltaCount = 0;
-    int zeroDeltaCount = 0;
-    int negativeDeltaCount = 0;
+    std::size_t metricComparisonCount = 0;
+    std::size_t comparableMetricCount = 0;
+    std::size_t contextChangedMetricCount = 0;
+    std::size_t missingSourceMetricCount = 0;
+    std::size_t missingResultMetricCount = 0;
+    std::size_t metricValueUnavailableCount = 0;
+    std::size_t unsupportedMetricCount = 0;
+    std::size_t positiveDeltaCount = 0;
+    std::size_t zeroDeltaCount = 0;
+    std::size_t negativeDeltaCount = 0;
 };
 
 AggregateOutcome DeriveAggregateOutcome(const MutableSummary& counts)
@@ -528,12 +544,14 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationIdValue,
         long long campaignApprovalIdValue,
+        std::string campaignIdentityHashValue,
         int contractVersionValue,
         int memberCountValue,
         std::string identityCanonicalValue,
         std::string identityHashValue)
     : materializationId(materializationIdValue),
       campaignApprovalId(campaignApprovalIdValue),
+      campaignIdentityHash(std::move(campaignIdentityHashValue)),
       contractVersion(contractVersionValue),
       memberCount(memberCountValue),
       identityCanonical(std::move(identityCanonicalValue)),
@@ -546,6 +564,8 @@
         memberCount > kMaximumRecommendationCampaignOutcomeAssessmentMembers)
         throw std::invalid_argument(
             "recommendation_campaign_outcome_assessment_materialization_member_count_invalid");
+    RequireText(campaignIdentityHash,
+        "recommendation_campaign_outcome_assessment_campaign_identity_hash_missing");
     RequireText(identityCanonical,
         "recommendation_campaign_outcome_assessment_materialization_identity_missing");
     if (identityHash != RecommendationCanonicalHash(identityCanonical))
@@ -699,16 +719,16 @@
             sourceEvidenceValue,
         std::optional<
             RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidenceValue)
+            resultEvidenceValue,
+        Consistency inputConsistencyValue)
     : identity(std::move(identityValue)),
       lifecycle(lifecycleValue),
+      inputConsistency(inputConsistencyValue),
       sourceEvidence(std::move(sourceEvidenceValue)),
       resultEvidence(std::move(resultEvidenceValue))
 {
     RequireValidLifecycle(lifecycle);
-    if (lifecycle != Lifecycle::Succeeded && resultEvidence)
-        throw std::invalid_argument(
-            "recommendation_campaign_outcome_assessment_result_evidence_unexpected_for_lifecycle");
+    RequireValidConsistency(inputConsistency);
 }
 
 RecommendationCampaignOutcomeAssessmentMetricComparison::
@@ -768,16 +788,16 @@
         int terminalFailedMemberCountValue,
         int terminalCancelledMemberCountValue,
         int inconsistentOutcomeMemberCountValue,
-        int metricComparisonCountValue,
-        int comparableMetricCountValue,
-        int contextChangedMetricCountValue,
-        int missingSourceMetricCountValue,
-        int missingResultMetricCountValue,
-        int metricValueUnavailableCountValue,
-        int unsupportedMetricCountValue,
-        int positiveDeltaCountValue,
-        int zeroDeltaCountValue,
-        int negativeDeltaCountValue)
+        std::size_t metricComparisonCountValue,
+        std::size_t comparableMetricCountValue,
+        std::size_t contextChangedMetricCountValue,
+        std::size_t missingSourceMetricCountValue,
+        std::size_t missingResultMetricCountValue,
+        std::size_t metricValueUnavailableCountValue,
+        std::size_t unsupportedMetricCountValue,
+        std::size_t positiveDeltaCountValue,
+        std::size_t zeroDeltaCountValue,
+        std::size_t negativeDeltaCountValue)
     : outcome(outcomeValue),
       memberCount(memberCountValue),
       notTerminalLifecycleCount(notTerminalLifecycleCountValue),
@@ -815,11 +835,11 @@
         succeededComparableMemberCount + succeededContextChangedMemberCount +
         succeededMetricGapMemberCount + terminalFailedMemberCount +
         terminalCancelledMemberCount + inconsistentOutcomeMemberCount;
-    const int comparisonTotal = comparableMetricCount +
+    const std::size_t comparisonTotal = comparableMetricCount +
         contextChangedMetricCount + missingSourceMetricCount +
         missingResultMetricCount + metricValueUnavailableCount +
         unsupportedMetricCount;
-    const int deltaTotal =
+    const std::size_t deltaTotal =
         positiveDeltaCount + zeroDeltaCount + negativeDeltaCount;
     if (memberCount <= 0 || lifecycleTotal != memberCount ||
         consistencyTotal != memberCount || outcomeTotal != memberCount ||
@@ -1026,7 +1046,9 @@
         RequireText(observedAt,
             "recommendation_campaign_outcome_assessment_observed_at_invalid");
         if (materializationIdentity.campaignApprovalId !=
-            campaignIdentity.campaignApprovalId)
+                campaignIdentity.campaignApprovalId ||
+            materializationIdentity.campaignIdentityHash !=
+                campaignIdentity.identityHash)
             throw std::invalid_argument(
                 "recommendation_campaign_outcome_assessment_campaign_materialization_mismatch");
         if (materializationIdentity.contractVersion !=
@@ -1065,7 +1087,6 @@
         std::set<long long> rankingMemberIds;
         std::set<long long> recommendationIds;
         std::set<long long> proposalIds;
-        std::set<long long> expectedExperimentIds;
         std::vector<WorkingMember> working;
         working.reserve(ordered.size());
         for (std::size_t index = 0; index < ordered.size(); ++index)
@@ -1079,25 +1100,31 @@
                      .second ||
                 !recommendationIds.insert(evidence.identity.recommendationId)
                      .second ||
-                !proposalIds.insert(evidence.identity.proposalId).second ||
-                (evidence.identity.expectedExperimentId &&
-                    !expectedExperimentIds
-                         .insert(*evidence.identity.expectedExperimentId)
-                         .second))
+                !proposalIds.insert(evidence.identity.proposalId).second)
                 throw std::invalid_argument(
                     "recommendation_campaign_outcome_assessment_member_identity_set_invalid");
 
             WorkingMember item{&evidence, {}};
+            if (evidence.inputConsistency == Consistency::Inconsistent)
+                item.diagnostics.push_back(
+                    Diagnostic::InputEvidenceInconsistent);
             if (evidence.sourceEvidence &&
                 evidence.sourceEvidence->sourceExperimentId !=
                     evidence.identity.sourceExperimentId)
                 item.diagnostics.push_back(
                     Diagnostic::SourceExperimentIdentityMismatch);
+            if ((evidence.lifecycle == Lifecycle::Succeeded ||
+                    evidence.lifecycle == Lifecycle::Failed ||
+                    evidence.lifecycle == Lifecycle::Cancelled) &&
+                !evidence.identity.expectedExperimentId)
+                item.diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityMissing);
+            if (evidence.lifecycle != Lifecycle::Succeeded &&
+                evidence.resultEvidence)
+                item.diagnostics.push_back(
+                    Diagnostic::ResultEvidenceUnexpectedForLifecycle);
             if (evidence.lifecycle == Lifecycle::Succeeded)
             {
-                if (!evidence.identity.expectedExperimentId)
-                    item.diagnostics.push_back(
-                        Diagnostic::ExpectedExperimentIdentityMissing);
                 if (!evidence.resultEvidence)
                     item.diagnostics.push_back(Diagnostic::ResultEvidenceMissing);
                 else
@@ -1124,18 +1151,33 @@
             working.push_back(std::move(item));
         }
 
+        std::map<long long, std::vector<std::size_t>> expectedExperiments;
         std::map<long long, std::vector<std::size_t>> resultExperiments;
+        std::map<long long, std::vector<std::size_t>> resultModels;
         std::map<std::pair<std::string, long long>, std::vector<std::size_t>>
             resultIdentities;
         for (std::size_t index = 0; index < working.size(); ++index)
         {
+            const auto& expected =
+                working[index].evidence->identity.expectedExperimentId;
+            if (expected) expectedExperiments[*expected].push_back(index);
             const auto& result = working[index].evidence->resultEvidence;
             if (!result) continue;
             if (result->experimentId)
                 resultExperiments[*result->experimentId].push_back(index);
+            if (result->modelId)
+                resultModels[*result->modelId].push_back(index);
             for (const auto& identity : result->resultIdentities)
                 resultIdentities[{identity.kind, identity.id}].push_back(index);
         }
+        for (const auto& [unused, indices] : expectedExperiments)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ExpectedExperimentIdentityReused);
+        }
         for (const auto& [unused, indices] : resultExperiments)
         {
             static_cast<void>(unused);
@@ -1144,6 +1186,14 @@
                 working[index].diagnostics.push_back(
                     Diagnostic::ResultExperimentReused);
         }
+        for (const auto& [unused, indices] : resultModels)
+        {
+            static_cast<void>(unused);
+            if (indices.size() < 2) continue;
+            for (const auto index : indices)
+                working[index].diagnostics.push_back(
+                    Diagnostic::ResultModelIdentityReused);
+        }
         for (const auto& [unused, indices] : resultIdentities)
         {
             static_cast<void>(unused);
@@ -1266,10 +1316,17 @@
 EA_OUTCOME_ASSESSMENT_TEXT_FUNCTION(
     RecommendationCampaignOutcomeAssessmentDiagnosticCodeText,
     RecommendationCampaignOutcomeAssessmentDiagnosticCode,
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::InputEvidenceInconsistent,
+        "input_evidence_inconsistent")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::SourceExperimentIdentityMismatch,
         "source_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityMissing,
         "expected_experiment_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ExpectedExperimentIdentityReused,
+        "expected_experiment_identity_reused")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(
+        Diagnostic::ResultEvidenceUnexpectedForLifecycle,
+        "result_evidence_unexpected_for_lifecycle")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultEvidenceMissing,
         "result_evidence_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultExperimentIdentityMissing,
@@ -1278,6 +1335,8 @@
         "result_experiment_identity_mismatch")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityMissing,
         "result_model_identity_missing")
+    EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultModelIdentityReused,
+        "result_model_identity_reused")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultIdentityMissing,
         "result_identity_missing")
     EA_OUTCOME_ASSESSMENT_TEXT_CASE(Diagnostic::ResultContextMissing,
diff --git a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
index c6e64d9c37f8e36b2ac8a251d0fc6eed03e2e71f..56c79f36d1c92f8220b974d0b397da3fafa5ae67
--- a/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
+++ b/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstddef>
 #include <optional>
 #include <string>
 #include <vector>
@@ -34,6 +35,7 @@
 {
     const long long materializationId;
     const long long campaignApprovalId;
+    const std::string campaignIdentityHash;
     const int contractVersion;
     const int memberCount;
     const std::string identityCanonical;
@@ -42,6 +44,7 @@
     RecommendationCampaignOutcomeAssessmentMaterializationIdentity(
         long long materializationId,
         long long campaignApprovalId,
+        std::string campaignIdentityHash,
         int contractVersion,
         int memberCount,
         std::string identityCanonical,
@@ -198,10 +201,18 @@
     Unknown
 };
 
+enum class RecommendationCampaignOutcomeAssessmentConsistencyState
+{
+    Consistent,
+    Inconsistent
+};
+
 struct RecommendationCampaignOutcomeAssessmentMemberEvidence
 {
     const RecommendationCampaignOutcomeAssessmentMemberIdentity identity;
     const RecommendationCampaignOutcomeAssessmentLifecycleState lifecycle;
+    const RecommendationCampaignOutcomeAssessmentConsistencyState
+        inputConsistency;
     const std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
         sourceEvidence;
     const std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
@@ -213,26 +224,27 @@
         std::optional<RecommendationCampaignOutcomeAssessmentSourceEvidence>
             sourceEvidence,
         std::optional<RecommendationCampaignOutcomeAssessmentResultEvidence>
-            resultEvidence);
+            resultEvidence,
+        RecommendationCampaignOutcomeAssessmentConsistencyState
+            inputConsistency =
+                RecommendationCampaignOutcomeAssessmentConsistencyState::Consistent);
     bool operator==(
         const RecommendationCampaignOutcomeAssessmentMemberEvidence&) const =
         default;
 };
 
-enum class RecommendationCampaignOutcomeAssessmentConsistencyState
-{
-    Consistent,
-    Inconsistent
-};
-
 enum class RecommendationCampaignOutcomeAssessmentDiagnosticCode
 {
+    InputEvidenceInconsistent,
     SourceExperimentIdentityMismatch,
     ExpectedExperimentIdentityMissing,
+    ExpectedExperimentIdentityReused,
+    ResultEvidenceUnexpectedForLifecycle,
     ResultEvidenceMissing,
     ResultExperimentIdentityMissing,
     ResultExperimentIdentityMismatch,
     ResultModelIdentityMissing,
+    ResultModelIdentityReused,
     ResultIdentityMissing,
     ResultContextMissing,
     ResultExperimentReused,
@@ -339,16 +351,16 @@
     const int terminalCancelledMemberCount;
     const int inconsistentOutcomeMemberCount;
 
-    const int metricComparisonCount;
-    const int comparableMetricCount;
-    const int contextChangedMetricCount;
-    const int missingSourceMetricCount;
-    const int missingResultMetricCount;
-    const int metricValueUnavailableCount;
-    const int unsupportedMetricCount;
-    const int positiveDeltaCount;
-    const int zeroDeltaCount;
-    const int negativeDeltaCount;
+    const std::size_t metricComparisonCount;
+    const std::size_t comparableMetricCount;
+    const std::size_t contextChangedMetricCount;
+    const std::size_t missingSourceMetricCount;
+    const std::size_t missingResultMetricCount;
+    const std::size_t metricValueUnavailableCount;
+    const std::size_t unsupportedMetricCount;
+    const std::size_t positiveDeltaCount;
+    const std::size_t zeroDeltaCount;
+    const std::size_t negativeDeltaCount;
 
     RecommendationCampaignOutcomeAssessmentSummary(
         const RecommendationCampaignOutcomeAssessmentSummary&) = default;
@@ -375,16 +387,16 @@
         int terminalFailedMemberCount,
         int terminalCancelledMemberCount,
         int inconsistentOutcomeMemberCount,
-        int metricComparisonCount,
-        int comparableMetricCount,
-        int contextChangedMetricCount,
-        int missingSourceMetricCount,
-        int missingResultMetricCount,
-        int metricValueUnavailableCount,
-        int unsupportedMetricCount,
-        int positiveDeltaCount,
-        int zeroDeltaCount,
-        int negativeDeltaCount);
+        std::size_t metricComparisonCount,
+        std::size_t comparableMetricCount,
+        std::size_t contextChangedMetricCount,
+        std::size_t missingSourceMetricCount,
+        std::size_t missingResultMetricCount,
+        std::size_t metricValueUnavailableCount,
+        std::size_t unsupportedMetricCount,
+        std::size_t positiveDeltaCount,
+        std::size_t zeroDeltaCount,
+        std::size_t negativeDeltaCount);
     friend struct RecommendationCampaignOutcomeAssessmentBuilder;
 };
 
diff --git a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
index 2dadb708210497c2e29dc7fbc2edcc9d77c07ea7..89e8284501fc6f3e4f977bd75c544fb3175ca0dc
--- a/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
+++ b/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp
@@ -112,8 +112,9 @@
 {
     const std::string canonical =
         "materialization-canonical:" + std::to_string(memberCount);
-    return {71, 41, kRecommendationCampaignMaterializationContractVersion,
-        memberCount, canonical, RecommendationCanonicalHash(canonical)};
+    return {71, 41, Campaign().identityHash,
+        kRecommendationCampaignMaterializationContractVersion, memberCount,
+        canonical, RecommendationCanonicalHash(canonical)};
 }
 
 Context ComparableContext(
@@ -388,6 +389,16 @@
         Classification::Comparable);
     assert(supportedSingleMetric.identity.hash != unsupported.identity.hash);
 
+    const auto missingSourceEvidence = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(missingSourceEvidence.members[0].consistency ==
+        Consistency::Consistent);
+    assert(missingSourceEvidence.members[0].outcome ==
+        MemberOutcome::SucceededMetricGap);
+    assert(missingSourceEvidence.members[0].comparisons.size() == 2);
+    assert(missingSourceEvidence.summary.missingSourceMetricCount == 2);
+
     // Lifecycle states do not require result payload. Proposal-only is simply
     // not ready, never a missing-result scientific classification.
     const auto failed = Assess(
@@ -422,12 +433,36 @@
     assert(proposalOnly.identity.canonicalText.find("result_evidence_missing") ==
         std::string::npos);
 
-    AssertInvalidArgument([]
-    {
-        (void)MemberEvidence(Identity(1, ResultExperiment(1)),
-            Lifecycle::Failed, std::nullopt,
-            Result(1, BaseResultMetrics()));
-    });
+    const auto unexpectedFailedResult = Assess({MemberEvidence(
+        Identity(1, ResultExperiment(1)), Lifecycle::Failed, std::nullopt,
+        Result(1, BaseResultMetrics()))});
+    assert(unexpectedFailedResult.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ResultEvidenceUnexpectedForLifecycle});
+    assert(unexpectedFailedResult.members[0].resultEvidence);
+    assert(unexpectedFailedResult.members[0].outcome ==
+        MemberOutcome::Inconsistent);
+
+    const auto failedWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Failed)});
+    assert(failedWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+    const auto cancelledWithoutExperiment =
+        Assess({LifecycleOnly(1, Lifecycle::Cancelled)});
+    assert(cancelledWithoutExperiment.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityMissing});
+
+    const auto inputInconsistent = Assess({MemberEvidence(Identity(1,
+        std::nullopt), Lifecycle::Unknown, std::nullopt, std::nullopt,
+        Consistency::Inconsistent)});
+    assert(inputInconsistent.members[0].lifecycle == Lifecycle::Unknown);
+    assert(inputInconsistent.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::InputEvidenceInconsistent});
+    assert(inputInconsistent.members[0].consistency ==
+        Consistency::Inconsistent);
+    assert(inputInconsistent.summary.outcome == AggregateOutcome::Inconsistent);
 
     // Malformed downstream evidence is retained when member identity remains
     // trustworthy, with fixed-order diagnostics and no scientific comparison.
@@ -532,6 +567,24 @@
     assert(resultReuse.members[1].diagnostics ==
         std::vector<Diagnostic>{Diagnostic::ResultIdentityReused});
 
+    const MemberEvidence reusedModelOne(
+        Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
+        Source(1, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(1), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(1))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const MemberEvidence reusedModelTwo(
+        Identity(2, ResultExperiment(2)), Lifecycle::Succeeded,
+        Source(2, BaseSourceMetrics()),
+        ResultEvidence(ResultExperiment(2), 7777,
+            {ResultIdentity("scientific_result", ResultRecord(2))},
+            ComparableContext(), Metrics(BaseResultMetrics())));
+    const auto modelReuse = Assess({reusedModelTwo, reusedModelOne});
+    assert(modelReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+    assert(modelReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{Diagnostic::ResultModelIdentityReused});
+
     // Aggregate precedence is deterministic: inconsistency, failure,
     // cancellation, not-ready, then successful context/gap/comparable states.
     const auto allPrecedence = Assess({Succeeded(5),
@@ -617,8 +670,19 @@
     assert(changedProvenanceAssessment.members[0].outcome ==
         MemberOutcome::SucceededComparable);
     assert(changedProvenanceAssessment.identity.hash != comparable.identity.hash);
+    assert(comparable.identity.contractVersion ==
+        kRecommendationCampaignOutcomeAssessmentContractVersion);
+    assert(comparable.identity.canonicalText.starts_with(
+        "experiment_recommendation_campaign_outcome_assessment_v2;"
+        "assessment_contract_version=2;"));
     assert(comparable.identity.hash == RecommendationCanonicalHash(
         comparable.identity.canonicalText));
+    assert(comparable.identity.canonicalText.find(
+               "campaign_identity=27:campaign-approval-canonical") !=
+        std::string::npos);
+    assert(comparable.identity.canonicalText.find(
+               "materialization_identity=27:materialization-canonical:1") !=
+        std::string::npos);
     assert(comparable.identity.canonicalText.find("lifecycle=succeeded") !=
         std::string::npos);
     assert(malformedProvenance.identity.canonicalText.find(
@@ -691,17 +755,38 @@
     });
     AssertInvalidArgument([]
     {
-        const MemberEvidence first(Identity(1, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        const MemberEvidence second(Identity(2, 9901), Lifecycle::NotTerminal,
-            std::nullopt, std::nullopt);
-        (void)Assess({first, second});
+        const std::string canonical = "materialization-canonical:1";
+        const MaterializationIdentity mismatched(71, 41,
+            RecommendationCanonicalHash("different-campaign-identity"),
+            kRecommendationCampaignMaterializationContractVersion, 1,
+            canonical, RecommendationCanonicalHash(canonical));
+        (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
+            mismatched, "2026-07-19T22:00:00Z", {Succeeded(1)});
     });
+    const MemberEvidence reusedExpectedOne(Identity(1, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const MemberEvidence reusedExpectedTwo(Identity(2, 9901),
+        Lifecycle::NotTerminal, std::nullopt, std::nullopt);
+    const auto expectedReuse =
+        Assess({reusedExpectedTwo, reusedExpectedOne});
+    assert(expectedReuse.members[0].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.members[1].diagnostics ==
+        std::vector<Diagnostic>{
+            Diagnostic::ExpectedExperimentIdentityReused});
+    assert(expectedReuse.summary.outcome == AggregateOutcome::Inconsistent);
     AssertInvalidArgument([]
     {
         (void)BuildRecommendationCampaignOutcomeAssessment(Campaign(),
             Materialization(1), "", {Succeeded(1)});
     });
+    AssertInvalidArgument([]
+    {
+        (void)MemberEvidence(Identity(1, std::nullopt), Lifecycle::Unknown,
+            std::nullopt, std::nullopt,
+            static_cast<Consistency>(999));
+    });
 
     const MemberEvidence overflowingDelta(
         Identity(1, ResultExperiment(1)), Lifecycle::Succeeded,
diff --git a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
index e5556b6ff7c498b954c6665d8f301793ce9d6b89..fa67d4b206dda8376ce497e0972f83e664f6d318
--- a/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
+++ b/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst
@@ -15,10 +15,12 @@
 Lifecycle is explicit (``not_terminal``, ``succeeded``, ``failed``,
 ``cancelled``, or ``unknown``).  Consistency is independent
 (``consistent`` or ``inconsistent``), and inconsistent downstream evidence is
-retained with deterministic diagnostic codes.  Scientific comparisons are
-derived only for consistent succeeded members.  Member outcomes distinguish
-not-ready, comparable success, context-changed success, metric-gap success,
-terminal failure, terminal cancellation, and inconsistency.
+retained with deterministic diagnostic codes.  Callers can also carry a known
+inconsistent lifecycle or workflow observation without changing or discarding
+the exact materialized-member identity.  Scientific comparisons are derived
+only for consistent succeeded members.  Member outcomes distinguish not-ready,
+comparable success, context-changed success, metric-gap success, terminal
+failure, terminal cancellation, and inconsistency.
 
 Multi-metric evidence
 ---------------------
@@ -33,16 +35,22 @@
 -------------------------------------
 
 Each materialized member may identify its expected downstream experiment.
-Succeeded-result evidence is checked against that exact experiment, and result
-experiment and typed result identities may not be reused across members.
-Provenance defects remain inconsistent members when top-level materialization
-membership is trustworthy; invalid or ambiguous member identity fails the
-assessment.
+Succeeded-result evidence is checked against that exact experiment.  Terminal
+members require an expected experiment, and expected experiments, observed
+result experiments, result models, and typed result identities may not be
+reused across members.  Unexpected result evidence is retained and diagnosed
+rather than discarded.  Provenance defects remain inconsistent members when
+top-level materialization membership is trustworthy; invalid or ambiguous
+member identity fails the assessment.  Campaign and materialization inputs
+must agree on both the persisted campaign-approval ID and its canonical
+identity hash.
 
 Canonical text and hashing bind lifecycle, consistency, diagnostics, canonical
 source and result metrics, comparisons, exact provenance, and aggregate member
-and metric summaries.  Canonical number formatting is locale-independent.
-``observed_at`` is intentionally excluded from evidence identity.
+and metric summaries.  Authoritative campaign and materialization canonical
+texts are length-framed into the assessment identity alongside their compact
+hashes.  Canonical number formatting is locale-independent.  ``observed_at``
+is intentionally excluded from evidence identity.
 
 Non-goals
 ---------

tokens used
240,784
## Architectural assessment

Phase 5 Step 5a is architecturally sound after correction. It now provides:

- Separate lifecycle, consistency, diagnostics, and scientific comparison models.
- Immutable, builder-controlled derived outputs.
- Deterministic metric, result-identity, diagnostic, member, and comparison ordering.
- Complete canonical identity binding, including authoritative campaign/materialization canonical text.
- Clear downstream-provenance ownership suitable for Step 5b.
- No repository, database, scheduler, experiment, persistence, or CLI behavior.

## Defects corrected

- Terminal success, failure, and cancellation now require an expected experiment identity.
- Duplicate expected experiment links produce member diagnostics instead of aborting trustworthy materialized membership.
- Known inconsistent workflow/lifecycle evidence can be carried into the assessment explicitly.
- Unexpected result evidence is retained and diagnosed instead of being rejected during construction.
- Cross-member model reuse is detected.
- Campaign and materialization identities must match by approval ID and canonical identity hash.
- Assessment identity now embeds authoritative upstream canonical texts, not merely compact hashes.
- Metric summary cardinalities use `std::size_t`, eliminating signed count-overflow risk.
- Tests were expanded for all corrected invariants and serialization versioning.

## Remaining concerns

No blocking code concerns.

Step 5b must populate the materialization’s stored approval identity hash and propagate known workflow inconsistency through `inputConsistency`. LeakSanitizer is unsupported on this macOS platform, but AddressSanitizer and UndefinedBehaviorSanitizer passed.

An unrelated modification to `refresh_candlestick_materialized_view.plpgsql` appeared during verification. I did not touch it; exclude it from the Step 5a commit.

## Files in the Step 5a commit set

- [OutcomeAssessment.hpp](</Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.hpp>)
- [OutcomeAssessment.cpp](</Volumes/Developer SSD/ExpertAdvisor/Sources/ExperimentRecommendationCampaignOutcomeAssessment.cpp>)
- [OutcomeAssessmentTests.cpp](</Volumes/Developer SSD/ExpertAdvisor/Tests/ExperimentRecommendationCampaignOutcomeAssessmentTests.cpp>)
- [Step 5a documentation](</Volumes/Developer SSD/ExpertAdvisor/docs/Phase5ExperimentRecommendationCampaignOutcomeAssessment.rst>)
- [Xcode project](</Volumes/Developer SSD/ExpertAdvisor/ExpertAdvisor.xcodeproj/project.pbxproj>)

## Verification results

- Focused Step 5a test with `-Wall -Wextra -Wpedantic -Werror`: passed.
- AddressSanitizer + UndefinedBehaviorSanitizer: passed with leak detection disabled because unsupported.
- Nearby pure regression tests passed:

  - Campaign execution
  - Campaign activation
  - Campaign launch
  - Campaign status
  - Campaign materialization

- Incremental Xcode Release build:

  ```text
  xcodebuild -project ExpertAdvisor.xcodeproj \
    -scheme "LSTM Release" -configuration Release \
    -destination "platform=macOS,arch=arm64" build
  ```

  Result: `BUILD SUCCEEDED`. DerivedData was reused and never cleaned. A non-fatal warning remains for an invalid local `LLVM22.xctoolchain` installation; Xcode’s default toolchain completed the build.

- Tracked and untracked whitespace checks: passed.
- Branch remains `phase5` at `8b9939e`.
- No commit created.

## Recommendation

Stage only the five Step 5a files listed above and exclude the unrelated PL/pgSQL modification.

**READY TO COMMIT**
      917.06 real        43.26 user        10.70 sys
           345030656  maximum resident set size
                   0  average shared memory size
                   0  average unshared data size
                   0  average unshared stack size
             1125485  page reclaims
               75732  page faults
                   0  swaps
                   0  block input operations
                   0  block output operations
                1747  messages sent
               21977  messages received
                  79  signals received
               45456  voluntary context switches
              298589  involuntary context switches
         47551882920  instructions retired
         22430506555  cycles elapsed
           118018680  peak memory footprint
